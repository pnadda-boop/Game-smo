# GameSmo

เกม **Pixel Art Top-Down Narrative Simulator** ภาษาไทย เล่าเรื่องของ **น้ำฟ้า** กับการทำงาน **สโม** (สโมสรนักศึกษา)

ฟีเจอร์หลักของเกม: ระบบบทสนทนา, **ระบบแชทข้อความ**, และ **อีเวนท์** ต่าง ๆ

- Engine: **Godot 4.6** (GL Compatibility), viewport 1920×1080, stretch mode `canvas_items`
  ⚠️ `default_texture_filter = 0` (**nearest**) — พิกเซลอาร์ตต้องไม่โดนเบลอ อย่าเผลอเปลี่ยนกลับ
- ภาษา: GDScript — คอมเมนต์และบทพูดในเกมเป็น **ภาษาไทย** เขียนโค้ดใหม่ให้คงสไตล์นี้
- Main scene: `start_manu.tscn`

## 📌 กฎการทำงานในโปรเจกต์นี้ (สำคัญ — อ่านก่อนเริ่ม)

1. **จดสิ่งที่คุยกันลง MD** — ข้อสรุปจากบทสนทนาต้องถูกบันทึก ไม่ปล่อยให้หายไปกับ session
2. **การตัดสินใจที่กระทบแนวคิดพื้นฐานของเกม → จดลง [DEVLOG.md](DEVLOG.md) ทุกครั้ง** (ใหม่สุดไว้บนสุด)
3. **แก้อะไรต้องแสดงเหตุผล** — บอกว่าจะแก้อะไร เพราะอะไร พิจารณาทางเลือกอื่นอะไรบ้าง ไม่แก้เงียบ ๆ
4. **ตรวจด้วย Godot headless ก่อนบอกว่าเสร็จ** — ดูคำสั่งหัวข้อถัดไป

**แบ่งหน้าที่เอกสาร:** ไฟล์นี้ (`CLAUDE.md`) = *"ตอนนี้เกมเป็นยังไง"* เขียนทับให้ทันสมัยเสมอ ·
[`DEVLOG.md`](DEVLOG.md) = *"ทำไมถึงมาเป็นแบบนี้"* สะสมไปเรื่อย ๆ ไม่ลบของเก่า

## คำสั่งตรวจสอบ (ใช้ทุกครั้งหลังแก้โค้ด)

Godot อยู่ที่ `D:\Godot_v4.6.2-stable_win64_console.exe` (ไม่ได้อยู่ใน PATH)

```bash
# ✅ วิธีหลัก — ตรวจทั้งโปรเจกต์ คอมไพล์ทุกสคริปต์พร้อม autoload
"D:\Godot_v4.6.2-stable_win64_console.exe" --headless --path "D:\game-smo" --import

# รันฉากใดฉากหนึ่งเพื่อดู error ตอน runtime
"D:\Godot_v4.6.2-stable_win64_console.exe" --headless --path "D:\game-smo" "res://level1.tscn" --quit-after 60
```

> ⚠️ **อย่าใช้ `--check-only --script`** — โหมดนี้ **ไม่โหลด autoload** ทำให้สคริปต์ที่อ้างถึง
> `SeatManager`, `NPCalignDatabase`, `ChatDatabase`, `Global`, `GameState` ขึ้น
> `Compile Error: Identifier not found` ทั้งที่โค้ดถูกต้องและทำงานได้จริงในเกม
> (ยืนยันแล้ว: `SeatManager.gd` ที่ใช้งานได้ปกติก็ FAIL ในโหมดนี้) — ใช้ `--import` แทนเสมอ

**เวลาอ่านผล `--import`:** จะมี error เรื่อง TileSet ~4,000 บรรทัดเป็นปกติ (ปัญหาค้าง ดูหัวข้อล่าง)
ให้กรองออกก่อน แล้วดูเฉพาะ `SCRIPT ERROR` / `Compile Error` / `Parse Error`

## Autoload (ตั้งใน project.godot แล้ว)

| ชื่อ | ไฟล์ | หน้าที่ |
|---|---|---|
| `Global` | `Global.gd` | ตำแหน่ง/ทิศทางที่ผู้เล่นจะ spawn |
| `GameState` | `Game_State.gd` | `next_spawn_point` — ผู้เล่นโผล่ตรงไหน · `pending_intro` — ฉากปลายทางต้องเล่นเนื้อเรื่องตอนมาถึงไหม · `chapter` — บทของเนื้อเรื่องตอนนี้ (มีสัญญาณ `chapter_changed`) · `carrying_box` / `box_delivered` — เควสต์กล่อง |
| `NPCalignDatabase` | `npcalign_database.gd` | ฐานข้อมูล NPC: ชื่อ, บทพูด, เพศ, เงื่อนไขที่นั่ง |
| `SeatManager` | `SeatManager.gd` | ตัวกลางคุมระบบที่นั่งทั้งหมด + ตรวจเงื่อนไข |
| `ChatDatabase` | `chat_database.gd` | ทะเบียนผู้ส่งข้อความในกรุ๊ปแชท (ชื่อ + รูป + ฝั่งซ้ายขวา) |
| `ClickEffect` | `click_effect.tscn` | เอฟเฟกต์วงกลมกระเพื่อม + เสียงตอนคลิก ทำงานทุกซีน |

> 🚨 **เพิ่ม autoload ผ่าน `Project > Project Settings > Globals > Autoload` เท่านั้น**
> ห้ามเติมบรรทัดลง `project.godot` เอง — Godot อ่านไฟล์นี้ตอนเปิดโปรเจกต์ครั้งเดียว
> พอผู้ใช้แตะ Project Settings เรื่องใดก็ตาม มันจะเขียนไฟล์ใหม่จากรายการในหน่วยความจำ
> แล้ว **autoload ที่เติมจากข้างนอกจะถูกลบทิ้งเงียบ ๆ** (เกิดแล้ว 2 ครั้ง — ดู DEVLOG 2026-07-30)

> ⚠️ คอมเมนต์หัวไฟล์ `npcalign_database.gd` บอกให้ตั้งชื่อ autoload ว่า `NPCDatabase` — **เป็นคอมเมนต์เก่าที่ผิด** ชื่อจริงคือ `NPCalignDatabase` (โค้ดที่เรียกใช้ถูกต้องแล้ว)

## Flow ของเกม

```
start_manu.tscn ──ปุ่ม "WORK START"(Start.gd)──► wokeup.tscn
                                                    │ มินิเกมคลิกปลุกตัวเอง
                                                    │ idle → halfasleep → wokeup → sit
                                                    ▼
                                            p.tscn (parking.gd) + bus.tscn
                                            มาจากแชท → รถออกเอง + น้ำฟ้าพูด (ขยับไม่ได้)
                                            มาทางอื่น → เดินห่างรถ >180px แล้วรถออก
                                                    ▼
                              level1.tscn ⇄ all_level_2.tscn ⇄ level_3.tscn
                              ตึก 3 ชั้น — กด E ที่บันได → StairMenu เลือกชั้น
                                                    ▼
                          คุย NPC (กด E) + ปริศนาจัดที่นั่ง (align_*.tscn)
```

### การเปลี่ยนฉาก / spawn point

`stair_menu.gd` ถือตาราง `floor_targets` ที่ map (ชั้น + ตัวเลือก) → (scene ปลายทาง + ชื่อ spawn point)
ก่อนเปลี่ยนฉากจะเซ็ต `GameState.next_spawn_point` ไว้ ให้ฉากปลายทางอ่านไปใช้

**เพิ่มทางเชื่อมใหม่ต้องแก้ 2 ที่:** `floor_targets` ใน `stair_menu.gd` และ spawn point ในฉากปลายทาง

### 🚨 ชั้น 2 = `all_level_2.tscn` ไม่ใช่ `level_2.tscn`

```
AllLevel2 (Node2D)  y_sort ✅          ← ซีนที่บันไดพาไปจริง ๆ
├── Level2      (instance ของ level_2.tscn)  y_sort ✅
│   ├── WorldObjects (Node2D)  y_sort ✅  ← ห้อง · บันได · ผู้เล่น · SpawManager
│   ├── StairMenu    (CanvasLayer 10)
│   └── CanvasLayer  (DialogueBox)
├── Level2_1    (instance ของ level_2_1.tscn)  y_sort ✅  ← เควสต์กล่อง
└── level2_2    (Node2D)  🚨 y_sort ยังไม่ติ๊ก      ← NPC (โชแชง)
```

`level_2.tscn` ยาวสองพันกว่าบรรทัด แก้อะไรทีก็ต้องคลำหาในนั้น — เลยห่อไว้อีกชั้น
แล้วเอาของใหม่มาต่อเป็น**พี่น้องกับ `Level2`** แทนการยัดเข้าไปในซีนแมพ

⚠️ **`stair_menu.gd` เก็บ path ไว้ที่ค่าคงที่ `LEVEL_2_SCENE` ตัวเดียว** (มี 2 ที่ที่ชี้ชั้น 2)
ย้าย/เปลี่ยนชื่อไฟล์เมื่อไหร่ต้องกลับมาแก้เอง — Godot ไม่ตามแก้ string ใน `.gd` ให้

🚨 **ทุกโหนดที่ห่อของในสายนี้ต้องเปิด `y_sort_enabled`** — ตอนนี้ `level2_2` ยังไม่ติ๊ก
⚠️ *โชแชงจะจมอยู่หลังพื้นห้อง ต้องไปทำเอง* (บทเรียนเดียวกับ `ChapterSlot` · `Level2_1` · `AllLevel2`)

### 🚨 สคริปต์ในซีนแมพต้องหาโหนดด้วย `find_child()` ไม่ใช่ `$path`

**ซีนชั้น 2 ถูกจัดกลุ่มใหม่บ่อย** (ห่อ `AllLevel2` → ห่อ `WorldObjects`) ทุกครั้งที่ห่อเพิ่ม
**path แบบ `$ชื่อโหนด` และ `$"../ชื่อโหนด"` พังเงียบ ๆ ทั้งหมด** — `--import` จับไม่ได้
เพราะเป็นการค้นตอนรัน ไม่ใช่ตอนคอมไพล์ (เกิดจริง 2026-08-20 พังพร้อมกัน 6 path)

| ไฟล์ | หาอะไร | วิธีที่ใช้ตอนนี้ |
|---|---|---|
| `level_2.gd` | โหนดห้อง 5 ห้อง | `find_child(ชื่อ, true, false)` จากตัวเอง |
| `stair.gd` | `StairMenu` | `owner.find_child("StairMenu", true, false)` |
| `level_base.gd` | spawn point | `find_child()` อยู่แล้วตั้งแต่แรก ✅ |

⚠️ **`stair.gd` ต้องค้นจาก `owner` ไม่ใช่จากตัวเอง** — `StairMenu` เป็นลูกของ**ราก**เสมอ
แต่ `Stair` อยู่ลึกไม่เท่ากันในแต่ละฉาก (`level1`/`level_3` อยู่ที่ราก · `level_2` อยู่ใต้ `WorldObjects`)
`$"../StairMenu"` จึงถูกเฉพาะสองฉากแรก · **อาการตอนพัง: กด E ที่บันไดแล้วเมนูไม่ขึ้น = ขึ้นลงชั้นไม่ได้เลย**

⚠️ **มีระบบ spawn สองชุดทับกันอยู่** (ของเก่า ยังไม่ยุบ)
`SpawManager.gd` (โหนดในซีน) และ `level_base.gd` (สคริปต์ของโหนดราก) ทำเรื่องเดียวกัน
ตัวไหนทำก่อนก็ล้าง `GameState.next_spawn_point` ทิ้ง อีกตัวจึงไม่ทำซ้ำ
(โหนดลูก `_ready()` ก่อนโหนดแม่ → **`SpawManager` ได้คิวก่อนเสมอ**)

✅ **ช่อง `player_path` / `spawn_root` ปล่อยว่างได้แล้ว** (แก้ 2026-08-20)
ว่าง → หาผู้เล่นจากกลุ่ม `"player"` และหา spawn point ด้วย `find_child()` จากรากของซีน
· เดิม `level_2` ไม่ได้กรอกไว้แล้ว `push_error` 2 บรรทัด**ทุกครั้งที่เข้าฉาก** (ตำแหน่งยังถูกเพราะ
`level_base` รับช่วงต่อ) — error ประจำแบบนั้นคือสิ่งที่ทำให้ error จริงกลืนหายไปในกองข้อความ
· ยังไม่ยุบเป็นระบบเดียวเพราะต้องแก้ซีนทั้ง 5 ฉากที่มีโหนดนี้อยู่

### เฟดเปลี่ยนฉาก

`scene_transition.tscn` → `transition_to(path)` ทำ fade out → เปลี่ยนฉาก → fade in ให้จบในตัวเอง
```gdscript
var fade := preload("res://scene_transition.tscn").instantiate()
get_tree().root.add_child(fade)   ## ⚠️ ต้องแขวนที่ root ไม่ใช่ในซีนตัวเอง
fade.transition_to("res://p.tscn")
```
⚠️ **ห้าม add_child ใต้ node ในซีนที่กำลังจะถูกเปลี่ยน** — `change_scene_to_file()` ลบซีนเดิมทิ้ง
ตัวเฟดจะหายไปด้วยแล้ว `await` ที่ค้างอยู่จะไม่ทำงานต่อ → จอค้างดำถาวร
(`fade_out()` / `fade_in()` แยกกันยังใช้ได้เหมือนเดิม — `Start.gd` ใช้แบบนั้นอยู่)

### 🚨 ลำดับชั้นของ CanvasLayer (`layer`)

**ช่วงเลขถูกจองไว้แล้ว** — UI ใหม่ให้หยิบเลขจากช่วงที่ตรงกับหน้าที่ของมัน (ทะเบียนจริงอยู่ที่ `UI_SCENES` ใน `scripts/ui_root.gd`)

| ช่วง | ใช้กับ | ตอนนี้มีอะไร |
|---|---|---|
| **1–9** | UI ประจำฉาก (สร้าง/ตายพร้อมฉาก) | `Chat` · `DialogueBox` = 1 |
| **10–29** | UI ที่อยู่ตลอดเวลา ไม่บังการเล่น | `hud` = **20** |
| **30–49** | แผงที่เปิดทับการเล่น (แมพ · เมนูพัก) | — (ยังไม่มี) |
| **100** | เอฟเฟกต์คลิก | `ClickEffect` |
| **128** | ม่านเฟดเปลี่ยนฉาก (ค่าสูงสุดที่ Godot รับ) | `SceneTransition` |

⚠️ **UI ช่วง 30–49 ต้องล็อกผู้เล่นเองด้วย** (`player.can_move = false`) — `layer` คุมแค่
"ใครวาดทับใคร" ไม่ได้กันอินพุตให้ · เปิดแมพทับจอแล้วผู้เล่นยังเดินอยู่ข้างหลังได้

**CanvasLayer ที่ `layer` เท่ากันเรียงตามลำดับใน tree ซึ่งเดาไม่ได้** — ตอนแรกทุกตัวเป็น 1 หมด
เปลี่ยนฉากทีไรกล่องแชทกับกล่องคำพูดจึงลอยอยู่เหนือม่านเฟด ไม่ยอมมืดตาม
`SceneTransition` ตั้ง `layer` **จากโค้ดใน `_ready()`** เพื่อให้ได้ผลแน่นอนแม้ซีนถูกเซฟทับ

**เพิ่ม CanvasLayer ใหม่ต้องตั้ง `layer` ให้ชัดเจนเสมอ** อย่าปล่อย default 1

### 🚨 Y-sort — ใครวาดทับใครในโลก 2D

**จุดเรียงของผู้เล่นคือ `เท้า − 22` ไม่ใช่เท้า** ⚠️ *ตัวเลขที่ต้องรู้ก่อนตั้ง `y_sort_origin` ทุกครั้ง*

โหนด `player` ในซีนแมพติ๊ก `y_sort_enabled = true` ไว้ → ลูกของมันถูกแยกออกมาเรียง**ทีละตัว**
สิ่งที่เข้าไปอยู่ในลิสต์จึงเป็น `AnimatedSprite2D` ซึ่งตั้ง `position = (0, -22)`
**ไม่ใช่ `CharacterBody2D` ที่เป็นตำแหน่งเท้า**

```
tile แถว r  →  จุดเรียง = r × 32 + 16 + y_sort_origin
ผู้เล่น      →  จุดเรียง = global_position.y − 22
```

จุดเรียงมากกว่า = วาดทีหลัง = **อยู่ข้างหน้า**

🚨 **ตั้ง `y_sort_origin` แล้ว "ไม่เห็นมีอะไรเกิดขึ้น" มักไม่ใช่ว่ามันไม่ทำงาน แต่คือยังไม่ข้ามเส้นแบ่ง**
เกิดจริง 2026-08-20 กับ `meetingroom/stage` — เปลี่ยนจาก `20` เป็น `-25` (ขยับ 45 หน่วย)
แต่เส้นแบ่งอยู่ที่ **≈ −27** ทั้งสองค่าจึงอยู่ฝั่งเดียวกัน **ภาพเหมือนเดิมทุกพิกเซล**
· คำนวณเส้นแบ่งก่อนแล้วค่อยตั้ง อย่าเดาแล้วลองทีละค่า

⚠️ **y-sort ไม่ได้ย้ายอะไร — มันตัดสินแค่ "ใครทับใคร ตรงที่ภาพซ้อนกัน"**
ภาพไม่ซ้อนกัน = ตั้งเท่าไหร่ก็ไม่มีผล · ที่ `stage` มีแถบซ้อนกันจริงแค่ **~21px** ที่ขอบบน
เพราะ collision ของ tile กั้นผู้เล่นไม่ให้เข้าใกล้กว่านั้น
· `stage2` (แถว 58-60) **ไม่ต้องตั้ง y-sort เลย** — สไปรท์ผู้เล่นไปไม่ถึงระยะที่จะซ้อนกับมันได้

⚠️ **ค่า `y_sort_origin = -15` ที่เห็นเกลื่อน `level_2.tscn` คือการชดเชย 22px นี้**
อยากให้ค่าตรงไปตรงมาต้องปิด `y_sort_enabled` ที่โหนด `player` (สไปรท์จะเรียงที่เท้าตามที่ควรเป็น)
**แต่กระทบการเรียงกับทุกอย่างในฉาก ต้องไล่ปรับใหม่ทั้งฉาก** — ยังไม่ทำ

**y-sort ของ `TileMapLayer` ต้องครบ 3 ชั้น ขาดชั้นใดชั้นหนึ่งก็เงียบสนิท**

| # | ต้องมี | ตั้งที่ไหน |
|---|---|---|
| 1 | `y_sort_origin` ของ tile | TileSet (ทีละช่อง) |
| 2 | `y_sort_enabled` ของ layer | โหนด `TileMapLayer` |
| 3 | โหนดแม่ทั้งสายเปิด y-sort | ทุกโหนดตั้งแต่ layer ขึ้นไปจนถึงชั้นที่ผู้เล่นอยู่ |

🚨 **ชั้นที่ 3 คือกับดักที่เจอมาแล้ว 3 ครั้ง** (`ChapterSlot` · `Level2_1` · `AllLevel2`)
Godot จัดลำดับ Y ให้เฉพาะ**ลูกโดยตรง**ของโหนดที่เปิด y-sort · โหนดกลางที่ปิดไว้จะถูกวาด
**ยกกลุ่มที่พิกัดของตัวมันเอง (มักเป็น 0,0)** = จมอยู่หลังทุกอย่าง

⚠️ **โหนดที่ตั้ง `y_sort_origin` ครบทุก tile แล้วแต่ลืมติ๊กที่ layer = งานทั้งหมดนั้นไม่ถูกใช้เลย**
ตอนนี้ใน `level_2.tscn` ยังมีค้างอยู่: `up_wall` (meetingroom) · `ToiletM/TileMapLayer` ·
`ToiletW/TileMapLayer2` — ทั้งสามยังไม่ติ๊ก `y_sort_enabled`

🚨 **แต่ "8/8 = ตั้งใจตั้งครบแล้วลืมติ๊ก" สรุปแบบนั้นไม่ได้** ⚠️ *ตรวจใหม่ 2026-08-21 — ยังไม่แก้*
`y_sort_origin` ไม่ได้อยู่ที่ layer แต่อยู่ใน **`TileSet_sgm4o` ซึ่งเป็นใบเดียวกันที่ใช้ร่วมกัน 7 layer**
และในนั้นมี `footpath/floor_wall4` · `Board` · `footpath/up_wall` ที่**ติ๊กแล้ว**
→ ค่า 8/8 ที่เห็นอาจถูกตั้งไว้เพื่อ layer พวกนั้น ไม่ใช่หลักฐานว่าสามตัวนี้ถูกลืม

**ติ๊กแล้วเปลี่ยนลำดับการวาดทั้งห้อง ตัดสินด้วยตาในเอดิเตอร์เท่านั้น — headless ตรวจให้ไม่ได้**
ลองติ๊กทีละตัวแล้วเดินดูว่าผู้เล่นลอดหลังกำแพงถูกไหม ไม่ใช่ติ๊กรวดสามตัวแล้วหาสาเหตุทีหลัง

### เฟดเข้าตอนเปิดฉาก (ไม่เปลี่ยนฉาก)

`start_manu.gd` ทำจอดำแล้วจางออกตอนเปิดเกม — ใช้ `fade_in()` เดี่ยว ๆ ไม่ต้องใช้ `transition_to()`
```gdscript
var fade := preload("res://scene_transition.tscn").instantiate()
add_child(fade)     ## ⚠️ ที่ตัวเอง ไม่ใช่ root
fade.fade_in()      ## จบแล้ว queue_free() ตัวเอง
```
⚠️ **ตอน `_ready()` ของซีนหลัก `get_tree().root` ยัง busy setting up children อยู่** — `add_child` ตรงนั้นจะ error
ใส่ที่ตัวเองแทนได้เพราะซีนไม่ถูกเปลี่ยนระหว่างเฟดเข้า (ต่างจาก `transition_to()` ที่ต้องอยู่ root)
ปิด/เปิดได้ที่ `@export fade_in_on_start` บน node `StartManu`

### เนื้อเรื่องตอนมาถึงฉาก (`GameState.pending_intro`)

ฉากเดียวกันเข้าได้หลายทาง แต่อินโทรต้องเล่น **เฉพาะตอนมาจากทางที่เนื้อเรื่องกำหนด**
ฝั่งที่สั่งเปลี่ยนฉากตั้งค่า → ฉากปลายทางอ่านใน `_ready()` แล้ว **ล้างทิ้งทันที**
(ไม่ล้าง = เดินออกแล้วกลับเข้ามาใหม่ อินโทรจะเล่นซ้ำ)

ตอนนี้มีอันเดียว: `GameState.INTRO_ARRIVE_BUS` (`"arrive_bus"`) — `phone.gd` ตั้ง → `parking.gd` อ่าน

## ระบบหลัก

### 1. ปริศนาจัดที่นั่ง — ระบบที่ซับซ้อนที่สุด (ทำงานได้แล้ว)

Constraint satisfaction puzzle: NPC แต่ละตัวมีเงื่อนไขที่นั่งของตัวเอง ผู้เล่นต้องจัดให้ผ่านครบทุกคน

- `seat.gd` (`class_name Seat`) — ที่นั่งหนึ่งช่อง มี `row`/`column` (`@export`) ลงทะเบียนตัวเองเข้า `SeatManager` ตอน `_ready()`
- `SeatManager.gd` — รองรับ **15 เงื่อนไข**: `front_row`, `back_row`, `not_front_row`, `not_middle_row`, `any`, `next_to_boy`, `next_to_girl`, `not_next_to_boy`, `not_next_to_girl`, `no_one_in_front`, `someone_in_front`, `left_edge`, `right_edge`, `edge`, `not_edge`, `middle_seat`
- `condition` เป็น **Array ได้** = ต้องผ่านทุกข้อ (AND) เช่น NPC7 = `["no_one_in_front", "not_front_row"]`
- Feedback: ที่นั่ง**เขียว = ถูก / แดง = ผิด** (`set_highlight`) และ NPC เปลี่ยนเป็น `sit_happy` / `sit_angry`

**หน้าตาของช่องที่นั่ง — เส้นขอบ vs สี่เหลี่ยมสี** (`refresh_visual()` ใน `seat.gd`)

| สถานะ | `Line2D` (เส้นขอบ) | `ColorRect` (สี่เหลี่ยมสี) |
|---|---|---|
| ว่าง | ✅ เห็น | ❌ |
| ลาก NPC มาลอยเหนือ | ✅ เห็น | ✅ **ติดสี** = กำลังเลือกตำแหน่งนี้อยู่ |
| มีคนนั่งแล้ว | ✅ เห็น | ❌ |

**เส้นขอบโชว์ตลอด** — เป็นผังของกระดาน บอกว่ามีช่องอยู่ตรงไหนบ้าง ต้องอ่านได้ทั้งกระดานเสมอ
ไม่ใช่หายเป็นหย่อม ๆ ตามที่คนไปนั่ง → **`seat.gd` ไม่แตะ `visible` ของ `Line2D` เลย**

🚨 **สี่เหลี่ยมสี = ตัวชี้ตำแหน่งที่กำลังเล็งอยู่เท่านั้น ไม่ใช่พื้นของช่อง**
ติดค้างไว้ตอนมีคนนั่งจะเป็นสัญญาณสองความหมายในภาพเดียว ผู้เล่นแยกไม่ออกว่า
สีที่เห็นแปลว่า "เล็งอยู่" หรือ "มีคนแล้ว"

⚠️ **เส้นขอบที่โชว์ตลอดเป็นตัวรับไฮไลต์เขียว/แดงไปในตัว** — `set_highlight()` ย้อม
`modulate` ของทั้งช่อง ลูกทุกตัวรับไปด้วย · ถ้าวันหลังไปซ่อนเส้นขอบตอนมีคนนั่ง
ไฮไลต์ถูก/ผิดจะไม่เหลืออะไรให้ย้อมแล้วหายไปเงียบ ๆ

**ช่องที่ลากอยู่เหนือเก็บไว้ที่ `SeatManager._hovered_seat` ไม่ใช่ที่ตัวช่อง**
เป็นสถานะที่มีได้ทีละช่องเดียวทั้งกระดาน เก็บที่ตัวกลางจึงดับช่องเก่าให้เองตอนย้ายไปช่องใหม่
(`click.gd` เรียก `SeatManager.set_hovered_seat()` ทุกเฟรมระหว่างลาก และส่ง `null` ตอนปล่อย)

⚠️ **ช่องที่ติดสีตอนลาก ต้องมาจาก `_find_nearby_seat()` ตัวเดียวกับตอนปล่อยจริง**
ใช้คนละเกณฑ์เมื่อไหร่ ช่องที่ไฟติดกับช่องที่ NPC ลงจริงจะเป็นคนละช่องในบางมุม
- `check_all()` คืน `true` เมื่อทุกที่นั่งถูกหมด → ใช้ตัดสินชนะ; `all_seats_filled()` เช็คว่านั่งครบทุกช่อง

**สำคัญ:** ขอบเขตแถว/คอลัมน์คำนวณจากที่นั่งที่ลงทะเบียนจริง (`get_min_row()` ฯลฯ) ไม่ hardcode → เพิ่ม/ลดที่นั่งได้โดยไม่ต้องแก้ logic
เรียก `SeatManager.check_all()` **ทุกครั้ง**ที่วาง/ย้าย NPC เพราะกระทบเพื่อนบ้านข้าง ๆ ด้วย

**ช่องยืน (`Stand`) — ที่พักของ NPC ที่ยังไม่ได้นั่ง** (`scripts/stand_zone.gd`)

**คนที่ยืนอยู่ที่พัก "ไม่ถูกตัดสินถูก/ผิด"** — `check_all()` วนเฉพาะ `seat.current_npc`
คนยืนจึงไม่มีวันโดน `sit_happy`/`sit_angry` และเล่นท่า `idle` ตั้งแต่ตอนถูกวางลง (`clear_seated_mood`)

**`ColorRect` ของช่องยืน = โผล่ตอนยกตัวละครขึ้นเท่านั้น** (คนละจังหวะกับของที่นั่ง!)

| | ที่นั่ง (`Seat`) | ที่ยืน (`AreaStand`) |
|---|---|---|
| `ColorRect` โผล่เมื่อ | **เล็งช่องนั้นอยู่** (ลากมาลอยเหนือ) | **ยกตัวละครขึ้น** (โชว์พร้อมกันทุกช่องที่วางได้) |
| `Line2D` | มี · โชว์ตลอด | **ไม่มีแล้ว** (ผู้ใช้ลบออก) |

⚠️ **โชว์เฉพาะช่องที่วางลงได้จริง** = ว่างอยู่ หรือเป็นช่องของตัวที่กำลังยกเอง (วางกลับที่เดิมได้)
โชว์ช่องที่คนอื่นยืนอยู่ด้วยจะกลายเป็นคำโกหก — ผู้เล่นเล็งไปปล่อยแล้วเด้งกลับ
🚨 **`click.gd` ต้องเรียก `set_dragged_npc(null)` ตอนปล่อยเสมอ** (อยู่ต้น `_end_drag` ก่อน `return` ทุกทาง)
ไม่งั้นไฟค้างสว่างทั้งกระดานหลังวางเสร็จ

แนบสคริปต์ที่โหนด `Stand` **ตัวเดียว** — ลูกทุกตัวที่มี `Marker2D` ข้างในถูกนับเป็นช่องยืนอัตโนมัติ
เพิ่ม/ลบจุดยืนในซีนได้เลย ไม่ต้องแตะโค้ดและไม่ต้องแนบสคริปต์เพิ่มทีละอัน

🚨 **ช่องยืนไม่ผูกกับ NPC ตัวใดตัวหนึ่ง ใครมาถึงก่อนได้ก่อน** — มาร์เกอร์ในซีนชื่อ
`MarkerNPC1` เหมือนกันหมดเพราะก๊อปกันมา **ไม่ได้แปลว่าเป็นของ NPC1** โค้ดจึงไม่อ่านชื่อมาจับคู่
· หนึ่งช่องรับได้คนเดียว (ซ้อนกันจะเหลือตัวที่มองเห็นแค่ตัวเดียว)

⚠️ **NPC จองช่องที่ตัวเองยืนอยู่ตั้งแต่เปิดฉาก** (`claim_initial_slot`) —
ไม่จองไว้ ทุกช่องจะถือว่าว่างทั้งที่มีคนยืน แล้วลากตัวอื่นมาปล่อยทับได้ตั้งแต่วินาทีแรก
· จองแบบ `call_deferred` เพราะลำดับ `_ready` ระหว่าง `Stand` กับ NPC ขึ้นกับลำดับโหนดในซีน

⚠️ **`StandSlot` เป็น `RefCounted` ไม่ใช่ Node** — เป็นแค่ข้อมูลว่าช่องนี้ใครยืนอยู่
แต่ตั้งชื่อเมธอดให้เหมือน `Seat` (`is_empty` · `get_placement_position` · `place_npc` · `remove_npc`)
→ `click.gd` ใช้โค้ดชุดเดียวกันจัดการได้ทั้งที่นั่งและที่ยืน

**ลำดับตอนปล่อยเมาส์ (`click.gd._end_drag`):** ที่นั่ง → ช่องยืน → กลับที่เดิม
ที่นั่งมาก่อนเพราะเป็นเป้าหมายของปริศนา ส่วนที่ยืนเป็นที่พัก
· ย้ายไปไหนก็ตามจะ `_leave_current_spot()` คืนทั้งที่นั่งและช่องยืนก่อนเสมอ
(ลืมคืนช่องยืนตอนไปนั่ง = ช่องนั้นถูกจองค้างตลอดเกม)
· ลุกจากที่นั่งไปยืนก็เรียก `SeatManager.check_all()` ด้วย เพราะเพื่อนบ้านของช่องที่ว่างลงมีผลเปลี่ยน

**การลาก NPC (`click.gd`)** — กดเริ่มลากที่ `Area2D.input_event` แต่ **ปล่อยดักที่ `_input`**

🚨 **วางไม่สำเร็จต้องคืนท่านั่งด้วย** (`_restore_seated_mood`) — การลากเคลียร์ท่านั่งทิ้งไปแล้ว
(`clear_seated_mood()` ตอนขยับเกิน `drag_threshold`) เด้งกลับเก้าอี้เดิมเฉย ๆ โดยไม่คืนท่า
= **ตัวละครยืนตัวตรงอยู่บนเก้าอี้** (บั๊กจริง แก้แล้ว 2026-08-18)
· เช็ค `_mood_cleared_this_press` ก่อน — แค่คลิกดูบับเบิลไม่ได้ลากจริง สั่ง `play()` ซ้ำจะกระตุกกลับเฟรมแรก

🚨 **อย่าย้ายการดักปุ่มปล่อยกลับไปไว้ใน `input_event`** — สัญญาณนั้นยิงเฉพาะตอนเมาส์
อยู่บนกรอบชนของตัวเอง · ปล่อยตอนเคอร์เซอร์หลุดออกนอกกรอบ (ลากเร็วจนภาพตามไม่ทัน ·
จับที่ขอบตัว · สลับหน้าต่างระหว่างลาก) จะไม่มีใครเรียก `_end_drag()`
→ **ตัวละครติดเมาส์ตามไปทั้งฉากโดยไม่ต้องกดอะไรอีก** (บั๊กจริง แก้แล้ว 2026-08-18)
· `_process` ยังเช็ค `Input.is_mouse_button_pressed()` เป็นตาข่ายรับสุดท้ายเผื่ออีเวนต์หายจริง ๆ

**ตัวนับ Moves** (`scripts/move_counter.gd` แนบที่ Label `CanvasLayer/TextureRect2/VBoxContainer/Number`)

**วาง NPC ลงที่นั่งสำเร็จ 1 ครั้ง = เสีย 1 move** เลขนับถอยหลังจาก `max_moves` (ตอนนี้ 15)

| การกระทำ | เสีย move ไหม |
|---|---|
| วางลงที่นั่งสำเร็จ | ✅ |
| ย้ายจากที่นั่งไปพักที่ช่องยืน | ❌ |
| วางไม่ลง (ช่องมีคน / ปล่อยกลางที่ว่าง) | ❌ |
| คลิกดูบับเบิล | ❌ |

🚨 **นับที่ `click.gd._end_drag()` ไม่ใช่ใน `Seat.place_npc()`** — "move" คือ**การกระทำของผู้เล่น**
ไม่ใช่ "การที่ NPC ไปอยู่บนเก้าอี้" · นับใน `place_npc()` แล้ววันหลังมีระบบจัดที่นั่งอัตโนมัติ
(สลับ/รีเซ็ต/เฉลย) จะกินโควตาผู้เล่นทั้งที่ผู้เล่นไม่ได้ทำอะไร

⚠️ **แก้จำนวนครั้งที่ช่อง `Max Moves` ใน Inspector ไม่ใช่พิมพ์ในช่อง Text ของ Label**
ตัวเลขในซีนถูกเขียนทับตอนเปิดฉาก — พิมพ์ที่ Text จะเห็นถูกตอนแก้ซีนแล้วเด้งกลับตอนรัน

⚠️ **โควตาหมด (0) แล้วยังเล่นต่อได้ตามปกติ** — ยังไม่ได้ตัดสินใจว่าหมดแล้วจะเกิดอะไร
มีสัญญาณ `moves_exhausted` เตรียมไว้แล้วแต่ยังไม่มีใครรับ · `spend()` คืน `false` ตอนหมด
เผื่ออยากให้ "หมดแล้ววางไม่ได้อีก" โดยไม่ต้องรื้อ API

**บับเบิลเปิดได้ทีละใบทั้งฉาก** — คลิกดูตัวใหม่ ใบเก่าปิดเอง

เก็บว่า "ตอนนี้ใครเปิดอยู่" ที่ **`static var _bubble_owner`** ใน `click.gd`
เพราะเป็นสถานะของทั้งกระดาน ไม่ใช่ของ NPC ตัวใดตัวหนึ่ง (กฎเดียวกับ `SeatManager._hovered_seat`)

⚠️ **ตัวแปร `static` อยู่ข้ามฉาก** — เปลี่ยนฉากแล้วมันยังชี้ NPC ที่ถูกลบไปแล้ว
ทุกที่ที่อ่านต้องผ่าน `is_instance_valid()` ก่อน

🚨 **`bubble_closed` มาถึงช้ากว่าการเปิดใบใหม่ 0.15 วิ** (อนิเมชันย่อหาย)
ตอนสัญญาณของใบเก่ามาถึง ตัวใหม่ตั้งตัวเองเป็นเจ้าของไปเรียบร้อยแล้ว
→ **ทุกการล้างสถานะต้องเช็คก่อนว่ายังเป็นของเราอยู่ไหม** (`_bubble_owner == self`)
ล้างทื่อ ๆ = ลบทะเบียนของตัวที่เพิ่งเปิด แล้วคลิกตัวถัดไปจะไม่มีใครถูกปิด กลับไปค้าง 2 ใบเหมือนเดิม

**สมุดจดข้อกำหนด** (`scripts/npc_note.gd` แนบที่ `CanvasLayer/Book/VBoxContainer` ใน `align_1.tscn`)

**คลิกดูตัวละคร → ข้อกำหนดของคนนั้นโผล่ในสมุดพร้อมบับเบิล** ตัดบรรทัดด้วย `\n` ใส่ทีละ Label
(NPC6 กับ NPC7 มี 2 บรรทัด ที่เหลือ 1 บรรทัด → `Label2` ว่าง)

**สมุด = ภาพสะท้อนของบับเบิลตัวที่กำลังดูอยู่ ไม่ใช่ที่สะสมของทุกคน**
คลิกตัวใหม่ = ทับของเดิม · ปิดบับเบิล = ล้างสมุด

🚨 **`erase()` เช็คเจ้าของข้อความก่อนล้าง — ถึงจะเปิดบับเบิลได้ทีละใบแล้วก็ยังต้องเช็ค**
ใบเก่ายิง `bubble_closed` ช้ากว่าใบใหม่ 0.15 วิ (อนิเมชันย่อหาย) ล้างทื่อ ๆ = สมุดของตัวที่
เพิ่งคลิกดูถูกลบทิ้งหลังโผล่มาแป๊บเดียว ซึ่งดูเหมือนสมุดพัง ไม่ใช่ปัญหาเรื่องจังหวะ

⚠️ **สมุดรับ `text` จาก `click.gd` ไม่ได้ดึงจาก `NPCalignDatabase` เอง** — แหล่งเดียวกับบับเบิล
สองที่อ่านคนละทางแล้ววันหลังแก้ไม่ครบ = บับเบิลพูดอย่าง สมุดจดอีกอย่าง

⚠️ **เก็บ Label ลูกอัตโนมัติตามลำดับในซีน ไม่ผูกกับชื่อ `Label1` / `Label2`**
อยากได้ 3 บรรทัดแค่เพิ่ม Label ในซีน ไม่ต้องแตะโค้ด · บรรทัดล้นจำนวน Label จะ `push_warning`
(หายเงียบ ๆ = ปริศนาแก้ไม่ได้โดยไม่มีอะไรบอก) · ฉากที่ไม่มีสมุดยังเล่นได้ปกติ (หาด้วยกลุ่ม `npc_note`)

**เพิ่ม NPC ใหม่:** แก้ `NPC_DATA` ใน `npcalign_database.gd` ที่เดียว
**เพิ่มเงื่อนไขใหม่:** เพิ่ม `match` case ใน `_check_single_condition()` (`SeatManager.gd`)

### 2. ระบบกล่องคำพูด (ทำงานได้แล้ว)

`Game/dialogue_system.gd` ติดที่ **`DialogueBox.tscn`** (node ราก `DialogueBox`)

⚠️ **เปลี่ยนชื่อไฟล์ `.tscn` แล้ว `preload("res://...")` ใน `.gd` ไม่อัปเดตตาม** — Godot แก้ให้เฉพาะ
การอ้างอิงในซีนกับ `@export` เท่านั้น string ในโค้ดต้องไล่แก้เอง (`Grep` ชื่อไฟล์เก่าทั้งโปรเจกต์)
ไฟล์นี้เคยชื่อ `canvas_layer.tscn` — ถ้าเจอชื่อเก่าที่ไหนอีกคือตกหล่น

**รูปแบบบทพูด — กำหนดอนิเมชันต่อบรรทัดได้**
```gdscript
{"name": "วาวา",  "text": "อรุณสวัสดิ์จ้า", "anim": "Angry"},
{"name": "น้ำฟ้า", "text": "สวัสดีค่ะ"},      # ไม่ใส่ anim = ใช้ default_anim ("talk")
```

**ข้อความสุ่ม — `random_text`** (สุ่มหนึ่งประโยคตอนขึ้นบรรทัดนั้น)
```gdscript
{"name": "วาวา", "random_text": ["มีอะไรสงสัยไหม?", "อยากให้ช่วยอะไรเหรอ?"]},
```
ใช้กับบทที่ผู้เล่นกลับมาคุยซ้ำได้เรื่อย ๆ — ประโยคเดิมทุกครั้งจะรู้สึกเหมือนตัวละครเป็นหุ่น
**ไม่พูดประโยคเดิมซ้ำติดกัน** (สุ่มล้วน ๆ กับ 2 ตัวเลือก = เจอซ้ำ 50% ซึ่งดูเหมือนระบบพัง)

⚠️ แยกเป็นคีย์ใหม่ ไม่ยอมให้ `"text"` เป็น Array ได้ด้วย — อ่านบทผ่าน ๆ ต้องบอกได้ทันที
ว่าบรรทัดไหนสุ่ม บรรทัดไหนตายตัว

**พฤติกรรมสำคัญ: อารมณ์ค้างไว้**
คนที่ไม่ได้พูดจะ **ย่อลง + มืดลง แต่คงอนิเมชันเดิม** — โกรธค้างจนกว่าจะมีบรรทัดสั่งเปลี่ยน
ทำได้โดย**ไม่เรียก `play()` กับคนที่ไม่ได้พูด** ห้ามใส่ `sprite.play("idle")` กลับเข้าไป

🚨 **บทที่ไม่ใส่ `"name"` ป้ายชื่อจะ "จางเป็น 0" ไม่ใช่ `visible = false`**
`Root/Box` เป็น `VBoxContainer` ซึ่ง**ตัดลูกที่ซ่อนออกจากการจัดวางทั้งช่อง** —
ซ่อนป้ายเมื่อไหร่ กล่องข้อความจะเลื่อนขึ้นไปแทนที่ทันที (67px) บทที่มีชื่อกับไม่มีชื่อจึงอยู่คนละที่
อาการที่เห็นคือ *"ฉาก wokeup ใช้กล่องคำพูดผิดใบ"* ทั้งที่เป็นไฟล์เดียวกันเป๊ะ (เจอ 2026-08-18)
· ป้ายถูกตั้ง `mouse_filter = IGNORE` ด้วย ไม่งั้นตอนจางจะเป็นแถบใสที่กินคลิก

**สลับโหมดมีรูป/ไม่มีรูป**
```gdscript
start_dialogue(lines)               # มีรูปตัวละคร
start_dialogue(lines, null, false)  # ข้อความล้วน (เสียงบรรยาย/ประกาศ)
```

**โชว์เฉพาะคนที่มีบทพูด** (`only_show_speakers` = true) — ไล่หาคีย์ `"name"` ทั้งชุด**ก่อนเริ่มเล่น**
แล้วซ่อนคนที่ไม่ได้อยู่ในฉากนั้น · เดิมโชว์ทุกคนใน `Root/Portraits` พร้อมกัน บทที่มีคนพูดคนเดียว
เลยมีอีก 3 คนยืนหมอง ๆ อยู่ด้วย · ปิดเป็น `false` ได้ถ้าอยากให้ทุกคนอยู่ในเฟรมตลอด (ฉากประชุม)
⚠️ ต้องตัดสินตั้งแต่ต้นบท ไม่ใช่ทยอยโชว์ตอนถึงคิว ไม่งั้นจะดูเหมือนตัวละครวาร์ปเข้าฉาก

**ตัวละครในกล่องคำพูด 4 คน** — key ต้องตรงกับค่า `"name"` ในบทพูดเป๊ะ

| key | node ใต้ `Root/Portraits` |
|---|---|
| `"น้ำฟ้า"` | `Namfha` |
| `"วาวา"` | `Wawa` |
| `"โชแชง"` | `Chochang` |
| `"คีริน"` | `Kirin` |

⚠️ **ชื่ออนิเมชันแยกตัวพิมพ์เล็กใหญ่** — ตอนนี้วาวาใช้ `Angry` (ตัวใหญ่) ตัวอื่นเป็นตัวเล็ก
พิมพ์ผิดจะขึ้น warning บอกชื่อที่ผิดแล้วเล่น `talk` แทน (ไม่ crash)

⚠️ **ใส่กลุ่ม `dialogue_ui` ที่ `DialogueBox` (CanvasLayer) เท่านั้น** ห้ามใส่ node ลูก
`NPC.gd` หาด้วย `get_first_node_in_group()` ซึ่งอาจคืน node ลูกที่ไม่มีเมธอด `start_dialogue()` แล้ว crash

โครงสร้าง node ทั้งหมด + ค่าที่ต้องตั้ง: ดู [DIALOGUE_UI.md](DIALOGUE_UI.md)

**ตัวเลือกคำตอบในกล่องคำพูด** (`ChoiceBox` — สูงสุด 4 ข้อ) — เขียนในบทได้เลย
```gdscript
{"name": "วาวา", "text": "เรากินข้าวรึยังนะ"},
{"choices": [
    "ตอบสั้น ๆ แล้วบทเดินต่อเหมือนเดิม",
    {"text": "กินแล้วค่ะ", "then": [          ## ← แตกบทย่อย แทรกเข้าคิวตรงนั้นเลย
        {"name": "น้ำฟ้า", "text": "กินแล้วค่ะ"},
        {"name": "วาวา", "text": "เก่งมาก!", "hold": 2.0},
    ]},
]},
```
รูปแบบเดียวกับระบบแชท (`chat_group.gd`) จะได้จำแบบเดียวใช้ได้ทั้งสองที่

⚠️ **ข้อความบนปุ่มกับคำที่น้ำฟ้าพูดจริงเป็นคนละอัน** — อยากให้คำตอบขึ้นเป็นบทพูดด้วย
ต้องใส่บรรทัดของน้ำฟ้าไว้ต้น `then` เอง (ปุ่มมีที่จำกัด คำพูดจริงมักยาวกว่า)

**ตัวเลือกที่โผล่เฉพาะบางสถานการณ์ — `"if"` / `"unless"`** (เพิ่ม 2026-08-20)
ชี้ชื่อ**ตัวแปรใน `GameState`** ตรง ๆ ไม่ต้องเขียนโค้ดเพิ่ม
```gdscript
{"text": "กล่องวางตรงไหนเหรอคะ", "if": "carrying_box", "then": [...]},   ## โผล่ตอนถือกล่องอยู่
{"text": "ขอบคุณสำหรับกล่องนะคะ", "unless": "carrying_box", "then": [...]},
```
ตัวเลือกที่ไม่ใส่คีย์พวกนี้ผ่านเสมอ · ข้อความเปล่า ๆ (String) ก็ผ่านเสมอ → **บทเดิมทุกบทไม่ต้องแก้**

🚨 **กรองที่ "ตัวเลือก" ไม่ใช่แยกเป็น "คนละบท" ตามสถานะ** — ตัวละครทักทายเหมือนเดิมทุกครั้ง
เปลี่ยนแค่ว่ามีเรื่องอะไรให้ถามได้บ้างในตอนนั้น · แยกเป็นคนละบทจะได้บทที่ก๊อปกันมาแก้คำท้ายบรรทัด
แล้ววันหลังแก้ไม่ครบทุกใบ

🚨 **`show_choices()` คืน index ของ "ปุ่มที่เห็นบนจอ" ไม่ใช่ index ในบทต้นฉบับ**
`_run_choice_line()` จึงอ่านผลจาก**ลิสต์ที่กรองแล้ว** — เผลออ่านจากลิสต์ก่อนกรองเมื่อไหร่
ผู้เล่นกดข้อหนึ่งแล้วจะได้บทของอีกข้อ

⚠️ **ทุกข้อถูกกรองออกหมด = ข้ามบรรทัดตัวเลือกไปเลย** ไม่ใช่โชว์กล่องเปล่า (ซึ่งจะกดอะไรไม่ได้ = บทค้าง)
⚠️ **พิมพ์ชื่อธงผิด → `push_error`** ไม่ใช่เงียบ ๆ ถือว่าเป็นเท็จ — ไม่งั้นตัวเลือกนั้นหายจากเกม
ตลอดกาลโดยไม่มีอะไรบอก ซึ่งดูเหมือน "เขียนบทไว้แล้วแต่ไม่เคยโผล่"

**บรรทัดตั้งธงเนื้อเรื่อง — `{"set": "ชื่อธงใน GameState"}`** (เพิ่ม 2026-08-21)
บอกว่า *"ถึงตรงนี้ในบท เรื่องนี้เกิดขึ้นแล้ว"* เช่นโชยื่นขนมปังให้กลางบท → `{"set": "has_bread"}`
แล้วช่องของกับไอคอนเควสอัปเดตตามเองทันที · ไม่มีอะไรให้แสดง ข้ามไปบรรทัดถัดไปเลย

🚨 **ต้องอยู่ก่อนบรรทัดที่มี `"hold"` ไม่ใช่หลัง** — `hold` ปิดบทเองเมื่อครบเวลา
วางไว้หลังจากนั้นบรรทัดตั้งธงจะไม่มีวันถูกอ่าน แล้วของจะไม่เข้าช่อง
🚨 **เขียนไว้ในบท ไม่ใช่ให้ NPC ตั้งธงตอนบทจบ** — จังหวะที่ของเปลี่ยนมือเป็นเรื่องของ*บท*
คนเขียนบทควรย้ายจุดนั้นได้เองโดยไม่ต้องแตะสคริปต์ NPC · และบทที่จบด้วย `hold` จะปิดตัวเอง
ก่อนถึงบรรทัดสุดท้ายเสมอ ผูกกับ "ตอนบทจบ" จะได้ธงที่ตั้งบ้างไม่ตั้งบ้างขึ้นกับว่าบทนั้นจบยังไง
⚠️ ตั้งได้เฉพาะเป็น `true` (บทเดินไปข้างหน้า ไม่มีเคสย้อนธงกลับ) · พิมพ์ชื่อธงผิด → `push_error`
⚠️ เป็นบรรทัดสุดท้ายได้ — โค้ดเช็คว่าหมดคิวแล้วปิดบทให้ ไม่ใช่อ่านเลย index จน crash

**ใส่ `"hold"` ที่บรรทัดสุดท้าย = พิมพ์จบแล้วรอตามเวลาที่ตั้ง แล้วบทปิดเอง**
บรรทัดกลาง ๆ ไม่ต้องใส่ ปล่อยให้รอกด E ตามปกติ · ตอนนี้บท `wawa_under_ch1` ตั้งไว้ 5 วินาที

ทางออกจากบรรทัดสุดท้ายมี 3 ทาง ทำงานได้ทั้งหมด:

| ผู้เล่นทำอะไร | ผลลัพธ์ |
|---|---|
| ปล่อยไว้เฉย ๆ | พิมพ์จบ → รอครบเวลา → ปิดเอง |
| กด E ตอนตัวอักษรยังขึ้นไม่ครบ | ข้อความขึ้นเต็มทันที แล้ว**เริ่มนับเวลาใหม่** → ปิดเอง |
| กด E หลังข้อความขึ้นครบ | ออกทันที |

🚨 **แถวกลางคือจุดที่พลาดง่าย** — เวลาของ `hold` ถูกต่อท้าย tween ตัวเดียวกับการพิมพ์
กด E ข้ามการพิมพ์คือการ `kill()` ทั้งสาย เวลาที่ตั้งไว้หายไปด้วย
ถ้าไม่ตั้งใหม่ (`_restart_hold_if_needed()`) บทจะค้างรอกดตลอดไป ทั้งที่บรรทัดนั้นควรปิดเอง

🚨 **`start_dialogue()` ทำ `lines.duplicate()`** — บทที่มีตัวเลือกจะ `insert()` บทย่อยเข้าคิวตอนเล่น
ใช้ Array ตัวเดียวกับใน `DialogueData` = แก้บทต้นฉบับถาวร คุยรอบสองบทจะบวมขึ้นเรื่อย ๆ

เรียกจากโค้ดตรง ๆ ก็ได้:
```gdscript
var i: int = await ui.show_choices(["รับไว้", "ขอตัวก่อน"])   ## คืน index ที่เลือก (-1 = ใช้ไม่ได้)
```
**เมาส์ชี้ปุ่ม → พื้นเปลี่ยนเป็น `#564aff` ตัวอักษรเป็นสีดำ** (ปรับได้ที่ `@export` กลุ่ม *"ปุ่มตัวเลือกตอนชี้"*)
ตั้งจากโค้ดไม่ใช่ในซีน เพราะ 4 ปุ่ม × 3 สถานะ × (พื้น + สีอักษร) = 24 ช่องที่ต้องกรอกให้ตรงกันด้วยมือ

**ระยะขอบในปุ่ม (ขอบกล่อง ↔ ตัวอักษร) — `choice_padding_h` / `choice_padding_v`** (เพิ่ม 2026-08-20)
ค่าเริ่มต้น **16 / 10** เท่ากับปุ่มตัวเลือกในแชท (`chat_group.gd` → `choice_padding_h/v`)
ปุ่มตัวเลือกทั้งสองระบบผู้เล่นเห็นในเกมเดียวกัน ระยะขอบต่างกันจะรู้สึกว่าเป็นคนละระบบ
· เดิม `StyleBoxFlat_l44sp` ในซีน**ไม่ได้ตั้ง content margin เลย** = ตัวอักษรชิดขอบ (ตั้ง 0 เพื่อกลับไปแบบเดิม)

🚨 **ตั้งระยะขอบ *ก่อน* `_apply_choice_hover_style()`** — ตัวนั้นก๊อป StyleBox ของ `normal`
ไปทำสถานะตอนชี้ · ใส่ทีหลัง = **ตัวอักษรขยับตำแหน่งตอนเอาเมาส์ชี้**
🚨 **`choice_padding_h` ถูกบวกเข้าไปในความกว้างที่คำนวณด้วย** — `content_margin` กินพื้นที่
ของตัวอักษรจริง ๆ · ลืมบวกแล้วอาการคือ *"เพิ่มระยะขอบแล้วปุ่มสูงขึ้นเองแบบไม่มีเหตุผล"*
⚠️ **`choice_button_padding` (24) ไม่ใช่ระยะขอบที่มองเห็น** — เป็นเศษเผื่อทางเทคนิค
กันตัวอักษรตัวสุดท้ายโดนตัด อย่าเอามาใช้จัดหน้าตา

⚠️ **ก๊อป `normal` ของปุ่มมาแก้เฉพาะสีพื้น** ไม่ได้สร้าง StyleBox ใหม่เปล่า ๆ —
วันหลังใส่มุมโค้ง/ขอบในซีน ตอนชี้จะตามเองโดยไม่ต้องกลับมาแก้โค้ด
⚠️ **ทับ `hover` · `hover_pressed` · `pressed` ทั้งสามตัว** — ทับแค่ `hover`
ตอนกดค้างจะเด้งไปใช้สีเทาของธีมมาตรฐาน Godot หนึ่งจังหวะ (ซีนทับไว้แค่ `normal`)
🚨 **ห้ามแตะ `focus` / `font_focus_color`** — สถานะโฟกัสวาดทับบนพื้น `normal` ที่เป็นสีดำ
เปลี่ยนสีอักษรตอนโฟกัสเป็นดำด้วยเมื่อไหร่ ตัวหนังสือจะหายตอนเลื่อนเลือกด้วยคีย์บอร์ด

ปุ่มจัดวางสลับฝั่ง: index 0,2 = ซ้าย · 1,3 = ขวา → **2 ตัวเลือกจะอยู่คนละฝั่ง** ไม่กองกันข้างเดียว
ลำดับอยู่ที่ `CHOICE_BUTTON_PATHS` แก้การจัดวางที่นั่นที่เดียว (ชื่อโหนดในซีนคนละลำดับกับ index โดยตั้งใจ)

**ความกว้างปุ่มถูกบังคับให้เท่ากันจากโค้ด** (`_equalize_choice_widths()` · เพิ่ม 2026-08-20)
`custom_minimum_size` ในซีนเป็นแค่ **ค่าต่ำสุด (200)** — `Button` ยืดตามข้อความเสมอ
บทที่มีตัวเลือกยาวกว่านั้นจะได้ปุ่มสองข้างไม่เท่ากันเอง · โค้ดวัดข้อความที่ยาวสุดในรอบนั้น
แล้วตั้งให้ทุกปุ่มเท่ากัน (เพดาน `choice_button_max_width` = 240)

🚨 **วัดจากฟอนต์เอง ไม่อ่าน `get_minimum_size()`** — ขนาดต่ำสุดของ Control คำนวณใหม่ตอนท้ายเฟรม
อ่านทันทีหลังเซ็ต `text` จะได้ค่าของ**ข้อความรอบก่อน** แล้วปุ่มจะกว้างตามบทที่แล้วเสมอ (ดูเหมือนสุ่ม)
· วิธีเดียวกับที่ `message_bubble.gd` ใช้วัดความกว้างบับเบิลแชท

#### ข้อความยาวแล้วปุ่มไม่ยืดไปชนกันอีกแล้ว (แก้ 2026-08-20 รอบ 8)

🚨 **`custom_minimum_size` เป็น "ค่าต่ำสุด" ไม่ใช่ "ค่าสูงสุด"** — `Button.get_minimum_size()`
คืน `max(custom_minimum_size, ขนาดข้อความ)` เสมอ · เพดาน `choice_button_max_width`
จึงเป็นตัวเลขที่**ไม่เคยมีผลกับภาพเลยสักพิกเซล** ทั้งที่โค้ดขึ้น warning ว่า "หั่นแล้ว"

ต้องแก้ 2 ชั้นถึงจะพอ ขาดชั้นไหนก็ยังเห็นอาการเดิม

| ชั้น | ตั้งที่ | แก้อะไร |
|---|---|---|
| `autowrap_mode = AUTOWRAP_WORD_SMART` | `_setup_choices()` | ข้อความยาว **ตกบรรทัดใหม่ (ปุ่มสูงขึ้น)** แทนการยืดออกข้าง → เพดานบังคับได้จริง |
| `grow_horizontal` | `_align_choice_columns()` | สองคอลัมน์ **โตหนีออกจากกัน** ไม่ใช่โตเข้าหากัน |

⚠️ **เดิมสองคอลัมน์โต "เข้าหากัน" ทั้งคู่** — `LeftBox` ไม่ได้ตั้ง `grow_horizontal`
เลยได้ค่าเริ่มต้น `END` (โตขวา) · `RightBox` ตั้ง `grow_horizontal = 0` (`BEGIN` = โตซ้าย)
ซึ่ง**เอดิเตอร์ใส่ให้เองตอนเลือก `anchors_preset = 6`** (ยึดขอบขวา) ไม่ได้มีใครตั้งใจตั้ง
→ ปุ่มยาวขึ้นเมื่อไหร่ทั้งสองฝั่งวิ่งเข้าหากลางจอพร้อมกัน · ตอนนี้โค้ดดู `anchor_left` แล้วตั้งให้
กล่องที่ยึดขอบขวาโตออกขวา · กล่องที่ยึดขอบซ้ายโตออกซ้าย → **ขอบด้านในถูกตรึง ช่องว่างกลางจอไม่หุบ**
· ดูจาก anchor ไม่ใช่ชื่อโหนด ย้าย/เปลี่ยนชื่อกล่องในซีนแล้วยังถูกอยู่

✅ **ผลพลอยได้: สองคอลัมน์สมมาตรกันเสมอไม่ว่าปุ่มจะกว้างเท่าไหร่**
เดิมสองกล่องยึดขอบไว้ที่ x=700 กับ x=1000 แล้วโตขวาทั้งคู่ → จุดกึ่งกลางของคู่ปุ่ม
**เลื่อนตามความยาวข้อความ** · ตอนนี้ตรึงที่ขอบใน (900 กับ 1000) สมมาตรรอบ **950** คงที่

✅ **สมมาตรกับกลางจอ (960) แล้ว** (แก้ 2026-08-21) — `RightBox` ตั้ง `offset_left = -900`
· `offset_right = -700` → ขอบในอยู่ที่ 1020 คู่กับขอบใน 900 ของ `LeftBox` = สมมาตรรอบ 960 เป๊ะ
(เดิมเป็น −920 / −720 ซึ่งเหลื่อม 10px)
⚠️ **เพราะสองกล่องยึดคนละขอบจอ ระยะนี้จะเพี้ยนตามความละเอียดหน้าจอ** — ย้ายมายึดกลางจอ
ทั้งคู่เมื่อไหร่ถึงจะหายขาด แต่ตอนนี้เกมล็อก viewport 1920×1080 อยู่แล้วจึงยังไม่จำเป็น

**`alignment` ของสองคอลัมน์ถูกตั้งให้ตรงกันจากโค้ด** (`_align_choice_columns()`)
ในซีน `LeftBox` ตั้ง `alignment = 1` แต่ `RightBox` ไม่ได้ตั้ง — ตอนมีข้างละปุ่มยังไม่เห็นผล
แต่พอตัวเลือกมี 3-4 ข้อ (ซ้าย 2 ขวา 1) ปุ่มสองฝั่งจะไม่อยู่ระดับเดียวกัน

⚠️ รับ `Array` ธรรมดา **ไม่ใช่ `Array[String]`** เพราะบทใน `DialogueData` เป็น Array ไม่มีชนิด
ประกาศเป็น typed array แล้วส่งบทเข้ามาตรง ๆ จะ error ตอนรัน

🚨 **ระหว่างรอเลือก บทต้องไม่เดินต่อ** — `_input()` และ `_advance()` เช็ค `_waiting` ก่อนเสมอ
ไม่กันไว้ = ผู้เล่นกด E หนีคำถามได้ แล้วบทเดินต่อทั้งที่ `await` ยังค้างรออยู่
(ปิด `_waiting` **ก่อน** `emit` ด้วย กันกดปุ่มที่สองในเฟรมเดียวกันแล้วยิงสัญญาณซ้ำ)

⚠️ **อย่าสั่งจบบทระหว่างรอคำตอบ** — `await` จะค้างตลอดไป (มี warning เตือนตอนเกิด)

**เลื่อนบทได้ 3 ทาง: กด E · กดปุ่ม `Next` · คลิกซ้ายตรงไหนก็ได้** (ทำงานเหมือนกันทุกประการ)

| ทำตอนไหน | ผลลัพธ์ |
|---|---|
| ตัวอักษรยังขึ้นไม่ครบ | ข้อความขึ้นเต็มทันที (ยังไม่เลื่อนบรรทัด) |
| ข้อความขึ้นครบแล้ว | ไปบรรทัดถัดไป |
| บรรทัดสุดท้าย | ปิดบทสนทนา |
| ระหว่างรอเลือกคำตอบ | ปุ่ม `Next` **ถูกซ่อน** · E และคลิกที่ว่าง **ไม่ทำอะไร** — หนีคำถามไม่ได้ |

ทั้งสามทางเรียก **`_step()` ตัวเดียวกัน** — เพิ่มพฤติกรรมใหม่ต้องแก้ที่นั่นที่เดียว
ไม่งั้นเมาส์กับคีย์บอร์ดจะทำงานคนละแบบ · ปิดการคลิกได้ที่ `@export advance_on_click`

🚨 **คลิกดักที่ `_unhandled_input` ส่วน E ดักที่ `_input` — ต่างกันโดยตั้งใจ**

`_input` ทำงาน**ก่อน**ระบบ GUI · กินคลิกตรงนั้นเมื่อไหร่ **ปุ่ม `Next` กับปุ่มตัวเลือกจะกดไม่ได้
ทั้งหมด** เพราะถูกดักไปก่อนที่ปุ่มจะได้เห็น · `_unhandled_input` ทำงานหลัง GUI →
คลิกที่โดนปุ่มถูกปุ่มกินไปแล้ว เหลือมาถึงเฉพาะคลิกบนพื้นที่ว่าง
**จึงไม่ต้องเขียนโค้ดเช็คเองว่าคลิกโดนปุ่มไหนอยู่** และกดปุ่ม `Next` ครั้งเดียวเลื่อนบรรทัดเดียว

⚠️ E ยังต้องอยู่ที่ `_input` + `set_input_as_handled()` เหมือนเดิม (เหตุผลอยู่ในหัวข้อระบบ NPC —
บทที่จบพอดีกับการกดครั้งนั้นจะเด้งกลับไปเริ่มใหม่ถ้าไม่กินอีเวนต์)

⚠️ **ไม่ชนกับการคลิกคุย NPC ของ `player.gd`** — ตัวนั้นเช็ค `can_move` ซึ่ง `start_dialogue()`
ล็อกไว้ตลอดช่วงที่กล่องคำพูดเปิดอยู่ · `ClickEffect` ใช้ `_input` และไม่กินอีเวนต์ วงกลมยังขึ้นปกติ

⚠️ **โค้ดหาปุ่มด้วย `find_child("Next")` ไม่ใช่ path** — ย้ายปุ่มไปไว้ตรงไหนในกล่องคำพูดก็ได้
แต่ **ชื่อโหนดต้องเป็น `Next` เป๊ะ ๆ** (คงที่ `NEXT_BUTTON_NAME`) · ไม่มีปุ่ม = `push_warning` แล้วเล่นด้วย E ต่อได้ปกติ

🚨 **ตัวแปรเป็น `BaseButton` ไม่ใช่ `Button`** — `TextureButton` (ปุ่มรูปล้วน ที่ซีนใช้อยู่ตอนนี้)
**ไม่ได้สืบทอดมาจาก `Button`** ทั้งคู่แยกสายกันใต้ `BaseButton`
แคสต์เป็น `Button` แล้วเอาปุ่มรูปมาใส่จะได้ null เงียบ ๆ แล้วปุ่มไม่ทำงานทั้งที่วางไว้ถูกทุกอย่าง

🚨 **`TextureButton` + `Ignore Texture Size` = ไม่ยืดตามรูปให้เอง** ลืมลากขนาดจะได้กรอบ 0×0
มองไม่เห็นและกดไม่โดน ทั้งที่ใส่รูปแล้ว — อาการเหมือนสัญญาณไม่ได้ต่อ ทำให้ไล่หาผิดจุด
โค้ดยืดให้เท่าขนาดรูปพร้อมเตือน แต่ควรตั้งในซีนเองเพราะการจัดหน้าจอต้องเห็นผลตั้งแต่ในเอดิเตอร์

✅ **ตั้งขนาดในซีนแล้ว 2026-08-21** — `Next` เป็น `1680,979 → 1744,1043` (64×64 เท่ารูป)
warning ที่เคยขึ้นทุกครั้งที่เปิดฉากหายแล้ว · ตาข่ายรับในโค้ดยังอยู่เผื่อใครลบขนาดทิ้ง

⚠️ **ห้ามต่อสัญญาณ `pressed` จาก editor** — โค้ดต่อให้แล้ว ต่อซ้ำ = กดครั้งเดียวบทเลื่อน 2 บรรทัด
และโค้ดตั้ง `focus_mode = FOCUS_NONE` ให้เอง (ปุ่มที่ถือโฟกัสจะกิน Enter/Space กลายเป็นปุ่มเลื่อนบทที่ไม่ได้ตั้งใจ)

**ทางเข้าบทสนทนา**
- `NPC.gd` — เข้าใกล้ขึ้นบับเบิล กด `interact` (E) แล้ว NPC หันหน้าหาผู้เล่นและเริ่มคุย
- เรียกอัตโนมัติตามเนื้อเรื่องได้ด้วย `start_dialogue()` — รองรับซีนที่ไม่มีผู้เล่น (เช่นบนรถเมล์) แล้ว
- ข้อมูลบทพูดตอนนี้อยู่ใน `DialogData/DialogueData.gd` เป็น **ค่า default ของ `@export`**
  ⚠️ `Game/player/new_resource.tres` ว่างเปล่าและยืมค่า default นั้น — ถ้าแก้ `dialogues` ใน Inspector
  Godot จะบันทึกสำเนาลง `.tres` แล้วบทพูดจะแช่แข็ง แก้สคริปต์ไม่มีผลอีก

### 3. ระบบแชทหลายกลุ่ม (ทำงานได้แล้ว — `Chat.tscn` เรียกจาก `wokeup.gd`)

**แยกเป็น 3 ชั้น: ข้อมูล → ห้องแชท → ตัวคุม**

| ไฟล์ | หน้าที่ |
|---|---|
| `chat_database.gd` (autoload) | ทะเบียนคน (`SENDERS`) + ทะเบียนกลุ่ม (`GROUPS`) + **บทแชททั้งหมด** (`HISTORY` / `INCOMING`) |
| `chat_group.gd` (`Chat_group.tscn`) | ห้องแชท **1 กลุ่ม** — ข้อความ ตัวคั่นเวลา กำลังพิมพ์ ตัวเลือกคำตอบ ปุ่มย้อนกลับ |
| `phone.gd` (`Chat.tscn` ราก `Chat`) | ตัวคุมบาง ๆ — ตัดสินใจว่าโชว์ห้องไหน / เด้งห้องไหนออกมา |
| `message_bubble.gd` | บับเบิลหนึ่งใบ: รูป/ชื่อ/ข้อความ/ซ้ายขวา/ความกว้าง |

**เพิ่มกลุ่มใหม่ = 3 ขั้น ไม่ต้องแตะสคริปต์เลย**
1. เพิ่มรายการใน `ChatDatabase.GROUPS` (ชื่อ + ไอคอน + สมาชิก) และ `HISTORY` / `INCOMING`
2. ลาก `Chat_group.tscn` เข้า `Chat.tscn` อีก 1 instance
3. ตั้ง `group_id` ที่ Inspector ให้ตรงกับคีย์ในข้อ 1

ชื่อกลุ่ม / ไอคอน / `"N online"` ดึงจากทะเบียนทั้งหมด ไม่ต้องกรอกซ้ำในซีน

**`HISTORY` vs `INCOMING` — เส้นแบ่งคือ "ผู้เล่นหยิบโทรศัพท์ขึ้นมาดูตอนไหน"**
ข้อความที่มาถึงก่อนผู้เล่นเปิด UI → `HISTORY` (ขึ้นครบทันที ไม่มีเสียง ไม่มีกำลังพิมพ์)
ข้อความที่มาถึงตอนผู้เล่นดูอยู่ → `INCOMING` (ทยอยเข้า + กำลังพิมพ์ + เสียงเตือน)

**`EVENTS` — บทที่มาถึงตอน "เหตุการณ์ในฉากเกิดขึ้น"** (เพิ่ม 2026-08-20)

| ตัว | ข้อความมาถึงเมื่อ |
|---|---|
| `HISTORY` | ก่อนผู้เล่นเปิดโทรศัพท์ — ขึ้นครบทันทีตอนเปิดห้อง |
| `INCOMING` | ตอนผู้เล่นเปิดห้องนั้นดูอยู่ |
| `EVENTS` | ตอน**เหตุการณ์ในเกมเกิดขึ้น** (วางกล่องเสร็จ ฯลฯ) |

```gdscript
"box_delivered": {"group": "smo", "lines": [{"sender": "chochang", "text": "..."}]},
```

🚨 **อย่าเอาบทที่ผูกกับเหตุการณ์ไปใส่ `INCOMING`** — `INCOMING[group]` ถูกเล่น
**ทุกครั้งที่เปิดห้องนั้น** · ข้อความของโชที่ควรมาหลังวางกล่อง จะไปโผล่ที่ฉาก `wokeup`
ตั้งแต่ตอนเช้าทั้งที่ผู้เล่นยังไม่ได้หยิบกล่องด้วยซ้ำ

⚠️ **`group` ชี้กลุ่มเดิมได้ ไม่ต้องสร้างกลุ่มใหม่** — กลุ่มคือ "ห้องคุย" ไม่ใช่ "ฉากของเนื้อเรื่อง"
เปิดขึ้นมาจะเห็นประวัติเช้าครบแล้วข้อความใหม่ต่อท้าย เหมือนแอปแชทจริง

**รูปแบบบรรทัดในบทแชท**
```gdscript
{"sender": "wawa", "text": "..."}                  # ข้อความปกติ
{"sender": "wawa", "text": "...", "typing": 2.5}   # กำหนดเวลา "กำลังพิมพ์" เอง (วินาที)
{"sender": "wawa", "text": "...", "typing": 0}     # ข้ามช่วงกำลังพิมพ์ เด้งมาเลย
{"sender": "wawa", "text": "...", "sound": false}  # ไม่ต้องมีเสียงเตือนบรรทัดนี้
{"time": "06:30 น."}                               # ตัวคั่นเวลา แทรกได้ทุกจุด
{"choices": [...]}                                 # ถึงคิวผู้เล่นตอบ - หยุดรอจนกด
```

**เสียงเตือน** (`soud_effects/notification.mp3`) ดังพร้อมข้อความโผล่ ไม่ใช่ตอนเริ่มพิมพ์
ดังเฉพาะ `INCOMING` จากคนอื่น — ประวัติเก่าและข้อความที่น้ำฟ้าส่งเองไม่ดัง (ไม่มีใครเตือนตัวเอง)
`AudioStreamPlayer` สร้างจากโค้ด ไม่ต้องเพิ่ม node ในซีน

**ตัวเลือกคำตอบ** — โผล่ท้ายรายการข้อความ **ชิดขวาเหมือนข้อความของน้ำฟ้า** จำนวนต่อรอบไม่จำกัด
กดแล้วอันที่เลือกกลายเป็นข้อความจริงทันที ตัวอื่นหายไปหมด
```gdscript
{"choices": [
    "ตอบสั้น ๆ แล้วจบ",
    {"text": "ตอบแล้วมีคนตอบกลับ", "then": [{"sender": "wawa", "text": "..."}]},
]}
```
⚠️ **ไม่ต้องสร้าง node ในซีน** — `chat_group.gd` สร้างปุ่มเองตอนเรียกใช้แล้วลบทิ้งเมื่อเลือกเสร็จ
ปรับสี/ขนาดได้ที่ `@export` กลุ่ม *"ตัวเลือกคำตอบ"* บน instance

**กลุ่มที่เด้งออกมาข้าง ๆ** — พอกลุ่ม `popout_after_group` คุยจบ กลุ่ม `popout_target_group`
จะไถลออกมาโผล่ข้าง ๆ (ค่าเริ่มต้น: `smo` จบ → `sawat` เด้งไปทางซ้าย 620px)
**ทั้งสองแผงค้างอยู่บนจอพร้อมกัน** ผู้เล่นเห็นว่าเรื่องต่อกันยังไงโดยไม่ต้องกดสลับ
ปรับตำแหน่ง/ความเร็วได้ที่ `@export` กลุ่ม *"กลุ่มที่เด้งออกมา"* บน node `Chat`

**คุยจบแล้วเปลี่ยนฉาก** — ตอบตัวเลือกในกลุ่ม `end_after_group` เสร็จ → ล็อกอินพุตทันที
→ หน่วง `end_delay` → เฟดไป `end_scene` พร้อมฝาก `GameState.pending_intro = end_intro_id`
(ค่าเริ่มต้น: `sawat` จบ → `res://p.tscn` + อินโทร `arrive_bus`)
ตั้งได้ที่ `@export` กลุ่ม *"คุยจบแล้วเปลี่ยนฉาก"* บน node `Chat`

**API หลัก**
```gdscript
$Chat.open_group("smo")        # เปิดห้องเดียว ซ่อนที่เหลือ
$Chat.lock_input()             # ล็อกไม่ให้กดอะไรได้อีก (ปิดปุ่มทุกตัวในกิ่ง ไม่ใช่แค่ซ่อน)
$Chat.popout_group("sawat")    # เด้งออกมาข้าง ๆ โดยไม่ซ่อนห้องอื่น
$Chat.show_list()              # กลับหน้ารายการแชท (ยังไม่มี node ChatList)

var g = $Chat.get_group_view("sawat")
await g.receive_messages(lines)                  # ทยอยเข้ามาพร้อมบับเบิลกำลังพิมพ์
await g.show_choices(["ตอบ A", "ตอบ B"])          # คืน index ที่เลือก
```
น้ำฟ้า (`namfa`) อยู่ฝั่งขวา ไม่โชว์รูปและชื่อ · เข้าห้องซ้ำไม่โหลดบทใหม่ (`_loaded` กัน)

**แถบพิมพ์ข้อความเป็นของประดับ** — `LineEdit` ตั้ง `focus_mode = NONE` + `mouse_filter = IGNORE` จากโค้ด
ผู้เล่นพิมพ์เองไม่ได้ ตอนต้องตอบจริงจะใช้ `show_choices()` แทน
⚠️ **ห้ามตั้ง `line_edit.editable = false`** — LineEdit จะสลับไปใช้ StyleBox ชื่อ `read_only` ไม่ใช่ `normal`
สไตล์แคปซูลมนที่ตั้งไว้จะหายตอนรัน แล้วหน้าตาไม่ตรงกับที่เห็นใน editor

โครงสร้าง node ทั้งหมด + ค่าที่ต้องตั้ง: ดู [CHAT_UI.md](CHAT_UI.md)

### 4. เสียงในเกม

| เสียง | ไฟล์ | อยู่ที่ | พฤติกรรม |
|---|---|---|---|
| ฝีเท้า | `soud_effects/walk.ogg` | `player.gd` | เล่นตอนขยับ หยุดตอนหยุด/ถูกล็อก |
| พิมพ์บทพูด | `soud_effects/keyboard.mp3` | `dialogue_system.gd` | เล่นตอนเริ่มพิมพ์ หยุดตอนพิมพ์จบ |
| ข้อความเข้า | `soud_effects/notification.mp3` | `chat_group.gd` · `wokeup.gd` | ดังพร้อมข้อความโผล่ |
| คลิก | `soud_effects/click.ogg` | `click_effect.gd` (autoload) | สุ่มระดับเสียง ±12% |
| เพลงฉาก `wokeup` | `soud/Morning Bus Stop.mp3` | `wokeup.gd` (node `BG` ในซีน) | วนลูป ค่อย ๆ ดังขึ้น 2 วิ · **ตายพร้อมซีน** |
| เพลงประจำช่วง | `soud/restSong.mp3` | `scripts/music.gd` (แขวนที่ root) | **ข้ามฉาก** เริ่มตอนออกจาก `wokeup` |

### เพลงประจำ "ช่วงของเนื้อเรื่อง" — เล่นข้ามฉาก (`scripts/music.gd`)

**เพลงบางเพลงเป็นของช่วงเนื้อเรื่อง ไม่ใช่ของฉาก** — สั่งครั้งเดียวแล้วเล่นยาวข้ามฉากไปเรื่อย ๆ
จนกว่าจะมีใครสั่งเปลี่ยน (ต่างจากเพลงฉาก `wokeup` ที่เป็นโหนดในซีนและตายพร้อมซีน)

```gdscript
Music.play_track("rest")          ## เริ่มเพลงพัก — เรียกซ้ำเท่าไหร่ก็ไม่รีสตาร์ท
Music.play_track("work_start", 3.0)   ## เปลี่ยนเพลง — ครอสเฟดให้เอง
Music.stop_music()                ## หยุด (ค่อย ๆ เบาลง)
Music.is_track_playing("rest")    ## เช็คว่าเพลงนี้เล่นอยู่ไหม
```

| คีย์ | ไฟล์ |
|---|---|
| `rest` | `soud/restSong.mp3` (5:25) |
| `morning_bus` | `soud/Morning Bus Stop.mp3` |
| `work_start` | `soud/WorkStart.ogg` |

**ตอนนี้สั่งที่เดียว: `parking.gd._ready()`** (`@export music_track` = `"rest"` บนโหนดรากของ `p.tscn`)
`p.tscn` เป็น**ฉากแรกหลังออกจาก `wokeup`** → `restSong` เล่นยาวทุกฉากตั้งแต่นั้น

🚨 **สั่งที่ฉากปลายทาง ไม่ใช่ที่ `phone.gd` ตอนกำลังจะเปลี่ยนฉาก**
ฉาก `wokeup` มีเพลงของตัวเอง (โหนด `BG` ในซีน) — สั่งจากที่นั่นเพลงใหม่จะเริ่มดัง
**ตั้งแต่ยังอยู่ในฉากนั้น** แล้วซ้อนกับเพลงเดิมระหว่างเฟด
แลกกับการมีช่วงเงียบสั้น ๆ ตอนจอดำ ซึ่งเป็นรอยต่อฉากอยู่แล้ว

⚠️ **เรียกก่อน early return ของ `_ready()`** — `parking.gd` คืนค่าออกทันทีถ้าไม่ได้มาจากแชท
วางไว้หลังจากนั้นจะได้เพลงเฉพาะตอนมาทางเนื้อเรื่อง เดินกลับเข้ามาทางอื่นจะเงียบ

🚨 **ไม่ใช่ autoload โดยตั้งใจ** — โปรเจกต์นี้เติม autoload ลง `project.godot` เองไม่ได้
(โดนลบทิ้งเงียบ ๆ มาแล้ว 2 ครั้ง) · ใช้วิธี**แขวนโหนดไว้ที่ `get_tree().root`** ซึ่งรอดจาก
`change_scene_to_file()` เพราะ root ไม่ถูกลบ — กลไกเดียวกับ `scene_transition.tscn`
เข้าถึงผ่าน static method จึงเรียกจากที่ไหนก็ได้โดยไม่ต้องหา node เอง

🚨 **เรียกซ้ำด้วยเพลงเดิม = ไม่ทำอะไรเลย** (เทียบด้วย `resource_path`)
เป็นหัวใจของ "เล่นจนกว่าจะบอกให้เปลี่ยน" — ฉากไหนอยากมั่นใจว่าเพลงถูกก็เรียกได้เลย
ไม่ต้องเช็คก่อน · ถ้ารีสตาร์ททุกครั้งที่เรียก เพลงจะกระตุกกลับต้นเพลงทุกครั้งที่เปลี่ยนฉาก

⚠️ **ชื่อเมธอดคือ `play_track` / `stop_music` / `is_track_playing` ไม่ใช่ `play` / `stop` / `is_playing`**
คลาสนี้ `extends AudioStreamPlayer` ซึ่งมีสามชื่อหลังอยู่แล้ว · ตั้ง static ชนกันจะขึ้น
`Parse Error: Could not resolve external class member` **ที่ไฟล์ผู้เรียก ไม่ใช่ที่ไฟล์นี้** (เจอจริงตอนเขียน)

⚠️ **`add_child` ต้อง `call_deferred` เสมอ** — ผู้เรียกอาจอยู่ใน `_ready()` ของซีนหลัก
ซึ่งตอนนั้น root ยัง busy อยู่ · และมี `static var _pending` กันสร้างซ้ำเมื่อเรียกสองครั้งในเฟรมเดียว
(ตอนนั้น `add_child` ยังไม่เกิด ค้นใน root จึงยังไม่เจอ แล้วจะได้ตัวเล่น 2 ตัวเล่นซ้อนกัน)

| ที่สั่งเปลี่ยนเพลง | ผลที่ได้ |
|---|---|
| **ฉากปลายทาง** (ที่ใช้อยู่) | ไม่ทับเพลงของฉากเดิม · มีช่วงเงียบสั้น ๆ ตอนจอดำ |
| ก่อนเฟดเปลี่ยนฉาก | เพลงคร่อมรอยต่อไม่มีช่วงเงียบ · **แต่ซ้อนกับเพลงของฉากเดิม** |

🚨 **ไฟล์เสียงยาว ๆ ต้องเล่นครั้งเดียวแล้วหยุด ห้ามสั่ง `play()` ซ้ำเป็นจังหวะ**
`walk.ogg` (8.098 วิ) และ `keyboard.mp3` (~19 วิ) เป็นเสียง**ต่อเนื่อง** ไม่ใช่เสียงก้าว/เคาะเดียว
สั่ง `play()` ทุกเฟรมจะได้ยินแค่เศษเสี้ยวแรกวนรัว ๆ (และ mp3 มักมีความเงียบต้นไฟล์ = ไม่ได้ยินอะไรเลย)
**เช็คขนาดไฟล์ก่อนออกแบบเสมอ** — เคยพลาดมาแล้วกับ `keyboard.mp3`

⚠️ **`loop` อยู่ที่ตัวทรัพยากร `AudioStream` ไม่ใช่ที่ `AudioStreamPlayer`** — ตั้งจากโค้ด `stream.loop = true`
ติ๊ก autoplay ใน editor อย่างเดียวได้เพลงที่เล่นรอบเดียวแล้วเงียบตลอดฉาก

`AudioStreamPlayer` ทุกตัวข้างบน**สร้างจากโค้ด** ไม่ต้องเพิ่ม node ในซีน (ผู้เล่นถูก instance หลายซีน)

### 5. เอฟเฟกต์คลิก (ใช้งานได้ทุกซีน)

`click_effect.tscn` เป็น autoload `ClickEffect` → วงกลมกระเพื่อม + เสียงคลิก (สุ่มระดับเสียง ±12%)
ใช้ `_input` ไม่ใช่ `_unhandled_input` เพื่อให้เกิดแม้คลิกโดนปุ่ม และไม่กลืนคลิก (ไม่เรียก `set_input_as_handled()`)

**วงกลมวาดเองด้วย `_draw()`** — `ui_ripple.tscn` = `Ripple` (`ripple.gd`) + ลูก `Circle` (`circle.gd`)

| ไฟล์ | หน้าที่ | `@export` |
|---|---|---|
| `circle.gd` | วาดวงกลมเส้นขอบตาม `radius` | `color` · `stroke_width` · `segments` |
| `ripple.gd` | `play(pos)` — tween รัศมีบาน + จางหาย | `max_radius` · `duration` · `start_alpha` |

⚠️ **วาดเองแทนการ `scale` ทั้งโหนด** — ยืดด้วย `scale` เส้นขอบจะหนาขึ้นตามวงที่บาน
วาดเองจึงได้เส้นบางคงที่ และ tween ตัวเลขรัศมีตรง ๆ ได้
⚠️ `radius` มี setter ที่เรียก `queue_redraw()` — ไม่มีจะเปลี่ยนแต่ตัวเลข ภาพไม่ขยับ

🚨 **จบแล้ว `hide()` ไม่ใช่ `queue_free()` — วงกลมถูกใช้ซ้ำ**
`click_effect.gd` เก็บเป็นสระ (สูงสุด `MAX_RIPPLES` = 12 ใบ) แล้วหยิบใบที่ซ่อนอยู่มาเล่นใหม่
ฉากปลุกน้ำฟ้าต้องกดรัว 20-30 ที การสร้าง/ทิ้งโหนดทุกคลิกคือการจ่ายฟรี
**ห้ามใส่ `queue_free()` กลับเข้ามา** — ใบที่ถูกลบยังค้างในสระ คลิกถัดไปจะเรียก `play()` ใส่โหนดที่ตายแล้ว
· `play()` ฆ่า tween เก่าก่อนเสมอ กันคลิกรัวจนมี tween สองตัวแย่งกันเขียน `radius`

⚠️ **`circle.gd` เทียบชนิดด้วย `get_class() == "Panel"` ไม่ใช่ `self is Panel`** —
สคริปต์ประกาศ `extends Control` ตัวตรวจชนิดตอนคอมไพล์จะฟันธงว่าเป็น Panel ไม่ได้
แล้ว **ขึ้น Parse Error ทั้งไฟล์** ทั้งที่ตอนรันจริงโหนดเป็น Panel ได้

### 6. ระบบ NPC — ซีนแม่ตัวเดียว (ทำงานได้แล้ว · ยังไม่ได้สลับในซีนแมพ)

NPC ทุกตัวสืบทอด (**inherited scene**) จาก `npc/npc_base.tscn` ตัวเดียว
แก้ระบบตรงกลางทีเดียวกระทบทุกตัว แต่ยังปรับ collision / อนิเมชันรายตัวได้ที่ซีนลูก

```
npc/
├── npc_base.tscn / npc_base.gd   ซีนแม่ + สคริปต์ระบบ (class_name NPCBase)
├── npc_data.gd                   Resource ข้อมูลรายตัว (class_name NPCData)
├── scenes/wawa.tscn              ซีนสืบทอด — override เฉพาะที่ต่าง
├── scenes/cho.tscn               ซีนสืบทอดของโชแชง (8 บรรทัด — ไม่ฝัง SpriteFrames ซ้ำ)
└── data/wawa.tres                ข้อมูลวาวา + SpriteFrames
    data/Cho.tres                 ข้อมูลโชแชง + SpriteFrames
    data/bubble_frames.tres       บับเบิล "คุยได้" (ใช้ร่วมกันทุกตัว)
```

**เพิ่ม NPC ใหม่ = 3 ขั้น ไม่ต้องแตะสคริปต์**
1. สร้าง `.tres` ใหม่ใน `npc/data/` (ชื่อ, `dialogue_id`, `SpriteFrames`)
2. `Scene > New Inherited Scene` จาก `npc_base.tscn` → เซฟลง `npc/scenes/`
3. ชี้ช่อง `data` ไปที่ `.tres` แล้วปรับขนาด collision ตามตัว

🚨 **ซีนสืบทอดไม่ต้องฝัง `SpriteFrames` ซ้ำ** — `.tres` มีชุดอนิเมชันอยู่แล้ว และ `npc_base.gd`
เป็น `@tool` → `_refresh_editor_preview()` ยัด `data.frames` ใส่ให้ตั้งแต่ในเอดิเตอร์
**จึงเห็นตัวละครในซีนโดยไม่ต้องก๊อปชุดอนิเมชันมาไว้ในซีน** (`cho.tscn` = 8 บรรทัด)
⚠️ `wawa.tscn` ยังฝัง `SpriteFrames_74owv` ไว้ 220 บรรทัดทั้งที่ `wawa.tres` มีชุดเดียวกัน —
เป็นชุดซ้ำที่โหลดเข้าหน่วยความจำเปล่า ๆ แล้วโดน `_apply_data()` เขียนทับตอนรัน (ยังไม่ล้าง)

**`dialogue_id_after` ควรอยู่ที่ไหน — ซีนสืบทอด vs โหนดในแมพ**

| ตั้งที่ | ใช้เมื่อ | ตัวอย่างจริง |
|---|---|---|
| **ซีนสืบทอด** (`cho.tscn`) | บทคุยซ้ำเป็น**นิสัยของตัวละคร** ใช้ได้ทุกฉาก | โชแชง → `Cho_idle` |
| **โหนดในแมพ** | บทผูกกับ**เหตุการณ์ของฉากนั้น** | วาวาที่ `under_ch_1` → `wawa_idle` (คู่กับ `dialogue_id_override`) |

🚨 **ตั้งในซีนสืบทอด = `talk_once` ใช้ไม่ได้กับตัวละครนั้นทุกตัว** (`npc_base.gd` → `can_interact()`
คืน `true` ทันทีถ้า `dialogue_id_after` ไม่ว่าง) อยากได้ตัวที่พูดครั้งเดียวแล้วเงียบต้องล้างช่องนี้ที่ instance ก่อน

🚨 **ชื่อโหนดลูกใน `npc_base.tscn` ห้ามเปลี่ยน** (`AnimatedSprite2D` `CollisionShape2D`
`InteractArea` `Indicator` `Quest`) — `npc_base.gd` ผูกด้วย `$` path เปลี่ยนแล้วพังทุกตัวพร้อมกัน

#### ไอคอนเควสลอยเหนือหัว — โหนด `Quest` (เพิ่ม 2026-08-21)

`image_ui/Quest.PNG` (64×64 เท่าบับเบิลเป๊ะ → ย่อ 0.4 เท่ากัน)
**ใช้ตำแหน่งและการลอยชุดเดียวกับบับเบิล** — `data.indicator_offset` ·
`indicator_float_distance` · `indicator_float_speed` · ขยับ `indicator_offset` ใน `.tres`
ที่เดียวขยับทั้งคู่

⚠️ **ชื่อไฟล์เป็น `.PNG` ตัวใหญ่** — `res://` ของ Godot case-sensitive ตอน export
พิมพ์ `.png` จะผ่านตอนทดสอบบน Windows **แต่รูปหายตอน build จริง** (กับดักเดียวกับ `item/ItemBox.PNG`)

**เปิด/ปิดรายตัวด้วยช่อง `Visible` ของโหนด `Quest` ใน Inspector**
ไม่ได้ทำ `@export has_quest` แยก เพราะจะเป็นสองที่ที่พูดเรื่องเดียวกัน
แล้วติ๊กไม่ตรงกันเมื่อไหร่จะงงว่าทำไมไอคอนไม่ขึ้น · `_quest_wanted` จำค่านี้ตอน `_ready`

### ช่วงที่ NPC "มีเควสค้างอยู่" — ตั้งใน `.tres` 3 ช่อง (2026-08-21 รอบ 4)

**เงื่อนไขชุดเดียวคุมทั้งไอคอนบนหัวและบทที่พูด** — เข้าเควสก็ขึ้นไอคอนพร้อมเปลี่ยนบท
พ้นเควสก็หายพร้อมกลับบทเดิม · แยกกันไม่ได้เพราะเป็นสถานะเดียวกันของตัวละคร

| ช่องใน `NPCData` | ความหมาย |
|---|---|
| `quest_if` | ชื่อธงบูลใน `GameState` ที่ต้องเป็น **จริง** (ว่าง = ไม่เช็ค) |
| `quest_unless` | ชื่อธงที่ต้องเป็น **เท็จ** (ว่าง = ไม่เช็ค) — ตัวปิดเควส |
| `quest_dialogue_id` | คีย์บทที่ใช้ระหว่างอยู่ในเควส (ว่าง = เปลี่ยนแค่ไอคอน) |

**โชแชง (`Cho.tres`)** = `box_delivered` / `has_bread` / `Cho_break`
→ ก่อนวางกล่อง **บับเบิล** · วางเสร็จ **ไอคอนเควส + บทเบรค** · รับขนมปังแล้ว **กลับเป็นบับเบิล + `Cho_idle`**

| ตั้ง 3 ช่องนี้ไหม | เงื่อนไขไอคอนเควส | ใช้กับ |
|---|---|---|
| **ไม่ตั้ง** | ยังไม่เคยคุยบทหลัก | **วาวา** — *"มีเรื่องมาฝาก จนกว่าจะรับไป"* |
| **ตั้ง** | ธงตามที่ระบุ | **โชแชง** — *"มีเรื่องให้ทำ ตั้งแต่จังหวะที่เนื้อเรื่องกำหนด"* |

🚨 **อยู่ใน `.tres` ไม่ใช่ที่โหนดในแมพ** — เป็นบทบาทของตัวละครในเนื้อเรื่อง เหมือนกันทุกฉาก
(กฎเดียวกับ `dialogue_id`) · เคยทำเป็น `@export` ที่โหนดแล้วย้ายมา เพราะการตั้งที่โหนด
แปลว่าต้องไปตั้งซ้ำทุกฉากที่ตัวละครโผล่

🚨 **ตั้งเงื่อนไขเควสแล้ว "ยังไม่เคยคุยบทหลัก" ไม่ถูกใช้อีก — แทนที่ ไม่ใช่ AND กัน**
ผู้เล่นคุยกับโชไปแล้วตั้งแต่ก่อนวางกล่อง (บท `Cho_intro` มีตัวเลือกถามเรื่องกล่องอยู่ด้วยซ้ำ)
เอามา AND กันเมื่อไหร่ ไอคอนของโชจะไม่มีวันขึ้นเลย

🚨 **บทเควสมาก่อน `dialogue_id_after` ใน `_resolve_dialogue_key()`**
โชตั้ง `dialogue_id_after = "Cho_idle"` ไว้และผู้เล่นคุยไปแล้ว — ไม่มาก่อนเมื่อไหร่
บทเบรคจะไม่มีวันถูกเล่น ได้ *"อยากให้ช่วยอะไรไหม?"* ทั้งที่ไอคอนเควสขึ้นอยู่บนหัว

🚨 **ห้ามเด้งบับเบิลระหว่างบทยังเปิดอยู่** — ธงปิดเควสถูกตั้ง**กลางบท**
(`{"set": "has_bread"}`) ไม่กันไว้ = บับเบิลป่องทับกล่องคำพูดตอนโชกำลังพูด
· `_refresh_head_icon()` เช็ค `_is_dialogue_open()` โดยอ่านสถานะสดจากกล่องคำพูด ไม่เก็บธงเอง

🚨 **`GameState.box_delivered` ต้องมี setter ที่ยิง `flag_changed`** — โชแชงอยู่ในซีนเดียว
กับจุดวางกล่อง (`all_level_2`) ผู้เล่นวางกล่องเสร็จ**โดยไม่มีการโหลดฉากใหม่เลย**
ตัวแปรเปล่า ๆ = ไอคอนไม่สลับจนกว่าจะเดินออกจากฉากแล้วกลับเข้ามา ซึ่งดูเหมือนระบบไม่ทำงาน

⚠️ **`_refresh_head_icon()` แตะบับเบิลด้วย ต่างจาก `_refresh_quest_icon()`**
เควสมาถึงตอนผู้เล่นยืนอยู่ข้าง ๆ พอดีคือกรณีปกติของโชแชง (วางกล่องเสร็จแล้วเดินมาหา)
เปลี่ยนแค่ไอคอนเควสจะเห็นสองอันทับกัน

⚠️ **ต่อ `flag_changed` ทุกตัวไม่ว่าจะตั้ง `quest_if` หรือไม่** — เคยเขียนเป็น
`if not quest_if.is_empty()` เพื่อประหยัด แต่นั่นผูกการต่อสัญญาณไว้กับค่าที่อ่านครั้งเดียว
ตอน `_ready()` · ใครตั้งจากโค้ดทีหลังจะไม่ได้รับสัญญาณโดยที่ค่าในตัวแปรถูกต้องทุกอย่าง

**สลับไอคอนตามว่าเคยคุยบทหลักหรือยัง** (แก้ 2026-08-21 รอบ 2 · ใช้เมื่อ `Quest If` ว่าง)

| จังหวะ | ไอคอน | ความหมาย |
|---|---|---|
| ยังไม่เคยคุยบทหลัก | **Quest** (โชว์ตลอด เห็นได้จากไกล) | "ตัวนี้มีเรื่องมาฝาก" |
| ระหว่างคุย | ไม่มีเลย | กล่องคำพูดครองจอแล้ว |
| คุยบทหลักจบแล้ว | **Bubble** (เฉพาะตอนเข้าระยะ) | "คุยได้" ตามปกติ |

สองอันอยู่จุดเดียวกันเป๊ะ โชว์พร้อมกันจะทับกันเป็นก้อนเดียวอ่านไม่ออก —
**ครั้งแรกที่เจอ Quest ชนะ** เพราะผู้เล่นควรอ่านได้ว่า "มีเรื่องมาฝาก" ไม่ใช่แค่ "คุยได้"

🚨 **ผูกกับ `_main_dialogue_used` ไม่ใช่ `_talked`** — `_talked` ถูกตั้งตั้งแต่ *เริ่ม* คุย
ซึ่งเกิด**ก่อน**ดึงบทพูด · และถ้าพิมพ์คีย์บทผิดจนหาบทไม่เจอ `_main_dialogue_used`
จะไม่ถูกตั้ง = ไอคอนเควสยังอยู่ ซึ่ง**ถูกแล้ว** เพราะผู้เล่นยังไม่ได้รับเรื่องอะไรไปจริง ๆ

🚨 **`_refresh_quest_icon()` ใน `interact()` ต้องอยู่หลัง `_on_interact()`**
`_main_dialogue_used` ถูกตั้งตอนดึงบทพูดในนั้น — เรียกก่อนจะอ่านค่าเก่า
แล้ว**ไอคอนเควสจะลอยค้างอยู่ระหว่างที่กล่องคำพูดเปิดอยู่**

⚠️ **ไม่ใช้ธง `_in_dialogue` แยก** — `_on_interact()` มีทางออกก่อนเปิดบทได้ 2 ทาง
(ไม่มีบทตามคีย์นั้น / หา `dialogue_ui` ไม่เจอ) แล้วธงจะค้างจน**ไอคอนหายถาวร**
· อ่านสถานะจริงแทน = บทไม่ได้เปิด ไอคอนก็กลับมาเอง

🚨 **`_update_float_process()` ต้องดูทั้งสองตัว** — เดิม `hide_indicator()` สั่ง
`set_process(false)` ทื่อ ๆ · เดินออกนอกระยะแล้วไอคอนเควสจะ**ค้างนิ่งกลางอากาศตลอดฉาก**

⚠️ **นับเวลาลอยคนละตัว (`_quest_time` / `_indicator_time`)** — ตัวหลังถูกรีเซ็ตเป็น 0
ทุกครั้งที่บับเบิลป่องออกมา (ตั้งใจ จะได้เริ่มจากจุดกลาง) ใช้ร่วมกันเมื่อไหร่
ไอคอนเควสจะกระตุกทุกครั้งที่ผู้เล่นเดินเข้าระยะ
⚠️ **ขยับ `offset` ไม่ใช่ `position`** — `position` คือจุดที่ `indicator_offset` ตั้งไว้
เขียนทับเมื่อไหร่ ตำแหน่งจาก `.tres` หายตั้งแต่เฟรมแรกที่ลอย

🚨 **ตอนนี้ `Quest` ใน `npc_base.tscn` ยังติ๊ก `Visible` ไว้ = NPC ทุกตัวมีไอคอนเควส**
⚠️ *ต้องไปติ๊กออกเอง* — ควรปิดที่ซีนแม่ให้เป็นค่าเริ่มต้น แล้วค่อยติ๊กเปิดเฉพาะตัวที่มีเควสจริง

#### 🚨 `null` ในซีนชนะค่า default ของ `@export` เสมอ (เจอจริง 2026-08-21)

`npc_base.tscn` โผล่บรรทัด **`quest_texture = null`** + **`quest_scale = null`** ขึ้นมาเอง
จากเอดิเตอร์ (กับดักเดียวกับ `initial_facing = null` ที่โหนด `Cho`) ผลคือ:

| ชั้นที่พัง | อาการ |
|---|---|
| `quest_texture` เป็น null | โหนด `Quest` ไม่มีรูป → วาดอะไรไม่ออก |
| `Quest` ยัง `visible = true` | `show_indicator()` early-return → **บับเบิลไม่ขึ้นด้วย** |

**รวมกัน = ไม่มีไอคอนอะไรบนหัว NPC เลย โดยไม่มี error สักบรรทัด**

แก้แล้ว 2 ชั้น (ขาดชั้นใดชั้นหนึ่งก็ยังเงียบ):
- `_quest_icon_texture()` / `_quest_icon_scale()` ถอยไปใช้ `QUEST_TEXTURE_FALLBACK` /
  `QUEST_SCALE_FALLBACK` ซึ่งเป็น `const` ที่ซีนเขียนทับไม่ได้ + `push_warning` ครั้งเดียวต่อรัน
- `_should_show_quest()` เช็ค `quest.texture != null` ด้วย — **ไอคอนที่วาดอะไรไม่ออก
  ต้องไม่ไปกดบับเบิลไม่ให้ขึ้น** ถอยไปใช้บับเบิลตามปกติดีกว่าเงียบทั้งคู่

⚠️ **`quest_scale` เทียบกับ `Vector2.ZERO` ไม่ใช่ null** — Godot แปลง `null` ในซีนเป็น (0,0)
ให้ · scale 0 = มองไม่เห็นเหมือนกัน แต่หาสาเหตุยากกว่าเพราะโหนดมีรูปครบและ `visible = true`

⚠️ **ค่า default ที่เขียนไว้ที่ `@export` ในโปรเจกต์นี้ไว้ใจไม่ได้** — ของที่ขาดไม่ได้จริง ๆ
ต้องมี `const` สำรองคู่กันเสมอ

| ชั้น | `collision_layer` | `collision_mask` |
|---|---|---|
| `NPCBase` | 4 (**Layer 3**) | 1 |
| `InteractArea` | 0 | 1 (`monitorable = false`) |

ผู้เล่นอยู่ layer 1 + กลุ่ม `"player"` → `InteractArea` จับเจอ

#### 🚨 เดินชน NPC — `player.gd` เปิดบิต 4 ให้ `collision_mask` เอง (แก้ 2026-08-21)

**เดิมผู้เล่นเดินทะลุ NPC ทุกฉาก** เพราะ **ทุกซีนตั้ง `collision_mask` ของผู้เล่นเอง
และตั้งไม่ตรงกัน — ไม่มีซีนไหนเปิดบิต 4 เลย**

| ซีน | `collision_mask` ที่ override ไว้ | ชนอะไรได้ |
|---|---|---|
| `Game/player/player.tscn` (ต้นฉบับ) | **ไม่ได้ตั้ง** → default 1 | layer 1 |
| `under.tscn` · `level_2.tscn` | 16 | กำแพงอย่างเดียว |
| `level1.tscn` | 17 | layer 1 + กำแพง |
| `p.tscn` | 17 (+ `collision_layer` = 17) | layer 1 + กำแพง |

🚨 **แก้ที่ `player.tscn` ที่เดียวไม่พอ — override ที่ instance ชนะค่าจากซีนต้นฉบับเสมอ**
`player.gd._ready()` จึงเรียก `_ensure_npc_collision()` ซึ่งทำ `collision_mask |= 4`
ได้ผลทุกฉากรวมถึงฉากที่ยังไม่ได้สร้าง

⚠️ **`|=` ไม่ใช่ `=`** — ทับทื่อ ๆ จะลบบิตกำแพง (16) ทิ้ง **แล้วผู้เล่นจะเดินทะลุกำแพงแทน**
⚠️ **ทางที่สะอาดกว่าคือไล่ลบ `collision_mask` ที่ override ไว้ทั้ง 4 ซีนทิ้ง** แล้วตั้งค่าเดียว
ที่ `player.tscn` — ยังไม่ทำเพราะต้องแก้ `.tscn` 4 ไฟล์ และยังไม่รู้ว่าทำไม `p.tscn`
ถึงตั้ง `collision_layer = 17` (อาจตั้งใจ อาจเผลอ)

⚠️ **`npc_base.tscn` ไม่มีบรรทัด `collision_mask` แล้ว** (Godot ตัดทิ้งตอน resave เพราะเป็น
ค่า default 1) — **ไม่ได้แปลว่าหาย** ค่ายังเป็น 1 เหมือนเดิม

**ชื่ออนิเมชันตั้งได้รายตัวใน `NPCData`** (`default_anim` · `anim_up` · `anim_side`)
⚠️ ชุดของวาวาใช้ `idle` เฉย ๆ **ไม่ใช่ `idle_down`** แบบที่ `NPC.gd` เดิมเรียก
ชื่อไม่ตรงจะขึ้น warning บอกว่ามีชุดอะไรบ้างแล้วเล่นชุดแรกแทน — ไม่ crash ไม่พ่น error ทุกเฟรม

**ตัวละครคนเดียวกันพูดคนละเรื่องตามฉาก → `dialogue_id_override` ที่โหนด**
วาวาใน `decor/under_ch_1.tscn` กับวาวาใน `level_2.tscn` ใช้ `wawa.tres` ใบเดียวกัน
(สไปรท์ ชื่อ ท่ายืน เหมือนกันหมด) ต่างกันแค่บทพูด → ตั้งคีย์ทับที่ Inspector ของ instance นั้น
ปล่อยว่าง = ใช้ `dialogue_id` จาก `.tres` ตามปกติ

🚨 **อย่าแก้ `dialogue_id` ใน `.tres` เพื่อเปลี่ยนบทของฉากใดฉากหนึ่ง** — เปลี่ยนพร้อมกันทุกฉาก
และก๊อป `.tres` แยกใบก็จะได้ `SpriteFrames` ซ้ำอีกชุด = ปัญหาเดิมที่ระบบซีนแม่หนีมา

| ที่ตั้ง | ใช้เมื่อ |
|---|---|
| `dialogue_id` ใน `.tres` | บทประจำตัวที่ใช้เป็นค่าตั้งต้นทุกฉาก |
| `dialogue_id_override` ที่โหนด | บทเฉพาะฉาก/บทนั้น ๆ |

**บทพูดดึงจาก `DialogueData` โดยไม่ผ่าน `.tres`** — ปล่อยช่อง `dialogue_data` ว่างไว้
`npc_base.gd` จะ `DialogueData.new()` เอาค่า default จากสคริปต์ จึงตรงกับ `DialogueData.gd` เสมอ
🚨 **จงใจไม่ preload `new_resource.tres`** เพราะไฟล์นั้นว่างและยืมค่า default อยู่ —
เผลอแก้ใน Inspector เมื่อไหร่บทพูดจะแช่แข็งทันที (ดูคำเตือนหัวข้อกล่องคำพูด)

**ทิศที่ยืนหันตอนเปิดฉาก — `initial_facing`** (`@export` ที่โหนด · เพิ่ม 2026-08-20)

| ค่า | ท่าที่เล่น | flip |
|---|---|---|
| `DEFAULT` | `data.default_anim` ตามเดิม ไม่ทับอะไร | ไม่แตะ |
| `DOWN` | `data.default_anim` | ❌ |
| `UP` | `data.anim_up` | ❌ |
| `LEFT` | `data.anim_side` | ❌ |
| `RIGHT` | `data.anim_side` | ✅ |

**ตั้งที่โหนดในซีนแมพ ไม่ใช่ใน `.tres`** — ทิศที่ยืนหันขึ้นกับ**ตำแหน่งที่วาง** เหมือน `position`
คนเดียวกันยืนหันข้างริมทางเดินในฉากหนึ่ง แล้วหันหน้าเข้าห้องในอีกฉากได้

🚨 **อย่าแก้ `default_anim` ใน `.tres` เพื่อให้ยืนหันข้าง** — `face_player()` ใช้ค่านั้นเป็น
"ท่าหันลง" ด้วย เปลี่ยนแล้วตัวละครจะ**หันลงไม่เป็นอีกเลยทุกฉาก**

⚠️ **ภาพที่ไม่ flip คือหันซ้าย** — ต้องตรงกับ `face_player()` ที่ใช้ `flip_h = diff.x > 0`
(ผู้เล่นอยู่ทางขวา → flip) · คิดกลับด้านแล้วคุยจบจะรีเซ็ตไปหันคนละทางกับตอนแรก

⚠️ **ค่านี้ถูกเก็บเป็น `_initial_anim` / `_initial_flip`** → คุยจบแล้ว `reset_direction()`
พากลับมาหันข้างเหมือนเดิม ไม่ใช่ค้างหันหน้าตลอดกาล
· `_apply_initial_facing()` ต้องถูกเรียก**หลัง** `_play_default_anim()` (ตัวนั้นเป็นที่เดียวที่มี
fallback + warning ตอนชื่อชุดไม่ตรง) และ**ก่อน**บรรทัดที่จำ `_initial_anim`

**เริ่มคุย = หันหน้าเข้าหากันทั้งคู่** · NPC หันด้วย `face_player()` · ผู้เล่นหันด้วย `player.face_toward()`
ทั้งคู่**สแนปเข้าทิศตรง** (บน/ล่าง/ข้าง) โดยดูว่าแกนไหนห่างกว่า — กฎเดียวกันจะได้หันเข้าหากันจริง
⚠️ ฝั่งผู้เล่นเรียกจาก `interact()` **ไม่ใช่ `_on_interact()`** เพราะตัวหลังให้ซีนลูก override
NPC ที่เขียนพฤติกรรมพิเศษทับโดยไม่เรียก `super()` จะได้ไม่หลุดข้อนี้
⚠️ `face_toward()` ตั้ง `animation_direction` ด้วย ไม่ใช่แค่ `play()` ครั้งเดียว —
`player._process()` เล่นท่ายืนจากค่านั้นทุกเฟรมระหว่างถูกล็อก ไม่ตั้งจะโดนทับในเฟรมถัดไป

**กด E — ผู้เล่นเป็นคนเลือกเป้าหมาย** (ดูหัวข้อ 10) `NPCBase` **ไม่ดักปุ่มเองแล้ว**
แค่ลงทะเบียนตัวเองเข้า `player.nearby_targets` ตอนเข้าระยะ แล้วรอให้ผู้เล่นเรียก `interact()`
- 🚨 **อย่าเพิ่ม `_unhandled_input` กลับเข้า `NPCBase`** จะกลายเป็นกด E ครั้งเดียวทำงานสองทาง
- 🚨 **`dialogue_system._input()` ต้อง `set_input_as_handled()` ด้วย** (แก้แล้ว 2026-08-15)
  การเช็ค `is_talking` อย่างเดียวกันไม่ได้ในจังหวะที่ **บทจบพอดีกับการกดครั้งนั้น** —
  `_advance()` → `end_dialogue()` → `is_talking = false` เกิดในเฟรมเดียวกัน
  อีเวนต์เดิมไหลต่อไปถึง NPC ที่เห็นว่า "ไม่ได้คุยอยู่" แล้วเริ่มบทใหม่ทันที
  **อาการ: กด E ปิดบทแล้วบทเด้งกลับไปเริ่มบรรทัดแรกเอง**

**`dialogue_id_after` — บทที่ใช้หลังคุยบทหลักจบแล้ว** (ตั้งที่ Inspector ของ instance)
เนื้อเรื่องเดินไปแล้วแต่ผู้เล่นยังเดินกลับมากด E ได้ · บทหลักเล่นซ้ำไม่ได้ (วาวาจะฝากกล่องซ้ำ)
แต่ไม่ขึ้นอะไรเลยก็เหมือนเกมค้าง → มีบทสั้น ๆ ไว้ตอบแทน (ตอนนี้วาวาใช้ `wawa_idle`)

**`wawa_idle` = ที่เก็บ "คำใบ้ว่าต้องทำอะไรต่อ" ไม่ใช่แค่บทกันความเงียบ**
ผู้เล่นที่ลืมว่ากล่องอยู่ไหนย่อมกลับมาถามคนที่ฝากงาน → ตัวเลือก *"กล่องไหนเหรอคะ"*
บอกทั้งที่ตั้ง (บนโต๊ะ) และวิธีสังเกต (เดินใกล้แล้วมีแสงสีเหลือง = อนิเมชัน `nearby` ของ `interact_box.gd`)
⚠️ **บทนี้ยังไม่ได้ใส่เงื่อนไข** — หยิบ/วางกล่องไปแล้วกลับมาถามซ้ำจะได้คำใบ้เดิม
ตอนนี้ระบบรองรับ `"if"` / `"unless"` แล้ว (ดูหัวข้อตัวเลือกด้านบน) เติม
`"unless": "box_delivered"` ที่ข้อ *"กล่องไหนเหรอคะ"* ได้เลยถ้าอยากให้เนียน — ยังไม่ทำเพราะยังไม่มีใครขอ

🚨 **ตั้งค่านี้แล้ว `talk_once` ไม่มีผล** — คนละความตั้งใจกัน
`talk_once` = *"พูดจบแล้วเงียบ"* · `dialogue_id_after` = *"พูดจบแล้วเปลี่ยนเรื่อง"*

⚠️ **ช่องคีย์บททุกช่องถูก `strip_edges()` ให้อัตโนมัติ + เตือนครั้งเดียวต่อค่า**
ช่องว่างที่พิมพ์ติดมาใน Inspector **มองไม่เห็นด้วยตา** แต่ทำให้หาบทไม่เจอ
อาการคือกด E แล้ว "ไม่มีอะไรเกิดขึ้น" ซึ่งดูเหมือนระบบพังทั้งระบบ (เกิดจริงแล้ว — `"wawa_idle "`)

⚠️ **`_main_dialogue_used` แยกจาก `_talked` เพราะจังหวะต่างกัน**
`_talked` ตั้งตั้งแต่ *เริ่ม* คุย ซึ่งเกิด**ก่อน**ดึงบทพูด — เอามาตัดสินบทตรง ๆ
จะกลายเป็นว่าคุยครั้งแรกก็ได้บทสำรองไปเลย · และตั้งธงเฉพาะตอนที่บทหลักมีอยู่จริง
(พิมพ์คีย์ผิดแล้วนับว่าคุยไปแล้ว = บทหลักหายทั้งที่ยังไม่เคยเล่น)

**`talk_once` — คุยได้ครั้งเดียว** ติ๊กที่ Inspector ของ instance นั้น
คุยจบแล้วกด E ไม่ขึ้นอะไรอีก และบับเบิลไม่กลับมา (ทั้งสองอย่างคุมด้วย `can_interact()` จุดเดียว)
ใช้กับ NPC ที่บทเป็น "เหตุการณ์" ไม่ใช่ "คำทักทาย" — บทที่คุยซ้ำได้จะกลายเป็นฝากงานซ้ำ
ตั้งที่โหนดไม่ใช่ใน `.tres` เพราะวาวาที่ `level_2.tscn` ยังต้องคุยซ้ำได้ตามปกติ

⚠️ **วาวาที่ `under_ch_1` ไม่ได้ใช้ `talk_once` — และไม่ควรใช้** เพราะโหนดนั้นตั้ง
`dialogue_id_after = "wawa_idle"` ไว้ ซึ่ง `can_interact()` เช็คก่อนแล้ว `return true` ทันที
→ **ติ๊ก `talk_once` ที่โหนดนั้นเป็น no-op** · การกันฝากกล่องซ้ำทำโดย `_main_dialogue_used`
ที่สลับไปบท `wawa_idle` หลังบทหลักเล่นจบ ไม่ใช่โดยการปิดปุ่มคุย

⚠️ **จำแค่ภายในฉาก** — เดินออกแล้วกลับเข้ามา NPC ถูกสร้างใหม่ ค่ารีเซ็ต คุยได้อีก
อยากให้จำถาวรต้องผูกกับ `GameState` (ยังไม่ได้ทำ)

**สถานะการย้ายมาใช้ระบบใหม่** (อัปเดต 2026-08-20)

| ฉาก | NPC ที่มี |
|---|---|
| `decor/under_ch_1.tscn` | ✅ วาวา — ระบบใหม่ (`npc/scenes/wawa.tscn`) |
| `level_2.tscn` | **ไม่มี NPC เหลืออยู่แล้ว** — วาวาตัวเก่าถูกถอดออกไปตอนแยกซีน `AllLevel2` |
| `Game/game.tscn` | ยังใช้ `wawa.tscn` + `NPC.gd` ตัวเก่า |

**โชแชง (`npc/scenes/cho.tscn` + `npc/data/Cho.tres`)** — ✅ ทำซีนสืบทอดแล้ว (2026-08-20)
วางที่ `all_level_2.tscn` → `level2_2/Cho` (85, 1900) · ชื่ออนิเมชัน `idle` / `idle_side` / `idle_up` ✅
`cho.tscn` ถือ `data` + `dialogue_id_after = "Cho_idle"` ให้แล้ว — **โหนดในแมพเหลือแค่ `position`**

**บทของโชแชงมี 2 ใบ — คุยครั้งแรกกับคุยซ้ำพูดคนละอย่าง**

| ครั้ง | คีย์ | ทักทายว่า | ตัวเลือก |
|---|---|---|---|
| แรก | `Cho_intro` | "อรุณสวัสดิ์น้ำฟ้า มีอะไรรึเปล่า?" | *กล่อง* (เฉพาะตอนถือกล่อง · น้ำฟ้าพูดจริงว่า "กล่องวางตรงไหนเหรอคะ") · *ไม่มีค่ะ* |
| ถัดไป | `Cho_idle` | "อยากให้ช่วยอะไรไหม?" | *ไม่มีค่ะ* |

✅ **`Dialogue Id After` = `Cho_idle` อยู่ใน `cho.tscn` แล้ว** ไม่ต้องตั้งที่โหนดในแมพ
ไม่มีค่านี้ = ทักทาย "อรุณสวัสดิ์" ซ้ำทุกครั้งที่กด E ซึ่งทำให้ตัวละครดูเหมือนลืมว่าเพิ่งคุยกันไป
· ระวังช่องว่างท้ายคีย์ (มองไม่เห็นด้วยตา · โค้ด `strip_edges()` ให้พร้อมเตือน แต่เคยเสียเวลาไล่หามาแล้ว)
✅ **`decor/under_ch_1.tscn` โหนด `Wawa` เคยมีช่องว่างท้าย `"wawa_idle "` — ลบแล้ว 2026-08-21**
ทำงานได้อยู่แล้ว (โค้ด `strip_edges()` ให้) แต่ `push_warning` ทุกครั้งที่เล่น
🚨 **warning ประจำแบบนั้นคือสิ่งที่ทำให้ warning จริงกลืนหายไปในกองข้อความ** — เจอเมื่อไหร่ให้ลบทันที

⚠️ **`Cho_idle` ไม่มีตัวเลือกถามเรื่องกล่อง** — ตั้งใจตามที่ผู้ใช้ระบุ
ผู้เล่นที่ยังหาห้องรับรองไม่เจอยังมีคำใบ้อีก 2 ทาง (เส้น `Line2D` กระพริบตอนเข้าเขต `RoomBox`
และบท `wawa_idle`) จึงไม่ตัน · อยากให้ถามซ้ำได้ก็ก๊อปข้อ `"if": "carrying_box"` จาก `Cho_intro` มาใส่

⚠️ **`cho.tscn` ยังไม่ override `CollisionShape2D`** — รับค่าจากซีนแม่ (24×14 ที่ y = −6)
ปรับให้พอดีตัวโชแชงได้ที่ไฟล์นี้ แล้วมีผลกับโชแชงทุกฉากพร้อมกัน (คือเหตุผลที่ทำซีนสืบทอด)

🚨 **`level2_2` ที่ห่อโชแชงอยู่ยังไม่ได้ติ๊ก `y_sort_enabled`** ⚠️ *ต้องไปทำเอง*
→ โชแชงจะถูกวาดยกกลุ่มที่ (0,0) **จมอยู่หลังพื้นห้อง**

**คีริน (`npc/Kirin.tres`)** — ยังไม่เสร็จ
⚠️ **อยู่ผิดโฟลเดอร์** (ควรอยู่ `npc/data/`) และ**ยังไม่มี `frames`** → ลากลงฉากตอนนี้จะมองไม่เห็นตัว
· บท `kirin_intro` เขียนรอไว้แล้ว

⚠️ **อย่าสลับด้วยการแก้ `path=` ใน `ext_resource`** — โครงสร้างโหนดคนละแบบ
(เก่า: `Area2D` + `AnimatedSprite2D2` / ใหม่: `InteractArea` + `Indicator`)
override กับ signal ที่ซีนแมพเก็บไว้จะชี้ไปโหนดที่ไม่มีอยู่ · ต้องลบตัวเก่าแล้วลากตัวใหม่เข้าไปใน editor

⚠️ **สลับจาก `npc_base.tscn` ตรง ๆ → ซีนสืบทอด ก็ต้องลบแล้วลากใหม่เหมือนกัน** ถึงโครงสร้างโหนด
จะตรงกันเป๊ะ (ซีนสืบทอดใช้ฐานเดียวกัน) แต่ `data = ExtResource(...)` ที่ค้างอยู่ที่โหนดในแมพจะกลาย
เป็น override ทับค่าจากซีนสืบทอด — วันหลังเปลี่ยน `.tres` ที่ซีนสืบทอดแล้วฉากนั้นจะไม่ตาม
**ให้เหลือ `position` อย่างเดียวที่โหนดในแมพ**

### 7. ของตกแต่งสลับตามบท — ห้องใบเดียวใช้ได้ทุกบท (ทำงานได้แล้ว · ใช้ที่ `under.tscn`)

ห้องเดิมทั้งใบ (พื้น กำแพง บันได จุดเกิด ผู้เล่น) อยู่ในซีนเดียวตลอดทุกบท
**ของตกแต่ง + NPC ที่เปลี่ยนตามเนื้อเรื่อง** ถูกแยกเป็นซีนย่อยใน `decor/` แล้วเสียบเข้ามาตอนรัน
→ แก้เลย์เอาต์ทีเดียวได้ผลทุกบท ไม่ต้องก๊อปซีนห้องทั้งใบต่อบท

```
Under (Node2D)  ← แนบ scripts/room.gd
├── Tile_set · StairArea2D · Area2D3 · SpawManager · player   ← ของถาวร ไม่ถูกแตะ
└── ChapterSlot (Node2D)   ← ว่างเปล่าตอนแก้ซีน สคริปต์ใส่ของให้ตอนรัน
```

**เพิ่มบทใหม่ = 2 ขั้น ไม่ต้องแตะสคริปต์**
1. สร้างซีนของตกแต่งบทนั้นใน `decor/` (เช่น `under_ch_2.tscn`)
2. ลากใส่ช่องถัดไปของ `chapter_decors` บนโหนดราก `Under` (**index 0 = บท 1**)

ใช้ `Array[PackedScene]` ไม่ใช่ `Dictionary` เพราะลากซีนใส่ Inspector ได้ตรง ๆ
**ช่องที่ปล่อยว่าง (null) = บทนั้นห้องโล่ง** ไม่ใช่ error (ตอนนี้บท 2 ตั้งเป็น null ไว้เป็นที่จองไว้)
บทที่เกินขอบ array → `push_warning` แล้วคงของเดิมไว้ ไม่ crash

**สลับบทตอนรัน:** `GameState.chapter = 2` — ห้องที่เปิดอยู่สลับของให้เองผ่าน `chapter_changed`
ไม่ต้องโหลดฉากใหม่ · ตั้งค่าเดิมซ้ำไม่ทำอะไร (setter เช็คก่อน emit)

🚨 **`_apply_chapter()` ล้างเฉพาะลูกของ `ChapterSlot`** — `player` / `Tile_set` / `SpawManager` อยู่นอก slot จึงรอดเสมอ
ห้ามย้ายของถาวรเข้าไปไว้ใน slot เด็ดขาด

⚠️ **`remove_child()` ก่อน `queue_free()`** — `queue_free` ลบจริงตอนท้ายเฟรม
ถ้าไม่ถอดออกจาก tree ก่อน ของบทเก่ากับบทใหม่จะซ้อนกัน 1 เฟรม แล้วชื่อโหนดชนกันจนโดนเปลี่ยนชื่อ

⚠️ **`ChapterSlot` ต้องเปิด `y_sort_enabled` ตาม `Under`** และซีนของตกแต่งต้องเปิดที่โหนดรากด้วย
Godot จัดลำดับ Y เฉพาะลูกโดยตรงของโหนดที่เปิด y-sort · โหนดกลางที่ปิดไว้จะถูกวาดยกกลุ่ม
ที่พิกัดของตัวมันเอง (0,0) → เก้าอี้กับ NPC จะจมอยู่หลังทุกอย่าง

⚠️ **`ChapterSlot` ต้องอยู่ที่ (0,0)** ไม่งั้นของตกแต่งจะเลื่อนจากตำแหน่งที่จัดไว้ตอนแก้ซีน

### 8. อนิเมชันผู้เล่น — เลือกชุดตามทิศ (`Game/player/player.gd`)

`play_animation(prefix, dir)` ต่อ prefix (`"walk"` / `"idle"`) เข้ากับส่วนท้ายตามทิศ
เดินเฉียง (ทั้งสองแกนเกิน `DIAGONAL_THRESHOLD` = 0.35) แยกไปที่ `_play_diagonal()`

| ทิศ | ชุดที่ใช้ | flip |
|---|---|---|
| บน / ล่าง / ข้าง | `_up` · `_down` · `_side` | `_side` flip เมื่อไปขวา |
| เฉียงซ้ายบน | `_obliquelyup` | ไม่ flip (วาดซ้ายบนอยู่แล้ว) |
| เฉียงขวาบน | `_obliquelyup` | **flip** |
| เฉียงซ้ายล่าง | `_obliquelyL` | ไม่ flip |
| เฉียงขวาล่าง | `_obliquelyR` | ไม่ flip |

**เพิ่มชุดที่วาดตรงทิศ = เติมชื่อลง `DIAGONAL_EXACT_SUFFIX` ที่เดียว** ไม่ต้องแตะตรรกะ
และมีผลกับทุก prefix ทันที (วาด `idle_obliquelyL` เมื่อไหร่ ยืนนิ่งเฉียงซ้ายล่างจะหยิบไปใช้เอง)

ลำดับการเลือก: **ชุดที่วาดตรงทิศ → ชุดที่ต้อง flip → `_side`**
เลือกชุดที่วาดตรงทิศก่อนเพราะภาพที่ flip มาจะกลับด้านทั้งตัว (กระเป๋าสลับข้าง แสงเงาผิดด้าน)

⚠️ **`walk_obliquelydown` ไม่มีแล้ว** — ถูกแยกเป็น `_obliquelyL` / `_obliquelyR` (2026-08-12)
ชื่อเดิมยังค้างอยู่ใน `DIAGONAL_FLIP_FALLBACK` เป็นตาข่ายรับเท่านั้น ไม่ได้ทำงานจริงแล้ว

**ถือของ → ชุด `_box`** ตั้ง `player.carrying = true` แล้วทุกท่าสลับเป็นชุดที่ลงท้าย `_box` เอง
ไม่ต้องเรียกอะไรเพิ่ม เพราะ `_process()` เล่นอนิเมชันใหม่ทุกเฟรมอยู่แล้ว

```gdscript
player.carrying = true    # หยิบของขึ้นมา -> walk_obliquelyL_box ฯลฯ
player.carrying = false   # วางลง
```

⚠️ **`carrying` คุมแค่ภาพ** ไม่ได้เก็บว่าถืออะไรอยู่ — คนที่ตั้งค่านี้ตอนนี้คือ
`interact_box.gd` (ตอนถูกหยิบ ดูหัวข้อ 9) ซึ่งซ่อนโหนดกล่องเองควบคู่ไปด้วย
⚠️ **ค่านี้ไม่ข้ามฉาก** — ผู้เล่นถูก instance ใหม่ทุกฉาก `carrying` กลับเป็น false เสมอ

🚨 **`_box` ต่อท้ายสุด หลังทิศ** (`walk_obliquelyL_box`) เติมที่ prefix ไม่ได้
จึงมีฟังก์ชัน `_play()` เป็น**จุดเดียวในไฟล์ที่เรียก `play()`** และเป็นที่เดียวที่เติม `_box`
เพิ่มทางเดินใหม่ใน `play_animation()` ต้องเรียกผ่าน `_play()` เสมอ ไม่งั้นท่านั้นจะลืมสลับชุดถือของ
(ท่าไหนไม่มีชุด `_box` → เล่นท่ามือเปล่าแทน + เตือนครั้งเดียวต่อชื่อ ไม่ท่วมคอนโซล)

### 9. ของในฉากที่ตอบสนองผู้เล่น — กล่อง (`scripts/interact_box.gd`)

แนบกับ **`AnimatedSprite2D`** ที่มี Area2D ลูกชื่อ `InteractArea` (ใช้ที่โหนด `Box` ใน `decor/under_ch_1.tscn`)

ผู้เล่นเข้าระยะ → เล่นอนิเมชัน `nearby` · ออกนอกระยะ → กลับ `normal`
**แต่จะเริ่มทำงานก็ต่อเมื่อคุยกับ NPC ที่ระบุใน `unlock_after_npc` จบแล้วเท่านั้น** (ค่าเริ่มต้น `../Wawa`)
ก่อนหน้านั้นเดินชนยังไงก็เป็นของตกแต่งเฉย ๆ — กล่องที่ตอบสนองก่อนวาวาฝากงานคือการสปอยล์

| `@export` | ค่าเริ่มต้น | หมายเหตุ |
|---|---|---|
| `normal_anim` · `nearby_anim` | `"normal"` · `"nearby"` | ชื่อต้องตรงกับใน SpriteFrames · ไม่ตรงจะเตือนพร้อมบอกชื่อที่มีจริง |
| `revert_on_exit` | `true` | ปิด = เข้าครั้งแรกแล้วค้างท่าใหม่ตลอด |
| `unlock_after_npc` | `^"../Wawa"` | **ปล่อยว่าง = ใช้งานได้ตั้งแต่ต้นฉาก** |

**ปลดล็อกโดยดัก `interacted` ของ NPC แล้วค่อย `await dialogue_finished`**
🚨 ไม่ดัก `dialogue_finished` ตรง ๆ ตั้งแต่ `_ready` เพราะสัญญาณนั้นเป็นของ**กล่องคำพูดกลาง**
ยิงตอนบทของใครจบก็ได้ → คุยกับ NPC ตัวอื่นในฉากแล้วกล่องจะปลดล็อกไปด้วย

⚠️ **ชี้ที่ตัวโหนด NPC ไม่ใช่ชื่อบท** — เปลี่ยน `dialogue_id_override` ของวาวาแล้วไม่ต้องกลับมาแก้กล่อง
· หา NPC ไม่เจอ → `push_error` แล้ว**ล็อกไว้** ไม่ปล่อยผ่าน (ทำงานผิดลำดับเนื้อเรื่องเงียบ ๆ เจอยากกว่ากล่องที่ไม่ขยับ)

⚠️ **สลับด้วยชื่ออนิเมชัน ไม่ใช่เลขเฟรม** — `Sprite2D` ที่เปิด `region_enabled` อยู่ **Godot ไม่สนช่อง `frame` เลย**
สั่งเปลี่ยนได้ ค่าเปลี่ยนจริง แต่ภาพนิ่งสนิทและไม่มี error บอก (กับดักที่หาสาเหตุยากที่สุดของโหนดนี้)

⚠️ **ยืนแช่ข้างกล่องตั้งแต่ก่อนคุยจบ → กล่องเปลี่ยนท่าทันทีที่ปลดล็อก** ไม่ต้องเดินออกแล้วเข้าใหม่
(เก็บ `_player_inside` ตลอดเวลาแม้ยังล็อกอยู่)

**กด E ใส่กล่องที่ปลดล็อกแล้ว = หยิบขึ้นมา**
`player.carrying = true` (ทุกท่าสลับเป็นชุด `_box` เอง) → **หันหน้ามาทางกล่อง** (`face_toward()`
เหมือนตอนคุยกับ NPC) → กล่อง **ซ่อนตัว** → กดไม่ได้อีก

⚠️ **ตั้ง `carrying` ก่อนแล้วค่อยหัน** — `face_toward()` เล่นท่ายืนทันทีในบรรทัดนั้น
สลับลำดับจะได้ท่ามือเปล่าหนึ่งเฟรมก่อนเปลี่ยนเป็นชุดถือของ

⚠️ **ไม่ได้ย้ายโหนดกล่องไปแปะที่มือผู้เล่น** — ชุด `_box` วาดกล่องมาในภาพอยู่แล้ว
มีกล่องจริงลอยตามอีกใบจะเห็นเป็นสองใบ
⚠️ **ซ่อน ไม่ใช่ `queue_free()`** — เผื่อระบบวางของเอากลับมาโชว์ (`_carried` เก็บสถานะไว้)
· ปิด `monitoring` ของ `InteractArea` ด้วย กัน `body_entered` ยิงซ้ำตอนเดินกลับเข้ามา

✅ **ยกกล่องข้ามฉากได้แล้ว** (2026-08-19) — `player.carrying` มี setter ที่เขียนกลับลง
`GameState.carrying_box` และอ่านคืนตอน `_ready()` ของทุกฉาก · ที่วางอยู่ที่ชั้น 2 (ดูหัวข้อ 11)

**สัญญาณ `unlocked` · `interacted` · `picked_up`** ให้เนื้อเรื่อง/เควสต์ดักต่อโดยไม่ต้องแก้ไฟล์นี้

### 10. ผู้เล่นเลือกเป้าหมายตอนกด E (`player.gd`)

**ผู้เล่นเป็นคนเลือกว่าจะคุยกับใคร ไม่ใช่ให้แต่ละตัวแย่งกันดักปุ่มเอง**

```
NPC / ของ เข้าระยะ ──► player.add_nearby_target(self)   ← ตัวเป้าหมายลงทะเบียนเอง
กด E     ──► pick_interact_target()        ──┐
คลิกซ้าย ──► _target_under_mouse(พิกัดคลิก) ──┴──► _pick_best() ──► target.interact()
```

**คลิกซ้ายที่ NPC/ของ = ทำแบบเดียวกับกด E** (เงื่อนไขเดียวกันทุกข้อ)
หาว่าคลิกโดนอะไรด้วยการ**ยิงจุดเข้าไปในโลกฟิสิกส์** (`intersect_point`) จึงใช้รูปร่างจริง
ไม่ต้องเดาระยะเอง และไม่ต้องเพิ่มโหนดรับคลิกให้เป้าหมายตัวไหนเลย

⚠️ เปิดทั้ง `collide_with_areas` และ `collide_with_bodies` เพราะสองระบบวางรูปร่างไว้คนละที่
NPC เป็น `CharacterBody2D` (โดนที่ตัวเอง) · กล่องเป็นสไปรท์ที่มี `InteractArea` เป็นลูก (โดนที่ลูก)
→ เช็คทั้งตัวที่โดนและพ่อของมัน

⚠️ **ขนาดพื้นที่คลิกของสองอย่างไม่เท่ากันโดยธรรมชาติ** — `InteractArea` ของ NPC ตั้ง
`collision_layer = 0` จึงไม่ถูกจุดยิงเจอ **คลิก NPC = ต้องคลิกโดนตัวจริง** (รูปร่าง 24×14)
ส่วนกล่องคลิกได้ทั้งวงรัศมี `InteractArea` · อยากให้คลิก NPC ง่ายขึ้นต้องตั้ง layer ให้ `InteractArea`

🚨 **ยังต้องอยู่ในระยะ (`nearby_targets`) เหมือนกด E** — คลิกคุยข้ามครึ่งแมพไม่ได้
(อยากให้คลิกไกลได้ค่อยผ่อนทีหลัง ง่ายกว่าเก็บกฎที่ปล่อยหลุดไปแล้ว)

⚠️ **ใช้พิกัดจากตัวอีเวนต์ ไม่ใช่ `get_global_mouse_position()`** — ตัวหลังคือ "เมาส์อยู่ตรงไหน
ตอนนี้" ซึ่งไม่จำเป็นต้องเป็น "คลิกลงตรงไหน" · และต้องแปลงด้วย `get_canvas_transform()`
เพราะกล้องเลื่อน/ซูมได้

**กฎการเลือก:** `priority` สูงกว่าชนะก่อนเสมอ → เสมอกันค่อยเอาตัวที่**ใกล้ที่สุด**

| ประเภท | `priority` | ตั้งที่ |
|---|---|---|
| NPC (`NPCBase`) | **1** | `@export` ใน `npc_base.gd` (ตั้งทับรายตัวได้) |
| ของในฉาก (`interact_box.gd`) | **0** | `@export` ใน `interact_box.gd` |
| ไม่ได้กำหนด | 0 | — |

→ ยืนตรงจุดที่ทั้งวาวาและกล่องอยู่ในระยะ กด E จะได้**คุยกับวาวาก่อนเสมอ** แม้กล่องจะใกล้กว่า

**ของที่อยากให้กดได้ ไม่ต้องสืบทอดคลาสใด** — ผู้เล่นเช็คด้วย `has_method` ล้วน ๆ

| เมธอด | จำเป็นไหม | ความหมาย |
|---|---|---|
| `interact()` | ✅ **ต้องมี** | ไม่มี = ไม่ถูกเลือกเลย |
| `can_interact() -> bool` | ไม่มีก็ได้ | ไม่มี = ถือว่ากดได้เสมอ (กล่องที่ยังล็อกถูกข้ามด้วยกฎนี้) |
| `priority` (property) | ไม่มีก็ได้ | ไม่มี = 0 |

⚠️ **เป้าหมายลงทะเบียนแม้ตอน `can_interact()` เป็น false** — ผู้เล่นกรองตอนกดอีกที
สถานะเปลี่ยนได้ระหว่างที่ยืนอยู่ในระยะ (คุยกับวาวาจบตรงข้างกล่องเลย / `talk_once` หลังคุยจบ)
กรองตั้งแต่ตอนลงทะเบียนแล้วรายการจะค้างสถานะเก่าจนกว่าจะเดินออกแล้วเข้าใหม่

⚠️ **ใช้ `InteractArea` ของตัวเป้าหมายเอง ไม่ได้เพิ่ม Area2D ให้ผู้เล่น** — ทั้ง NPC และกล่อง
มี Area2D ที่จับผู้เล่นอยู่แล้ว ใช้ของเดิมจึงไม่ต้องแตะซีนไหนเลย

⚠️ **`pick_interact_target()` กวาดตัวที่ถูกลบไปแล้วออกทุกครั้ง** — ของที่ `queue_free()`
ตอนผู้เล่นยังยืนทับอยู่จะไม่มีวันยิง `body_exited` แล้วผีจะค้างในรายการตลอดฉาก

⚠️ **`NPC.gd` ตัวเก่า (เหลือที่ `Game/game.tscn` ที่เดียวแล้ว) ยังดัก `_input` เอง** ไม่ได้เข้าระบบนี้
ไม่ชนกันเพราะมันไม่เคยลงทะเบียน — แต่ในฉากพวกนั้นจะยังไม่มีเรื่อง priority/ระยะทาง

### 11. เควสต์เอากล่องไปวางที่ชั้น 2 (ทำงานได้แล้ว · ต่อซีนครบแล้ว 2026-08-20)

```
คุยวาวาจบ (under) → กด E หยิบกล่อง → GameState.carrying_box = true
                                            │ ข้ามฉากได้แล้ว
                                            ▼
        เข้า all_level_2 (โหนด Level2_1 ต้องอยู่ในซีนตลอดเวลา แต่เงียบอยู่)
                                            ▼
        เข้าเขต RoomBox → Line2D กระพริบ → เข้าเขต Box → เส้นดับ + กด E วางได้
                                            ▼
                    วางแล้ว: กล่องโผล่ที่ตำแหน่งในซีน · ผู้เล่นกลับชุดมือเปล่า · GameState.box_delivered = true
```

✅ **`Level2_1` ถูกลากเข้า `all_level_2.tscn` เป็นพี่น้องกับ `Level2` แล้ว** (พร้อมติ๊ก `y_sort_enabled`)
เคยหลุดหายไปช่วงหนึ่งตอนแยกซีน `AllLevel2` — ถ้าวันหลังกล่องหายอีกให้เช็คที่นี่ก่อน

🚨 **`Level2_1` ต้อง instance ค้างไว้ในซีน ไม่ใช่สร้างตอนเนื้อเรื่องถึงคิว**
เงื่อนไขเนื้อเรื่องอยู่ที่ **"โชว์อะไรไหม" ไม่ใช่ "สร้างโหนดไหม"**
· `level_2.gd` เคยมี `_spawn_box_quest()` แล้ว**ถอดออก** — มีทั้งสองทางจะได้กล่อง 2 ใบซ้อนกัน
**อย่าเพิ่มโค้ดเสียบซีนกลับเข้ามาโดยไม่ลบ instance ในซีนก่อน**

**สถานะอยู่ที่ `GameState` ไม่ใช่ที่ตัวผู้เล่น** — ผู้เล่นถูก instance ใหม่ทุกฉาก
`player.carrying` จึงเป็นแค่ "สำเนาที่ใช้เลือกอนิเมชันในฉากนี้" · มี **setter** ที่เขียนกลับลง
`GameState.carrying_box` ทุกครั้งที่เปลี่ยน และอ่านคืนใน `_ready()`
→ ตั้ง `player.carrying = true` ที่ไหนก็ได้เหมือนเดิม ไม่ต้องแตะ `GameState` เอง

| ตัวแปรใน `GameState` | ความหมาย |
|---|---|
| `carrying_box` | ถือกล่องอยู่ในมือ — **เป็นจริงได้ก็ต่อเมื่อคุยวาวาจบแล้ว** (กล่องล็อกอยู่ก่อนหน้านั้น) จึงใช้เป็นเงื่อนไข "คุยแล้ว + มีกล่อง + เป็นท่าถือของ" ได้ครบในตัวเดียว |
| `box_delivered` | วางเสร็จแล้ว — แยกกันเพราะ "ไม่ได้ถืออยู่" กับ "วางเสร็จแล้ว" คนละเรื่อง (ก่อนคุยวาวาก็ไม่ได้ถือเหมือนกัน) |

**ไฟล์ที่เกี่ยวข้อง**

| ไฟล์ | แนบที่ | หน้าที่ |
|---|---|---|
| `scripts/box_drop_point.gd` | `Box` ใน `level_2_1.tscn` | จุดวาง — ซ่อนจนกว่าจะวาง · กด E แล้ววาง |
| `scripts/drop_zone_hint.gd` | `RoomBox` ใน `level_2_1.tscn` | เส้นบอกจุดกระพริบ |

**เงื่อนไขที่คุมการโชว์ — ทั้งสองสคริปต์อ่านจาก `GameState` ตรง ๆ ไม่เก็บสำเนา**

| สถานะ | `carrying_box` | `box_delivered` | เส้น | กล่องปลายทาง |
|---|---|---|---|---|
| ยังไม่คุยกับวาวา | false | false | เงียบ | ซ่อน |
| ถือกล่องมาแล้ว | **true** | false | กระพริบ (ในเขต) | ซ่อน |
| วางเสร็จแล้ว | false | **true** | เงียบ | โชว์ |

🚨 **เส้นเช็ค `carrying_box` ไม่ใช่ `not box_delivered`** — สองอันนี้ต่างกันตรงแถวแรก
เขียนเป็น `not box_delivered` เส้นจะกระพริบตั้งแต่ยังไม่ได้คุยกับวาวา = สปอยล์ว่าตรงนี้จะมีอะไร
· ใช้ `carrying_box` เงื่อนไขเดียวครอบได้ทั้งสามแถว เพราะมันเป็นเท็จทั้งก่อนหยิบและหลังวาง

⚠️ **อ่านจาก `GameState` ทุกครั้ง ไม่เก็บ `_placed` ไว้ในตัวเอง** — เก็บสำเนาแล้วเส้นจะเชื่อสำเนานั้น
ซึ่งอัปเดตได้ก็ต่อเมื่อสัญญาณ `placed` มาถึงจริง · `drop_point_path` ชี้ผิดเมื่อไหร่
เส้นจะกระพริบต่อทั้งที่วางไปแล้ว (มี warning เตือน แต่บนจอคือฟีเจอร์ทำงานผิด)

🚨 **`box_drop_point.gd` เป็นคนละไฟล์กับ `interact_box.gd` โดยตั้งใจ — "วางลง" กับ "หยิบขึ้น" ตรงข้ามกัน**
เงื่อนไขกดได้ · ท่าเริ่มต้น · สิ่งที่เกิดตอนกด ตรงข้ามกันหมด ยัดรวมกันแล้วทุกเมธอดจะมี `if โหมดไหน` คร่อม

⚠️ **ก่อนวาง กล่องปลายทางซ่อนอยู่** — ชุดอนิเมชัน `_box` ของผู้เล่นวาดกล่องมาในภาพแล้ว
โชว์ที่จุดหมายด้วยจะเห็นกล่อง 2 ใบในเฟรมเดียว ผู้เล่นแยกไม่ออกว่าใบไหนคือใบที่ถืออยู่

**สองวงซ้อนกัน คนละหน้าที่**

| เขต | ขนาด | ความหมาย |
|---|---|---|
| `RoomBox` (Area2D) | ทั้งมุมห้อง | "ของต้องไปวางแถวนี้" → เส้นกระพริบ |
| `Box/InteractArea` | รัศมี 50 | "ยืนตรงนี้กด E ได้เลย" → เส้นดับ |

🚨 **เส้นดับตอนเข้าเขตกด E ไม่ใช่ตอนวางเสร็จ** — เส้นมีหน้าที่เดียวคือ "พาไปให้ถึง"
ถึงแล้วยังกระพริบก็เป็นเสียงรบกวนทับกล่องที่กำลังจะวาง
🚨 **`_refresh()` เป็นจุดเดียวที่เปิด/ปิดเส้น** — เหตุการณ์ที่กระทบมี 4 อย่างและมาไม่เรียงลำดับ
สั่งเปิด/ปิดกระจายตามแต่ละเหตุการณ์จะมีลำดับที่ทำให้เส้นค้างสว่าง/ค้างดับแบบทำซ้ำไม่ได้

🚨 **โหนด `Level2_1` ในซีนยังไม่ได้เปิด `y_sort_enabled`** ⚠️ *ต้องไปติ๊กเอง*
Godot จัดลำดับ Y ให้เฉพาะลูกโดยตรงของโหนดที่เปิด y-sort · โหนดกลางที่ปิดไว้จะถูกวาดยกกลุ่ม
ที่พิกัดของตัวมันเอง (0,0) = **กล่องจมอยู่หลังพื้นห้อง** (บทเรียนเดียวกับ `ChapterSlot`)

### วางกล่องเสร็จ → โทรศัพท์เด้ง โชทักในกลุ่มสโม (`scripts/chat_event_trigger.gd`)

แนบที่โหนด `Node` เปล่า ๆ ในซีนแมพ · ดัก `placed` ของ `BoxDropPoint` แล้วเปิด `Chat.tscn`
พร้อมบทจาก `ChatDatabase.EVENTS["box_delivered"]`

🚨 **`Chat.tscn` ถูกตั้งค่าไว้สำหรับเนื้อเรื่องตอนเช้าโดยเฉพาะ — ต้องปิด 2 ช่องก่อน `add_child()`**

| `@export` บนโหนด `Chat` | ค่าในซีน | ไม่ปิดแล้วเป็นยังไง |
|---|---|---|
| `popout_after_group` | `"smo"` | โชทักเสร็จ → กลุ่มสวัสดิการเด้งมาเล่นบทขนของตอนเช้าซ้ำ |
| `end_after_group` | `"sawat"` | บทนั้นจบ → **เกมเฟดพาผู้เล่นไปลานจอดรถ (`p.tscn`)** |

ตั้งก่อน `add_child` เพราะ `phone.gd._ready()` อ่านค่าไปต่อสัญญาณทันที

🚨 **แยกเป็นสคริปต์ต่างหาก ไม่ยัดลง `box_drop_point.gd`** — ไฟล์นั้นเขียนกำกับไว้เองว่า
สัญญาณ `placed` มีไว้ *"ให้เนื้อเรื่อง/เควสต์ดักต่อโดยไม่ต้องแก้ไฟล์นี้"*
จุดวางกล่องเป็น**ของในฉาก** แชทที่เด้งตามมาเป็น**เนื้อเรื่อง** — เปลี่ยนใจให้แชทเด้งตอนอื่น
ก็ย้ายโหนดนี้ ไม่ต้องแตะระบบวางกล่อง

🚨 **ปิดแชทไม่ได้จนกว่าข้อความจะมาถึงครบ** (`_can_close`) — เหตุการณ์นี้เกิดครั้งเดียวตลอดเกม
ผู้เล่นที่รัวปุ่มอยู่พอดีตอนโทรศัพท์เด้งจะปิดทิ้งก่อนโชพิมพ์เสร็จ **แล้วไม่มีทางกลับมาอ่านได้อีก**

#### บทที่ปิดท้ายด้วยตัวเลือก = แชทเก็บตัวเองแล้วปลดล็อกผู้เล่น (เพิ่ม 2026-08-21)

บท `box_delivered` จบด้วย `{"choices": ["รับทราบค่ะ"]}` → กดแล้วคำตอบขึ้นเป็นข้อความของน้ำฟ้า
→ หน่วง `close_after_choice_delay` (0.7 วิ) → แชทไถลลงแล้วปิด → `can_move = true`

🚨 **ตัวสั่งปิดคือ "บทเขียนไว้ยังไง" ไม่ใช่ธงเปิด/ปิดที่โหนด** (`_ends_with_choice()`)
บทที่ปิดท้ายด้วยตัวเลือกแปลว่าคนเขียนบทกำหนดจุดจบไว้แล้ว — ให้กด E ปิดอีกทีคือ
**ขอให้ยืนยันสองรอบ** · ส่วนบทที่ไม่มีตัวเลือกยังต้องกด E/Esc เองเหมือนเดิม เพราะไม่มีอะไรบอก
ว่าอ่านจบหรือยัง · ตั้งเป็น `@export` แยกจะกลายเป็นค่าที่ต้องจำให้ตรงกับบททุกครั้งที่แก้บท

⚠️ **ดูเฉพาะบรรทัดสุดท้าย** — ตัวเลือกกลางบทคือการคุยต่อ ไม่ใช่การจบบท
⚠️ **ต้องมี `close_after_choice_delay`** — คำตอบที่กดกลายเป็นข้อความในห้องแชทด้วย
ปิดทันทีจะเห็นบับเบิลของตัวเองแวบเดียวแล้วจอหาย เหมือนกดพลาดมากกว่าเหมือนตอบจบ
⚠️ **เช็ค `is_inside_tree()` หลัง `await receive_messages()`** — ผู้เล่นออกจากฉากระหว่างที่
ข้อความยังทยอยเข้าอยู่ โหนดนี้ถูกลบไปแล้ว เรียกต่อจะได้ error "previously freed instance"

⚠️ **ปุ่มย้อนกลับที่หัวแชท = "เก็บโทรศัพท์" ในบริบทนี้** — `phone.gd` ต่อ `back_pressed` →
`show_list()` ไว้ให้แล้ว ปล่อยไว้จะได้ห้องแชทถูกซ่อนหมด + warning *"ยังไม่มี node หน้ารายการแชท"*
เหลือจอเปล่าที่ปิดไม่ได้ · สคริปต์จึง **`disconnect()` ของเดิมออกก่อน** แล้วค่อยต่อของตัวเอง
· ปิดได้ 4 ทาง: กดตัวเลือกปิดท้าย (ปิดเอง) · ปุ่มย้อนกลับ · กด E · กด Esc
⚠️ **ตอนนี้บท `box_delivered` จบด้วยตัวเลือก → ทางที่ผู้เล่นเจอจริงคือทางแรก**
สามทางที่เหลือกลายเป็นตาข่ายรับ (`_unhandled_input` ไม่เคยถูกเปิดในเส้นทางนั้นด้วยซ้ำ)

⚠️ **ค้นหา `BoxDropPoint` เองจาก `get_tree().current_scene`** ถ้าไม่กรอก `drop_point_path`
ค้นจากตัวเองไม่ได้ — โหนดนี้อยู่ใน `level_2_1.tscn` ซึ่งถูก instance เข้า `all_level_2.tscn` อีกที
(กฎเดียวกับ `stair.gd` ที่ต้องค้นจาก `owner`)

**ล็อกผู้เล่นด้วย `player.can_move` ตัวเดิมที่บทสนทนาใช้** ไม่ได้สร้างระบบล็อกใหม่
`player.gd:381` จัดการให้ครบทั้งหยุด velocity · สลับเป็นท่ายืน · **หยุดเสียงฝีเท้า**
(ไม่หยุดเสียง = โดนล็อกตอนกำลังเดินแล้วเสียงดังค้างไปเรื่อย ๆ)

🚨 **ล็อกตั้งแต่วินาทีที่วางกล่องเสร็จ ไม่ใช่ตอนโทรศัพท์เด้ง**
ระหว่าง `open_delay` (1.2 วิ) ผู้เล่นยังกดเดินได้ แล้วจะโดนหยุดกลางก้าวตอนแผงแชทโผล่
ซึ่งดูเหมือนเกมค้าง ไม่ใช่เหมือน "มีสายเข้า"

🚨 **ทุกทางออกก่อนเปิดแชทสำเร็จต้องปลดล็อกคืน** — พิมพ์ `event_id` ผิดแล้วออกเฉย ๆ
= **ผู้เล่นขยับไม่ได้อีกเลยตลอดฉาก** ซึ่งแย่กว่าการที่แชทไม่เด้งมาก

**ยังไม่ได้ทำ:** กล่องที่ `under.tscn` ยังโผล่ให้หยิบซ้ำได้ถ้าเดินกลับไป (`interact_box.gd`
จำแค่ภายในฉาก ยังไม่ผูกกับ `GameState.carrying_box`)

### 12. ช่องของที่ถืออยู่ — HUD (`scripts/hud.gd` แนบที่ราก `HUD` ของ `hud.tscn`)

พาแนลเรียงกันด้านล่างจอ · หยิบกล่อง → รูป `item/ItemBox.PNG` โผล่ที่**ช่องแรก** · คลิกหรือกดเลข = เลือกช่อง → **กรอบสีเหลือง `#e9de00`** (ปรับที่ `selected_border_color`)

**โผล่เองตอนผู้เล่นหยิบของชิ้นแรก แล้วอยู่ยาวตลอดเกม** — `interact_box.gd._pick_up()`
เรียก `UIRoot.show_ui("hud")` **ครั้งเดียว** · ไม่ต้องลากเข้าซีนและไม่ต้องสั่งซ้ำทุกฉาก
(`UIRoot` แขวนที่ `get_tree().root` ซึ่งรอดจาก `change_scene_to_file()` — ดูหัวข้อ 13)

⚠️ **วางกล่องเสร็จแล้ว HUD ยังอยู่** หายแค่รูปไอเทม — ช่องของเป็นส่วนหนึ่งของหน้าจอเกม
นับจากจุดที่ผู้เล่นรู้จักระบบของแล้ว โผล่ ๆ หาย ๆ ตามว่ามีของอยู่ในมือไหมจะดูเหมือนบั๊ก

✅ **HUD หลบให้กล่องคำพูดตอนคุย แล้วกลับมาตอนบทจบ** (เลือกทางนี้ 2026-08-21)

| | ช่วง y (จอ 1080) |
|---|---|
| `HUD/HBoxContainer` | 950 – 1030 |
| `DialogueBox/Root/Box` | 780 – 1047 |

HUD นั่งอยู่กลางกล่องข้อความพอดี · ทางที่ไม่ได้เลือกคือย้าย HUD ไปมุมขวาบน —
ซ่อนตอนคุยดีกว่าเพราะ **ช่องของไม่มีอะไรให้อ่านระหว่างบทสนทนาอยู่แล้ว**
ส่วนการย้ายมุมทำให้ช่องของอยู่ไกลจากสายตาตลอดเวลาเพื่อแก้ปัญหาที่เกิดแค่ตอนคุย

**คุมที่ `dialogue_system.gd`** — `_hide_hud_for_dialogue()` ตอนเริ่มบท ·
`_restore_hud_after_dialogue()` ตอนจบบท

⚠️ **ซ่อน ไม่ใช่ลบ — ของในช่องไม่หายไปไหน** `UIRoot.hide_ui()` เก็บโหนดไว้ครบ
กลับมาแล้วเป็น**โหนดเดิม** ทั้งรูปไอเทมและช่องที่เลือกอยู่ยังอยู่เหมือนเดิม

🚨 **ห้ามเรียก `UIRoot.show_ui("hud")` ทื่อ ๆ ตอนจบบท** — ตัวนั้น**สร้าง UI ให้ถ้ายังไม่มี**
คุยกับ NPC ตั้งแต่ก่อนหยิบของชิ้นแรกแล้วจบบท จะได้ช่องของโผล่มาเองทั้งที่ผู้เล่นยังไม่มีของ
· โค้ดจึงจำ `_hud_hidden_by_dialogue` ว่า **"เราเป็นคนซ่อน"** ไม่ใช่แค่ "ตอนนี้ซ่อนอยู่"

🚨 **`_exit_tree()` ต้องคืน HUD ด้วย** — กล่องคำพูดตายพร้อมฉาก แต่ HUD แขวนที่ `root`
ซึ่งข้ามฉาก · เปลี่ยนฉากระหว่างบทค้างอยู่ = ไม่มีใครเหลือให้เอากลับมา **ช่องของหายถาวร**
และผู้เล่นจะเห็นเป็น "ของหาย" ทั้งที่ `GameState` ยังถืออยู่ครบ

⚠️ **แชท (`Chat.tscn`) ยังไม่ได้ทำแบบเดียวกัน** — แผงแชทก็บังจอเหมือนกัน
แต่ตอนนี้เด้งเฉพาะหลังวางกล่อง ยังไม่มีใครขอ

| ทำอะไร | ผลลัพธ์ |
|---|---|
| หยิบกล่อง (`GameState.carrying_box = true`) | รูปกล่องโผล่ช่องแรก |
| รับขนมปังจากพี่โช (`GameState.has_bread = true`) | `item/Bread.PNG` โผล่**ช่องที่สอง** |
| **วางกล่องเสร็จ** | รูป**หายไป** — ช่องนี้คือ "ของที่ถืออยู่ตอนนี้" ไม่ใช่ "งานที่เคยทำ" |
| คลิกช่อง / กดเลข `1`-`9` | ช่องนั้นขึ้นกรอบ |
| กดช่องเดิมซ้ำ | ยกเลิกการเลือก (ไม่งั้นผู้เล่นเอากรอบออกไม่ได้เลย) |

**`GameState.carrying_box` มี setter + สัญญาณ `carrying_box_changed` แล้ว** (เพิ่ม 2026-08-20)
เดิมเป็นตัวแปรเปล่า — HUD จะรู้ว่าหยิบกล่องเมื่อไหร่ได้ก็ต้องไปตรวจทุกเฟรม
⚠️ **setter เช็คค่าเดิมก่อน emit** (กฎเดียวกับ `chapter`) — `player.gd._ready()` เขียนค่าเดิม
กลับลงไปทุกครั้งที่เข้าฉากใหม่ ไม่เช็คก่อน = ทุกฉากยิงสัญญาณ "หยิบกล่อง" ซ้ำ

🚨 **HUD อ่าน `GameState` ตอน `_ready()` ด้วย ไม่ใช่รอสัญญาณอย่างเดียว**
HUD ถูก instance ใหม่ทุกฉาก แต่สัญญาณยิงไปตั้งแต่ฉากห้องใต้ตึกแล้ว —
รอสัญญาณอย่างเดียว = เดินขึ้นชั้น 2 ปุ๊บกล่องหายจากช่องทั้งที่ยังถืออยู่ในมือ
· **ช่องที่เลือกไว้ไม่ข้ามฉาก** (รีเซ็ตทุกครั้งที่เปลี่ยนฉาก) — ยังไม่ได้ผูกกับ `GameState`

⚠️ **นับพาแนลจากลูกในซีนอัตโนมัติ ไม่ผูกกับชื่อ `Panel1`/`Panel2`** เพิ่มช่องที่ 4
ก็แค่ก๊อปพาแนลในซีน · **รูปไอเทมกับกรอบสร้างจากโค้ดทั้งคู่** ไม่ต้องเพิ่ม node

🚨 **ก๊อป StyleBox คนละใบต่อช่อง ไม่แก้ใบในซีนโดยตรง** — ในซีนพาแนลทั้ง 3 ช่องใช้
`StyleBoxFlat_n2snw` **ใบเดียวกัน** แก้ตรง ๆ = กรอบขึ้นพร้อมกันทั้งสามช่อง
(กับดักเดียวกับปุ่มตัวเลือกในกล่องคำพูด)

🚨 **กดเลขอ่าน `keycode` ตรง ๆ ไม่ได้เพิ่ม input action ใหม่** — การเพิ่ม action ต้องเขียนลง
`project.godot` ซึ่งโปรเจกต์นี้เติมเองไม่ได้ (Godot ลบทิ้งเงียบเมื่อผู้ใช้แตะ Project Settings —
กับดักเดียวกับ autoload) · อยากได้ action จริงต้องไปเพิ่มเองใน Project Settings แล้วแก้โค้ดตาม

⚠️ **คลิกตรงพาแนลจะไม่เลื่อนบทสนทนา** — พาแนลเป็น Control ที่กินคลิกไปแล้ว
ส่วนระบบเลื่อนบทดักที่ `_unhandled_input` ซึ่งได้เฉพาะคลิกที่ไม่โดน GUI · ถูกต้องแล้วแต่ต้องรู้ไว้

### 13. ที่แขวน UI ที่อยู่ข้ามฉาก (`scripts/ui_root.gd` · `class_name UIRoot`)

UI อย่าง HUD ช่องของ · ตัวบอกเควส · แมพ **ไม่ได้เป็นของฉากใดฉากหนึ่ง** — มันอยู่กับ*ผู้เล่น*ซึ่งเดินข้ามฉากไปเรื่อย ๆ

```gdscript
UIRoot.show_ui("hud")     ## สร้างถ้ายังไม่มี แล้วโชว์ (เรียกซ้ำได้ ไม่สร้างซ้ำ)
UIRoot.hide_ui("hud")     ## ซ่อน ไม่ลบ — สถานะข้างในยังอยู่ครบ
UIRoot.hide_all()         ## เข้าเมนูหลัก / ฉากคัตซีน
var hud = UIRoot.get_ui("hud")   ## เอาไปสั่งงานตรง ๆ (ไม่สร้างให้)
```

**เพิ่ม UI ใหม่ = เพิ่ม 1 บรรทัดใน `UI_SCENES`** ไม่ต้องแตะสคริปต์อื่นและไม่ต้องแตะซีนไหนเลย
```gdscript
const UI_SCENES := {
    "hud":   {"scene": "res://hud.tscn", "layer": 20},
    "quest": {"scene": "res://ui/quest_tracker.tscn", "layer": 21},
}
```

🚨 **ไม่ใช่ autoload โดยตั้งใจ** — แขวนโหนดที่ `get_tree().root` + static API
**กลไกเดียวกับ `scripts/music.gd`** (โปรเจกต์นี้เติม autoload ลง `project.godot` เองไม่ได้)

⚠️ **ทำไมไม่ลาก `hud.tscn` เข้าไปในทุกซีน** — UI 1 ตัว × ฉาก 8 ฉาก = ลากเข้าซีน 8 ครั้ง
พอมี 4 UI ก็ 32 ครั้ง · **ลืมฉากเดียว = บั๊กที่เจอตอนเล่นไปถึงฉากนั้นแล้วเท่านั้น**
· แลกกับการที่มองไม่เห็น UI ตอนแก้ซีนในเอดิเตอร์ ซึ่งรับได้เพราะ UI พวกนี้ยึดขอบจอ
ไม่ต้องจัดตำแหน่งเทียบกับของในฉาก

⚠️ **`layer` ตั้งจากโค้ดตอน spawn ไม่พึ่งค่าในซีน** — ค่าใน `.tscn` ถูกเซฟทับได้ตอนใครไปแก้ซีนนั้น
แล้ว UI จะไปโผล่ผิดชั้นแบบเงียบ ๆ (เหตุผลเดียวกับที่ `scene_transition.tscn` ตั้ง layer ใน `_ready()`)

⚠️ **`hide_ui()` ซ่อน ไม่ลบ** — HUD ที่ถูกลบแล้วสร้างใหม่จะลืมว่าผู้เล่นเลือกช่องไหนอยู่
⚠️ **`get_ui()` ไม่สร้างให้ ต่างจาก `show_ui()`** — ตัวนี้มีไว้ถามว่า "มีอยู่ไหม"
สร้างให้ด้วยจะทำให้แค่ไปอ่านค่าก็เผลอเปิด UI ที่ไม่มีใครตั้งใจเปิด

⚠️ **ใช้ `load()` ไม่ใช่ `preload()`** ในทะเบียน — `preload` จะโหลดซีน UI **ทุกตัว**
เข้าหน่วยความจำตั้งแต่เกมเริ่ม ทั้งที่บางตัวอาจไม่ถูกเปิดเลยทั้งรอบการเล่น

🚨 **สั่งเปิดครั้งเดียวพอ ไม่ต้องสั่งซ้ำทุกฉาก** — โหนดแขวนอยู่ที่ root ซึ่งรอดจากการเปลี่ยนฉาก
โชว์ทีเดียวแล้วติดค้างข้ามฉากไปเอง · เรียกซ้ำก็ไม่เสียหาย `show_ui()` ไม่สร้างตัวใหม่

| UI | ใครสั่งเปิด | เมื่อไหร่ |
|---|---|---|
| `hud` | `scripts/interact_box.gd` → `_pick_up()` | ผู้เล่นหยิบของชิ้นแรก แล้วอยู่ยาวตลอดเกม |

⚠️ **ไม่มี UI ตัวไหนเปิดเองตั้งแต่เกมเริ่ม** — เมนูหลักกับฉากตื่นนอนไม่ควรมี HUD โผล่มา
ทั้งที่ผู้เล่นยังไม่มีของสักชิ้น
⚠️ **ยังไม่มีใครเรียก `hide_all()`** — ถ้าวันหลังมีทางกลับไปเมนูหลักระหว่างเล่น
HUD จะติดค้างอยู่บนหน้าเมนู · ใส่ `UIRoot.hide_all()` ที่ `start_manu.gd._ready()` ได้เลยตอนนั้น

## โครงสร้างโฟลเดอร์ asset

| โฟลเดอร์ | เก็บอะไร |
|---|---|
| `decor/` | ซีนของตกแต่ง + NPC แยกตามบท (`under_ch_1.tscn`) |
| `scripts/` | สคริปต์ระบบที่ไม่ผูกกับซีนใดซีนหนึ่ง (`room.gd` · `interact_box.gd`) |
| `item/` | รูปไอเทมสำหรับช่องของใน HUD (`ItemBox.PNG` — **นามสกุลตัวใหญ่**) |
| `image/` | รูปฉาก พื้นหลัง tileset |
| `image/align_npc/` | สไปรท์ NPC ปริศนาที่นั่ง |
| `image_avatar/` | รูปโปรไฟล์วงกลมสำหรับแชท (256×256) |
| `image_fullAvatar/` | รูปครึ่งตัวสำหรับกล่องคำพูด |
| `image_ui/` | ไอคอน UI |
| `font/` | ฟอนต์ — **ไม่มีธีมกลาง ตั้งทีละโหนดในซีน** (ดูตารางล่าง) |
| `soud/` · `soud_effects/` | เพลง · เสียงเอฟเฟกต์ |

### ฟอนต์ตั้งอยู่ที่ไหนบ้าง

**ไม่มีธีมกลาง** — `project.godot` ไม่มี `[gui]` และไม่มีไฟล์ `Theme` สักไฟล์
ทุกที่ตั้งเองด้วย `theme_override_fonts/font` + `theme_override_font_sizes/font_size` ที่โหนด

| UI | ซีน | ฟอนต์ | ขนาด |
|---|---|---|---|
| บับเบิลปริศนาที่นั่ง | `speech_bubble.tscn` | `2005_iannnnnAMD.ttf` | 32 |
| กล่องคำพูด (+ ปุ่มตัวเลือก) | `DialogueBox.tscn` | `PK Nakhon Pathom Demo.ttf` | 28 |
| ห้องแชท | `chat_group.tscn` | `2005_iannnnnAMD.ttf` | 73 / 47 |
| บับเบิลข้อความแชท | `message_bubble.tscn` | `PK Nakhon Pathom Demo.ttf` | 28 |
| เมนูบันได | `StairMenu.tscn` | **ไม่ได้ตั้ง** (default ของ Godot) | 64 |
| เมนูหลัก | `start_manu.tscn` | `2005_iannnnnAMD.ttf` | 138 |

🚨 **อยากตั้งครั้งเดียวใช้ทั้งเกมต้องทำ 2 ขั้น** — ตั้ง `Project Settings > GUI > Theme`
**แล้วไล่ลบ `theme_override_fonts` ที่โหนดออกด้วย** เพราะ override ที่โหนด**ชนะธีมกลางเสมอ**
ตั้งธีมอย่างเดียวจะเห็นผลแค่ `StairMenu.tscn` ที่ไม่เคยตั้ง override ไว้

> ⚠️ **ชื่อไฟล์รูปต้องพิมพ์ตรงตัวพิมพ์เล็กใหญ่เป๊ะ ๆ**
> `res://` ของ Godot เป็น case-sensitive ตอน export แต่ Windows ไม่สน
> พิมพ์ผิดตัวเล็กใหญ่จะไม่พังตอนทดสอบ **แต่รูปจะหายตอน build จริง**
> ตอนนี้ใน `image_avatar/` ส่วนใหญ่ขึ้นต้นตัวใหญ่ (`Wawa.png`, `Jaja.png`, `Namking.png`,
> `Somsom.png`, `Chicken.png`) ยกเว้น `chochang.png` กับ `kame.png` ที่เป็นตัวเล็ก

## Input actions ที่ใช้

`interact` (E), `up`, `down`, `cancel` — ดูรายละเอียดใน `project.godot`

## ปัญหาที่รู้อยู่แล้ว (ยังไม่แก้)

0. **บท `wawa_intro` / `npc1` ใช้ชื่อผู้พูดว่า `"พี่วา"` ซึ่งไม่มีโหนดรูป** ⚠️ *เจอ 2026-08-14 ยังไม่แก้*

   `Root/Portraits` มีแค่ `Wawa` · `Namfha` · `Chochang` · `Kirin` (คีย์คือ `"วาวา"` `"น้ำฟ้า"` ฯลฯ)
   `"พี่วา"` ไม่ตรงกับอันไหน → บทนั้นรูปวาวาไม่ขึ้น เหลือแต่ข้อความ
   แก้ได้โดยเปลี่ยนเป็น `"วาวา"` แต่ **ชื่อที่โชว์ในกล่องจะเปลี่ยนตามด้วย** จึงเป็นเรื่องน้ำเสียงบท ต้องตัดสินใจก่อน
   (บทใหม่ `wawa_under_ch1` ใช้ `"วาวา"` ถูกต้องแล้ว)

1. **`p.tscn` — ต้นไม้แถวล่างหายไป 37 ช่อง** ⚠️ *บั๊กภาพจริง ยังไม่แก้ (ต้องตัดสินใจเรื่องอาร์ต)*

   เหลือ error 18 บรรทัด / นิยาม 6 อัน — **ทุกอันมีช่องบนแมพใช้จริง** จึงจงใจไม่ลบทิ้ง

   | ภาพ | `margins` | ตารางที่ใช้ได้ | ช่องที่วาดไว้ | ช่องที่โหว่ |
   |---|---|---|---|---|
   | `IMG_9068.PNG` | 0,22 | 3×4 | (0,4) (1,4) (2,4) | 9 |
   | `Tree2.png` | 0,10 | 5×4 | (1,4) (2,4) | 15 |
   | `Three1.png` | 0,8 | 5×4 | (2,4) | 13 |

   เลเยอร์ที่กระทบ: `tree` (24) · `treeP_5` (7) · `tree_P` (4) · `tree_P3` (2)

   **สาเหตุ:** ตั้ง `margins` แนวตั้งทีหลัง → พื้นที่ใช้งานหดลง เหลือ 4 แถวแทน 5
   **แถวสุดท้ายที่วาดไว้ก่อนหน้าเลยหลุดออกนอกตาราง** ช่องพวกนั้นวาดเป็นความว่างเปล่า

   ทางแก้ (ต้องเลือกเอง): เอา `margins` ออกแล้วจัดตำแหน่ง tile ใหม่ · หรือถมช่องเหล่านั้นด้วย tile ที่มีจริง

   > 🚨 **ห้ามลบนิยาม 6 อันนี้เพื่อให้ error เงียบ** — error คือสัญญาณเดียวที่บอกว่ามีช่องโหว่อยู่

2. ~~**TileSet นิยาม tile เกินขอบภาพ (2,549 อัน)**~~ ✅ **แก้แล้ว 2026-07-31**

   ไฟล์ภาพถูกย่อทีหลัง แต่นิยาม tile ยังอ้างพิกัดของภาพเวอร์ชันเดิม (`walk.png`, `Roongahan.png`,
   `upper.png`, `parking (1).png`, `Meeting room2.png`, `IMG_9067.PNG`)
   ถอดรหัส `tile_map_data` ทุกเลเยอร์แล้วยืนยันว่า **ไม่มีช่องไหนใช้เลย** จึงลบนิยามทิ้ง 2,549 บรรทัด
   จาก `align_1` `level1` `level_2` `level_3` `p` `under` → 5 ใน 6 ซีนเหลือ 0 error

   **บทเรียน:** ตารางจริงของ atlas = `(ขนาดภาพ − margins) ÷ (texture_region_size + separation)`
   ลืมคิด `margins` แล้วจะสรุปผิดว่า tile ปกติดี ทั้งที่มันหลุดขอบ (เกือบลบของที่ใช้อยู่ทิ้งเพราะเรื่องนี้)

3. ~~**Signal ต่อซ้ำใน `Start.gd`**~~ ✅ **แก้แล้ว 2026-08-01**

   `mouse_entered` / `mouse_exited` ต่อทั้งจาก editor และจากโค้ด → error 2 บรรทัดทุกครั้งที่เปิดเกม
   แก้เป็นเช็ค `is_connected()` ก่อนต่อ — เก็บทั้งสองฝั่งไว้ได้โดยไม่มี error
   และสคริปต์ยังทำงานได้ถ้าเอาไปใช้กับ node ที่ไม่ได้ต่อจาก editor

4. ~~**โค้ด debug ค้างใน `Start.gd`**~~ ✅ **แก้แล้ว 2026-08-01** (`print`, `print_stack`, `mouse_filter` ซ้ำ)

## Git

- Branch `main` → remote `origin` = https://github.com/pnadda-boop/Game-smo (**private**)
- `.gitignore` กัน `.godot/` (cache) และ `/SmoGame/` (exported build 127MB — เกินลิมิต 100MB ของ GitHub)
