# GameSmo

เกม **Pixel Art Top-Down Narrative Simulator** ภาษาไทย เล่าเรื่องของ **น้ำฟ้า** กับการทำงาน **สโม** (สโมสรนักศึกษา)

ฟีเจอร์หลักของเกม: ระบบบทสนทนา, **ระบบแชทข้อความ**, และ **อีเวนท์** ต่าง ๆ

- Engine: **Godot 4.6** (GL Compatibility), viewport 1920×1080, stretch mode `viewport`
- ภาษา: GDScript — คอมเมนต์และบทพูดในเกมเป็น **ภาษาไทย** เขียนโค้ดใหม่ให้คงสไตล์นี้
- Main scene: `start_manu.tscn`

## 📌 กฎการทำงานในโปรเจกต์นี้ (สำคัญ — อ่านก่อนเริ่ม)

1. **จดสิ่งที่คุยกันลง MD** — ข้อสรุปจากบทสนทนาต้องถูกบันทึก ไม่ปล่อยให้หายไปกับ session
2. **การตัดสินใจที่กระทบแนวคิดพื้นฐานของเกม → จดลง [DEVLOG.md](DEVLOG.md) ทุกครั้ง** (ใหม่สุดไว้บนสุด)
3. **แก้อะไรต้องแสดงเหตุผล** — บอกว่าจะแก้อะไร เพราะอะไร พิจารณาทางเลือกอื่นอะไรบ้าง ไม่แก้เงียบ ๆ
4. **ตรวจด้วย Godot headless ก่อนบอกว่าเสร็จ** — ดูคำสั่งหัวข้อถัดไป

**แบ่งหน้าที่เอกสาร:** ไฟล์นี้ (`CLAUDE.md`) = *"ตอนนี้เกมเป็นยังไง"* เขียนทับให้ทันสมัยเสมอ ·
[`DEVLOG.md`](DEVLOG.md) = *"ทำไมถึงมาเป็นแบบนี้"* สะสมไปเรื่อย ๆ ไม่ลบของเก่า

## คำสั่งตรวจสอบ (ใช้ทุกครั้งหลังแก้โค้ด)

Godot อยู่ที่ `D:\Godot_v4.6.2-stable_win64_console.exe` (ไม่ได้อยู่ใน PATH)

```bash
# ✅ วิธีหลัก — ตรวจทั้งโปรเจกต์ คอมไพล์ทุกสคริปต์พร้อม autoload
"D:\Godot_v4.6.2-stable_win64_console.exe" --headless --path "D:\game-smo" --import

# รันฉากใดฉากหนึ่งเพื่อดู error ตอน runtime
"D:\Godot_v4.6.2-stable_win64_console.exe" --headless --path "D:\game-smo" "res://level1.tscn" --quit-after 60
```

> ⚠️ **อย่าใช้ `--check-only --script`** — โหมดนี้ **ไม่โหลด autoload** ทำให้สคริปต์ที่อ้างถึง
> `SeatManager`, `NPCalignDatabase`, `ChatDatabase`, `Global`, `GameState` ขึ้น
> `Compile Error: Identifier not found` ทั้งที่โค้ดถูกต้องและทำงานได้จริงในเกม
> (ยืนยันแล้ว: `SeatManager.gd` ที่ใช้งานได้ปกติก็ FAIL ในโหมดนี้) — ใช้ `--import` แทนเสมอ

**เวลาอ่านผล `--import`:** จะมี error เรื่อง TileSet ~4,000 บรรทัดเป็นปกติ (ปัญหาค้าง ดูหัวข้อล่าง)
ให้กรองออกก่อน แล้วดูเฉพาะ `SCRIPT ERROR` / `Compile Error` / `Parse Error`

## Autoload (ตั้งใน project.godot แล้ว)

| ชื่อ | ไฟล์ | หน้าที่ |
|---|---|---|
| `Global` | `Global.gd` | ตำแหน่ง/ทิศทางที่ผู้เล่นจะ spawn |
| `GameState` | `Game_State.gd` | `next_spawn_point` — ผู้เล่นโผล่ตรงไหน · `pending_intro` — ฉากปลายทางต้องเล่นเนื้อเรื่องตอนมาถึงไหม |
| `NPCalignDatabase` | `npcalign_database.gd` | ฐานข้อมูล NPC: ชื่อ, บทพูด, เพศ, เงื่อนไขที่นั่ง |
| `SeatManager` | `SeatManager.gd` | ตัวกลางคุมระบบที่นั่งทั้งหมด + ตรวจเงื่อนไข |
| `ChatDatabase` | `chat_database.gd` | ทะเบียนผู้ส่งข้อความในกรุ๊ปแชท (ชื่อ + รูป + ฝั่งซ้ายขวา) |
| `ClickEffect` | `click_effect.tscn` | เอฟเฟกต์วงกลมกระเพื่อม + เสียงตอนคลิก ทำงานทุกซีน |

> 🚨 **เพิ่ม autoload ผ่าน `Project > Project Settings > Globals > Autoload` เท่านั้น**
> ห้ามเติมบรรทัดลง `project.godot` เอง — Godot อ่านไฟล์นี้ตอนเปิดโปรเจกต์ครั้งเดียว
> พอผู้ใช้แตะ Project Settings เรื่องใดก็ตาม มันจะเขียนไฟล์ใหม่จากรายการในหน่วยความจำ
> แล้ว **autoload ที่เติมจากข้างนอกจะถูกลบทิ้งเงียบ ๆ** (เกิดแล้ว 2 ครั้ง — ดู DEVLOG 2026-07-30)

> ⚠️ คอมเมนต์หัวไฟล์ `npcalign_database.gd` บอกให้ตั้งชื่อ autoload ว่า `NPCDatabase` — **เป็นคอมเมนต์เก่าที่ผิด** ชื่อจริงคือ `NPCalignDatabase` (โค้ดที่เรียกใช้ถูกต้องแล้ว)

## Flow ของเกม

```
start_manu.tscn ──ปุ่ม "WORK START"(Start.gd)──► wokeup.tscn
                                                    │ มินิเกมคลิกปลุกตัวเอง
                                                    │ idle → halfasleep → wokeup → sit
                                                    ▼
                                            p.tscn (parking.gd) + bus.tscn
                                            มาจากแชท → รถออกเอง + น้ำฟ้าพูด (ขยับไม่ได้)
                                            มาทางอื่น → เดินห่างรถ >180px แล้วรถออก
                                                    ▼
                              level1.tscn ⇄ level_2.tscn ⇄ level_3.tscn
                              ตึก 3 ชั้น — กด E ที่บันได → StairMenu เลือกชั้น
                                                    ▼
                          คุย NPC (กด E) + ปริศนาจัดที่นั่ง (align_*.tscn)
```

### การเปลี่ยนฉาก / spawn point

`stair_menu.gd` ถือตาราง `floor_targets` ที่ map (ชั้น + ตัวเลือก) → (scene ปลายทาง + ชื่อ spawn point)
ก่อนเปลี่ยนฉากจะเซ็ต `GameState.next_spawn_point` ไว้ ให้ฉากปลายทางอ่านไปใช้

**เพิ่มทางเชื่อมใหม่ต้องแก้ 2 ที่:** `floor_targets` ใน `stair_menu.gd` และ spawn point ในฉากปลายทาง

### เฟดเปลี่ยนฉาก

`scene_transition.tscn` → `transition_to(path)` ทำ fade out → เปลี่ยนฉาก → fade in ให้จบในตัวเอง
```gdscript
var fade := preload("res://scene_transition.tscn").instantiate()
get_tree().root.add_child(fade)   ## ⚠️ ต้องแขวนที่ root ไม่ใช่ในซีนตัวเอง
fade.transition_to("res://p.tscn")
```
⚠️ **ห้าม add_child ใต้ node ในซีนที่กำลังจะถูกเปลี่ยน** — `change_scene_to_file()` ลบซีนเดิมทิ้ง
ตัวเฟดจะหายไปด้วยแล้ว `await` ที่ค้างอยู่จะไม่ทำงานต่อ → จอค้างดำถาวร
(`fade_out()` / `fade_in()` แยกกันยังใช้ได้เหมือนเดิม — `Start.gd` ใช้แบบนั้นอยู่)

### 🚨 ลำดับชั้นของ CanvasLayer (`layer`)

| node | layer | เหตุผล |
|---|---|---|
| `SceneTransition` | **128** | ม่านเฟดต้องคลุมทุกอย่าง (ค่าสูงสุดที่ Godot รับ) |
| `ClickEffect` | 100 | เอฟเฟกต์คลิกอยู่เหนือ UI ปกติ |
| `Chat` · `DialogueBox` | 1 | UI ทั่วไป |

**CanvasLayer ที่ `layer` เท่ากันเรียงตามลำดับใน tree ซึ่งเดาไม่ได้** — ตอนแรกทุกตัวเป็น 1 หมด
เปลี่ยนฉากทีไรกล่องแชทกับกล่องคำพูดจึงลอยอยู่เหนือม่านเฟด ไม่ยอมมืดตาม
`SceneTransition` ตั้ง `layer` **จากโค้ดใน `_ready()`** เพื่อให้ได้ผลแน่นอนแม้ซีนถูกเซฟทับ

**เพิ่ม CanvasLayer ใหม่ต้องตั้ง `layer` ให้ชัดเจนเสมอ** อย่าปล่อย default 1

### เฟดเข้าตอนเปิดฉาก (ไม่เปลี่ยนฉาก)

`start_manu.gd` ทำจอดำแล้วจางออกตอนเปิดเกม — ใช้ `fade_in()` เดี่ยว ๆ ไม่ต้องใช้ `transition_to()`
```gdscript
var fade := preload("res://scene_transition.tscn").instantiate()
add_child(fade)     ## ⚠️ ที่ตัวเอง ไม่ใช่ root
fade.fade_in()      ## จบแล้ว queue_free() ตัวเอง
```
⚠️ **ตอน `_ready()` ของซีนหลัก `get_tree().root` ยัง busy setting up children อยู่** — `add_child` ตรงนั้นจะ error
ใส่ที่ตัวเองแทนได้เพราะซีนไม่ถูกเปลี่ยนระหว่างเฟดเข้า (ต่างจาก `transition_to()` ที่ต้องอยู่ root)
ปิด/เปิดได้ที่ `@export fade_in_on_start` บน node `StartManu`

### เนื้อเรื่องตอนมาถึงฉาก (`GameState.pending_intro`)

ฉากเดียวกันเข้าได้หลายทาง แต่อินโทรต้องเล่น **เฉพาะตอนมาจากทางที่เนื้อเรื่องกำหนด**
ฝั่งที่สั่งเปลี่ยนฉากตั้งค่า → ฉากปลายทางอ่านใน `_ready()` แล้ว **ล้างทิ้งทันที**
(ไม่ล้าง = เดินออกแล้วกลับเข้ามาใหม่ อินโทรจะเล่นซ้ำ)

ตอนนี้มีอันเดียว: `GameState.INTRO_ARRIVE_BUS` (`"arrive_bus"`) — `phone.gd` ตั้ง → `parking.gd` อ่าน

## ระบบหลัก

### 1. ปริศนาจัดที่นั่ง — ระบบที่ซับซ้อนที่สุด (ทำงานได้แล้ว)

Constraint satisfaction puzzle: NPC แต่ละตัวมีเงื่อนไขที่นั่งของตัวเอง ผู้เล่นต้องจัดให้ผ่านครบทุกคน

- `seat.gd` (`class_name Seat`) — ที่นั่งหนึ่งช่อง มี `row`/`column` (`@export`) ลงทะเบียนตัวเองเข้า `SeatManager` ตอน `_ready()`
- `SeatManager.gd` — รองรับ **15 เงื่อนไข**: `front_row`, `back_row`, `not_front_row`, `not_middle_row`, `any`, `next_to_boy`, `next_to_girl`, `not_next_to_boy`, `not_next_to_girl`, `no_one_in_front`, `someone_in_front`, `left_edge`, `right_edge`, `edge`, `not_edge`, `middle_seat`
- `condition` เป็น **Array ได้** = ต้องผ่านทุกข้อ (AND) เช่น NPC7 = `["no_one_in_front", "not_front_row"]`
- Feedback: ที่นั่ง**เขียว = ถูก / แดง = ผิด** (`set_highlight`) และ NPC เปลี่ยนเป็น `sit_happy` / `sit_angry`
- `check_all()` คืน `true` เมื่อทุกที่นั่งถูกหมด → ใช้ตัดสินชนะ; `all_seats_filled()` เช็คว่านั่งครบทุกช่อง

**สำคัญ:** ขอบเขตแถว/คอลัมน์คำนวณจากที่นั่งที่ลงทะเบียนจริง (`get_min_row()` ฯลฯ) ไม่ hardcode → เพิ่ม/ลดที่นั่งได้โดยไม่ต้องแก้ logic
เรียก `SeatManager.check_all()` **ทุกครั้ง**ที่วาง/ย้าย NPC เพราะกระทบเพื่อนบ้านข้าง ๆ ด้วย

**เพิ่ม NPC ใหม่:** แก้ `NPC_DATA` ใน `npcalign_database.gd` ที่เดียว
**เพิ่มเงื่อนไขใหม่:** เพิ่ม `match` case ใน `_check_single_condition()` (`SeatManager.gd`)

### 2. ระบบกล่องคำพูด (ทำงานได้แล้ว)

`Game/dialogue_system.gd` ติดที่ **`DialogueBox.tscn`** (node ราก `DialogueBox`)

⚠️ **เปลี่ยนชื่อไฟล์ `.tscn` แล้ว `preload("res://...")` ใน `.gd` ไม่อัปเดตตาม** — Godot แก้ให้เฉพาะ
การอ้างอิงในซีนกับ `@export` เท่านั้น string ในโค้ดต้องไล่แก้เอง (`Grep` ชื่อไฟล์เก่าทั้งโปรเจกต์)
ไฟล์นี้เคยชื่อ `canvas_layer.tscn` — ถ้าเจอชื่อเก่าที่ไหนอีกคือตกหล่น

**รูปแบบบทพูด — กำหนดอนิเมชันต่อบรรทัดได้**
```gdscript
{"name": "วาวา",  "text": "อรุณสวัสดิ์จ้า", "anim": "Angry"},
{"name": "น้ำฟ้า", "text": "สวัสดีค่ะ"},      # ไม่ใส่ anim = ใช้ default_anim ("talk")
```

**พฤติกรรมสำคัญ: อารมณ์ค้างไว้**
คนที่ไม่ได้พูดจะ **ย่อลง + มืดลง แต่คงอนิเมชันเดิม** — โกรธค้างจนกว่าจะมีบรรทัดสั่งเปลี่ยน
ทำได้โดย**ไม่เรียก `play()` กับคนที่ไม่ได้พูด** ห้ามใส่ `sprite.play("idle")` กลับเข้าไป

**สลับโหมดมีรูป/ไม่มีรูป**
```gdscript
start_dialogue(lines)               # มีรูปตัวละคร
start_dialogue(lines, null, false)  # ข้อความล้วน (เสียงบรรยาย/ประกาศ)
```

**โชว์เฉพาะคนที่มีบทพูด** (`only_show_speakers` = true) — ไล่หาคีย์ `"name"` ทั้งชุด**ก่อนเริ่มเล่น**
แล้วซ่อนคนที่ไม่ได้อยู่ในฉากนั้น · เดิมโชว์ทุกคนใน `Root/Portraits` พร้อมกัน บทที่มีคนพูดคนเดียว
เลยมีอีก 3 คนยืนหมอง ๆ อยู่ด้วย · ปิดเป็น `false` ได้ถ้าอยากให้ทุกคนอยู่ในเฟรมตลอด (ฉากประชุม)
⚠️ ต้องตัดสินตั้งแต่ต้นบท ไม่ใช่ทยอยโชว์ตอนถึงคิว ไม่งั้นจะดูเหมือนตัวละครวาร์ปเข้าฉาก

**ตัวละครในกล่องคำพูด 4 คน** — key ต้องตรงกับค่า `"name"` ในบทพูดเป๊ะ

| key | node ใต้ `Root/Portraits` |
|---|---|
| `"น้ำฟ้า"` | `Namfha` |
| `"วาวา"` | `Wawa` |
| `"โชแชง"` | `Chochang` |
| `"คีริน"` | `Kirin` |

⚠️ **ชื่ออนิเมชันแยกตัวพิมพ์เล็กใหญ่** — ตอนนี้วาวาใช้ `Angry` (ตัวใหญ่) ตัวอื่นเป็นตัวเล็ก
พิมพ์ผิดจะขึ้น warning บอกชื่อที่ผิดแล้วเล่น `talk` แทน (ไม่ crash)

⚠️ **ใส่กลุ่ม `dialogue_ui` ที่ `DialogueBox` (CanvasLayer) เท่านั้น** ห้ามใส่ node ลูก
`NPC.gd` หาด้วย `get_first_node_in_group()` ซึ่งอาจคืน node ลูกที่ไม่มีเมธอด `start_dialogue()` แล้ว crash

โครงสร้าง node ทั้งหมด + ค่าที่ต้องตั้ง: ดู [DIALOGUE_UI.md](DIALOGUE_UI.md)

**ทางเข้าบทสนทนา**
- `NPC.gd` — เข้าใกล้ขึ้นบับเบิล กด `interact` (E) แล้ว NPC หันหน้าหาผู้เล่นและเริ่มคุย
- เรียกอัตโนมัติตามเนื้อเรื่องได้ด้วย `start_dialogue()` — รองรับซีนที่ไม่มีผู้เล่น (เช่นบนรถเมล์) แล้ว
- ข้อมูลบทพูดตอนนี้อยู่ใน `DialogData/DialogueData.gd` เป็น **ค่า default ของ `@export`**
  ⚠️ `Game/player/new_resource.tres` ว่างเปล่าและยืมค่า default นั้น — ถ้าแก้ `dialogues` ใน Inspector
  Godot จะบันทึกสำเนาลง `.tres` แล้วบทพูดจะแช่แข็ง แก้สคริปต์ไม่มีผลอีก

### 3. ระบบแชทหลายกลุ่ม (ทำงานได้แล้ว — `Chat.tscn` เรียกจาก `wokeup.gd`)

**แยกเป็น 3 ชั้น: ข้อมูล → ห้องแชท → ตัวคุม**

| ไฟล์ | หน้าที่ |
|---|---|
| `chat_database.gd` (autoload) | ทะเบียนคน (`SENDERS`) + ทะเบียนกลุ่ม (`GROUPS`) + **บทแชททั้งหมด** (`HISTORY` / `INCOMING`) |
| `chat_group.gd` (`Chat_group.tscn`) | ห้องแชท **1 กลุ่ม** — ข้อความ ตัวคั่นเวลา กำลังพิมพ์ ตัวเลือกคำตอบ ปุ่มย้อนกลับ |
| `phone.gd` (`Chat.tscn` ราก `Chat`) | ตัวคุมบาง ๆ — ตัดสินใจว่าโชว์ห้องไหน / เด้งห้องไหนออกมา |
| `message_bubble.gd` | บับเบิลหนึ่งใบ: รูป/ชื่อ/ข้อความ/ซ้ายขวา/ความกว้าง |

**เพิ่มกลุ่มใหม่ = 3 ขั้น ไม่ต้องแตะสคริปต์เลย**
1. เพิ่มรายการใน `ChatDatabase.GROUPS` (ชื่อ + ไอคอน + สมาชิก) และ `HISTORY` / `INCOMING`
2. ลาก `Chat_group.tscn` เข้า `Chat.tscn` อีก 1 instance
3. ตั้ง `group_id` ที่ Inspector ให้ตรงกับคีย์ในข้อ 1

ชื่อกลุ่ม / ไอคอน / `"N online"` ดึงจากทะเบียนทั้งหมด ไม่ต้องกรอกซ้ำในซีน

**`HISTORY` vs `INCOMING` — เส้นแบ่งคือ "ผู้เล่นหยิบโทรศัพท์ขึ้นมาดูตอนไหน"**
ข้อความที่มาถึงก่อนผู้เล่นเปิด UI → `HISTORY` (ขึ้นครบทันที ไม่มีเสียง ไม่มีกำลังพิมพ์)
ข้อความที่มาถึงตอนผู้เล่นดูอยู่ → `INCOMING` (ทยอยเข้า + กำลังพิมพ์ + เสียงเตือน)

**รูปแบบบรรทัดในบทแชท**
```gdscript
{"sender": "wawa", "text": "..."}                  # ข้อความปกติ
{"sender": "wawa", "text": "...", "typing": 2.5}   # กำหนดเวลา "กำลังพิมพ์" เอง (วินาที)
{"sender": "wawa", "text": "...", "typing": 0}     # ข้ามช่วงกำลังพิมพ์ เด้งมาเลย
{"sender": "wawa", "text": "...", "sound": false}  # ไม่ต้องมีเสียงเตือนบรรทัดนี้
{"time": "06:30 น."}                               # ตัวคั่นเวลา แทรกได้ทุกจุด
{"choices": [...]}                                 # ถึงคิวผู้เล่นตอบ - หยุดรอจนกด
```

**เสียงเตือน** (`soud_effects/notification.mp3`) ดังพร้อมข้อความโผล่ ไม่ใช่ตอนเริ่มพิมพ์
ดังเฉพาะ `INCOMING` จากคนอื่น — ประวัติเก่าและข้อความที่น้ำฟ้าส่งเองไม่ดัง (ไม่มีใครเตือนตัวเอง)
`AudioStreamPlayer` สร้างจากโค้ด ไม่ต้องเพิ่ม node ในซีน

**ตัวเลือกคำตอบ** — โผล่ท้ายรายการข้อความ **ชิดขวาเหมือนข้อความของน้ำฟ้า** จำนวนต่อรอบไม่จำกัด
กดแล้วอันที่เลือกกลายเป็นข้อความจริงทันที ตัวอื่นหายไปหมด
```gdscript
{"choices": [
    "ตอบสั้น ๆ แล้วจบ",
    {"text": "ตอบแล้วมีคนตอบกลับ", "then": [{"sender": "wawa", "text": "..."}]},
]}
```
⚠️ **ไม่ต้องสร้าง node ในซีน** — `chat_group.gd` สร้างปุ่มเองตอนเรียกใช้แล้วลบทิ้งเมื่อเลือกเสร็จ
ปรับสี/ขนาดได้ที่ `@export` กลุ่ม *"ตัวเลือกคำตอบ"* บน instance

**กลุ่มที่เด้งออกมาข้าง ๆ** — พอกลุ่ม `popout_after_group` คุยจบ กลุ่ม `popout_target_group`
จะไถลออกมาโผล่ข้าง ๆ (ค่าเริ่มต้น: `smo` จบ → `sawat` เด้งไปทางซ้าย 620px)
**ทั้งสองแผงค้างอยู่บนจอพร้อมกัน** ผู้เล่นเห็นว่าเรื่องต่อกันยังไงโดยไม่ต้องกดสลับ
ปรับตำแหน่ง/ความเร็วได้ที่ `@export` กลุ่ม *"กลุ่มที่เด้งออกมา"* บน node `Chat`

**คุยจบแล้วเปลี่ยนฉาก** — ตอบตัวเลือกในกลุ่ม `end_after_group` เสร็จ → ล็อกอินพุตทันที
→ หน่วง `end_delay` → เฟดไป `end_scene` พร้อมฝาก `GameState.pending_intro = end_intro_id`
(ค่าเริ่มต้น: `sawat` จบ → `res://p.tscn` + อินโทร `arrive_bus`)
ตั้งได้ที่ `@export` กลุ่ม *"คุยจบแล้วเปลี่ยนฉาก"* บน node `Chat`

**API หลัก**
```gdscript
$Chat.open_group("smo")        # เปิดห้องเดียว ซ่อนที่เหลือ
$Chat.lock_input()             # ล็อกไม่ให้กดอะไรได้อีก (ปิดปุ่มทุกตัวในกิ่ง ไม่ใช่แค่ซ่อน)
$Chat.popout_group("sawat")    # เด้งออกมาข้าง ๆ โดยไม่ซ่อนห้องอื่น
$Chat.show_list()              # กลับหน้ารายการแชท (ยังไม่มี node ChatList)

var g = $Chat.get_group_view("sawat")
await g.receive_messages(lines)                  # ทยอยเข้ามาพร้อมบับเบิลกำลังพิมพ์
await g.show_choices(["ตอบ A", "ตอบ B"])          # คืน index ที่เลือก
```
น้ำฟ้า (`namfa`) อยู่ฝั่งขวา ไม่โชว์รูปและชื่อ · เข้าห้องซ้ำไม่โหลดบทใหม่ (`_loaded` กัน)

**แถบพิมพ์ข้อความเป็นของประดับ** — `LineEdit` ตั้ง `focus_mode = NONE` + `mouse_filter = IGNORE` จากโค้ด
ผู้เล่นพิมพ์เองไม่ได้ ตอนต้องตอบจริงจะใช้ `show_choices()` แทน
⚠️ **ห้ามตั้ง `line_edit.editable = false`** — LineEdit จะสลับไปใช้ StyleBox ชื่อ `read_only` ไม่ใช่ `normal`
สไตล์แคปซูลมนที่ตั้งไว้จะหายตอนรัน แล้วหน้าตาไม่ตรงกับที่เห็นใน editor

โครงสร้าง node ทั้งหมด + ค่าที่ต้องตั้ง: ดู [CHAT_UI.md](CHAT_UI.md)

### 4. เสียงในเกม

| เสียง | ไฟล์ | อยู่ที่ | พฤติกรรม |
|---|---|---|---|
| ฝีเท้า | `soud_effects/walk.ogg` | `player.gd` | เล่นตอนขยับ หยุดตอนหยุด/ถูกล็อก |
| พิมพ์บทพูด | `soud_effects/keyboard.mp3` | `dialogue_system.gd` | เล่นตอนเริ่มพิมพ์ หยุดตอนพิมพ์จบ |
| ข้อความเข้า | `soud_effects/notification.mp3` | `chat_group.gd` · `wokeup.gd` | ดังพร้อมข้อความโผล่ |
| คลิก | `soud_effects/click.ogg` | `click_effect.gd` (autoload) | สุ่มระดับเสียง ±12% |
| เพลงฉาก | `soud/Morning Bus Stop.mp3` | `wokeup.gd` | วนลูป ค่อย ๆ ดังขึ้น 2 วิ |

🚨 **ไฟล์เสียงยาว ๆ ต้องเล่นครั้งเดียวแล้วหยุด ห้ามสั่ง `play()` ซ้ำเป็นจังหวะ**
`walk.ogg` (8.098 วิ) และ `keyboard.mp3` (~19 วิ) เป็นเสียง**ต่อเนื่อง** ไม่ใช่เสียงก้าว/เคาะเดียว
สั่ง `play()` ทุกเฟรมจะได้ยินแค่เศษเสี้ยวแรกวนรัว ๆ (และ mp3 มักมีความเงียบต้นไฟล์ = ไม่ได้ยินอะไรเลย)
**เช็คขนาดไฟล์ก่อนออกแบบเสมอ** — เคยพลาดมาแล้วกับ `keyboard.mp3`

⚠️ **`loop` อยู่ที่ตัวทรัพยากร `AudioStream` ไม่ใช่ที่ `AudioStreamPlayer`** — ตั้งจากโค้ด `stream.loop = true`
ติ๊ก autoplay ใน editor อย่างเดียวได้เพลงที่เล่นรอบเดียวแล้วเงียบตลอดฉาก

`AudioStreamPlayer` ทุกตัวข้างบน**สร้างจากโค้ด** ไม่ต้องเพิ่ม node ในซีน (ผู้เล่นถูก instance หลายซีน)

### 5. เอฟเฟกต์คลิก (ใช้งานได้ทุกซีน)

`click_effect.tscn` เป็น autoload `ClickEffect` → วงกลมกระเพื่อม + เสียงคลิก (สุ่มระดับเสียง ±12%)
ใช้ `_input` ไม่ใช่ `_unhandled_input` เพื่อให้เกิดแม้คลิกโดนปุ่ม และไม่กลืนคลิก (ไม่เรียก `set_input_as_handled()`)

## โครงสร้างโฟลเดอร์ asset

| โฟลเดอร์ | เก็บอะไร |
|---|---|
| `image/` | รูปฉาก พื้นหลัง tileset |
| `image/align_npc/` | สไปรท์ NPC ปริศนาที่นั่ง |
| `image_avatar/` | รูปโปรไฟล์วงกลมสำหรับแชท (256×256) |
| `image_fullAvatar/` | รูปครึ่งตัวสำหรับกล่องคำพูด |
| `image_ui/` | ไอคอน UI |
| `font/` | ฟอนต์ (`2005_iannnnnAMD.ttf` ใช้ทั้งเกม) |
| `soud/` · `soud_effects/` | เพลง · เสียงเอฟเฟกต์ |

> ⚠️ **ชื่อไฟล์รูปต้องพิมพ์ตรงตัวพิมพ์เล็กใหญ่เป๊ะ ๆ**
> `res://` ของ Godot เป็น case-sensitive ตอน export แต่ Windows ไม่สน
> พิมพ์ผิดตัวเล็กใหญ่จะไม่พังตอนทดสอบ **แต่รูปจะหายตอน build จริง**
> ตอนนี้ใน `image_avatar/` ส่วนใหญ่ขึ้นต้นตัวใหญ่ (`Wawa.png`, `Jaja.png`, `Namking.png`,
> `Somsom.png`, `Chicken.png`) ยกเว้น `chochang.png` กับ `kame.png` ที่เป็นตัวเล็ก

## Input actions ที่ใช้

`interact` (E), `up`, `down`, `cancel` — ดูรายละเอียดใน `project.godot`

## ปัญหาที่รู้อยู่แล้ว (ยังไม่แก้)

1. **`p.tscn` — ต้นไม้แถวล่างหายไป 37 ช่อง** ⚠️ *บั๊กภาพจริง ยังไม่แก้ (ต้องตัดสินใจเรื่องอาร์ต)*

   เหลือ error 18 บรรทัด / นิยาม 6 อัน — **ทุกอันมีช่องบนแมพใช้จริง** จึงจงใจไม่ลบทิ้ง

   | ภาพ | `margins` | ตารางที่ใช้ได้ | ช่องที่วาดไว้ | ช่องที่โหว่ |
   |---|---|---|---|---|
   | `IMG_9068.PNG` | 0,22 | 3×4 | (0,4) (1,4) (2,4) | 9 |
   | `Tree2.png` | 0,10 | 5×4 | (1,4) (2,4) | 15 |
   | `Three1.png` | 0,8 | 5×4 | (2,4) | 13 |

   เลเยอร์ที่กระทบ: `tree` (24) · `treeP_5` (7) · `tree_P` (4) · `tree_P3` (2)

   **สาเหตุ:** ตั้ง `margins` แนวตั้งทีหลัง → พื้นที่ใช้งานหดลง เหลือ 4 แถวแทน 5
   **แถวสุดท้ายที่วาดไว้ก่อนหน้าเลยหลุดออกนอกตาราง** ช่องพวกนั้นวาดเป็นความว่างเปล่า

   ทางแก้ (ต้องเลือกเอง): เอา `margins` ออกแล้วจัดตำแหน่ง tile ใหม่ · หรือถมช่องเหล่านั้นด้วย tile ที่มีจริง

   > 🚨 **ห้ามลบนิยาม 6 อันนี้เพื่อให้ error เงียบ** — error คือสัญญาณเดียวที่บอกว่ามีช่องโหว่อยู่

2. ~~**TileSet นิยาม tile เกินขอบภาพ (2,549 อัน)**~~ ✅ **แก้แล้ว 2026-07-31**

   ไฟล์ภาพถูกย่อทีหลัง แต่นิยาม tile ยังอ้างพิกัดของภาพเวอร์ชันเดิม (`walk.png`, `Roongahan.png`,
   `upper.png`, `parking (1).png`, `Meeting room2.png`, `IMG_9067.PNG`)
   ถอดรหัส `tile_map_data` ทุกเลเยอร์แล้วยืนยันว่า **ไม่มีช่องไหนใช้เลย** จึงลบนิยามทิ้ง 2,549 บรรทัด
   จาก `align_1` `level1` `level_2` `level_3` `p` `under` → 5 ใน 6 ซีนเหลือ 0 error

   **บทเรียน:** ตารางจริงของ atlas = `(ขนาดภาพ − margins) ÷ (texture_region_size + separation)`
   ลืมคิด `margins` แล้วจะสรุปผิดว่า tile ปกติดี ทั้งที่มันหลุดขอบ (เกือบลบของที่ใช้อยู่ทิ้งเพราะเรื่องนี้)

3. ~~**Signal ต่อซ้ำใน `Start.gd`**~~ ✅ **แก้แล้ว 2026-08-01**

   `mouse_entered` / `mouse_exited` ต่อทั้งจาก editor และจากโค้ด → error 2 บรรทัดทุกครั้งที่เปิดเกม
   แก้เป็นเช็ค `is_connected()` ก่อนต่อ — เก็บทั้งสองฝั่งไว้ได้โดยไม่มี error
   และสคริปต์ยังทำงานได้ถ้าเอาไปใช้กับ node ที่ไม่ได้ต่อจาก editor

4. ~~**โค้ด debug ค้างใน `Start.gd`**~~ ✅ **แก้แล้ว 2026-08-01** (`print`, `print_stack`, `mouse_filter` ซ้ำ)

## Git

- Branch `main` → remote `origin` = https://github.com/pnadda-boop/Game-smo (**private**)
- `.gitignore` กัน `.godot/` (cache) และ `/SmoGame/` (exported build 127MB — เกินลิมิต 100MB ของ GitHub)
