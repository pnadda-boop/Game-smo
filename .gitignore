# Godot 4+ specific ignores
.godot/
/android/

# Exported builds (ไฟล์ที่ export ออกมา ไม่ต้องเก็บใน git)
/SmoGame/
*.exe
*.pck
export_presets.cfg

# ค่าตั้งเฉพาะเครื่อง (permission ของ Claude Code) ไม่ต้องแชร์
.claude/settings.local.json
