# DEVLOG — บันทึกการตัดสินใจและการเปลี่ยนแปลง

บันทึกว่า **ทำไม** ถึงตัดสินใจแบบนั้น ไม่ใช่แค่ทำอะไรไป
(สถานะปัจจุบันของเกมอยู่ใน [CLAUDE.md](CLAUDE.md) — ไฟล์นี้เก็บที่มาที่ไป)

**เรียงใหม่สุดไว้บนสุด** — เพิ่มรายการใหม่ที่ด้านบน

---

## 2026-07-30 — ⚠️ กฎเหล็ก: autoload ต้องเพิ่มผ่าน Godot เท่านั้น

**เกิดขึ้น 2 ครั้งในวันเดียว** — autoload ที่เติมลง `project.godot` จากข้างนอกตอน Godot เปิดอยู่ **หายไปเอง**

**กลไก:** Godot อ่าน `project.godot` ตอนเปิดโปรเจกต์ครั้งเดียวแล้วเก็บรายการ autoload ไว้ในหน่วยความจำ
เมื่อผู้ใช้แตะ Project Settings เรื่องใดก็ตาม Godot เขียนไฟล์ใหม่ทั้งบล็อกจากรายการในหน่วยความจำ
→ **autoload ที่มันไม่รู้จักถูกลบทิ้ง** โดยไม่มีคำเตือนอะไร

| ครั้ง | เกิดอะไร |
|---|---|
| 1 | `ClickEffect` ที่เพิ่มไว้หาย ตอนปิด-เปิด `Snap 2D Transforms To Pixel` เพื่อทดสอบเรื่องแสง |
| 2 | `ChatDatabase` (เติมจากข้างนอก) หาย ตอนเพิ่ม `ClickEffect` ผ่าน Project Settings |

**อาการเวลาเกิด:** สคริปต์ทุกไฟล์ที่เรียก autoload นั้นขึ้น `Identifier "X" not declared in the current scope`
พร้อมกันหมด ทั้งที่โค้ดไม่ได้ผิดอะไร

**กฎ:** เพิ่ม autoload ผ่าน `Project > Project Settings > Globals > Autoload` เท่านั้น
Claude ห้ามเติมบรรทัดลง `project.godot` เอง — ถ้าจำเป็นต้องให้ผู้ใช้ปิด Godot ก่อน

---

## 2026-07-30 — ปรับกล่องคำพูด: กำหนดอนิเมชันต่อบรรทัด + อารมณ์ค้าง

**ตัดสินใจ: refactor ไม่ใช่เขียนใหม่**
ตรรกะพิมพ์ทีละตัวอักษร / กดข้าม / เลื่อนบรรทัด / ไฮไลต์คนพูด ของเดิมทำงานถูกแล้วและใช้เวลาจูนนาน
ปัญหาทั้งหมดอยู่ที่**วิธีจัดเก็บข้อมูลตัวละคร** ซึ่ง refactor แก้ตรงจุด

**ฟีเจอร์ใหม่: `anim` ต่อบรรทัด**
```gdscript
{"name": "วาวา", "text": "...", "anim": "Angry"}   # ไม่ใส่ anim = ใช้ default_anim ("talk")
```

**ตัดสินใจ: อารมณ์ค้างไว้ ไม่รีเซ็ตเป็น idle**
คนไม่พูด → ย่อลง (`scale_idle` 0.9) + มืดลง (`color_idle` 0.35) แต่**คงอนิเมชันเดิมไว้**

**วิธีทำ:** ลบบรรทัด `sprite.play("idle")` ของคนที่ไม่ได้พูดออก — แค่นั้น
อารมณ์ค้างเองโดยไม่ต้องเขียนระบบจำสถานะเพิ่ม

**เหตุผล:** ถ้ารีเซ็ตเป็น idle ทุกครั้งที่อีกฝ่ายพูด อารมณ์ในฉากจะขาดตอน
คนโกรธควรโกรธค้างจนกว่าจะมีบรรทัดสั่งเปลี่ยน

**โครงสร้าง UI ใหม่ (`canvas_layer.tscn`)** — สร้างด้วย node ของ Godot ล้วน ไม่ใช้ nine-patch texture
รายละเอียดทั้งหมดอยู่ใน [CHAT_UI.md](CHAT_UI.md) และ [DIALOGUE_UI.md](DIALOGUE_UI.md)

**กลไกสำคัญ 2 อย่าง:**
- **กล่องข้อความขนาดคงที่** — `TextPanel.custom_minimum_size.y = 200` + `TextLabel.fit_content = false`
  ถ้าปล่อยให้ขยายตามข้อความ กล่องจะกระโดดเปลี่ยนขนาดทุกบรรทัด กวนสายตา
- **ป้ายชื่อยืดตามชื่อ** — `NamePlate.size_flags_horizontal = Shrink Begin`
  ชื่อ `"น้ำฟ้า"` กับ `"พี่วาวา หัวหน้าสวัสดิการ"` ยาวไม่เท่ากัน ต้องยืดหดได้

**บั๊กที่แก้ไปด้วย:**
1. **มี 3 node ในกลุ่ม `dialogue_ui`** (CanvasLayer, Control, Label) → `get_first_node_in_group()`
   อาจคืน node ที่ไม่มีเมธอด `start_dialogue()` แล้ว crash — เหลือ 1 node แล้ว
2. **`player.can_move` ไม่เช็ค null** → บทพูดอัตโนมัติในซีนที่ไม่มีผู้เล่น (รถเมล์) จะ crash
3. **`Root` มีขนาดแค่ 40×40** (ค่าเริ่มต้นตอนสร้าง Control) → กล่องคำพูดไปโผล่มุมซ้ายบนแทนที่จะอยู่ล่างจอ
4. ลบ `Game/NPC/np_cdialogue.tscn` — เวอร์ชันเก่าซ้ำซ้อน ไม่มีใครอ้างถึง

---

## 2026-07-30 — ซีน wokeup: ไล่หาสาเหตุแสงกระพริบ (ซ้อนกัน 3 ชั้น)

ใช้เวลานานเพราะเป็นปัญหาซ้อนกันหลายชั้น และแต่ละชั้นบังชั้นถัดไป

**ชั้นที่ 1 — `shimmer_strength` ในซีนทับค่า default ของ shader**
shader ตั้ง default `0.0` ไว้พร้อมคอมเมนต์ว่าปิดเพื่อกันกระพริบ
แต่ `ShaderMaterial` ใน `wokeup.tscn` เก็บ `0.06` ไว้ — **ค่าที่บันทึกในซีนชนะ default ของ shader เสมอ**

**ชั้นที่ 2 — Polygon2D ไม่มี `uv`**
shader คำนวณจาก `UV` โดยสมมติว่าไล่ 0→1 แต่ Polygon2D ไม่มี uv จึงได้ค่าที่ไม่ใช่ 0–1
→ `edge_fade * length_fade` เป็น 0 ทั้งรูป → **แสงที่เห็นมาตลอดคือ shimmer เพียงอย่างเดียว**
อธิบายทั้งสองอาการพร้อมกัน: กระพริบทั้งลำ (shimmer เต้นทั่วรูป) และปิด shimmer แล้วหายเกลี้ยง (เหลือ 0×0)

แก้โดยใส่ `uv = PackedVector2Array(1, 0, 0, 0, 0, 0.39, 0, 1, 1, 1)` คำนวณจากรูปทรง polygon จริง

**ชั้นที่ 3 — ตั้ง `shimmer_strength` เป็น 0.3 (ค่าสูงสุดของสไลเดอร์)**
shader **บวก** shimmer เข้ากับความสว่าง ที่ 0.3 แกว่งได้ถึง ±0.45
→ ขึ้นสุด `clamp` ตัดเป็น 1.0 ทั่วรูป (เฉดไล่แสงหาย) → ลงสุดเป็น 0 (แสงดับ)
รอบคลื่น ~10.5 วิ ตรงกับอาการ "จางหายไปสักพักแล้วจางกลับมา"

**ทฤษฎีที่ผิด:** เคยสงสัย `camerashaking..gd` (สั่น 1 Hz) + `snap_2d_transforms_to_pixel`
แต่จังหวะ 1 Hz ไม่เข้ากับอาการที่ช้าเป็นสิบวินาที — **กล้องสั่นถูกต้องแล้ว** เพราะซีนนี้คือตื่นบนรถเมล์

**บทเรียน:** อาการที่ผู้ใช้อธิบายละเอียด ("ค่อย ๆ จางหายแล้วค่อย ๆ กลับมา") มีค่ามากกว่าคำสั้น ๆ
("กระพริบเป็นจังหวะ") — รอบคลื่นที่ต่างกัน 10 เท่าชี้ไปคนละสาเหตุเลย

**ตัดสินใจ: ถอดระบบซูมกล้องตอนปลุกออก**
ทำเสร็จแล้วแต่ผู้ใช้ทดลองแล้วรู้สึกว่าไม่เวิร์ค — ถอดออกหมด ไม่เก็บไว้เพราะเสียดายที่เขียนไปแล้ว
เหตุผลเชิงออกแบบ: ซีนนี้มีกล้องสั่นอยู่แล้ว การซูมเพิ่มเข้าไปทำให้ภาพวุ่นแทนที่จะตื่นเต้น

**เก็บไว้: เพิ่มจำนวนคลิกปลุก** 8–13 → **22–32 ครั้ง** (ทำเป็น `@export` ปรับได้ที่ Inspector)
เพื่อให้ผู้เล่นต้องกดย้ำ ๆ เหมือนพยายามปลุกคนจริง

---

## 2026-07-30 — เอฟเฟกต์คลิก (ripple + เสียง)

`click_effect.tscn` เป็น autoload → ทำงานทุกซีนโดยไม่ต้องลากใส่ทีละซีน (มี 24 ซีน ลืมแน่)

**ตัดสินใจ: ใช้ `_input` ไม่ใช่ `_unhandled_input`**
`_unhandled_input` ทำงานหลังสุด ถ้าคลิกโดนปุ่มหรือ Control ที่ `mouse_filter = Stop` จะไม่เกิด ripple
`_input` ได้รับก่อนใคร และ**ไม่เรียก `set_input_as_handled()`** คลิกจึงยังส่งต่อให้ปุ่มทำงานปกติ
(ถ้าเรียก จะกลืนคลิกทั้งเกม ปุ่มกดไม่ได้เลยสักปุ่ม)

**บั๊กที่แก้:** `Ripple` ตั้ง `anchors_preset = 15` (เต็มจอ) แต่ `Circle` ข้างใน anchor กึ่งกลาง
→ วงกลมไปโผล่ที่ จุดคลิก + (960, 540) เยื้องครึ่งจอ แก้เป็น `anchors_preset = 0`

**เพิ่ม: สุ่มระดับเสียง ±12% ทุกครั้งที่คลิก**
เพราะเกมมีฉากกดรัว 20–30 ที ถ้าเสียงเหมือนกันเป๊ะจะฟังเหมือนเครื่องจักรและน่ารำคาญเร็ว

---

## 2026-07-30 — จัดระเบียบโฟลเดอร์ asset

แยกรูปตามหน้าที่ ทำใน Godot editor จึงอัปเดต path ในซีนให้เองอัตโนมัติ

| โฟลเดอร์ | เก็บอะไร |
|---|---|
| `image_avatar/` | รูปโปรไฟล์วงกลมสำหรับแชท (256×256) |
| `image_fullAvatar/` | รูปครึ่งตัวสำหรับกล่องคำพูด |
| `image_ui/` | ไอคอน UI (ปุ่มส่ง) |
| `image/align_npc/` | สไปรท์ NPC ปริศนาที่นั่ง |
| `soud_effects/` | เสียงเอฟเฟกต์ |

> ⚠️ นามสกุลไฟล์รูป avatar เป็น **`.PNG` ตัวใหญ่** — `res://` ของ Godot เป็น case-sensitive ตอน export
> Windows ไม่สนตัวพิมพ์ จึงไม่พังตอนทดสอบ แต่รูปจะหายตอน build จริง ต้องเขียนให้ตรงเป๊ะ

---

## 2026-07-29 — ออกแบบระบบแชทกรุ๊ปสโม

**ตัดสินใจหลัก: แยก "ใครเป็นใคร" ออกจาก "พูดอะไร"**

บทแชทอ้างถึงคนด้วย `sender_id` เท่านั้น ส่วนชื่อ/รูป/ฝั่งซ้ายขวา เก็บใน `chat_database.gd` (autoload `ChatDatabase`)

**เหตุผล:** ถ้าใส่รูปทีละข้อความ พอคนหนึ่งพูด 20 ครั้งต้องลากรูปเดิม 20 รอบ
วันไหนเปลี่ยนรูป profile ต้องไล่แก้ 20 จุด ลืมจุดเดียวจะมี avatar ผิดคนโผล่
และการผูกฝั่งซ้าย/ขวาไว้กับ **ตัวคน** แทนที่จะเป็น **ข้อความ** ทำให้ใส่ผิดฝั่งไม่ได้เลยตั้งแต่ต้น

ใช้แพตเทิร์นเดียวกับ `npcalign_database.gd` ที่มีอยู่แล้ว เพื่อให้โค้ดทั้งเกมเป็นแนวเดียวกัน

**ตัดสินใจ: น้ำฟ้าส่งข้อความได้ แต่ไม่โชว์รูปและชื่อ**
ตรงกับพฤติกรรมแชทจริง (LINE/Discord ไม่โชว์ profile ตัวเองฝั่งขวา) — เก็บเป็น `side: "right"` + `avatar: null`
ถ้าอยากให้โชว์ชื่อตัวเองด้วย ลบ 2 บรรทัดใน `message_bubble.gd` ได้

**ตัดสินใจ: `ChoiceContainer` ย้ายไปอยู่ใต้ `Down`**
เดิม `ChoiceContainer` เป็นลูกของ `Phone` (CanvasLayer) ตรง ๆ และ `Down` เป็นแถบว่างเปล่า
**เหตุผล:** น้ำฟ้าตอบด้วยการ**เลือกตัวเลือก** ไม่ได้พิมพ์เอง แถบล่างจึงไม่ควรเป็นช่องพิมพ์
แต่ควรเป็นที่อยู่ของปุ่มตัวเลือก — ย้ายที่เดียวแก้ได้ 2 ปัญหาพร้อมกัน

**ปัญหาความกว้างบับเบิล และวิธีแก้**
Godot ไม่มี property "ความกว้างสูงสุด" ให้ container และ Label ที่เปิด autowrap
มี minimum width เท่ากับคำที่ยาวที่สุด → ถ้าปล่อยอิสระบับเบิลจะยืดยาวเป็นบรรทัดเดียว
แต่ถ้าปัก `custom_minimum_size` ตายตัว ข้อความสั้นอย่าง "รับทราบค่ะ" ก็จะกลายเป็นแท่งยาว

**วิธีที่เลือก:** วัดความกว้างข้อความจริงตอนสร้างบับเบิล (วัดทีละบรรทัดแล้วเอาบรรทัดกว้างสุด
เพราะ `get_string_size` ไม่เข้าใจ `\n`) ถ้าเกิน `max_width` (420) ค่อยปักไว้ให้ autowrap ตัดบรรทัด
ถ้าไม่เกินก็ตั้งเป็น 0 ปล่อยให้หดตามเนื้อหา — และตั้ง y = 0 เสมอเพื่อให้ความสูงมาจากจำนวนบรรทัดจริง

**auto-scroll ต้องรอ 2 เฟรม**
เฟรมแรก VBox คำนวณความสูงใหม่หลังเพิ่มบับเบิล เฟรมที่สอง scrollbar ถึงอัปเดต `max_value`
ถ้าเลื่อนทันทีจะได้ค่าเก่า ข้อความใหม่ยังโผล่ไม่พ้นจอ

**จังหวะข้อความ:** หน่วงตามความยาวข้อความ (`0.6 + 0.02 ต่อตัวอักษร` เพดาน 2.5 วิ)
ข้อความสั้นเด้งเร็ว ข้อความยาวรอนานขึ้น เหมือนอีกฝ่ายกำลังพิมพ์จริง

**ไฟล์ที่เพิ่ม:** `chat_database.gd`, `message_bubble.gd`, `phone.gd` + autoload `ChatDatabase` ใน `project.godot`

---

## 2026-07-29 — ⚠️ แก้ความเข้าใจผิด: `--check-only` ใช้ตรวจโค้ดโปรเจกต์นี้ไม่ได้

เดิมบันทึกไว้ใน CLAUDE.md ว่าใช้ `--headless --check-only --script` ตรวจ syntax ได้ — **ผิด**

**สิ่งที่เกิดขึ้น:** สคริปต์แชททั้ง 3 ไฟล์ FAIL ด้วย `Compile Error: Identifier not found: ChatDatabase`
ทดสอบเทียบกับ `SeatManager.gd` ซึ่งทำงานได้จริงในเกมมาตลอด → **FAIL ด้วยข้อความเดียวกัน**
(`Identifier not found: NPCalignDatabase`)

**สรุป:** โหมด `--check-only` ไม่โหลด autoload จึงมองไม่เห็น `Global`, `GameState`,
`NPCalignDatabase`, `SeatManager`, `ChatDatabase` — รายงาน error ที่ไม่มีอยู่จริง

**ผลกระทบ:** ถ้าเชื่อผลจากโหมดนี้จะเสียเวลาไล่แก้บั๊กที่ไม่มีอยู่ หรือแย่กว่านั้นคือ
ไปแก้โค้ดที่ถูกอยู่แล้วจนพังจริง

**ใช้ `--import` แทนเสมอ** — โหลด autoload ครบ แก้ CLAUDE.md แล้ว

---

## 2026-07-29 — วางระบบบันทึกความรู้ของโปรเจกต์

**ตัดสินใจ:** แยกเอกสารเป็น 2 ไฟล์ แทนที่จะรวมไว้ไฟล์เดียว

| ไฟล์ | ตอบคำถาม | ลักษณะ |
|---|---|---|
| `CLAUDE.md` | "ตอนนี้เกมเป็นยังไง" | เขียนทับได้ ให้สะท้อนสถานะล่าสุดเสมอ |
| `DEVLOG.md` | "ทำไมถึงมาเป็นแบบนี้" | สะสมเรื่อย ๆ ไม่ลบของเก่า |

**เหตุผล:** ถ้ารวมไฟล์เดียว เอกสารจะบวมจนหาข้อมูลปัจจุบันไม่เจอ เพราะปนกับประวัติเก่า
และเวลากลับมาอ่านย้อน มักอยากรู้อย่างใดอย่างหนึ่ง ไม่ใช่ทั้งสองพร้อมกัน

**กฎการทำงานที่ตกลงกันไว้:**
1. สิ่งที่คุยกันแล้วมีข้อสรุป → จดลง MD ไม่ปล่อยให้หายไปกับ session
2. การตัดสินใจที่กระทบแนวคิดพื้นฐานของเกม → จดลง DEVLOG ทุกครั้ง
3. เวลาแก้โค้ด → อธิบายเหตุผลและทางเลือกที่พิจารณา ไม่แก้เงียบ ๆ
4. หลังแก้ `.gd` / `.tscn` → ตรวจด้วย Godot headless ก่อนบอกว่าเสร็จ

---

## 2026-07-29 — ตั้ง git และ GitHub repo

**ตัดสินใจ:** ตั้ง git ก่อนเริ่มแก้โค้ดใด ๆ

**เหตุผล:** โปรเจกต์ทำมาตั้งแต่เมษายน 2026 โดยไม่มี version control เลย — แก้อะไรพลาดแล้วย้อนไม่ได้
ความเสี่ยงนี้สูงกว่าความเสี่ยงจากการให้ AI แก้โค้ดหลายเท่า จึงต้องมี safety net ก่อน

- Branch `main` → https://github.com/pnadda-boop/Game-smo (**private**)
- Commit จุดเซฟแรก `f360362` — 258 ไฟล์

**ตัดสินใจย่อย:** ไม่เอา `SmoGame/Test.exe` (127MB) ขึ้น git
**เหตุผล:** เป็น exported build ที่สร้างใหม่ได้เสมอ + เกินลิมิต 100MB ของ GitHub (push จะถูกปฏิเสธ)
ไฟล์ยังอยู่ในเครื่องปกติ แค่ git ไม่แตะ — เพิ่ม `/SmoGame/`, `*.exe`, `*.pck` ลง `.gitignore`

---

## 2026-07-29 — ตรวจสุขภาพโปรเจกต์ครั้งแรก

รัน Godot headless ตรวจทั้งโปรเจกต์ เพื่อตอบคำถามว่า "โค้ดยังเชื่อมกับ Godot สมบูรณ์ไหม"

**ผล: โครงสร้างแข็งแรง** — 0 script error, 0 warning, 0 resource หาย, autoload ครบ 4 ตัว, เกมรันจบ exit 0

**แต่เจอปัญหา 3 ข้อ** (รายละเอียดใน [CLAUDE.md](CLAUDE.md) หัวข้อ "ปัญหาที่รู้อยู่แล้ว"):

1. **TileSet อ้างพิกัดเกินขอบภาพ** — ~4,000 errors
   ยืนยันแล้วว่าเกิดตอน **runtime จริง** ไม่ใช่แค่ใน editor (รัน `level1.tscn` ตรง ๆ ได้ 816 errors)
   วินิจฉัย: ไฟล์ภาพถูกเปลี่ยนเป็นขนาดเล็กลงทีหลัง แต่ tile ที่วางไว้ในฉากยังอ้างพิกัดของภาพเวอร์ชันเดิม
   เช่น `image/walk.png` กว้าง 192px = 6 คอลัมน์ (0–5) แต่ฉากอ้างถึง (6,0) และ (7,0)

   **ยังไม่แก้ เพราะเป็นการตัดสินใจเชิงอาร์ต ไม่ใช่เชิงโค้ด** — มี 2 ทางที่ให้ผลต่างกันมาก:
   - (ก) หาไฟล์ภาพเวอร์ชันเดิมที่ใหญ่กว่ามาใส่คืน → แมปกลับมาเหมือนเดิม
   - (ข) ลบ tile ที่เกินขอบทิ้ง → แมปจะโหว่ตรงนั้นจริง ๆ ต้องวาดใหม่

2. **Signal ต่อซ้ำใน Start.gd** — ต่อทั้งในโค้ดและใน scene file
3. **โค้ด debug ค้างใน Start.gd** — `print()`, `print_stack()`, ตั้ง `mouse_filter` ซ้ำ

---

## 2026-07-29 — บันทึกตัวตนของเกม

**เกม:** Pixel Art Top-Down Narrative Simulator ภาษาไทย
เล่าเรื่องของ **น้ำฟ้า** กับการทำงาน **สโม** — มีระบบแชทข้อความและอีเวนท์ต่าง ๆ

**สถานะฟีเจอร์ ณ วันที่บันทึก:**

| ฟีเจอร์ | สถานะ |
|---|---|
| บทสนทนา (NPC + บับเบิล) | 🟢 ใช้งานได้ |
| ปริศนาจัดที่นั่ง (15 เงื่อนไข) | 🟢 ใช้งานได้ |
| เดินสำรวจ / บันได / เปลี่ยนชั้น | 🟢 ใช้งานได้ |
| มินิเกมปลุกตัวเอง | 🟢 ใช้งานได้ |
| ระบบแชทข้อความ | 🔴 UI เปล่า ยังไม่มีโค้ด |
| ระบบอีเวนท์ | ❓ ยังไม่เจอระบบกลางที่คุมอีเวนท์ |

**ข้อสังเกต:** ระบบปริศนาจัดที่นั่งออกแบบไว้ดี — ขอบเขตแถว/คอลัมน์คำนวณจากที่นั่งที่ลงทะเบียนจริง
ไม่ hardcode จึงเพิ่ม/ลดที่นั่งได้โดยไม่ต้องแก้ logic และรองรับเงื่อนไขหลายข้อพร้อมกัน (AND)

**คำถามที่ยังไม่มีคำตอบ:** "อีเวนท์" หมายถึงอะไร — เหตุการณ์ตามเนื้อเรื่องที่ trigger เอง,
งานของสโมที่ผู้เล่นต้องไปทำ, หรือระบบสุ่มเหตุการณ์? ยังไม่เจอ event manager กลางในโค้ด
