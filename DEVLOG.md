# DEVLOG — บันทึกการตัดสินใจและการเปลี่ยนแปลง

บันทึกว่า **ทำไม** ถึงตัดสินใจแบบนั้น ไม่ใช่แค่ทำอะไรไป
(สถานะปัจจุบันของเกมอยู่ใน [CLAUDE.md](CLAUDE.md) — ไฟล์นี้เก็บที่มาที่ไป)

**เรียงใหม่สุดไว้บนสุด** — เพิ่มรายการใหม่ที่ด้านบน

---

## 2026-08-21 (รอบ 5) — สลับ Quest → Bubble ตามว่าเคยคุยบทหลักหรือยัง

**ที่ผู้ใช้ขอ:** บทตอนเจอวาวาครั้งแรกใช้ไอคอน Quest พอคุยจบถึงเปลี่ยนเป็น Bubble

**กลับคำตัดสินใจของรอบ 4** ที่ให้ *"ไอคอนเควสหลบให้บับเบิลตอนผู้เล่นเข้าระยะ"*
ตอนนั้นยังไม่รู้ว่าสองไอคอนนี้ควรสัมพันธ์กันยังไง เลยเลือกกฎที่ปลอดภัยที่สุด
(อันที่กดได้เดี๋ยวนี้ชนะ) · ตอนนี้รู้แล้วว่ามันคือ **สัญญาณสองจังหวะของเรื่องเดียวกัน**
ไม่ใช่สองสัญญาณที่แข่งกันอยู่ — "มีเรื่องมาฝาก" แล้วจึงเป็น "คุยได้"

### เงื่อนไขที่ถูกคือ `_main_dialogue_used` ไม่ใช่ `_talked`

ทั้งสองตัวมีอยู่แล้วในไฟล์ และไฟล์ก็เขียนกำกับไว้เองว่าทำไมถึงต้องแยกกัน:
`_talked` ตั้งตั้งแต่ *เริ่ม* คุย ซึ่งเกิด**ก่อน**ดึงบทพูด

ผลพลอยได้ที่ถูกโดยบังเอิญแต่ถูกจริง ๆ: **พิมพ์คีย์บทผิดจนหาบทไม่เจอ →
`_main_dialogue_used` ไม่ถูกตั้ง → ไอคอนเควสยังอยู่** ซึ่งเป็นสิ่งที่ควรเกิด
เพราะผู้เล่นยังไม่ได้รับเรื่องอะไรไปจริง ๆ (ตรงกับเหตุผลที่ `_get_dialogue_lines()`
ตั้งธงหลังยืนยันว่าบทมีอยู่จริงเท่านั้น)

### ทำไมไม่ใช้ธง `_in_dialogue` แยก

ต้องซ่อนไอคอนระหว่างคุยด้วย ทางที่ดูตรงไปตรงมาคือธง `_in_dialogue`
ตั้ง `true` ตอน `interact()` แล้ว `false` ตอน `reset_direction()`

**แต่ `_on_interact()` มีทางออกก่อนเปิดบทได้ 2 ทาง** (ไม่มีบทตามคีย์นั้น ·
หา `dialogue_ui` ไม่เจอ) ทั้งสองทาง `reset_direction()` จะไม่มีวันถูกเรียก
→ **ธงค้าง `true` แล้วไอคอนหายถาวรตลอดฉาก**

ที่ใช้แทน: เรียก `_refresh_quest_icon()` **หลัง** `_on_interact()` แล้วอ่านสถานะจริง
· บทเปิดสำเร็จ → ธงถูกตั้งแล้ว → ไอคอนหาย ✓
· บทไม่ได้เปิด → ไอคอนกลับมาเอง ✓ (ถูกแล้ว — ไม่มีอะไรเกิดขึ้น)
**ไม่มีสถานะค้างเพราะไม่มีสถานะให้ค้าง**

⚠️ ลำดับสำคัญ — วาง `_refresh_quest_icon()` ไว้ก่อน `_on_interact()` จะอ่านค่าเก่า
แล้วไอคอนเควสลอยค้างอยู่ระหว่างที่กล่องคำพูดเปิดอยู่

### ตรวจตามลำดับที่ผู้เล่นเจอจริง 6 จังหวะ — ผ่านทุกข้อ

ยังไม่เจอกัน → Quest · เข้าระยะครั้งแรก → **Quest ไม่ใช่ Bubble** · ระหว่างคุย → ไม่มีอะไร ·
คุยจบยังยืนใกล้ → **Bubble ขึ้นแทน Quest หาย** · เดินออก → ไม่เหลืออะไร ·
กลับเข้ามาใหม่ → **Bubble ไม่ใช่ Quest** (และยังลอยอยู่)

---

## 2026-08-21 (รอบ 4) — ไอคอนเควสลอยเหนือหัว NPC (โหนด `Quest`)

**ที่ผู้ใช้ขอ:** ให้ `Quest` ใช้รูป `Quest.png` ลอยขึ้นลงและตำแหน่งแบบเดียวกับ Bubble

### ใช้ค่าชุดเดียวกับบับเบิล ไม่สร้างชุดใหม่ขนาน

`data.indicator_offset` · `indicator_float_distance` · `indicator_float_speed`
ผู้ใช้บอกว่า *"แบบเดียวกับ Bubble"* — ถ้าทำ `quest_offset` / `quest_float_*` แยก
ก็จะได้ค่าที่ต้องคอยกรอกให้ตรงกันเอง แล้ววันหนึ่งขยับบับเบิลแล้วลืมขยับไอคอน
**ตอนนี้ขยับ `indicator_offset` ใน `.tres` ที่เดียว ขยับทั้งคู่**
(เหลือไว้แค่ `quest_texture` / `quest_scale` ซึ่งเป็นของเฉพาะตัวจริง ๆ)

### ผลพวงที่ต้องตัดสิน: สองอันอยู่จุดเดียวกันเป๊ะ

"ตำแหน่งแบบเดียวกับ Bubble" แปลว่าถ้าโชว์พร้อมกันจะทับกันเป็นก้อนเดียวอ่านไม่ออก
→ **ไอคอนเควสหลบให้บับเบิลตอนผู้เล่นเข้าระยะ** แล้วกลับมาตอนเดินออก
บับเบิลชนะเพราะเป็นสัญญาณของ *"กดได้เดี๋ยวนี้"* ส่วนไอคอนเควสเป็น
*"ตัวนี้มีเรื่องให้ทำ"* ซึ่งรอได้ · เป็นการตัดสินใจของผม ผู้ใช้ยังสลับได้

### เปิด/ปิดด้วยช่อง `Visible` ของโหนด ไม่ทำ `@export has_quest`

สองที่ที่พูดเรื่องเดียวกันจะติ๊กไม่ตรงกันสักวัน แล้วอาการคือ "ไอคอนไม่ขึ้นทั้งที่ติ๊กแล้ว"
· `_quest_wanted` จำค่าตอน `_ready` เพราะระหว่างเล่นเราซ่อนมันชั่วคราวตอนบับเบิลขึ้น
แล้วต้องรู้ว่าจะคืนสถานะเป็นอะไร — คืนเป็น `true` ทื่อ ๆ = NPC ที่ไม่มีเควสจะมีไอคอนงอก

### กับดักที่เจอตอนต่อของเข้าระบบเดิม

**`hide_indicator()` เดิมสั่ง `set_process(false)` ทื่อ ๆ** เพราะตอนนั้น `_process`
มีหน้าที่เดียวคือทำให้บับเบิลลอย · พอมีของลอยเป็นตัวที่สอง การเดินออกนอกระยะ
จะไปหยุดการลอยของไอคอนเควสด้วย **แล้วมันค้างนิ่งกลางอากาศตลอดฉาก**
→ แยกเป็น `_update_float_process()` ที่ดูทั้งสองตัว

**นับเวลาคนละตัว** — `_indicator_time` ถูกรีเซ็ตเป็น 0 ทุกครั้งที่บับเบิลป่องออกมา
(ตั้งใจ จะได้เริ่มจากจุดกลาง) ใช้ร่วมกันเมื่อไหร่ ไอคอนเควสจะกระตุกทุกครั้งที่ผู้เล่นเข้าระยะ

⚠️ **ไฟล์จริงชื่อ `Quest.PNG` นามสกุลตัวใหญ่** — `res://` case-sensitive ตอน export
พิมพ์ `.png` ตามที่ผู้ใช้เขียนมาจะผ่านตอนทดสอบบน Windows **แต่รูปหายตอน build จริง**
(กับดักเดียวกับ `item/ItemBox.PNG` ที่จดไว้แล้ว)

### ตรวจด้วยซีนทดสอบชั่วคราว 6 ข้อ — ผ่านทุกข้อ แล้วลบทิ้ง

รูปเป็น `Quest.PNG` · `quest.position == indicator.position` · `offset.y` เปลี่ยนจริง
และไม่เกินระยะที่ตั้งไว้ · หลบตอนบับเบิลขึ้น · กลับมาตอนบับเบิลหาย · **ลอยต่อไม่ค้างนิ่ง**

---

## 2026-08-21 (รอบ 3) — เดินชน NPC ไม่ได้ เพราะทุกซีนตั้ง mask ผู้เล่นเอง

**ที่ผู้ใช้แจ้ง:** เดินชน NPC ไม่ได้ (เดินทะลุ)

### ไม่ได้เดาจากการอ่านโค้ด — วัดค่าจริงตอนรัน

พิมพ์ `collision_layer` / `collision_mask` ของทั้งสองฝั่งออกมา แล้วยิง `move_and_collide()`
เข้าหา NPC · ได้ **player mask = 16 · NPC layer = 4** → ไม่มีบิตตรงกันเลย
แล้วลองเซ็ต `mask |= 4` ในเทสต์เดียวกัน → ชนทันที **ยืนยันสาเหตุก่อนแตะโค้ดจริง**

ถ้าเชื่อ `player.tscn` อย่างเดียวจะสรุปผิด — ไฟล์นั้น**ไม่ได้ตั้ง `collision_mask` เลย**
(ได้ default 1) ค่า 16 ที่เห็นตอนรันมาจาก override ที่ instance ในซีนแมพ

### ปัญหาจริงคือ "ค่าเดียวกันถูกตั้งไว้ 4 ที่ และไม่ตรงกัน"

| ซีน | mask | ชนอะไรได้ |
|---|---|---|
| `player.tscn` ต้นฉบับ | ไม่ได้ตั้ง (1) | layer 1 |
| `under.tscn` · `level_2.tscn` | 16 | กำแพงอย่างเดียว |
| `level1.tscn` | 17 | layer 1 + กำแพง |
| `p.tscn` | 17 (+ layer 17) | layer 1 + กำแพง |

**ไม่มีซีนไหนเปิดบิต 4 → NPC ระบบใหม่ถูกเดินทะลุมาตั้งแต่วันที่สร้าง**
ไม่ใช่ของที่เพิ่งพัง · NPC ตัวเก่า (`wawa.tscn`) ก็ทะลุเหมือนกัน แค่ยังไม่มีใครสังเกต

🚨 **แก้ที่ `player.tscn` ที่เดียวไม่พอ — override ที่ instance ชนะค่าจากซีนต้นฉบับเสมอ**
นี่คือกับดักเดียวกับที่จดไว้เรื่อง UI: *"ค่าเดียวกันกระจายอยู่ทุกซีน ลืมซีดเดียว
= บั๊กที่เจอตอนเล่นไปถึงฉากนั้นแล้วเท่านั้น"*

### ทางที่เลือก: `collision_mask |= 4` ใน `player.gd._ready()`

ทางที่ทิ้ง — **ไล่ลบ override ทั้ง 4 ซีนแล้วตั้งค่าเดียวที่ `player.tscn`** ซึ่งสะอาดกว่าจริง
แต่ (ก) ต้องแก้ `.tscn` 4 ไฟล์ **ตอนที่ผู้ใช้เปิด Godot ทำงานอยู่** ซึ่งเป็นสิ่งที่โปรเจกต์นี้
ห้ามไว้ (ปุ่ม Reload from disk merge ไฟล์ผิด — DEVLOG 2026-07-31) และ (ข) ค่าที่ override
ไว้ต่างกันอาจตั้งใจต่างกันจริง — ยังไม่รู้ว่าทำไม `p.tscn` ถึงต้องมี `collision_layer = 17`
**ลบทิ้งทั้งหมดโดยไม่รู้เหตุผลคือการแลกบั๊กหนึ่งกับอีกบั๊กหนึ่ง**

บรรทัดในโค้ดจึงเป็น**ตาข่ายรับที่ทำงานทุกฉากรวมถึงฉากที่ยังไม่ได้สร้าง**
และไม่ปิดทางที่จะยุบให้เหลือค่าเดียวทีหลัง

⚠️ **`|=` ไม่ใช่ `=`** — ทับทื่อ ๆ จะลบบิตกำแพง (16) ทิ้ง **แล้วผู้เล่นจะเดินทะลุกำแพงแทน**
ซึ่งแย่กว่าอาการเดิมมาก · เทสต์จึงเช็คทั้งสองบิต ไม่ใช่เช็คแค่ว่าชน NPC ได้
· ผล: `under` และ `all_level_2` ได้ mask 20 (16+4) · ชน `Wawa` และ `Cho` ได้ · กำแพงยังกั้นอยู่

---

## 2026-08-21 (รอบ 2) — HUD ช่องของหลบให้กล่องคำพูดตอนคุย NPC

**ที่ผู้ใช้ขอ:** คุยกับ NPC แล้วกล่องไอเทมหายไป พอคุยเสร็จกลับมา ของไม่ได้หายไปไหน

**ปิดการตัดสินใจที่ค้างมาตั้งแต่รอบ 13** — CLAUDE.md เขียนไว้เองว่ามี 2 ทาง
(ซ่อนตอนคุย / ย้าย HUD ไปมุมขวาบน) และ *"ยังไม่ทำเพราะเป็นการตัดสินใจเรื่องหน้าตาเกม
ไม่ใช่เรื่องเทคนิค"* · ผู้ใช้เลือกทางแรก

ทางที่ไม่ได้เลือกแย่กว่าตรงที่ **ย้ายมุมคือการทำให้ช่องของอยู่ไกลสายตาตลอดเวลา
เพื่อแก้ปัญหาที่เกิดแค่ตอนคุย** ส่วนช่องของก็ไม่มีอะไรให้อ่านระหว่างบทสนทนาอยู่แล้ว

### "ของไม่หาย" ได้ฟรีจากสถาปัตยกรรมที่วางไว้รอบ 12

`UIRoot.hide_ui()` **ซ่อน ไม่ลบ** — ข้อนี้เขียนกำกับไว้ตั้งแต่ตอนสร้าง `ui_root.gd` แล้ว
ด้วยเหตุผลว่า *"HUD ที่ถูกลบแล้วสร้างใหม่จะลืมว่าผู้เล่นเลือกช่องไหนอยู่"*
คำขอ "ของไม่ได้หายไปไหน" จึงไม่ต้องเขียนอะไรเพิ่มเลย กลับมาเป็นโหนดเดิมทุกอย่าง

### สองกับดักที่ต้องกันไว้ ไม่งั้นพังทีหลังแบบหาสาเหตุยาก

**1. ห้ามเรียก `show_ui()` ทื่อ ๆ ตอนจบบท — ตัวนั้นสร้าง UI ให้ถ้ายังไม่มี**
คุยกับ NPC ตั้งแต่ก่อนหยิบของชิ้นแรก (ซึ่งเป็นสิ่งที่ผู้เล่นทำแน่ ๆ — วาวาฝากกล่อง
*ก่อน* ผู้เล่นจะมีของ) แล้วจบบท จะได้ช่องของโผล่มาเองทั้งที่ยังไม่มีของสักชิ้น
ขัดกับกฎที่ `ui_root.gd` เขียนไว้ว่า *"ไม่มี UI ตัวไหนเปิดเองตั้งแต่เกมเริ่ม"*
→ จำ `_hud_hidden_by_dialogue` ว่า **"เราเป็นคนซ่อน"** ไม่ใช่แค่ "ตอนนี้ซ่อนอยู่"

**2. `_exit_tree()` ต้องคืน HUD ด้วย — อายุของสองอย่างนี้ไม่เท่ากัน**
กล่องคำพูดตายพร้อมฉาก แต่ HUD แขวนที่ `root` ซึ่งข้ามฉาก
เปลี่ยนฉากระหว่างบทค้างอยู่ = ไม่มีใครเหลือให้เอากลับมา **ช่องของหายถาวร**
และอาการที่ผู้เล่นเห็นคือ *"ของหาย"* ทั้งที่ `GameState` ยังถืออยู่ครบ — ซึ่งจะถูกไล่หา
ผิดที่ไปที่ระบบไอเทมแทนที่จะเป็นเรื่องลำดับการตายของโหนด

### ตรวจด้วยซีนทดสอบชั่วคราว 4 กรณี

A) ยังไม่เคยหยิบของ → จบบทแล้ว HUD **ไม่งอก** ·
B) มี HUD อยู่ → ซ่อนตอนคุย กลับมาตอนจบ และเป็น**โหนดเดิม** ·
C) เปลี่ยนฉากระหว่างบทค้าง → HUD กลับมา ไม่หายถาวร — **ผ่านทุกข้อ** แล้วลบซีนทดสอบทิ้ง

⚠️ **แชท (`Chat.tscn`) ยังไม่ได้ทำแบบเดียวกัน** — แผงแชทก็บังจอเหมือนกัน
แต่ตอนนี้เด้งเฉพาะหลังวางกล่อง ยังไม่มีใครขอ จึงไม่ทำเผื่อ

---

## 2026-08-21 — แชทหลังวางกล่องจบด้วย "รับทราบค่ะ" แล้วเก็บโทรศัพท์ให้เอง

**ที่ผู้ใช้ขอ:** ให้มีช้อยส์ "รับทราบค่ะ" พอกดแล้วให้ระบบปิดแชทเองแล้วปลดล็อกให้เดินได้

### ไม่ได้เขียนระบบใหม่เลย — ของที่ต้องใช้มีอยู่ครบแล้ว

`chat_group._play_lines()` เจอบรรทัด `{"choices": [...]}` แล้ว `await show_choices()` อยู่แล้ว
และ `_close_chat()` ก็เรียก `_set_player_locked(false)` ปิดท้ายอยู่แล้ว
งานจริงจึงเหลือแค่ **บทหนึ่งบรรทัด + กฎว่าเมื่อไหร่ควรปิดเอง**

### ตัวสั่งปิดคือ "บทเขียนไว้ยังไง" ไม่ใช่ธงที่โหนด

ทางเลือกที่ทิ้ง: `@export var close_after_choice: bool = true` ที่ `BoxChatTrigger`
ดูยืดหยุ่นกว่า แต่กลายเป็น**ค่าที่ต้องจำให้ตรงกับบททุกครั้งที่แก้บท** — เพิ่มตัวเลือก
ปิดท้ายให้อีกเหตุการณ์แล้วลืมติ๊ก = แชทค้างรอกด E ทั้งที่ผู้เล่นตอบไปแล้ว

ที่ใช้: `_ends_with_choice(lines)` อ่านจากบทตรง ๆ
**บทที่ปิดท้ายด้วยตัวเลือกแปลว่าคนเขียนบทกำหนดจุดจบไว้แล้ว** ให้กดปิดอีกทีคือขอให้ยืนยันสองรอบ
· บทที่ไม่มีตัวเลือกยังต้องกด E/Esc เองเหมือนเดิม เพราะไม่มีอะไรบอกว่าอ่านจบหรือยัง
· ตรงกับหลักที่จดไว้เองรอบ 12: *"ควรทำตอนมีที่ใช้หลายจุด ไม่ใช่เพื่อบรรทัดเดียว"*

⚠️ **ดูเฉพาะบรรทัดสุดท้าย** — ตัวเลือกกลางบทคือการคุยต่อ ไม่ใช่การจบบท

### ทำไมต้องหน่วง 0.7 วิ ก่อนปิด

คำตอบที่กด **กลายเป็นข้อความของน้ำฟ้าในห้องแชทด้วย** (`show_choices` auto_send)
ปิดทันทีจะเห็นบับเบิลของตัวเองแวบเดียวแล้วจอหาย ซึ่งอ่านออกมาเป็น *"กดพลาด"*
ไม่ใช่ *"ตอบจบแล้วเก็บโทรศัพท์"*

### ตรวจด้วยซีนทดสอบชั่วคราวที่ขับ flow จริง ไม่ใช่แค่ `--import`

`--import` ผ่านไม่ได้แปลว่า flow ทำงาน — เขียนซีนที่วาร์ปผู้เล่นไปที่จุดวาง กด `interact()`
รอปุ่มตัวเลือกโผล่ แล้ว `pressed.emit()` แทนผู้เล่น จากนั้นเช็คว่าแชทหายและ `can_move` กลับมา
ได้ผล: `box_delivered = true` · `can_move = false` ตอนล็อก · ปุ่ม `"รับทราบค่ะ"` โผล่ ·
แชทปิดเอง · `can_move = true` — **ผ่านทุกข้อ** แล้วลบซีนทดสอบทิ้ง

**สองอย่างที่เจอเพราะทดสอบจริง ไม่ใช่เพราะอ่านโค้ด**
1. `Area2D.body_entered` **ไม่ยิงภายใน 1-2 เฟรมหลังวาร์ปตัวละคร** ต้องรอจริง ~0.5 วิ
   (ทีแรกสรุปผิดว่าจุดวางกล่องพัง ทั้งที่แค่ทดสอบเร็วเกินไป)
2. เพิ่ม `is_inside_tree()` หลัง `await receive_messages()` — ผู้เล่นขึ้นบันไดหนีระหว่างที่
   ข้อความยังทยอยเข้าอยู่ โหนดถูกลบแล้วแต่ `await` ยังเดินต่อ → error freed instance
   (จุดหน่วงอีกจุดในไฟล์นี้เช็คไว้แล้ว จุดนี้ตกหล่นมาตั้งแต่แรก)

⚠️ **error `Rect2i size is negative` 2 บรรทัดตอนเปิดแชทเป็นของเดิม ไม่ใช่ของรอบนี้**
พิสูจน์โดยปิดบรรทัดตัวเลือกออกแล้วรันซ้ำ — ได้ 2 บรรทัดเท่ากัน (ยังไม่ได้ตามแก้)

---

## 2026-08-20 (รอบ 13) — HUD โผล่ตอนหยิบกล่อง แล้วอยู่ยาวตลอดเกม

**ที่ผู้ใช้ขอ:** หลังหยิบกล่องให้ UI นี้ขึ้นยาว ๆ ตลอดเกม

**กลับคำตัดสินใจของรอบ 12** ที่เขียนไว้ว่า *"ไม่เปิด UI อัตโนมัติ — ฉากที่อยากได้ต้องเรียกเอง"*
ตอนนั้นยังไม่รู้ว่าผู้ใช้อยากให้มันโผล่ตอนไหน จึงเลือกทางที่ไม่เดาแทน · ตอนนี้รู้แล้ว

### เรียกครั้งเดียวพอ — ไม่ต้องสั่งซ้ำทุกฉาก

จุดที่สำคัญที่สุดของรอบนี้: **`UIRoot` แขวนอยู่ที่ `get_tree().root` ซึ่งรอดจาก
`change_scene_to_file()`** → โชว์ทีเดียวแล้วติดค้างข้ามฉากไปเอง
"ตลอดเกม" จึงเป็นผลพลอยได้ของสถาปัตยกรรมที่วางไว้รอบก่อน ไม่ต้องเขียนอะไรเพิ่มเลย
· ถ้ายังลาก `hud.tscn` เข้าทีละซีนอยู่ คำขอนี้จะแปลว่า "ไปลากเข้าอีก 7 ฉาก"

### วางที่ `interact_box.gd` ไม่ใช่ที่เควสต์กล่อง

ผู้ใช้พูดถึงกล่องใบนี้ แต่เงื่อนไขจริงที่ควรใช้คือ **"ผู้เล่นได้ของชิ้นแรก"**
`interact_box.gd` เป็นสคริปต์กลางของของที่หยิบได้ทุกชิ้น — วางไว้ตรงนี้แล้วของชิ้นต่อ ๆ ไป
ได้พฤติกรรมนี้ฟรี และไม่ต้องกลับมาเติมโค้ดเปิด HUD ทุกครั้งที่เพิ่มไอเทมใหม่
· ถ้าผูกกับ `GameState.carrying_box` แทน จะกลายเป็น HUD ที่เกี่ยวกับกล่องใบเดียวตลอดไป

### วางกล่องเสร็จแล้ว HUD ยังอยู่ (หายแค่รูปไอเทม)

**ช่องของเป็นส่วนหนึ่งของหน้าจอเกมนับจากจุดที่ผู้เล่นรู้จักระบบของแล้ว** —
โผล่ ๆ หาย ๆ ตามว่ามีของอยู่ในมือไหมจะดูเหมือนบั๊ก ไม่ใช่เหมือนดีไซน์
(ตรงกับคำว่า "ยาว ๆ ตลอดเกม" ที่ผู้ใช้ระบุ)

⚠️ **ยังไม่มีใครเรียก `hide_all()`** — ตอนนี้เกมไม่มีทางกลับไปเมนูหลักระหว่างเล่น
ถ้าวันหลังมี HUD จะติดค้างอยู่บนหน้าเมนู · แก้ด้วย `UIRoot.hide_all()` ที่ `start_manu.gd` บรรทัดเดียว

---

## 2026-08-20 (รอบ 12) — ที่แขวน UI ที่อยู่ข้ามฉาก (`UIRoot`)

**ที่ผู้ใช้ขอ:** อนาคตจะมีฟังก์ชันแมพ · ตัวบอกเควส · UI อีกหลายอัน ทำเผื่อไว้ด้วยก็ได้

### สิ่งที่ **ไม่** ทำ — และเหตุผล

ไม่ได้เขียนระบบแมพ ระบบเควส หรือ UI framework รอไว้
โปรเจกต์นี้มีหลักการที่จดไว้เองตั้งแต่ 2026-08-19 ว่า *"ควรทำตอนมีที่ใช้หลายจุด
ไม่ใช่เพื่อบรรทัดเดียว"* (ตอนเลื่อนการทำตัวเลือกมีเงื่อนไข) — สร้างระบบให้ความต้องการ
ที่ยังไม่มีรูปร่างจะได้ของที่ต้องรื้อตอนความต้องการจริงโผล่มา

**ทำเฉพาะสิ่งที่ "ตอนนี้ถูก ตอนหลังแพง"** คือการตัดสินใจเชิงโครงสร้างที่แก้ทีหลังต้องไล่ทุกไฟล์

### ปัญหาจริง 2 ข้อที่เจอตอนเช็ค ไม่ใช่ปัญหาสมมติ

**1. `hud.tscn` ไม่ได้ตั้ง `layer` → ได้ default 1 ซึ่งชนกับ `DialogueBox` และ `Chat`**
CLAUDE.md เขียนเตือนกับดักนี้ไว้เองว่า *"CanvasLayer ที่ layer เท่ากันเรียงตามลำดับใน tree
ซึ่งเดาไม่ได้"* — เคยทำให้กล่องแชทลอยเหนือม่านเฟดมาแล้ว
→ **จองช่วงเลขไว้เลย** (1–9 UI ประจำฉาก · 10–29 UI ถาวร · 30–49 แผงทับจอ · 100 · 128)
มี UI เพิ่มทีหลังจะได้หยิบเลขจากช่วงที่ตรงหน้าที่ ไม่ใช่เดาเอาว่าต้องมากกว่าอะไร

**2. UI 1 ตัว × ฉาก 8 ฉาก = ลากเข้าซีน 8 ครั้ง** · มี 4 UI ก็ 32 ครั้ง
**ลืมฉากเดียว = บั๊กที่เจอตอนเล่นไปถึงฉากนั้นแล้วเท่านั้น** ซึ่งเป็นบั๊กที่หาเจอช้าที่สุดแบบหนึ่ง
นี่คือต้นทุนที่โตแบบคูณตามจำนวน UI — ตรงกับที่ผู้ใช้บอกว่าจะมีอีกหลายอัน

### ทางที่ใช้: กลไกเดียวกับ `music.gd` ไม่ได้คิดสถาปัตยกรรมใหม่

แขวนโหนดไว้ที่ `get_tree().root` + static API — รอดจาก `change_scene_to_file()`
เพราะ root ไม่ถูกลบ · เป็นวิธีที่โปรเจกต์นี้ใช้อยู่แล้วสองที่ (`scene_transition` · `music`)
และมีเอกสารกำกับไว้ครบ **การใช้สำนวนเดิมของโปรเจกต์ ≠ การเพิ่มสถาปัตยกรรม**

**เพิ่ม UI ใหม่ = เพิ่ม 1 บรรทัดใน `UI_SCENES`** ไม่ต้องแตะสคริปต์อื่นและไม่ต้องแตะซีนไหน

⚠️ **`load()` ไม่ใช่ `preload()`** — `preload` จะโหลดซีน UI ทุกตัวเข้าหน่วยความจำตั้งแต่
เกมเริ่ม ทั้งที่บางตัว (แมพ · เมนูพัก) อาจไม่ถูกเปิดเลยทั้งรอบการเล่น
· ต่างจาก `music.gd` ที่เก็บ path เป็น String ด้วยเหตุผลเดียวกัน

⚠️ **`hide_ui()` ซ่อน ไม่ลบ** — HUD ที่ถูกลบแล้วสร้างใหม่จะลืมว่าผู้เล่นเลือกช่องไหนอยู่
⚠️ **`get_ui()` ไม่สร้างให้ ต่างจาก `show_ui()`** — แค่ไปอ่านค่าแล้วเผลอเปิด UI
ที่ไม่มีใครตั้งใจเปิดคือพฤติกรรมที่หาสาเหตุยาก

**ไม่เปิด UI อัตโนมัติ** — เมนูหลักกับฉากคัตซีนไม่ควรมี HUD โผล่มาเอง
ฉากที่อยากได้ต้องเรียก `UIRoot.show_ui("hud")` เอง

### 🚨 เจอระหว่างทาง: HUD ทับกล่องคำพูดบนจอจริง ๆ

| | ช่วง y (จอ 1080) |
|---|---|
| `HUD/HBoxContainer` | 950 – 1030 |
| `DialogueBox/Root/Box` | 780 – 1047 |

HUD นั่งอยู่กลางกล่องข้อความพอดี · **ไม่แก้ให้เพราะเป็นการตัดสินใจเรื่องหน้าตาเกม**
ทางเลือกคือซ่อน HUD ตอนคุย (`UIRoot.hide_ui("hud")` ที่ `dialogue_system.gd`)
หรือย้าย HUD ไปมุมอื่นในซีน — คนละความรู้สึกกัน ต้องให้ผู้ใช้เลือก
· ตัวคันโยกเตรียมไว้ให้แล้ว เรียกบรรทัดเดียวจบ

---

## 2026-08-20 (รอบ 11) — ช่องของที่ถืออยู่ใน HUD + กรอบช่องที่เลือก

**ที่ผู้ใช้ขอ:** หยิบกล่องแล้วให้รูป `ItemBox` ไปโผล่ที่พาแนลช่องแรกของ HUD
และช่องที่ผู้เล่นเลือกให้ขึ้นกรอบสี `#fbebe3`

### ต้องแก้ที่ต้นทางก่อน: `GameState.carrying_box` เป็นตัวแปรเปล่า

ไม่มีสัญญาณ → HUD จะรู้ว่าผู้เล่นหยิบกล่องเมื่อไหร่ได้ก็มีทางเดียวคือ**ตรวจทุกเฟรม**
เติม setter + `carrying_box_changed` แบบเดียวกับ `chapter` ที่มีอยู่แล้ว

⚠️ **setter ต้องเช็คค่าเดิมก่อน emit** — `player.gd._ready()` อ่านค่าจาก `GameState`
แล้วเขียนกลับผ่าน setter ของ `carrying` **ทุกครั้งที่เข้าฉากใหม่**
ไม่เช็คก่อน = ทุกฉากยิงสัญญาณ "หยิบกล่อง" ซ้ำทั้งที่ไม่มีอะไรเปลี่ยน

### 🚨 HUD ต้องอ่าน `GameState` ตอน `_ready()` ด้วย ไม่ใช่รอสัญญาณอย่างเดียว

HUD ถูก instance ใหม่ทุกฉาก แต่สัญญาณ "หยิบกล่อง" ยิงไปตั้งแต่ฉากห้องใต้ตึกแล้ว
ต่อสัญญาณอย่างเดียว = **เดินขึ้นชั้น 2 ปุ๊บกล่องหายจากช่องทั้งที่ยังถืออยู่ในมือ**
ปัญหาเดียวกับที่ `player.carrying` เคยเจอตอนทำเควสต์กล่อง (DEVLOG 2026-08-19) —
สถานะข้ามฉากต้องมีทั้ง "อ่านตอนเปิดฉาก" และ "ฟังตอนเปลี่ยน" ขาดข้อไหนก็เพี้ยนคนละแบบ

### วางกล่องเสร็จแล้วรูปหายจากช่องด้วย

ช่องนี้คือ **"ของที่ถืออยู่ตอนนี้" ไม่ใช่ "รายการงานที่เคยทำ"** — ส่งของแล้วยังค้างอยู่ในมือ
ผู้เล่นจะอ่านว่ายังไม่ได้ส่ง แล้วเดินกลับไปหาจุดวางซ้ำ
(ทำได้ฟรีเพราะผูกกับ `carrying_box` ตัวเดียว ซึ่งเป็นเท็จทั้งก่อนหยิบและหลังวางอยู่แล้ว)

### 🚨 ก๊อป StyleBox คนละใบต่อช่อง

พาแนลทั้ง 3 ช่องใน `hud.tscn` ใช้ `StyleBoxFlat_n2snw` **ใบเดียวกัน** —
ใส่กรอบลงใบนั้นตรง ๆ = **กรอบขึ้นพร้อมกันทั้งสามช่อง** แล้วจะดูเหมือนเลือกทุกช่องอยู่ตลอดเวลา
กับดักเดียวกับปุ่มตัวเลือกในกล่องคำพูด (รอบ 10) ซึ่งเจอมาแล้ววันเดียวกัน

### เลือกช่องด้วยคลิก + กดเลข — และเหตุผลที่ไม่เพิ่ม input action

ผู้ใช้ไม่ได้ระบุว่า "เลือก" ด้วยอะไร · ทำทั้งสองทางเพราะเกมนี้ใช้เมาส์เป็นหลักอยู่แล้ว
(คลิกคุย NPC · คลิกเลื่อนบท) แต่ hotbar ที่กดเลขไม่ได้ก็ผิดความคาดหวัง

🚨 **อ่าน `keycode` ตรง ๆ ไม่ได้เพิ่ม `input action`** — action ต้องเขียนลง `project.godot`
ซึ่งโปรเจกต์นี้เติมเองไม่ได้: Godot เขียนไฟล์ใหม่จากรายการในหน่วยความจำทุกครั้งที่ผู้ใช้
แตะ Project Settings แล้วของที่เติมจากข้างนอกหายเงียบ (เกิดกับ autoload มาแล้ว 2 ครั้ง)
· ข้อเสียคือรีแมปปุ่มไม่ได้ — แลกกับการไม่พังเงียบ ซึ่งคุ้มกว่าสำหรับปุ่มเสริมแบบนี้

**กดช่องเดิมซ้ำ = ยกเลิกการเลือก** ไม่งั้นเลือกไปแล้วผู้เล่นเอากรอบออกไม่ได้เลย

⚠️ **ผลข้างเคียง: คลิกตรงพาแนลจะไม่เลื่อนบทสนทนา** พาแนลเป็น Control ที่กินคลิกไปแล้ว
ส่วนระบบเลื่อนบทดักที่ `_unhandled_input` ซึ่งได้เฉพาะคลิกที่ไม่โดน GUI
ถูกต้องตามที่ควรเป็น แต่เป็นพื้นที่เล็ก ๆ กลางล่างจอที่คลิกแล้วบทไม่เดิน

**⚠️ `hud.tscn` ยังไม่ได้ถูก instance ในฉากไหนเลย** — เขียนโค้ดรอไว้ ต้องลากเข้าซีนก่อนถึงจะเห็น

---

## 2026-08-20 (รอบ 10) — ระยะขอบในปุ่มตัวเลือก (ขอบกล่อง ↔ ตัวอักษร)

**ที่ผู้ใช้ถาม:** กำหนดความชิดระหว่างขอบข้อความกับกล่องของปุ่มตัวเลือกได้ไหม

**ได้ — อยู่ที่ `content_margin_*` ของ StyleBox** · เปิดเป็น `@export` ให้ทั้งสองที่ที่มีตัวเลือก

| ที่ | เดิม | ตอนนี้ |
|---|---|---|
| กล่องคำพูด (`dialogue_system.gd`) | **ไม่ได้ตั้งเลย** — ตัวอักษรชิดขอบ | `choice_padding_h/v` = 16 / 10 |
| แชท (`chat_group.gd`) | hardcode 16 / 10 ใน `_choice_style()` | `@export` ตัวเดียวกัน ค่าเดิม |

ตั้งค่าเริ่มต้นของกล่องคำพูดให้**เท่ากับของแชท** เพราะเป็นปุ่มตัวเลือกเหมือนกัน
ผู้เล่นเห็นทั้งสองแบบในเกมเดียว ระยะขอบต่างกันจะรู้สึกว่าเป็นคนละระบบ

🚨 **ต้องตั้งระยะขอบ *ก่อน* `_apply_choice_hover_style()`**
ตัวนั้นก๊อป StyleBox ของ `normal` ไปทำสถานะตอนชี้ — ใส่ทีหลังจะได้ปุ่มที่
**ตัวอักษรขยับตำแหน่งตอนเอาเมาส์ชี้** เพราะสองสถานะมีระยะขอบไม่เท่ากัน

🚨 **ระยะขอบต้องถูกบวกเข้าไปในความกว้างที่คำนวณด้วย**
`content_margin` กินพื้นที่ของตัวอักษรจริง ๆ — ลืมบวกแล้วอาการคือ
*"ปรับ `choice_padding_h` แล้วปุ่มสูงขึ้นเองแบบไม่มีเหตุผล"* (ข้อความตกบรรทัดเร็วกว่าที่ควร)
ทั้งสองไฟล์แก้จุดนี้พร้อมกัน · ฝั่งแชทเดิมเป็นเลข `32.0` ตายตัว (= 16 × 2) ตอนนี้อ่านจาก `@export` เดียวกัน

⚠️ **ก๊อป StyleBox มาแก้ ไม่ได้แก้ใบในซีนโดยตรง** — ปุ่มทั้ง 4 ตัวใน `DialogueBox.tscn`
ใช้ `StyleBoxFlat_l44sp` ใบเดียวกัน แก้ตรง ๆ = ไปแก้ทรัพยากรที่ซีนเป็นเจ้าของ
ซึ่งใครมาใช้ต่อในอนาคตก็โดนด้วยโดยไม่รู้ตัว

**เจอระหว่างทาง — แก้ข้อมูลที่จดผิดไว้ในรอบ 8:** `RightBox` ตั้ง `grow_horizontal = 0`
(`BEGIN` = โตซ้าย) ไว้ในซีนอยู่แล้ว ซึ่ง**เอดิเตอร์ใส่ให้เองตอนเลือก `anchors_preset = 6`**
→ ของเดิมไม่ใช่ "โตขวาทั้งคู่" อย่างที่จดไว้ แต่เป็น **"โตเข้าหากันทั้งคู่"** ซึ่งแย่กว่า
· ข้อสรุปกับโค้ดที่แก้ไปยังถูกเหมือนเดิม (ตั้งทิศให้ทั้งสองฝั่งชัดเจน ไม่พึ่งค่าเริ่มต้น)

---

## 2026-08-20 (รอบ 9) — วางกล่องเสร็จ → โทรศัพท์เด้ง โชทักในกลุ่มสโม

**ที่ผู้ใช้ขอ:** วางกล่องเสร็จแล้วให้ขึ้นแชทกลุ่มสโมอัตโนมัติ ใช้ UI ตัวเดียวกับฉาก `wokeup`
มีข้อความจากโชเด้งเข้ามาว่า *"ใครวางมาเอาเบรคเช้าไปก่อนเลยนะ หน้าห้องประชุม"*

### กับดักที่ต้องเช็คก่อนเขียนอะไร: `Chat.tscn` ไม่ใช่ UI เปล่า ๆ

โหนด `Chat` ถือ `@export` ที่ **ผูกกับเนื้อเรื่องตอนเช้าไว้แล้ว**
`popout_after_group = "smo"` และ `end_after_group = "sawat"` → เอามาใช้ซ้ำดื้อ ๆ
พอโชทักเสร็จ กลุ่มสวัสดิการจะเด้งมาเล่นบทขนของตอนเช้าซ้ำ **แล้วเกมจะเฟดพาผู้เล่นไปลานจอดรถ**
ซึ่งอาการจะดูเหมือนเกมพังสุ่ม ๆ ไม่ใช่เหมือน "ลืมปิดค่าที่ตั้งไว้"

ทางเลือกคือแยก `Chat.tscn` เป็นสองใบ (ใบเนื้อเรื่องเช้า / ใบทั่วไป) แต่จะได้ซีนซ้ำที่ต้องแก้สองที่
ทุกครั้งที่ปรับ UI แชท · **เลือกตั้งค่าทับตอน `instantiate()` แทน** — ค่าพวกนั้นเป็น
"ค่าเริ่มต้นของฉาก `wokeup`" อยู่แล้ว การที่ผู้เรียกรายอื่นตั้งทับจึงเป็นเรื่องปกติ
🚨 ต้องตั้ง**ก่อน** `add_child()` เพราะ `phone.gd._ready()` อ่านไปต่อสัญญาณทันที

### `ChatDatabase.EVENTS` — บทที่ผูกกับเหตุการณ์ ไม่ใช่กับการเปิดห้อง

ที่มีอยู่ไม่พอ: `INCOMING[group]` ถูกเล่น**ทุกครั้งที่เปิดห้องนั้น** — เอาข้อความของโชไปใส่
`INCOMING["smo"]` ฉาก `wokeup` จะเล่นมันตั้งแต่ตอนเช้าทั้งที่ยังไม่ได้หยิบกล่องด้วยซ้ำ

เส้นแบ่งของสามตัวคือ **"อะไรเป็นตัวสั่งให้ข้อความมาถึง"** (เวลาเปิดห้อง / เวลาดูอยู่ / เหตุการณ์ในฉาก)

**ไม่สร้างกลุ่มใหม่ — ใช้กลุ่ม `smo` เดิม** · กลุ่มคือ "ห้องคุย" ไม่ใช่ "ฉากของเนื้อเรื่อง"
เปิดขึ้นมาจะเห็นประวัติเช้าครบแล้วข้อความใหม่ต่อท้าย เหมือนแอปแชทจริง
· ถ้าแยกกลุ่มใหม่จะได้ห้องที่มีข้อความสองบรรทัดลอยอยู่โดยไม่มีบริบท

### แยกเป็นสคริปต์ต่างหาก ไม่ยัดลง `box_drop_point.gd`

ไฟล์นั้นเขียนกำกับไว้เองตั้งแต่ตอนสร้างว่าสัญญาณ `placed` มีไว้
*"ให้เนื้อเรื่อง/เควสต์ดักต่อโดยไม่ต้องแก้ไฟล์นี้"* — จุดวางกล่องเป็น**ของในฉาก**
ส่วนแชทที่เด้งตามมาเป็น**เนื้อเรื่อง** อายุไม่เท่ากัน
วันหลังเปลี่ยนใจให้แชทเด้งตอนอื่นก็ย้ายโหนด ไม่ต้องแตะระบบวางกล่องเลย

### 🚨 ปิดแชทไม่ได้จนกว่าข้อความจะมาถึงครบ

เหตุการณ์นี้เกิด**ครั้งเดียวตลอดเกม** ไม่มีระบบเปิดโทรศัพท์ดูย้อนหลัง —
ผู้เล่นที่กำลังรัวปุ่ม E อยู่พอดีตอนโทรศัพท์เด้ง จะปิดทิ้งก่อนโชพิมพ์เสร็จ
**แล้วไม่มีทางกลับมาอ่านคำใบ้ว่าต้องไปไหนต่อได้อีกเลย** = ตันแบบที่ผู้เล่นไม่รู้ตัวว่าพลาดอะไรไป

### ล็อกการเดินของผู้เล่น — ล็อกตอน "วางเสร็จ" ไม่ใช่ตอน "แชทเด้ง"

ผู้ใช้ขอเพิ่มให้ล็อกตอนแชทขึ้น · ใช้ `player.can_move` ตัวเดิมที่บทสนทนาใช้
ไม่ได้สร้างระบบล็อกใหม่ — `player.gd` จัดการให้ครบอยู่แล้วทั้งหยุด velocity
สลับเป็นท่ายืน และ**หยุดเสียงฝีเท้า** (ข้อสุดท้ายเป็นสิ่งที่ระบบล็อกที่เขียนใหม่มักลืม)

**แต่ล็อกตอนแชทเด้งยังมีช่องโหว่ 1.2 วินาที** (`open_delay`) — ผู้เล่นเดินออกไปได้
แล้วโดนหยุดกลางก้าวตอนแผงแชทโผล่ ซึ่งอ่านว่า "เกมค้าง" ไม่ใช่ "มีสายเข้า"
· ล็อกตั้งแต่วินาทีที่วางกล่องเสร็จแทน ช่วงนั้นผู้เล่นเพิ่งกด E อยู่ ยังไม่ทันขยับ การหยุดคุมจึงไม่สะดุด

🚨 **ย้ายจุดล็อกให้เร็วขึ้น = ต้องไล่ปลดล็อกทุกทางออกก่อนเปิดแชทสำเร็จ**
เดิมล็อกทีหลังจึงไม่มีปัญหานี้ · ตอนนี้พิมพ์ `event_id` ผิดแล้ว `_open_chat()` ออกกลางคัน
= **ผู้เล่นขยับไม่ได้อีกเลยตลอดฉาก** ซึ่งแย่กว่าแชทไม่เด้งเฉย ๆ มาก
· และเพิ่ม `is_inside_tree()` หลัง `await` ด้วย เผื่อฉากถูกเปลี่ยนระหว่างหน่วง

### ปุ่มย้อนกลับต้องถอดสัญญาณเดิมออกก่อน

`phone.gd._ready()` ต่อ `back_pressed` → `show_list()` ไว้ให้ทุกห้องอัตโนมัติ
ในบริบทนี้ปุ่มนั้นแปลว่า **"เก็บโทรศัพท์"** ไม่ใช่ "กลับไปหน้ารายการแชท" (ซึ่งยังไม่มีโหนด `ChatList`)
ปล่อยไว้ = กดแล้วห้องแชทถูกซ่อนหมด + warning แล้วเหลือ**จอเปล่าที่ปิดไม่ได้**
→ `disconnect()` ของเดิมก่อนแล้วค่อยต่อของตัวเอง

### จุดที่เกือบพลาด: `await view.chat_finished` จะค้างถ้าสัญญาณยิงไปก่อน

`open()` ยิง `chat_finished` แม้ห้องไม่มี `INCOMING` เลย — ถ้ามันยิงก่อนบรรทัด `await`
ของเราจะรอตลอดกาลแล้วข้อความของโชไม่มีวันมา · ตรวจแล้วว่าปลอดภัย:
`open()` → `load_history()` → `_scroll_to_bottom()` ซึ่ง **await 2 process frame**
ตอน `add_child()` คืนค่ากลับมา สัญญาณจึงยังไม่ยิงแน่นอน

**⚠️ ยังไม่ได้ verify ด้วย Godot** — editor ยังเปิดค้างอยู่ (เหตุผลเดียวกับรอบ 8)
และยังต้องเพิ่มโหนด `Node` + แนบสคริปต์ในซีนก่อนถึงจะทดสอบได้

---

## 2026-08-20 (รอบ 8) — ทิศที่ NPC ยืนหัน + ปุ่มตัวเลือกยืดไปชนกัน

### ก. `initial_facing` — โชแชงยืนหันข้างรอ แล้วค่อยหันมาตอนคุย

**ที่ผู้ใช้ขอ:** โชแชงใน `level2_2` หันข้างอยู่ก่อน จนผู้เล่นมาคุยถึงหันหาผู้เล่น

**ทางที่ตัดทิ้ง: แก้ `default_anim` ใน `Cho.tres` เป็น `idle_side`**
ดูเหมือนเป็นทางที่สั้นที่สุดและไม่ต้องแตะโค้ดเลย — **แต่พังทันที**
`face_player()` ใช้ `default_anim` เป็น "ท่าหันลง" ด้วย (`_play_if_exists(data.anim_up if diff.y < 0
else data.default_anim)`) → โชแชงจะหันลงไม่เป็นอีกเลย ยืนหันข้างตลอดแม้ตอนคุย
ซึ่งตรงข้ามกับสิ่งที่ขอพอดี · **ช่องเดียวถูกใช้สองความหมายอยู่แล้ว จะเพิ่มความหมายที่สามไม่ได้**

**ตั้งที่โหนด ไม่ใช่ใน `.tres`** — เส้นแบ่งเดียวกับ `dialogue_id_override`:
ทิศที่ยืนหันขึ้นกับ**ตำแหน่งที่วางในแมพ** ไม่ใช่นิสัยตัวละคร (เหมือน `position`)
คนเดียวกันยืนหันข้างริมทางเดินฉากหนึ่ง แล้วหันหน้าเข้าห้องในอีกฉากได้

**ใช้ enum ทิศ ไม่ใช่ช่องพิมพ์ชื่ออนิเมชัน + ติ๊ก flip**
พิมพ์ชื่อชุดเองได้ก็จริงแต่จะพิมพ์ผิดได้ (ปัญหาเดิมของช่องคีย์บท) และต้องรู้ว่า
"หันขวา = ติ๊ก flip" ซึ่งเป็นรายละเอียดของภาพ ไม่ใช่ของเนื้อเรื่อง
· enum แปลงเป็นชื่อชุด**โดยอ่านจาก `data`** (`anim_side` / `anim_up`) ไม่ hardcode `"idle_side"`
เพราะแต่ละตัวตั้งชื่อชุดคนละแบบได้ — hardcode แล้วตัวที่ตั้งชื่อไม่เหมือนชาวบ้านจะหันไม่ได้เงียบ ๆ

⚠️ **ภาพที่ไม่ flip = หันซ้าย** ต้องตรงกับ `face_player()` (`flip_h = diff.x > 0`)
คิดกลับด้านเมื่อไหร่ คุยจบแล้ว `reset_direction()` จะพากลับไปหันคนละทางกับตอนเปิดฉาก

🚨 **ลำดับใน `_apply_data()` สำคัญ: `_play_default_anim()` → `_apply_initial_facing()` → จำ `_initial_*`**
· ตัวแรกเป็นที่เดียวที่มี fallback + warning ตอนชื่อชุดใน `.tres` ไม่ตรงกับ SpriteFrames
ข้ามไปแล้วจะได้ NPC ที่หายไปเงียบ ๆ · ตัวสุดท้ายต้องอยู่หลังสุด ไม่งั้นคุยจบแล้ว
NPC จะหันหน้าค้างตลอดกาลแทนที่จะกลับไปหันข้าง
· `_refresh_editor_preview()` ก็โชว์ทิศที่เลือกด้วย ไม่งั้นจะเห็นหันหน้าตอนแก้ซีนแล้วหันข้างตอนรัน

### ข. ปุ่มตัวเลือกยืดออกขวาจนไปชิดปุ่มข้าง ๆ

**ที่ผู้ใช้เจอ:** ตัวเลือกที่ข้อความยาว ปุ่มยืดไปทางขวาจนติดปุ่มอีกฝั่ง

**ต้นตอ: `choice_button_max_width` = 240 เป็นเพดานที่ไม่เคยบังคับอะไรได้เลยสักครั้ง**
`_equalize_choice_widths()` หั่นตัวเลขแล้วสั่ง `btn.custom_minimum_size.x = widest`
แต่ `custom_minimum_size` คือ **ค่าต่ำสุด** — `Button.get_minimum_size()` ยังคืน
`max(custom_minimum_size, ขนาดข้อความ)` อยู่ดี · โค้ดขึ้น `push_warning` ว่า "ข้อความจะถูกตัดท้าย"
มาตลอดทั้งที่การตัดนั้นไม่เคยเกิดขึ้นจริง — **warning ที่โกหกแย่กว่าไม่มี warning**
เพราะมันพาไปหาสาเหตุผิดจุด (นึกว่าเป็นเรื่องความยาวข้อความในบท)

**ทำไมถึงไปทางขวา:** `LeftBox` ไม่ได้ตั้ง `grow_horizontal` เลยได้ค่าเริ่มต้น
`GROW_DIRECTION_END` (โตขวา) · ยึด offset ไว้ที่ x=700..900 พอ min size โตเกิน 200
จึงโตขวาตรงเข้าหา `RightBox` ที่ x=1000 พอดี
· ฝั่ง `RightBox` ตั้ง `grow_horizontal = 0` (`BEGIN` = โตซ้าย) ซึ่ง**เอดิเตอร์ใส่ให้เอง**
ตอนเลือก `anchors_preset = 6` (ยึดขอบขวา) — **สองคอลัมน์จึงโตเข้าหากันทั้งคู่**
ไม่ใช่แค่ฝั่งซ้ายวิ่งไปหาฝั่งขวา

**แก้ 2 ชั้น ขาดชั้นไหนก็ยังเห็นอาการเดิม**

| ชั้น | แก้อะไร |
|---|---|
| `autowrap_mode = AUTOWRAP_WORD_SMART` | ข้อความยาวตกบรรทัดใหม่ (ปุ่มสูงขึ้น) แทนการยืดออกข้าง → **เพดานบังคับได้จริง** |
| `grow_horizontal` ตาม `anchor_left` | สองคอลัมน์โตหนีออกจากกัน ขอบด้านในถูกตรึง ช่องว่างกลางจอไม่หุบ |

**เลือก autowrap ไม่ใช่ `clip_text` + `TRIM_ELLIPSIS`**
สองตัวหลังก็หยุดปุ่มไม่ให้ยืดได้เหมือนกัน แต่**ผู้เล่นจะอ่านตัวเลือกไม่ครบ** —
เกมที่ให้เลือกคำตอบแล้วตัดคำตอบทิ้งท้ายด้วย `…` คือการซ่อนข้อมูลที่จำเป็นต่อการตัดสินใจ
· ปุ่มสูงขึ้นสองบรรทัดเสียแค่ความสวย

**`WORD_SMART` ไม่ใช่ `WORD`** — ภาษาไทยไม่มีช่องว่างระหว่างคำ ประโยคยาว ๆ อาจถูกมองเป็น
คำเดียว · `WORD_SMART` ยอมตัดกลางคำเฉพาะตอนคำเดียวยาวเกินปุ่มจริง ๆ ซึ่งเป็นทางออกสุดท้ายที่ถูก

⚠️ **ดู `anchor_left` ไม่ใช่ชื่อโหนด** ตอนตัดสินทิศการโต — ย้าย/เปลี่ยนชื่อกล่องในซีนแล้วยังถูกอยู่

✅ **ผลพลอยได้: สองคอลัมน์สมมาตรกันเสมอ** เดิมทั้งคู่โตขวา จุดกึ่งกลางของคู่ปุ่มจึง
**เลื่อนตามความยาวข้อความ** · ตอนนี้ตรึงขอบด้านใน (900 / 1000) สมมาตรรอบ 950 คงที่
เหลือแค่ขยับ `RightBox` เป็น −900/−700 ในซีน ก็สมมาตรรอบ 960 เป๊ะ (ข้อค้างเดิมจากรอบก่อน)

**⚠️ ยังไม่ได้ verify ด้วย Godot** — editor เปิดค้างพร้อม unsaved changes รัน `--import`
ตอนนี้จะไปชนกับ cache แล้วเด้ง dialog ใส่ผู้ใช้ · รอปิดแล้วค่อยรัน

---

## 2026-08-20 (รอบ 7) — โชแชงเป็นซีนสืบทอด (`npc/scenes/cho.tscn`)

**ที่ผู้ใช้ขอ:** ทำให้โชแชงเป็น inherited scene แทนการ instance `npc_base.tscn` ตรง ๆ ในซีนแมพ

### ตัดสินใจ 1: ไม่ฝัง `SpriteFrames` ซ้ำในซีนสืบทอด (ต่างจาก `wawa.tscn`)

`wawa.tscn` มี 246 บรรทัด โดย ~220 บรรทัดเป็น `AtlasTexture` + `SpriteFrames` ที่**ซ้ำกับ
`wawa.tres` ทุกประการ** แล้วโดน `_apply_data()` เขียนทับตอนรันอยู่ดี — ก๊อปแพทเทิร์นนั้นมา
จะได้ชุดซ้ำอีกชุดของโชแชง ซึ่งคือ*"ปัญหาเดิมที่ระบบซีนแม่หนีมา"* ที่ CLAUDE.md เตือนไว้เอง

เหตุผลที่เดาว่าเคยต้องฝัง คือ**ต้องเห็นตัวละครตอนลากวางในเอดิเตอร์** — แต่ `npc_base.gd` เป็น
`@tool` และมี `_refresh_editor_preview()` ที่ยัด `data.frames` ใส่ให้ตั้งแต่ในเอดิเตอร์แล้ว
(ยืนยันจากของจริง: โหนด `Cho` ในแมพตอนนี้ตั้งแค่ `data` แล้วก็มองเห็นตัว)
→ **`cho.tscn` เหลือ 8 บรรทัด** · ไม่ล้างของ `wawa.tscn` ตอนนี้เพราะนอกขอบเขตที่ขอ (จดไว้ใน CLAUDE.md แล้ว)

### ตัดสินใจ 2: ย้าย `dialogue_id_after = "Cho_idle"` มาไว้ในซีนสืบทอด

เดิมค้างเป็นงานที่ต้องไปติ๊กเองที่ Inspector ของโหนดในแมพ

**เส้นแบ่งคือ "บทคุยซ้ำนี้เป็นของตัวละคร หรือของฉาก"**
· วาวาที่ `under_ch_1` ตั้งที่โหนดถูกแล้ว เพราะ `wawa_idle` เป็นคำใบ้ของ**เควสต์กล่อง** คู่กับ
`dialogue_id_override = "wawa_under_ch1"` — ไปฉากอื่นก็ไม่ควรพูดเรื่องนี้
· `Cho_idle` = *"อยากให้ช่วยอะไรไหม?"* ไม่ผูกกับเหตุการณ์ไหนเลย เป็น**นิสัยของโชแชง**
ตั้งที่ซีนสืบทอด = โชแชงตัวที่สองในฉากอื่นได้พฤติกรรมนี้ฟรี และยังตั้งทับรายตัวได้เหมือนเดิม

🚨 **ผลข้างเคียงที่ต้องรู้: `talk_once` จะใช้ไม่ได้กับโชแชงทุกตัว**
`npc_base.gd → can_interact()` คืน `true` ทันทีถ้า `dialogue_id_after` ไม่ว่าง (ตั้งใจไว้แต่แรก)
รับได้เพราะโชแชงเป็นตัวที่ควรคุยซ้ำได้อยู่แล้ว · ถ้าวันหลังอยากได้โชแชงที่พูดครั้งเดียวแล้วเงียบ
ต้องล้างช่องนี้ที่ instance ก่อน

### ไม่ override `CollisionShape2D`

ปล่อยรับ 24×14 จากซีนแม่ไปก่อน — ยังไม่รู้ว่าตัวโชแชงต้องการเท่าไหร่ และ**การเดาค่าใส่ไว้
แย่กว่าไม่ใส่** เพราะจะดูเหมือนมีคนตั้งใจตั้งมาแล้ว · ช่องนี้คือเหตุผลหลักที่ทำซีนสืบทอด
ปรับทีเดียวมีผลทุกฉาก

### ไม่แตะ `all_level_2.tscn` — Godot เปิดค้างอยู่พร้อม unsaved changes

`Get-Process *odot*` เจอ `(*) all_level_2.tscn` (มีดอกจัน = ยังไม่เซฟ) + เกมรัน DEBUG อยู่
แก้ไฟล์จากข้างนอกตอนนี้จะโดนเซฟทับ และปุ่ม *Reload from disk* ของ Godot 4.6 merge ไฟล์ผิด
→ สร้างแค่ไฟล์ใหม่ (Godot ไม่ได้ถืออยู่) แล้วส่งขั้นตอนสลับโหนดให้ผู้ใช้ทำในเอดิเตอร์

⚠️ **ถึงโครงสร้างโหนดจะตรงกันเป๊ะ ก็ห้ามสลับด้วยการแก้ `path=` ใน `ext_resource`**
`data = ExtResource(...)` ที่ค้างอยู่ที่โหนดในแมพจะกลายเป็น override ทับค่าจากซีนสืบทอด
แล้ววันหลังเปลี่ยน `.tres` ที่ซีนสืบทอด ฉากนั้นจะไม่ตาม — **ต้องลบโหนดแล้วลากใหม่**

**เจอระหว่างทาง:** `decor/under_ch_1.tscn` โหนด `Wawa` มี `dialogue_id_after = "wawa_idle "`
ช่องว่างท้ายจริง ๆ (บั๊กเดิมที่ 2026-08-16 เคยเสียเวลาไล่หา) — โค้ดตัดให้แล้วแต่ยัง `push_warning`
ทุกครั้งที่เล่น · แจ้งผู้ใช้ให้ลบที่ Inspector ไม่แก้ให้เพราะเป็นไฟล์ `.tscn`

---

## 2026-08-20 (รอบ 6) — คลิกตรงไหนก็ได้เพื่อเลื่อนบท

**ที่ผู้ใช้ขอ:** คลิกซ้าย 1 ครั้งตรงไหนก็ได้ = เลื่อนบทเหมือนกด E

**จุดตัดสินใจเดียวที่สำคัญ: ดักที่ `_unhandled_input` ไม่ใช่ `_input`**

ทางที่ตรงไปตรงมาที่สุดคือเติมเงื่อนไขเมาส์ลงใน `_input()` ที่ดัก E อยู่แล้ว — **แต่พังทันที**
`_input` ทำงาน**ก่อน**ระบบ GUI และเราเรียก `set_input_as_handled()` ที่นั่น
→ คลิกจะถูกกินก่อนที่ปุ่ม `Next` และปุ่มตัวเลือกจะได้เห็น **ปุ่มทั้งหมดในกล่องคำพูดจะกดไม่ได้เลย**

ทางแก้ที่คิดไว้ตอนแรกคือเช็คเองว่าคลิกตกอยู่ในกรอบของปุ่มไหนไหม (`get_global_rect().has_point()`)
แล้วค่อยข้าม — ใช้ได้แต่เป็นการเขียนงานที่เอนจินทำให้อยู่แล้วขึ้นมาใหม่ และต้องไล่เพิ่มทุกครั้ง
ที่มีปุ่มใหม่ในกล่องคำพูด

`_unhandled_input` ทำงาน**หลัง** GUI → คลิกที่โดนปุ่มถูกปุ่มกินไปแล้ว เหลือมาถึงเฉพาะคลิกบนที่ว่าง
**ปัญหาหายไปทั้งข้อโดยไม่ต้องเขียนโค้ดเช็คอะไรเลย**

⚠️ **E ยังอยู่ที่ `_input` เหมือนเดิม — สองปุ่มดักคนละที่โดยตั้งใจ**
E ต้องกินอีเวนต์ตั้งแต่ก่อน GUI เพราะเหตุผลเดิม (บทที่จบพอดีกับการกดครั้งนั้นจะเด้งกลับไปเริ่มใหม่
ถ้าไม่กิน — ดู 2026-08-15) · ส่วนคลิกไม่มีปัญหานั้นเพราะไม่มีใครใช้คลิกเริ่มบทสนทนาผ่าน `_input`

⚠️ **ระหว่างรอเลือกคำตอบ คลิกที่ว่างต้องไม่กินอีเวนต์** (ต่างจากฝั่ง E ที่กินก่อนแล้วค่อยเช็ค)
กินทิ้งไม่ได้ช่วยอะไรเพราะผู้เล่นถูกล็อกอยู่แล้ว แต่ทำให้ระบบอื่นที่รอคลิกอยู่เงียบไปด้วย

**ตรวจแล้วว่าไม่ชนกับใคร** — `player.gd` (คลิกคุย NPC) เช็ค `can_move` ซึ่ง `start_dialogue()`
ล็อกไว้ตลอดช่วงบท · `ClickEffect` ใช้ `_input` และไม่กินอีเวนต์ วงกลมกระเพื่อมยังขึ้นปกติ

**ทดสอบด้วยซีนชั่วคราวแล้วลบทิ้ง** — คลิก#1 ข้ามการพิมพ์ (index เท่าเดิม visible_ratio = 1.0) ·
คลิก#2 เลื่อนไปบรรทัด 2 · กดปุ่ม `Next` 1 ครั้งเลื่อนจังหวะเดียว ไม่ใช่สอง ·
ระหว่าง `_waiting` คลิกที่ว่างแล้ว index ไม่ขยับ

---

## 2026-08-20 (รอบ 5) — ตัวเลือกที่มีเงื่อนไข + บทของโชแชง

**ที่ผู้ใช้ขอ:** ยังไม่วางกล่องแล้วไปคุยกับโชแชง → ทักทาย แล้วมีตัวเลือก
*"ไม่มีค่ะ"* กับ *"กล่องวางตรงไหนเหรอคะ"* · ถามเรื่องกล่องแล้วโชแชงบอกทางไปห้องรับรอง

**ต้องเพิ่มความสามารถให้ระบบก่อน — ตัวเลือกยังไม่มีเงื่อนไข**
เป็นข้อจำกัดที่จดไว้ตั้งแต่ทำคำใบ้ของวาวา (2026-08-19) แล้วเลือกไม่ทำเพราะ*"ควรทำตอนมีที่ใช้
หลายจุด ไม่ใช่เพื่อบรรทัดเดียว"* — ตอนนี้มี 2 ที่แล้ว จึงถึงเวลา

**กรองที่ "ตัวเลือก" ไม่ใช่แยกเป็น "คนละบท" ตามสถานะ**
ทางเลือกอื่นคือเขียน `Cho_intro_carrying` กับ `Cho_intro_done` แล้วสลับคีย์ตามสถานะ
แต่คำทักทายกับข้ออื่นจะซ้ำกันทั้งสองใบ วันหลังแก้คำพูดจะแก้ไม่ครบ
· ตัวละครทักทายเหมือนเดิมทุกครั้ง เปลี่ยนแค่ว่ามีเรื่องอะไรให้ถามได้บ้างในตอนนั้น
ซึ่งตรงกับที่คนจริงคุยกันด้วย

**ชี้ชื่อตัวแปรใน `GameState` ตรง ๆ ไม่สร้างภาษาเงื่อนไขใหม่**
`{"if": "carrying_box"}` อ่านออกทันทีว่าหมายถึงอะไร และไม่ต้องมี parser
· รองรับ `"unless"` ด้วยเพราะเงื่อนไขครึ่งหนึ่งเป็นแบบ "ตราบใดที่ยังไม่..."
เขียน `"if": "not box_delivered"` จะกลายเป็นต้องแยกวิเคราะห์ข้อความ

🚨 **`show_choices()` คืน index ของปุ่มที่เห็นบนจอ ไม่ใช่ index ในบทต้นฉบับ**
จุดที่พลาดง่ายที่สุดของฟีเจอร์นี้ — ต้องอ่านผลจากลิสต์ที่กรองแล้วเท่านั้น
เผลออ่านจากลิสต์เดิมเมื่อไหร่ ผู้เล่นกดข้อหนึ่งแล้วได้บทของอีกข้อ
**และจะเกิดเฉพาะตอนมีข้อถูกกรองออกจริง ๆ** = เทสต์ตอนเงื่อนไขเป็นจริงหมดจะไม่เจอ

⚠️ **ทุกข้อถูกกรองออกหมด → ข้ามบรรทัดไปเลย** ไม่ใช่โชว์กล่องเปล่าซึ่งกดอะไรไม่ได้ = บทค้างถาวร
⚠️ **พิมพ์ชื่อธงผิด → `push_error`** ไม่ใช่เงียบ ๆ ถือว่าเป็นเท็จ — ตัวเลือกจะหายจากเกม
ตลอดกาลโดยไม่มีอะไรบอก ดูเหมือน "เขียนบทไว้แล้วแต่ไม่เคยโผล่"

**เลือก `"if": "carrying_box"` ไม่ใช่ `"unless": "box_delivered"`**
ผู้ใช้พูดว่า *"ยังไม่วางกล่อง"* ซึ่งตรงกับตัวหลังตามตัวอักษร แต่ตัวหลังเป็นจริงตั้งแต่
**ก่อนผู้เล่นได้รับกล่องด้วย** — เดินขึ้นชั้น 2 ก่อนไปเจอวาวาแล้วเห็นตัวเลือกถามเรื่องกล่อง
ที่ตัวเองยังไม่รู้จัก · `carrying_box` เป็นจริงเฉพาะตอนถืออยู่จริง = ช่วงที่คำถามมีความหมาย
(CLAUDE.md เขียนไว้อยู่แล้วว่าตัวแปรนี้ครอบ "คุยวาวาแล้ว + มีกล่อง" ได้ในตัวเดียว)

**ทดสอบด้วยซีนชั่วคราวแล้วลบทิ้ง** — `carrying_box = true` → เห็น 2 ข้อ ·
`false` → เห็นข้อเดียว · ธงที่ไม่มีจริง → 0 ข้อ + error

### บทคุยซ้ำ `Cho_idle` — "อรุณสวัสดิ์" พูดได้ครั้งเดียว

ผู้ใช้ระบุเพิ่มว่ารอบสองต้องไม่ทักทายซ้ำ เปลี่ยนเป็น *"อยากให้ช่วยอะไรไหม?"* และเหลือตัวเลือกเดียว

**ใช้ `dialogue_id_after` ที่มีอยู่แล้ว ไม่ได้เพิ่มกลไกใหม่** — ช่องนี้ทำหน้าที่ *"พูดจบแล้วเปลี่ยนเรื่อง"*
ตรงกับที่ต้องการพอดี (ต่างจาก `talk_once` ที่แปลว่า *"พูดจบแล้วเงียบ"*)

**"อรุณสวัสดิ์" เป็นคำทักตอนเจอกันครั้งแรกของวัน ไม่ใช่คำเปิดบทสนทนา**
พูดซ้ำทุกครั้งที่เดินมากด E จะทำให้ตัวละครดูเหมือนหุ่นที่ลืมว่าเพิ่งคุยกันไป —
เป็นเหตุผลเดียวกับที่ `wawa_idle` มีอยู่ และเป็นสาเหตุที่บทคุยซ้ำควรมีทุกตัวที่คุยได้ไม่จำกัดครั้ง

⚠️ **`Cho_idle` ไม่มีตัวเลือกถามเรื่องกล่อง — ทำตามที่ผู้ใช้ระบุ ทั้งที่มีความเสี่ยง**
ผู้เล่นที่คุยรอบแรกแล้วกด "ไม่มีค่ะ" จะถามเรื่องกล่องกับโชแชงไม่ได้อีกเลย
ตรวจแล้วว่า**ไม่ตัน** เพราะยังมีคำใบ้อีก 2 ทาง (เส้น `Line2D` กระพริบตอนเข้าเขต `RoomBox`
ที่ `drop_zone_hint.gd` และบท `wawa_idle`) จึงทำตามคำขอตรง ๆ ไม่ไปเพิ่มเงื่อนไขเอง
· ถ้าวันหลังเอาคำใบ้ทางอื่นออก **ต้องกลับมาดูจุดนี้**

---

## 2026-08-20 (รอบ 4) — เพลงที่เล่นข้ามฉาก (`restSong` ตั้งแต่ออกจาก `wokeup`)

**ที่ผู้ใช้ขอ:** ออกจากฉาก `wokeup` แล้วให้ `restSong` เล่นไปเรื่อย ๆ จนกว่าจะสั่งเปลี่ยนในช่วงอื่น

**ปัญหาที่ต้องแก้ก่อน: ทุกวันนี้ตัวเล่นเพลงเป็นโหนดในซีน จึงตายพร้อมซีน**
`wokeup.gd` ใช้โหนด `BG` ในซีน — ใช้ได้กับ "เพลงประจำฉาก" แต่คำขอนี้เป็น
**"เพลงประจำช่วงของเนื้อเรื่อง"** ซึ่งเป็นคนละแนวคิด: อายุของมันไม่ผูกกับฉากใดฉากหนึ่ง

### ทางเลือกที่ตัดทิ้ง: autoload

วิธีมาตรฐานคือทำ autoload `Music` แต่โปรเจกต์นี้ **เติม autoload ลง `project.godot` เองไม่ได้**
(Godot เขียนไฟล์ใหม่จากรายการในหน่วยความจำทุกครั้งที่ผู้ใช้แตะ Project Settings แล้วรายการที่
เติมจากข้างนอกหายเงียบ — เกิดมาแล้ว 2 ครั้ง ดู DEVLOG 2026-07-30)

จะให้ผู้ใช้ไปลงทะเบียนเองก็ได้ แต่**ระหว่างที่ยังไม่ได้ลงทะเบียน `Music.` จะเป็น identifier ที่ไม่รู้จัก
→ `phone.gd` คอมไพล์ไม่ผ่าน → เกมพังทั้งเกม** ไม่ใช่แค่ไม่มีเพลง

### ทางที่ใช้: แขวนโหนดไว้ที่ `get_tree().root` + static method

`root` ไม่ถูกลบตอน `change_scene_to_file()` — เป็นกลไกเดียวกับที่ `scene_transition.tscn`
ใช้อยู่แล้วและมีเอกสารกำกับไว้ใน CLAUDE.md · `class_name Music` ทำให้เรียกได้ทุกที่
โดยไม่ต้องลงทะเบียนอะไรเลย **ไม่แตะ `project.godot` แม้แต่บรรทัดเดียว**

**เรียกซ้ำด้วยเพลงเดิม = ไม่ทำอะไร คือหัวใจของคำขอนี้**
"เล่นจนกว่าจะบอกให้เปลี่ยน" แปลว่าฉากไหนจะเรียก `Music.play_track("rest")` ก็ได้โดยไม่ต้อง
เช็คก่อน · ถ้ารีสตาร์ททุกครั้งที่เรียก เพลงจะกระตุกกลับต้นเพลงทุกครั้งที่เปลี่ยนฉาก
ซึ่งแย่กว่าไม่มีเพลงเสียอีก · เทียบด้วย `resource_path` ไม่ใช่ `==` เพราะ `load()`
อาจคืนคนละ instance หลังทรัพยากรถูกปล่อยจากแคชตอนเปลี่ยนฉาก

**สั่งเปลี่ยนเพลงที่ฉากปลายทาง ไม่ใช่ก่อนเฟด** (แก้ตามที่ผู้ใช้ทักในรอบเดียวกัน)

ตอนแรกวางไว้ที่ `phone.gd._on_final_chat_finished()` ก่อนสร้างตัวเฟด เหตุผลคือเพลงจะคร่อม
รอยต่อพอดีไม่มีช่วงเงียบตอนจอดำ · **แต่แปลว่าเพลงใหม่เริ่มดังตั้งแต่ยังอยู่ในฉาก `wokeup`
ซึ่งมีเพลงของตัวเองอยู่ (โหนด `BG`)** — ซ้อนกันสองเพลงระหว่างเฟด

ผู้ใช้ยืนยันว่า `wokeup` ใช้ `BG` ต่อไปตามเดิม และเพลงใหม่เป็นของ**ฉากหลังจากนั้น**
→ ย้ายไป `parking.gd._ready()` (`p.tscn` = ฉากแรกหลังออกจาก `wokeup`)

| ที่สั่ง | ได้ | เสีย |
|---|---|---|
| ฉากปลายทาง (ที่เลือก) | ไม่ทับเพลงฉากเดิม | เงียบสั้น ๆ ตอนจอดำ |
| ก่อนเฟด | เพลงคร่อมรอยต่อ | ซ้อนกับเพลงฉากเดิม |

**ช่วงเงียบตอนจอดำเป็นรอยต่อฉากอยู่แล้ว** ผู้เล่นไม่ได้อ่านว่าเป็นความผิดพลาด
ต่างจากเพลงสองเพลงดังพร้อมกันซึ่งฟังออกทันทีว่าผิด

⚠️ **ต้องเรียกก่อน early return ของ `parking.gd._ready()`** — ฟังก์ชันนั้นคืนค่าออกทันที
ถ้าไม่ได้มาจากแชท · วางหลังจากนั้นจะได้เพลงเฉพาะตอนมาทางเนื้อเรื่อง เดินกลับเข้ามาทางอื่นจะเงียบ

**บทเรียน: "ตายพร้อมซีน" ไม่ใช่เหตุผลพอที่จะไม่ต้องสั่งหยุด**
เดิมให้เหตุผลว่าเพลงของ `wokeup` หายเองตอนซีนถูกลบ จึงไม่ต้องจัดการอะไร — จริง
แต่มองข้ามช่วงเวลา**ก่อน**ซีนถูกลบ ซึ่งเพลงเก่ายังดังอยู่และเพลงใหม่เริ่มไปแล้ว

### กับดักที่เจอระหว่างเขียน

🚨 **`static func play()` ชนกับ `AudioStreamPlayer.play()`** → `Parse Error: Could not resolve
external class member "play"` **ที่ไฟล์ผู้เรียก (`phone.gd`) ไม่ใช่ที่ `music.gd`** —
error ชี้ผิดไฟล์แบบนี้ไล่หายากมาก · เปลี่ยนเป็น `play_track` / `stop_music` / `is_track_playing`

⚠️ **พารามิเตอร์ชื่อ `volume_db` ในเมธอดของ instance บังชื่อ property เดียวกัน** เขียนค่าไม่เข้า
โดยไม่มี error เลย · เปลี่ยนเป็น `target_db`

⚠️ **`add_child` ต้อง `call_deferred`** (ผู้เรียกอาจอยู่ใน `_ready()` ของซีนหลักที่ root ยัง busy)
และต้องมี `static var _pending` กันสร้างซ้ำเมื่อเรียกสองครั้งในเฟรมเดียว — ตอนนั้น `add_child`
ยังไม่เกิด ค้นใน root จึงยังไม่เจอ แล้วจะได้ตัวเล่นสองตัวเล่นเพลงซ้อนกันแบบหาสาเหตุยากมาก

**ทดสอบด้วยซีนชั่วคราวแล้วลบทิ้ง** (`--import` ผ่านไม่ได้แปลว่าทำงานถูก):
เรียกจาก `_ready()` ตอน root busy → สร้างได้ 1 ตัว · เรียกซ้ำ → ยังตัวเดียว ไม่รีสตาร์ท ·
`change_scene_to_file("res://p.tscn")` → **เพลงยังเล่นต่อ ตัวเล่นยังมีตัวเดียว volume ถึง -12 dB** ·
`stream.loop = true` ยืนยันแล้ว (เพลงยาว 324.9 วิ ถ้าไม่วนจะเงียบยาวหลังจากนั้น)

---

## 2026-08-20 (รอบ 3) — เก็บ error ประจำ + ปุ่มตัวเลือกเท่ากัน

### `SpawManager` — ให้ NodePath ว่างได้

`level_2.tscn` ไม่ได้กรอก `player_path` / `spawn_root` → `push_error` 2 บรรทัดทุกครั้งที่เข้าฉาก
ตำแหน่งผู้เล่นยังถูกเพราะ `level_base.gd` รับช่วงต่อ **จึงไม่ใช่บั๊กที่เห็นบนจอ**

**เลือกใส่ทางสำรองในโค้ด ไม่ใช่ไปกรอก NodePath ในซีน**
กรอกในซีนก็หาย error เหมือนกัน แต่เป็นการซ่อมทีละฉาก และซีนชั้น 2 เพิ่งถูกจัดกลุ่มใหม่
จนพาธพังมาแล้วรอบหนึ่ง — NodePath ที่กรอกมือคือของที่จะพังอีกรอบหน้าที่ลากโหนด
· ว่าง → หาผู้เล่นจากกลุ่ม `"player"` และหา spawn ด้วย `find_child()` จากราก
**ทางสำรองทนต่อการย้ายโหนดมากกว่าค่าที่กรอกไว้ด้วยซ้ำ** แต่ยังเคารพช่องที่กรอกก่อน เผื่อฉากที่มีผู้เล่นหลายตัว

⚠️ **ยังไม่ยุบระบบ spawn สองชุดให้เหลือชุดเดียว** — ต้องแก้ซีน 5 ฉาก และต้องตัดสินใจว่าจะเก็บอันไหน
รอบนี้เลือกทำให้มัน**เงียบและถูกต้อง**ก่อน ไม่ใช่รื้อระหว่างที่ซีนยังจัดไม่ลงตัว

### ปุ่มตัวเลือก — บังคับความกว้างเท่ากันจากโค้ด

`custom_minimum_size` ในซีนเป็นแค่ค่าต่ำสุด (200) · `Button` ยืดตามข้อความเสมอ
บทที่มีตัวเลือกยาวเกิน 200px จะได้ปุ่มสองข้างไม่เท่ากันโดยไม่มีอะไรบอก

**ทำในโค้ดไม่ใช่ในซีนด้วยเหตุผลเดียวกับสีตอนชี้** — ความกว้างที่ถูกต้องขึ้นกับ**ข้อความของบทนั้น**
ซึ่งเปลี่ยนทุกครั้งที่เรียก `show_choices()` กรอกตายตัวในซีนให้ตรงทุกบทเป็นไปไม่ได้

🚨 **วัดจากฟอนต์เอง ไม่อ่าน `get_minimum_size()`** — ขนาดต่ำสุดคำนวณใหม่ตอนท้ายเฟรม
อ่านทันทีหลังเซ็ต `text` จะได้ค่าของข้อความ**รอบก่อน** แล้วปุ่มจะกว้างตามบทที่แล้วเสมอ
อาการจะดูเหมือนความกว้างสุ่ม ๆ ซึ่งไล่หาสาเหตุยากมาก

### วัดจริงแล้วพบว่าคำวินิจฉัยเดิมเรื่อง "ปุ่มไม่เท่ากัน" ผิด

เขียนซีนทดสอบชั่วคราวเรียก `show_choices()` ตรง ๆ แล้ววัดค่าจริง:

```
"กล่องไหนเหรอคะ" = 176px   "ไม่มีค่ะ" = 64px    (font 32)
LeftBox   x 700..900   กึ่งกลาง 800   ห่างกลางจอ 160
RightBox  x 1000..1200 กึ่งกลาง 1100  ห่างกลางจอ 140      (กลางจอ = 960)
```

**ข้อความยาวสุด 176px ยังไม่ถึงพื้น 200px ที่ซีนตั้งไว้ → ปุ่มสองข้างกว้างเท่ากันมาตลอด**
สิ่งที่ผู้ใช้เห็นจริงคือ **คู่ปุ่มเยื้องซ้าย 10px** (ซ้ายห่างกลางจอ 160 · ขวา 140)

**บทเรียน: เดาจากโครงสร้างไฟล์แล้วต้องวัดยืนยันก่อนสรุป**
ผมสรุปจาก `.tscn` ว่า "ปุ่มยืดตามข้อความ" ซึ่งเป็นกลไกที่มีอยู่จริงแต่**ยังไม่ถูกกระตุ้นในบทปัจจุบัน**
· โค้ดที่เพิ่มยังคุ้มเพราะกันบทในอนาคต แต่**ไม่ได้แก้สิ่งที่ผู้ใช้เห็น** — อันนั้นอยู่ที่ offset ในซีน
(เป็นความผิดพลาดแบบเดียวกับที่เสนอให้แก้ `stage2` ทั้งที่ภาพไม่มีโอกาสซ้อนกัน — **สองครั้งแล้ว
ที่สรุปจากไฟล์โดยไม่วัดระยะจริง**)

---

## 2026-08-20 (รอบ 2) — จัดกลุ่มโหนดใหม่ทำ `$path` พังพร้อมกัน 6 จุด

**ผู้ใช้จัดเรียงซีนใหม่แล้วขอให้ตรวจ** — สิ่งที่เปลี่ยน: ต่อ `Level2_1` เข้า `AllLevel2` ·
เพิ่ม `level2_2/Cho` · **ห่อทุกอย่างใน `level_2.tscn` ไว้ใต้โหนดใหม่ชื่อ `WorldObjects`**

### ของที่พังเพราะการห่อโหนด (พังเงียบ ทั้งคู่ `--import` จับไม่ได้)

**1. `level_2.gd` — ห้องทั้ง 5 กลายเป็น null**
`@onready var rooms = { "meetingroom": $meetingroom, ... }` — `$meetingroom` เคยเป็นลูกของราก
พอถูกห่อก็ต้องเป็น `$WorldObjects/meetingroom` · ผลคือ `tween_property(null, ...)` พ่น error รัว ๆ
ทุกครั้งที่เดินเข้า/ออกห้อง **และไฮไลต์ห้องไม่ทำงานทั้งระบบ**

**2. `stair.gd` — `$"../StairMenu"` ชี้ผิดที่**
`StairMenu` เป็นลูกของ**ราก**เสมอ แต่ `Stair` ถูกย้ายลงไปใต้ `WorldObjects` แล้ว
`../` จึงกลายเป็น `WorldObjects` ซึ่งไม่มี `StairMenu`
**อาการ: กด E ที่บันไดแล้วเมนูไม่ขึ้น = ขึ้นลงชั้นไม่ได้เลย** — ร้ายแรงที่สุดในรอบนี้

### เลือกแก้ด้วย `find_child()` ไม่ใช่เติม `WorldObjects/` เข้าไปในพาธ

เติมพาธก็ใช้ได้ทันทีเหมือนกัน แต่**ซีนนี้ถูกห่อเพิ่มมาแล้ว 2 ชั้นในสองวัน** (`AllLevel2` แล้ว `WorldObjects`)
และยังจัดไม่ลงตัว — เขียนพาธตายตัวคือการรับปากว่าจะกลับมาแก้ทุกครั้งที่ลากโหนด
`find_child()` ค้นแบบไล่ลูกหลาน จึงไม่สนใจว่าของถูกห่ออยู่กี่ชั้น

⚠️ **`stair.gd` ต้องค้นจาก `owner` ไม่ใช่จากตัวเอง** — `StairMenu` อยู่นอกกิ่งของ `Stair`
ค้นจากตัวเองยังไงก็ไม่เจอ · `owner` = โหนดรากของซีน ซึ่งเป็นที่ที่ `StairMenu` อยู่จริงทุกฉาก

⚠️ **หาไม่เจอต้อง `push_error` ทันทีตอนเปิดฉาก ไม่ใช่ปล่อยเป็น null**
ของเดิมพังแบบ "ไม่มีอะไรเกิดขึ้นตอนกด E" ซึ่งดูเหมือนปุ่มไม่ทำงาน ไม่ใช่พาธผิด
· `level_2.gd` ก็ไม่ใส่ห้องที่หาไม่เจอลง dictionary เลย — ใส่ null ไว้จะไปตายที่ `tween_property`
ซึ่ง backtrace ชี้ไปคนละบรรทัดกับต้นเหตุ

### บทเรียนหลัก: `--import` ไม่ใช่การตรวจว่า "ซีนยังทำงานอยู่"

พาธโหนดถูกค้นตอนรัน ไม่ใช่ตอนคอมไพล์ — จัดกลุ่มโหนดใหม่จึงพังได้โดยที่ทุกอย่างยังคอมไพล์ผ่าน
**ต้องรันฉากจริงแล้วอ่าน error ด้วยเสมอ** และรอบนี้ยังต้องตั้ง `GameState.next_spawn_point`
ก่อนเข้าฉาก (ทำด้วยซีนทดสอบชั่วคราวแล้วลบทิ้ง) เพราะเส้นทาง "มาถึงจากบันได" ไม่ถูกแตะเลยถ้ารันฉากตรง ๆ

### เจอของเก่าติดมาด้วย: ระบบ spawn ซ้อนกัน 2 ชุด

`SpawManager.gd` (กรอก NodePath เอง) กับ `level_base.gd` (`find_child` อัตโนมัติ) ทำงานเรื่องเดียวกัน
`level1` `level_3` `p` `under` กรอกไว้ครบ → `SpawManager` ทำแล้วล้าง `next_spawn_point` ทิ้ง
**`level_2` ไม่ได้กรอก** → `push_error` 2 บรรทัดทุกครั้งที่มาถึง แล้ว `level_base` รับช่วงต่อ
**ตำแหน่งถูกทั้งสองทาง** จึงไม่ใช่บั๊กที่เห็นบนจอ — แต่ error ประจำแบบนี้คือสิ่งที่ทำให้
error จริงกลืนหายไปในกองข้อความ (ยังไม่แก้ ต้องตัดสินใจก่อนว่าจะเก็บระบบไหน)

---

## 2026-08-20 — แยกซีน `AllLevel2` ห่อชั้น 2 + ตรวจของที่หลุดตอนแยก

**ที่ผู้ใช้ทำ:** สร้าง `all_level_2.tscn` (Node2D ชื่อ `AllLevel2`) มี `level_2.tscn` เป็นลูกชื่อ `Level2`
เพราะ *"ทำในซีนนั้นเองมันจะยาวมาก"* — `level_2.tscn` ยาว 2,500+ บรรทัด

**ทำไมการห่อถึงเป็นทางที่ถูก**
ทางเลือกอื่นคือยัด `Level2_1` กับ NPC เข้าไปในซีนแมพตรง ๆ ซึ่งทำให้ไฟล์ที่ยาวอยู่แล้วยาวขึ้นอีก
และของเนื้อเรื่อง (เควสต์ · NPC) จะปนกับของโครงสร้าง (พื้น กำแพง บันได) แยกกันไม่ออก
· ห่อไว้แล้วของใหม่มาเป็น**พี่น้อง**ของ `Level2` — แก้ซีนแมพเมื่อไหร่ของเนื้อเรื่องไม่ถูกแตะ
· เป็นแนวคิดเดียวกับ `ChapterSlot` ใน `under.tscn` แค่คนละกลไก

**ยืนยันว่าการซ้อนอีกชั้นไม่ทำให้ระบบเดิมพัง** (ตรวจแล้ว ไม่ได้เดา)
· `level_base._apply_spawn_point()` ใช้ `find_child()` ซึ่งค้นในกิ่งของ `Level2` เอง ✅
· `level_2.gd` ใช้ `$meetingroom` ฯลฯ = path เทียบตัวเอง ไม่ใช่เทียบราก ✅
· รัน headless 90 เฟรมแล้วห้องยังไฮไลต์ตามผู้เล่นได้ปกติ ✅

**เก็บ path ชั้น 2 ไว้ที่ `const LEVEL_2_SCENE` ตัวเดียวใน `stair_menu.gd`**
`floor_targets` ชี้ชั้น 2 อยู่ **2 ที่** (จากชั้น 1 ขึ้น · จากชั้น 3 ลง) แก้ทีละที่แล้วตกหล่นข้างเดียว
จะกลายเป็นบันไดฝั่งหนึ่งไป AllLevel2 อีกฝั่งไปซีนเปล่า ซึ่งเป็นบั๊กที่ทำซ้ำได้ยากมาก
· ระหว่างทำ ผู้ใช้ย้ายไฟล์จาก `npc/data/` มาไว้ที่รากพอดี → แก้ค่าคงที่จุดเดียวจบ (พิสูจน์ตัวเองทันที)

### ของที่หลุดหายไปตอนแยกซีน — เจอจากการตรวจ ไม่ใช่จากอาการ

🚨 **`level_2_1.tscn` (เควสต์กล่อง) ไม่ถูก instance ที่ไหนเลยแล้ว**
grep ทั้งโปรเจกต์เจอแต่ในคอมเมนต์ของ `box_drop_point.gd` / `drop_zone_hint.gd`
เดิม CLAUDE.md บันทึกว่าอยู่ใน `level_2.tscn` — ถูกถอดออกตอนแยกซีนแล้วยังไม่ได้เอาไปต่อที่ใหม่
**อาการจริง: ถือกล่องขึ้นชั้น 2 แล้วไม่มีอะไรเลย เควสต์ไม่มีทางจบ**

**บทเรียน: ฟีเจอร์ที่ "ทำงานได้แล้ว" พังได้จากการจัดบ้าน ไม่ใช่จากการแก้โค้ด**
โค้ดทั้งสองไฟล์ยังถูกต้องทุกบรรทัด `--import` ก็ผ่าน — สิ่งเดียวที่หายคือ**โหนดในซีน**
ซึ่งไม่มีเครื่องมือไหนเตือน · ต่อไปเวลาแยก/ย้ายซีนต้อง `grep` หาว่าใครเคย instance ของที่อยู่ในนั้นบ้าง

**`AllLevel2` ยังไม่ได้เปิด `y_sort_enabled`** — ยังไม่แสดงอาการเพราะมีลูกตัวเดียว
แต่ทั้งจุดประสงค์ของซีนนี้คือเอาของมาต่อเป็นพี่น้องกัน พอต่อจริงจะจมหลังพื้นห้องทันที
**เป็นกับดักเดียวกันกับ `ChapterSlot` และ `Level2_1` = ครั้งที่ 3 แล้ว** จึงเขียนไว้ใน CLAUDE.md
ทั้งสามที่ ไม่ใช่ที่เดียว

### `Cho.tres` สะกดชื่ออนิเมชันผิด — บั๊กที่ไม่ส่งเสียง

ชุดชื่อ `idel` / `idel_side` / `idel_up` แต่ `NPCData` ตั้งต้นเป็น `idle` / `idle_up` / `idle_side`

**เลือกให้แก้ที่ชื่อชุดใน SpriteFrames ไม่ใช่ไปตั้ง `default_anim = "idel"` ทับ**
ตั้งทับก็ใช้งานได้เหมือนกัน แต่คำสะกดผิดจะอยู่ในโปรเจกต์ต่อไปเรื่อย ๆ แล้ว NPC ตัวถัดไป
ที่ก๊อป `.tres` นี้ไปจะได้ `idel` ติดไปด้วย — ค่าตั้งต้นมีไว้ให้ตรงกันทั้งโปรเจกต์ ไม่ใช่ให้หลบ

⚠️ **อาการของมันไม่เหมือนบั๊ก** — ท่ายืนยังขึ้น (โค้ดเลือกชุดแรกแทนพร้อมเตือน)
ที่หายเงียบ ๆ คือ `face_player()` ซึ่งใช้ `_play_if_exists()` = หาไม่เจอแล้วไม่ทำอะไร
ผลบนจอคือ "โชแชงไม่หันหน้ามาตอนคุย" ซึ่งดูเหมือนยังไม่ได้ทำฟีเจอร์ ไม่ใช่พิมพ์ผิด

### `stage` ตั้ง `y_sort_origin` แล้วภาพไม่ขยับ — เพราะยังไม่ข้ามเส้นแบ่ง

**อาการที่ผู้ใช้เจอ:** เปลี่ยน `y_sort_origin` ของ `meetingroom/stage` จาก `20` เป็น `-25`
(ขยับตั้ง 45 หน่วย) แล้ว *"ไม่เห็นมีอะไรเกิดขึ้นเลย"*

**สาเหตุ: จุดเรียงของผู้เล่นไม่ใช่ตำแหน่งเท้า แต่เป็น `เท้า − 22`**
โหนด `player` ติ๊ก `y_sort_enabled` ไว้ → ลูกถูกแยกออกมาเรียงทีละตัว สิ่งที่เข้าลิสต์จึงเป็น
`AnimatedSprite2D` ที่ `position = (0, -22)` ไม่ใช่ `CharacterBody2D`

```
tile แถว 57 จุดเรียง = 57×32 + 16 + y_sort_origin = 1840 + yso
ผู้เล่นเข้าใกล้ได้สุด  เท้า ≈ 1835 (collision ของ tile กั้นที่ 1832)
จุดเรียงผู้เล่น       = 1835 − 22 = 1813
เส้นแบ่ง             yso ≈ −27
```

`20` → 1860 · `-25` → 1815 · **ทั้งคู่ยังมากกว่า 1813** = เวทีทับผู้เล่นเหมือนกันทั้งสองค่า

**บทเรียนที่ต้องจำ: คำนวณเส้นแบ่งก่อนแล้วค่อยตั้ง อย่าเดาแล้วลองทีละค่า**
การลองไปเรื่อย ๆ ในกรณีนี้จะสรุปผิดว่า "y-sort ของ TileMapLayer ใช้ไม่ได้" ทั้งที่มันทำงานถูกต้อง
· ค่า `-15` ที่เห็นเกลื่อนทั้งฉากน่าจะเกิดจากการชดเชย 22px นี้มาตลอดโดยไม่รู้ตัว

**ผู้ใช้ถูกเรื่อง `stage2` — ผมเสนอผิดไปก่อนหน้านี้**
ตอนแรกผมชี้ว่า `stage2` ลืมติ๊ก `y_sort_enabled` ทั้งที่ตั้ง `y_sort_origin` ครบ 7/7
แต่พอคำนวณระยะจริง: สไปรท์ผู้เล่น 64×64 ที่ offset −22 กินพื้นที่ `เท้า−54` ถึง `เท้า+10`
และ collision กั้นไม่ให้เข้าใกล้ `stage2` (แถว 58-60) พอที่ภาพจะซ้อนกันได้เลย → **ไม่ต้องตั้งจริง ๆ**

🚨 **"ตั้ง `y_sort_origin` ครบทุก tile แต่ไม่ติ๊กที่ layer" ไม่ได้แปลว่าลืมเสมอไป**
ต้องดูก่อนว่าภาพสองอันมีโอกาสซ้อนกันจริงไหม — y-sort ตัดสินแค่ "ใครทับใครตรงที่ซ้อนกัน"
ไม่ได้ย้ายอะไร · ตัดสินจากตารางอย่างเดียวโดยไม่วัดระยะจริงจะเสนอให้ไปแก้ของที่ไม่ได้พัง

### error `Can't add child '@HBoxContainer@...'` — ไม่ใช่ของโปรเจกต์นี้

สรุปว่าเป็น error ภายในเอดิเตอร์เอง ด้วยหลักฐาน 2 ข้อ:
1. ไม่มีสคริปต์ไหนสร้างหรือ `add_child` ใส่ `HBoxContainer` เลย (`add_child` มี 14 จุด เป้าหมายเป็น
   `CanvasLayer` · `VBoxContainer` · `Node2D` · `self` ทั้งหมด)
2. ชื่อ `@HBoxContainer@137934` เป็นชื่อที่ Godot ตั้งให้โหนดที่ถูกสร้างโดยไม่มีชื่อ —
   โหนดจากซีนมีชื่อจริงเสมอ (`Header` · `InputBar` · `HBoxContainer` ใน `hud.tscn`)

**บันทึกไว้เพื่อไม่ให้ไล่หาซ้ำ** — ครั้งหน้าเจอ error ที่ชื่อโหนดขึ้นต้นด้วย `@` ให้สงสัยเอดิเตอร์ก่อน

---

## 2026-08-19 — คำใบ้ "กล่องไหนเหรอคะ" ในบทคุยซ้ำของวาวา

**ที่ผู้ใช้ขอ:** คุยเรื่องช่วยยกของจบแล้ว ผู้เล่นกลับมาคุยอีกรอบ ให้มีตัวเลือกถามว่า
*"กล่องไหนเหรอคะ"* แล้ววาวาตอบว่ากล่องบนโต๊ะ · เดินไปใกล้ ๆ จะมีแสงสีเหลืองขึ้น ยกไปได้เลย

**เติมลง `wawa_idle` ไม่ได้สร้างคีย์บทใหม่**
เงื่อนไข *"ผ่านเรื่องช่วยยกของมาแล้ว"* ที่ผู้ใช้ระบุ ตรงกับนิยามของ `dialogue_id_after` พอดี —
บทนี้จะถูกหยิบมาเล่นก็ต่อเมื่อ `wawa_under_ch1` (บทที่วาวาฝากกล่อง) เล่นจบไปแล้วเท่านั้น
สร้างคีย์ใหม่แล้วต้องไปหาที่แขวนใหม่ในซีน ทั้งที่ช่องที่มีอยู่ให้เงื่อนไขเดียวกันฟรี ๆ

**เปลี่ยนสถานะของ `wawa_idle` จาก "บทกันความเงียบ" เป็น "ที่เก็บคำใบ้"**
เดิมเขียนกำกับไว้ว่าเป็นบทชั่วคราวไว้กันไม่ให้กด E แล้วเงียบ · ตอนนี้มันทำหน้าที่จริงแล้ว
คือเป็นที่ที่ผู้เล่นซึ่งลืมว่าต้องทำอะไรต่อจะกลับมาถามได้ ซึ่งเป็นที่ที่คนเล่นเกมมองหาโดยสัญชาตญาณ
(ถามคนที่ฝากงานให้) จึงไม่ต้องมีระบบ quest log แยก

**วางไว้ปุ่มซ้าย (index 0) ให้ "ไม่มีค่ะ" ย้ายไปขวา**
สองตัวเลือกจะอยู่คนละฝั่งจอตามการจัดวางของ `CHOICE_BUTTON_PATHS` · ข้อที่พาเนื้อเรื่องเดินต่อ
ควรอยู่ตำแหน่งที่สายตากวาดถึงก่อน ส่วน "ไม่มีค่ะ" คือทางออกซึ่งหาเจอง่ายอยู่แล้ว

**คำใบ้พูดถึง "แสงสีเหลือง" ซึ่งคืออนิเมชัน `nearby` ของ `interact_box.gd`**
บอกวิธี**สังเกต**ด้วย ไม่ใช่บอกแค่ตำแหน่ง — ผู้เล่นที่ยังไม่รู้ว่าของในฉากมีสัญญาณบอกว่ากดได้
จะเดินผ่านกล่องไปเฉย ๆ ทั้งที่ยืนถูกที่แล้ว

⚠️ **ยอมรับข้อจำกัด: บทไม่รู้ว่าหยิบกล่องไปแล้วหรือยัง**
ระบบตัวเลือกยังไม่มีเงื่อนไขรายข้อ (ต้องแก้ `dialogue_system.gd` ให้อ่าน `GameState` ได้)
หยิบไปแล้วกลับมาถามซ้ำจะได้คำใบ้เดิม — เลือกปล่อยไว้ก่อนเพราะผลเสียแค่ "ซ้ำ" ไม่ถึงกับผิด
และการใส่เงื่อนไขในบทเป็นการตัดสินใจเชิงระบบที่ควรทำตอนมีที่ใช้หลายจุด ไม่ใช่เพื่อบรรทัดเดียว

---

## 2026-08-19 — เควสต์เอากล่องไปวางที่ชั้น 2 + สถานะถือของข้ามฉาก

**ที่ผู้ใช้ขอ:** หยิบกล่องจากวาวาที่ห้องใต้ดิน → ขึ้นมาชั้น 2 ทั้งที่ยังถืออยู่ → เข้าเขต `RoomBox`
เส้น `Line2D` กระพริบบอกจุด → เข้าใกล้ `Box` เส้นดับแล้วกด E วางลง → กลับไปใช้ชุดอนิเมชันปกติ

**ต้องแก้รากก่อน: `carrying` ไม่เคยข้ามฉากได้เลย**
ผู้เล่นถูก instance ใหม่ทุกซีน ตัวแปรที่ตัวผู้เล่นจึงรีเซ็ตทุกครั้ง — เดินออกจากห้องใต้ดินปุ๊บ
กล่องหายจากมือทันที **เงื่อนไข "มีกล่อง" ที่ชั้น 2 เป็นเท็จเสมอ** ฟีเจอร์ทั้งอันจึงเป็นไปไม่ได้
จนกว่าจะย้ายตัวจำไปไว้ที่ `GameState` (เป็นข้อที่ CLAUDE.md เขียนเตือนไว้ตั้งแต่ทำระบบหยิบของ)

**ทำเป็น setter ที่เขียนกลับลง GameState ให้เอง ไม่ให้คนเรียกไปตั้งสองที่**
มีที่ตั้ง `carrying` หลายจุด (หยิบกล่อง · วางกล่อง · เนื้อเรื่องในอนาคต) ลืมจุดใดจุดหนึ่งแล้ว
เกมจะจำผิดแบบเงียบ ๆ · แบบนี้โค้ดเดิมทุกบรรทัดที่เขียน `player.carrying = true` ยังใช้ได้เหมือนเดิม

**`carrying_box` ครอบคลุมเงื่อนไขทั้งสามข้อที่ผู้ใช้ระบุในตัวแปรเดียว**
"คุยกับวาวาแล้ว + มีกล่อง + เป็นอนิเมชันถือกล่อง" — เพราะกล่องที่ห้องใต้ดิน**ล็อกอยู่จนกว่าจะคุยจบ**
หยิบขึ้นมาได้แปลว่าผ่านทั้งสามข้อแล้วโดยอัตโนมัติ · แยกเป็นสามตัวแปรจะเป็นข้อมูลซ้ำที่หลุดกันได้

**`box_delivered` แยกจาก `carrying_box`** — "ไม่ได้ถืออยู่" กับ "วางเสร็จแล้ว" คนละเรื่อง
(ก่อนคุยวาวาก็ไม่ได้ถือเหมือนกัน) · ใช้ตอนตัดสินใจว่าจะเสียบซีนเควสต์ไหมด้วย ไม่งั้น
วางเสร็จแล้วเดินไปชั้นอื่นแล้วกลับเข้ามา กล่องที่วางไว้จะหายไปเฉย ๆ

🚨 **`box_drop_point.gd` เป็นสคริปต์ใหม่ ไม่ใช่เพิ่มโหมดใน `interact_box.gd`**
ซีน `level_2_1.tscn` เดิมแนบ `interact_box.gd` (สคริปต์หยิบของ) ไว้ที่ `Box` พร้อมค่า
`unlock_after_npc = ^"../Wawa"` ซึ่ง**ไม่มีโหนดนั้นในซีน** → `push_error` แล้วกล่องล็อกตลอดฉาก

"วางลง" เป็นเรื่องตรงข้ามกับ "หยิบขึ้น" ทุกด้าน: ท่าเริ่มต้น (ซ่อน vs โชว์) · เงื่อนไขกดได้
(ต้องถือของอยู่ vs ต้องไม่ถือ) · สิ่งที่เกิดตอนกด (`carrying = false` vs `true`)
ยัดรวมไฟล์เดียวแล้วทุกเมธอดจะต้องมี `if โหมดไหน` คร่อมไว้ ซึ่งอ่านยากกว่าสองไฟล์ที่แต่ละไฟล์ทำเรื่องเดียว

**ก่อนวาง กล่องปลายทางซ่อนอยู่** (ผู้ใช้เลือก) — ชุดอนิเมชัน `_box` วาดกล่องมาในภาพผู้เล่นแล้ว
โชว์ที่จุดหมายด้วยจะเห็นกล่อง 2 ใบในเฟรมเดียว แล้วผู้เล่นแยกไม่ออกว่าใบไหนคือใบที่ถืออยู่
· ตัวบอกจุดก่อนวางคือเส้นกระพริบอย่างเดียว

**สองวงซ้อนกัน คนละหน้าที่** — `RoomBox` (ทั้งมุมห้อง) = "ของต้องไปวางแถวนี้" → เส้นกระพริบ ·
`Box/InteractArea` (รัศมี 50) = "ยืนตรงนี้กดได้" → เส้นดับ
🚨 **เส้นดับตอนเข้าเขตกด E ไม่ใช่ตอนวางเสร็จ** — เส้นมีหน้าที่เดียวคือพาไปให้ถึง
ถึงแล้วยังกระพริบก็กลายเป็นเสียงรบกวนทับกล่องที่กำลังจะวาง และตัวบอกว่ากดได้คือระบบเป้าหมาย
ของผู้เล่นซึ่งทำงานของมันอยู่แล้ว

🚨 **`_refresh()` เป็นจุดเดียวที่เปิด/ปิดเส้น** — เหตุการณ์ที่กระทบมี 4 อย่าง (เข้าห้อง · ออกห้อง ·
เข้าใกล้กล่อง · วางเสร็จ) และ**มาไม่เรียงลำดับ** สั่งเปิด/ปิดกระจายตามแต่ละเหตุการณ์
จะมีลำดับที่ทำให้เส้นค้างสว่างหรือค้างดับแบบทำซ้ำไม่ได้ ซึ่งเป็นบั๊กประเภทที่ debug ยากที่สุด

**กลับลำระหว่างทาง: `Level2_1` อยู่ในซีนตลอดเวลา ไม่ได้สร้างตอนเนื้อเรื่องถึงคิว**

ตอนแรกเขียน `_spawn_box_quest()` ใน `level_2.gd` ให้ instantiate `level_2_1.tscn` เข้ามา
เมื่อเงื่อนไขครบ (เหตุผลตอนนั้น: เดินมาชั้น 2 ก่อนวาวาฝากงานแล้วเจอเส้นเรืองแสง = สปอยล์)
· ระหว่างนั้นผู้ใช้ instance โหนดนั้นเข้า `level_2.tscn` เองในเอดิเตอร์
→ **มีสองกลไกพร้อมกัน = กล่อง 2 ใบซ้อนกันทันทีที่เงื่อนไขครบ** จึงถอดโค้ดออก ยึดของในซีน

**เงื่อนไขเนื้อเรื่องย้ายจาก "สร้างโหนดไหม" ไปเป็น "โชว์อะไรไหม"** ซึ่งรัดกุมกว่าเดิม —
ไม่ต้องมีจังหวะที่โหนดโผล่มากลางฉาก และไม่ต้องตั้ง `y_sort_enabled` จากโค้ด

🚨 **เส้นเช็ค `carrying_box` ไม่ใช่ `not box_delivered`** — จุดที่พลาดง่ายที่สุดของการกลับลำนี้
สองเงื่อนไขนี้ให้ผลเหมือนกันทุกกรณี **ยกเว้นตอนที่ยังไม่ได้คุยกับวาวา**
(ตอนนั้น `box_delivered` ก็เป็น false เหมือนกัน → เส้นจะกระพริบตั้งแต่ยังไม่ได้รับงาน = สปอยล์
ซึ่งเป็นสิ่งที่การสร้างโหนดตามเงื่อนไขเคยกันไว้ให้)
· `carrying_box` เงื่อนไขเดียวครอบได้ทั้งสามสถานะ เพราะเป็นเท็จทั้ง**ก่อนหยิบ**และ**หลังวาง**

⚠️ **ไม่เก็บ "วางแล้ว" เป็นตัวแปรในตัวเส้น อ่านจาก `GameState` ทุกครั้ง**
เก็บสำเนา = เส้นเชื่อสำเนานั้น ซึ่งอัปเดตได้ก็ต่อเมื่อสัญญาณ `placed` มาถึงจริง
`drop_point_path` ชี้ผิดเมื่อไหร่ (เปลี่ยนชื่อโหนด/ย้ายที่) เส้นจะกระพริบต่อทั้งที่วางไปแล้ว
· มีเทสต์จำลองสายขาดไว้แล้วเพื่อกันการถอยกลับไปเก็บสำเนา

**เทสต์:** ซีนทดสอบชั่วคราวรัน headless ผ่าน 25 ข้อ — ยังไม่มีกล่องแล้วเส้นเงียบ/กดวางไม่ได้ ·
ถือมาแล้วเข้าเขตห้องเส้นโผล่แต่ยังกดไม่ได้ · เข้าเขตกล่องเส้นดับและกดได้ ·
วางแล้วกล่องอยู่พิกัดเดิมในซีน (632, 2085) ไม่ขยับตามผู้เล่น · ผู้เล่นมือว่าง · กดซ้ำไม่ได้ ·
เดินถอยกลับเขตห้องเส้นไม่กลับมา · เข้าฉากใหม่กล่องยังอยู่ · สายสัญญาณขาดแล้วเส้นยังดับเองได้
(ลบไฟล์ทดสอบทิ้งแล้ว)

**ยังไม่ได้ทำ:** กล่องที่ `under.tscn` ยังหยิบซ้ำได้ถ้าเดินกลับไป — `interact_box.gd`
จำแค่ภายในฉาก ยังไม่ได้อ่าน `GameState.carrying_box` · และวางเสร็จแล้วยังไม่มีอะไรเกิดต่อ
(ไม่มีบทพูด ไม่เปลี่ยนบท) ตามที่ผู้ใช้ระบุ

---

## 2026-08-19 — สมุดจดข้อกำหนดในปริศนาจัดที่นั่ง

**ที่ผู้ใช้อธิบาย:** `Label1` / `Label2` ใต้โหนด `Book` ใน `align_1.tscn` ทำค้างไว้ตั้งแต่จัดซีน
ตั้งใจให้เป็น**ที่จดข้อกำหนดของตัวละครที่คลิกดู** — คลิกดูใครข้อกำหนดของคนนั้นมาโผล่ที่นี่ด้วย
(ก่อนหน้านี้เป็นโหนดเปล่าที่ไม่มีสคริปต์ไหนเขียนลงไปเลย)

**สมุด = ภาพสะท้อนของบับเบิลตัวที่กำลังดูอยู่ ไม่ใช่ที่สะสมของทุกคน** (ผู้ใช้เลือกทั้ง 3 ข้อ)

| คำถาม | ที่เลือก | ทางที่ไม่เอา |
|---|---|---|
| 2 Label แบ่งหน้าที่ยังไง | บรรทัดที่ 1 / บรรทัดที่ 2 ของข้อกำหนด | `Label1` = ชื่อ NPC · `Label2` = ข้อกำหนด |
| คลิกตัวใหม่ | ทับของเดิม โชว์ทีละคน | สะสมเป็นรายการที่จดไว้แล้ว |
| ปิดบับเบิล | ล้างสมุด | ค้างข้อความไว้ให้อ่านต่อ |

ข้อ 2 กับ 3 ทำให้ **สมุดไม่ใช่ระบบจดจำ** — เป็นแค่ที่อ่านข้อความซ้ำให้ชัดกว่าบับเบิลตัวเล็ก
ถ้าวันหลังอยากได้ "สมุดที่จดสะสม" จริง ๆ ต้องรื้อ: 10 ตัวใส่ 2 ช่องไม่พอ ต้องสร้าง Label จากโค้ด
และต้องเก็บสถานะว่าใครถูกจดไปแล้วบ้าง

🚨 **`erase()` เช็คเจ้าของข้อความก่อนล้าง ไม่ล้างทันทีที่บับเบิลใบไหนก็ได้ปิด**
ตอนแรกจำเป็นเพราะ `click.gd` เก็บบับเบิลไว้ที่ NPC แต่ละตัว (`_current_bubble`)
คลิกตัวที่สองไม่ได้ปิดใบแรก → เปิดค้างพร้อมกันได้หลายใบ

**ผู้ใช้สั่งแก้เป็น "เปิดอันใหม่ปิดอันเก่า" ทันทีที่รู้เรื่องนี้** (ดูหัวข้อถัดไป)
แต่ **การเช็คเจ้าของยังต้องอยู่** — เหตุผลเปลี่ยนไปเป็นเรื่องจังหวะแทน: ใบเก่ายิง
`bubble_closed` หลังอนิเมชันย่อหายจบ ซึ่งช้ากว่าใบใหม่ 0.15 วิ ล้างทื่อ ๆ = สมุดของตัวที่
เพิ่งคลิกดูถูกลบทิ้งหลังโผล่มาแป๊บเดียว

⚠️ **สมุดรับ `text` ที่ `click.gd` ส่งให้ ไม่ได้ไปดึงจาก `NPCalignDatabase` เอง**
แหล่งเดียวกับบับเบิลเป๊ะ ๆ — สองที่ที่อ่านคนละทางแล้ววันหลังแก้ไม่ครบ จะได้บับเบิลพูดอย่าง
สมุดจดอีกอย่าง ซึ่งผู้เล่นเชื่อสมุดแล้วแก้ปริศนาไม่ผ่านโดยไม่รู้ว่าผิดตรงไหน

⚠️ **เก็บ Label ลูกอัตโนมัติ ไม่ผูกกับชื่อ `Label1` / `Label2`**
กฎเดียวกับที่ `StandZone` นับช่องยืนจากลูกที่มี `Marker2D` — อยากได้ข้อกำหนด 3 บรรทัด
แค่เพิ่ม Label ในซีน ไม่ต้องแตะโค้ด · บรรทัดล้นจำนวน Label จะ `push_warning` บอก
(หายเงียบ ๆ = ปริศนาแก้ไม่ได้โดยไม่มีอะไรบอก ตอนนี้ NPC6 กับ NPC7 มี 2 บรรทัด ที่เหลือ 1)

⚠️ **`strip_edges()` ทุกบรรทัด** — ช่องว่างหน้าบรรทัดใน `NPC_DATA` (เช่น NPC6 `" -ฉันอยากอยู่ด้านหลัง"`)
เป็นการจัดหน้าให้บับเบิล ไม่ใช่ของสมุดซึ่งชิดซ้ายอยู่แล้ว

**หาสมุดด้วยกลุ่ม `npc_note` และยอมให้ไม่มี** — ฉากปริศนาที่ไม่มีสมุดยังเล่นได้ตามปกติ
บับเบิลเป็นตัวหลัก สมุดเป็นตัวช่วยจำ (แบบเดียวกับ `MoveCounter` / `StandZone`)

**ยังไม่ได้ทำ:** ต้องแนบ `scripts/npc_note.gd` ที่โหนด `CanvasLayer/Book/VBoxContainer` เอง
ในเอดิเตอร์ — ไม่แก้ `.tscn` จากข้างนอกเพราะ Godot เปิดอยู่ตอนทำงานนี้

---

## 2026-08-19 — บับเบิลปริศนาที่นั่งเปิดได้ทีละใบ

**ที่ผู้ใช้สั่ง:** "เปิดอันใหม่ปิดอันเก่า" — เดิมคลิก NPC ตัวที่สองแล้วใบแรกยังค้างอยู่บนจอ
เพราะ `click.gd` เก็บ `_current_bubble` ไว้ที่ NPC แต่ละตัว ต่างคนต่างถือของตัวเอง ไม่มีใครรู้จักใคร

**เก็บ "ตอนนี้ใครเปิดอยู่" ที่ `static var _bubble_owner` ไม่ใช่ไล่ถามทุกตัว**
เป็นสถานะที่มีได้ทีละตัวทั้งกระดาน จึงควรอยู่ที่เดียว — กฎเดียวกับที่ `SeatManager._hovered_seat`
เก็บช่องที่กำลังเล็งไว้ที่ตัวกลาง ไม่ใช่ที่ตัว `Seat` แต่ละช่อง

ทางที่ไม่เอา: ใส่ NPC ทุกตัวเข้ากลุ่มแล้ววนปิดทุกตัวตอนคลิก — ต้องแตะซีนเพิ่มกลุ่มให้ครบ 10 ตัว
และค่าใช้จ่ายเป็น O(n) ทุกคลิกเพื่อรู้สิ่งที่จำไว้ตัวเดียวก็พอ

⚠️ **ตัวแปร `static` อยู่ข้ามฉาก** — เปลี่ยนฉากแล้ว NPC ตัวเก่าถูกลบ แต่ตัวแปรยังชี้อยู่
ทุกที่ที่อ่านจึงผ่าน `is_instance_valid()` ก่อนเสมอ (ไม่เช็ค = กลับเข้าฉากปริศนารอบสอง
แล้วคลิกครั้งแรกจะพังทันที ซึ่งเป็นบั๊กที่ไม่มีวันเจอตอนเทสต์ฉากเดียวรอบเดียว)

🚨 **`bubble_closed` มาถึงช้ากว่าการเปิดใบใหม่ 0.15 วิ** เพราะ `speech_bubble.gd` ยิงสัญญาณ
หลัง tween ย่อหายจบ ไม่ใช่ตอนสั่งปิด → ตอนสัญญาณของใบเก่ามาถึง ตัวใหม่ตั้งตัวเองเป็นเจ้าของแล้ว
**ทั้ง `_bubble_owner` และสมุดจึงต้องเช็คก่อนล้างว่ายังเป็นของตัวเองอยู่ไหม**
ล้างทื่อ ๆ = ลบทะเบียนของตัวที่เพิ่งเปิด แล้วคลิกตัวถัดไปจะไม่มีใครถูกปิด กลับไปค้าง 2 ใบเหมือนเดิม
(บั๊กที่จะโผล่เฉพาะตอนคลิกสลับตัวเร็ว ๆ ซึ่งเป็นสิ่งที่ผู้เล่นทำจริงตอนไล่อ่านข้อกำหนด)

**ไม่รอให้ใบเก่าปิดเสร็จก่อนขึ้นใบใหม่** — ปล่อยให้ใบเก่าย่อหายพร้อมใบใหม่ที่งอกขึ้นมา
ดูลื่นกว่ารอ 0.15 วิ แล้วค่อยขึ้น ซึ่งจะรู้สึกเหมือนเกมหน่วง

⚠️ **`_close_other_bubble()` อยู่หลังด่านเช็ค `bubble_scene` / `ui_layer`**
ปิดใบเก่าทิ้งแล้วใบใหม่ขึ้นไม่ได้ = คลิกแล้วบับเบิลหายไปเฉย ๆ โดยไม่มีอะไรมาแทน

---

## 2026-08-18 — ตัวนับ Moves ในปริศนาจัดที่นั่ง

**ที่ผู้ใช้ขอ:** Label `Number` ใต้ `TextureRect2` นับจำนวนครั้ง — วางลงที่นั่ง 1 ครั้ง = เสีย 1
เริ่มที่ 15

🚨 **นับที่ `click.gd._end_drag()` ไม่ใช่ใน `Seat.place_npc()`**
`place_npc()` ดูเป็นที่ที่เหมาะกว่าเพราะเป็นทางผ่านเดียวของการวางลงที่นั่ง —
แต่ "move" คือ**การกระทำของผู้เล่น** ไม่ใช่ "การที่ NPC ไปอยู่บนเก้าอี้"
วันหลังมีระบบจัดที่นั่งอัตโนมัติ (ปุ่มสลับ/รีเซ็ต/เฉลย) จะกินโควตาผู้เล่นทั้งที่ผู้เล่นไม่ได้ทำอะไร
ผูกกับจังหวะที่ผู้เล่นปล่อยเมาส์สำเร็จจึงตรงความหมายกว่า

**ย้ายไปพักที่ช่องยืนไม่เสีย move** (ตามที่ผู้ใช้กำหนด: เสียเฉพาะ "จับวางที่ Seat")
· วางไม่ลงแล้วเด้งกลับก็ไม่เสีย

⚠️ **ตัวเลขในซีนถูกเขียนทับด้วย `max_moves` ตอนเปิดฉาก**
ตั้งใจให้มีแหล่งความจริงเดียว แต่เป็นกับดักคลาสสิก: พิมพ์เลขใหม่ในช่อง Text ของ Label
จะเห็นถูกตอนแก้ซีนแล้วเด้งกลับทันทีที่กดรัน หาสาเหตุไม่เจอ → เขียนเตือนไว้ทั้งในสคริปต์และ CLAUDE.md

**ยังไม่ตัดสินใจ: หมดโควตาแล้วเกิดอะไร**
ตอนนี้เลขหยุดที่ 0 (ไม่ติดลบ) และ**ยังเล่นต่อได้ตามปกติ** — จงใจไม่คิดกฎแพ้ให้เอง
เตรียมทางไว้แล้ว: สัญญาณ `moves_exhausted` + `spend()` คืน `false` ตอนหมด
วันหลังจะทำเป็น "แพ้" หรือ "วางไม่ได้อีก" ก็เกาะได้โดยไม่ต้องรื้อ

**เทสต์:** เปิดฉาก 15 · วางลงที่นั่ง→14 · วางทับช่องที่มีคน→ยัง 14 · ปล่อยกลางที่ว่าง→ยัง 14 ·
ย้ายไปพักที่ช่องยืน→ยัง 14 · กลับมานั่งใหม่→13 · เรียก `spend()` รัว 30 ครั้ง→หยุดที่ 0
และสัญญาณหมดโควตายิงครั้งเดียว

---

## 2026-08-18 — เอฟเฟกต์คลิกวาดวงกลมเอง + ใช้ซ้ำแทนสร้างใหม่ทุกคลิก

**ที่ผู้ใช้ขอ:** เปลี่ยนเอฟเฟกต์คลิกเป็น `circle.gd` (วาดวงด้วย `_draw()`) + `ripple.gd`
(`play(at_position)` tween รัศมี แล้ว `hide()`)

**ของเดิมยืดทั้งโหนดด้วย `scale`** → เส้นขอบหนาขึ้นตามวงที่บาน
วาดเอง (`draw_arc`) ได้เส้นบางคงที่ และ tween ตัวเลขรัศมีตรง ๆ อ่านง่ายกว่า tween `scale`

🚨 **`hide()` แทน `queue_free()` เปลี่ยนสถาปัตยกรรมทั้งระบบ ไม่ใช่แค่บรรทัดเดียว**
สคริปต์ที่ผู้ใช้ให้มาจบด้วย `hide()` = ตัวเอฟเฟกต์ตั้งใจให้**ใช้ซ้ำ**
แต่ `click_effect.gd` เดิม `instantiate()` ใหม่ทุกคลิกแล้วพึ่ง `queue_free()` ของตัว ripple เป็นคนเก็บกวาด
เอามาชนกันตรง ๆ = **โหนดกองสะสมใน autoload ตลอดทั้งเกมโดยไม่มีใครลบ**
จึงเปลี่ยน `click_effect.gd` เป็นสระ (pool) หยิบใบที่ `visible == false` มาใช้ซ้ำ
เพดาน 12 ใบ · เต็มแล้ววนเอาใบเก่าสุดมาเริ่มใหม่ทับ (ตาไม่เห็นเพราะใบนั้นจางจนเกือบหายแล้ว)

⚠️ `play()` ต้อง `kill()` tween เก่าก่อน — ใบที่ถูกหยิบมาใช้ซ้ำตอนยังเล่นไม่จบ
จะมี tween สองตัวแย่งกันเขียน `radius` และ `modulate:a` วงจะกระตุกและบางใบค้างไม่ยอมจาง

⚠️ **`if self is Panel:` ทำให้ทั้งไฟล์ Parse Error**
สคริปต์ประกาศ `extends Control` ตัวตรวจชนิดตอนคอมไพล์เลยฟันธงว่า "เป็น Panel ไม่ได้"
ทั้งที่ตอนรันจริงเป็นได้ (Panel สืบทอดจาก Control) → เทียบด้วย `get_class() == "Panel"` แทน
(มีเช็คนี้เพราะโหนด `Circle` ในซีนยังเป็น `Panel` ที่มีสไตล์วงกลมทึบ ซึ่งจะบังวงที่วาดเอง)

⚠️ **ลืมแนบ `circle.gd` = error 2 บรรทัดต่อการคลิกหนึ่งครั้ง** ท่วมคอนโซลจนกลบ error จริง
→ เช็ค `"radius" in circle` ตอน `_ready()` แล้วปิดเอฟเฟกต์ทั้งตัวพร้อมเตือนครั้งเดียว

**เทสต์:** ซ่อนตอนเกิด · เล่นแล้วโผล่ตรงจุดคลิก รัศมีบาน alpha จาง ·
จบแล้วซ่อนตัวเองแต่ยังอยู่ในซีน · สั่งเล่นซ้ำกลางคันแล้วรีเซ็ตถูกต้อง ·
กดรัว 40 ครั้งกับซีนที่ยังไม่แนบสคริปต์ → สระไม่บานปลาย (1 ใบ) และไม่พ่น error ซ้ำ

---

## 2026-08-18 — บั๊ก "นั่งอยู่แต่เป็นท่ายืน"

**อาการ:** บางครั้งจัดให้นั่งแล้วตัวละครกลายเป็นท่ายืนบนเก้าอี้

**ไล่หาสาเหตุ — ตัดข้อสงสัยที่ผิดออกก่อน**
เช็ค `SpriteFrames` ของ NPC ทั้ง 10 ตัวก่อนเลย เผื่อบางตัวไม่มี `sit_happy`/`sit_angry`
แล้ว `set_seated_mood()` เตือนเงียบ ๆ ทิ้งท่าเดิมไว้ — **มีครบทุกตัว ไม่ใช่สาเหตุ**

**สาเหตุจริง: ทางที่ "วางไม่สำเร็จ"**
```
_process()  ระหว่างลาก → clear_seated_mood()          ## เปลี่ยนเป็นท่ายืน idle
_end_drag() วางไม่ลง   → _return_to_current_position() ## เด้งกลับเก้าอี้เดิม
                                                       ## ← ไม่มีใครคืนท่านั่งให้
```
ลากคนที่นั่งอยู่แล้วปล่อยไม่ลง (เล็งพลาด · ช่องปลายทางมีคนอยู่) → กลับไปนั่งเก้าอี้ตัวเดิม
แต่ยืนตัวตรง · ตรงกับคำว่า "บางครั้ง" เพราะเกิดเฉพาะตอนวางพลาด ไม่ใช่ทุกครั้งที่จัดที่นั่ง

**แก้ที่ `_return_to_current_position()`** ไม่ใช่ที่ `_end_drag()` —
เป็นจุดที่ "กลับไปที่เดิม" ทุกทางไหลมารวมกัน (วางพลาด · คลิกเฉย ๆ · ปล่อยกลางที่ว่าง)
แก้ที่นี่ที่เดียวครอบคลุมหมด ไม่ต้องไล่เติมทีละทาง

⚠️ **เช็ค `_mood_cleared_this_press` ก่อนคืน** — ทาง "คลิกดูบับเบิล" ไม่ได้ลากจริง
ท่านั่งยังอยู่ครบ สั่ง `play()` ซ้ำจะทำให้อนิเมชันกระตุกกลับไปเฟรมแรกโดยไม่มีเหตุผล
⚠️ ใช้ `check_seat()` ตัวเดียว ไม่ใช่ `check_all()` — วางไม่สำเร็จแปลว่าไม่มีอะไรบนกระดานเปลี่ยน

**เทสต์:** A นั่ง seat1 → ลากไปทับ seat2 ที่ B นั่งอยู่ → เด้งกลับ seat1 ท่า `sit_angry` (ไม่ใช่ idle) ·
ปล่อยกลางที่ว่าง → เด้งกลับพร้อมท่านั่ง · คลิกเฉย ๆ → ท่าไม่ถูกแตะเลย

---

## 2026-08-18 — ไฟบอกที่พักตอนยกตัวละครขึ้น

**ที่ผู้ใช้ขอ:** `ColorRect` ใน `AreaStand` แต่ละอันขึ้นเมื่อ**ตัวละครถูกยกขึ้น**
(และลบ `Line2D` ของช่องยืนออกแล้ว เหลือแต่ของ `Seat`)

**ที่นั่งกับที่ยืนใช้สีคนละจังหวะกันโดยตั้งใจ**

| | ที่นั่ง | ที่ยืน |
|---|---|---|
| สีโผล่เมื่อ | **เล็งช่องนั้นอยู่** (ทีละช่อง) | **ยกตัวละครขึ้น** (พร้อมกันทุกช่องที่วางได้) |

คนละหน้าที่กัน: ของที่นั่งตอบว่า *"ปล่อยตรงนี้แล้วจะลงช่องนี้"* ·
ของที่ยืนตอบว่า *"ตอนนี้มีที่พักว่างตรงไหนบ้าง"* — คำถามที่สองต้องตอบตั้งแต่ยกขึ้น
ไม่ใช่ตอนเล็งถึงจะรู้ ไม่งั้นผู้เล่นต้องกวาดเมาส์ไปทั่วกระดานเพื่อหาที่ว่าง

**โชว์เฉพาะช่องที่วางลงได้จริง** — ว่างอยู่ หรือเป็นช่องของตัวที่กำลังยกเอง (วางกลับที่เดิมได้)
โชว์ช่องที่คนอื่นยืนอยู่ด้วยจะเป็นคำโกหก: เล็งไปปล่อยแล้วเด้งกลับโดยไม่มีอะไรอธิบาย

🚨 **`set_dragged_npc(null)` ต้องอยู่ต้น `_end_drag()` ก่อน `return` ทุกทาง**
บทเรียนเดียวกับไฟของที่นั่งเมื่อชั่วโมงก่อน — ทางที่ "คลิกเฉย ๆ ไม่ได้ลาก" ออกจากฟังก์ชันก่อนถึงท้าย
(เทสต์ข้อนี้แยกไว้: คลิกเฉย ๆ แล้วปล่อย → ไฟดับหมด)

⚠️ **ไฟล์ซีนบนดิสก์ยังเป็นโครงเก่าตอนเขียน** (ยังมี `Line2D` ยังไม่มี `ColorRect`)
โค้ดจึงเทสต์กับซีนที่แปลงโครงให้เป็นแบบใหม่ตอนรัน และออกแบบให้**ไม่มี `ColorRect` ก็ไม่พัง**
แค่ `push_warning` บอกทีละช่อง — ยืนได้ตามปกติ แค่ไม่มีไฟบอก

**เทสต์:** ยังไม่ยก→0 ช่อง · ยก A (ช่องอื่นมีคนหมด)→1 ช่อง คือช่องของ A เอง ·
A ไปนั่ง→0 ช่อง · ยก B→2 ช่อง (ช่องที่ A ทิ้งไว้ + ช่องของ B) · คลิกเฉย ๆ แล้วปล่อย→0 ช่อง

---

## 2026-08-18 — บั๊ก "NPC ติดเมาส์" ในปริศนาจัดที่นั่ง

**อาการ:** ลาก NPC แล้วปล่อย แต่ตัวละครยังตามเมาส์ไปทั้งฉากโดยไม่ต้องกดอะไรอีก

**สาเหตุ: ดักปุ่มปล่อยผิดที่มาตั้งแต่ต้น**
`click.gd` ดักทั้งกดและปล่อยไว้ใน `_on_input_event` ซึ่งเป็นสัญญาณของ `Area2D`
ที่ **ยิงเฉพาะตอนเมาส์อยู่บนกรอบชนของมัน**

ตอนกดไม่มีปัญหา (ต้องกดโดนตัวอยู่แล้ว) แต่ตอนปล่อยเคอร์เซอร์อาจไม่ได้อยู่บนตัวละครแล้ว:
ลากเร็วจนภาพตามไม่ทัน · จับที่ขอบตัวจน `_drag_offset` ดันเคอร์เซอร์ออกนอกกรอบ ·
สลับหน้าต่างระหว่างลาก → **ไม่มีใครเรียก `_end_drag()` เลย** `_dragging` ค้าง true ตลอดกาล

**"ปล่อยเมาส์" เป็นเหตุการณ์ระดับทั้งจอ ไม่ใช่ของโหนดใดโหนดหนึ่ง** — ย้ายไปดักที่ `_input()`
ส่วนการกดยังอยู่ที่ `input_event` เหมือนเดิม เพราะการ**เริ่ม**ลากต้องเจาะจงว่ากดโดนตัวไหน
(ตัวที่ไม่ได้ลากอยู่ `return` ทิ้งตั้งแต่บรรทัดแรก ไม่ได้เสียอะไร)

**เพิ่มตาข่ายรับใน `_process`** — เช็ค `Input.is_mouse_button_pressed()` ทุกเฟรมระหว่างลาก
เผื่ออีเวนต์ปล่อยหายไปจริง ๆ (สลับหน้าต่างแล้วปล่อยเมาส์นอกเกม — เกมไม่ได้รับอีเวนต์นั้นเลย)
ไม่มีตัวนี้แล้วจะติดเมาส์ค้างจนต้องปิดเกมทิ้ง

**เทสต์:** เริ่มลาก → ยิงอีเวนต์ปล่อยที่มุมจอ (ไม่โดนตัวละคร) → หลุดจากโหมดลาก ·
เริ่มลาก → ไม่มีอีเวนต์ปล่อยเลย ปล่อยให้ `_process` เห็นว่าปุ่มไม่ได้ถูกกด → หลุดเช่นกัน

---

## 2026-08-18 — ช่องยืน (`Stand`) ในปริศนาจัดที่นั่ง

**ที่ผู้ใช้ขอ:** มาร์เกอร์ในโหนด `Stand` คือที่ที่ NPC กลับไปยืนได้ **ไม่บังคับตำแหน่ง**

**"ไม่บังคับตำแหน่ง" = ไม่ได้จองไว้ให้ใคร ไม่ใช่ยืนซ้อนกันได้**
มาร์เกอร์ทุกช่องในซีนชื่อ `MarkerNPC1` เหมือนกันหมด (ก๊อปกันมา) —
ถ้าอ่านชื่อโหนดมาจับคู่กับ NPC จะได้ระบบที่ผิดทั้งระบบ โค้ดจึงไม่แตะชื่อโหนดเลย
ส่วนการให้ยืนซ้อนกันได้จะเหลือ NPC ที่มองเห็นแค่ตัวเดียว จึงคุมเป็นหนึ่งช่องต่อหนึ่งคน

**สคริปต์เดียวที่โหนดแม่ ไม่ใช่ 10 อันที่ลูก**
ทางที่ตรงกับ `seat.gd` คือแนบสคริปต์ที่ `AreaStand` ทุกอันแล้วให้ลงทะเบียนตัวเอง
แต่แปลว่าผู้ใช้ต้องแนบมือ 10 ครั้ง (และทุกครั้งที่เพิ่มจุดยืนใหม่ต้องไม่ลืม)
เลือกให้ `StandZone` ที่โหนดแม่กวาดลูกเองแทน — **แนบครั้งเดียวจบ**
เพิ่ม/ลบจุดยืนในซีนแล้วโค้ดตามเองทันที

**`StandSlot` เป็น `RefCounted` ไม่ใช่ Node** — เป็นแค่ข้อมูล "ช่องนี้ใครยืนอยู่"
ไม่ต้องอยู่ใน tree ไม่ต้องมี `_process` · แต่**ตั้งชื่อเมธอดให้เหมือน `Seat` ทุกตัว**
(`is_empty` · `get_placement_position` · `place_npc` · `remove_npc`)
→ `click.gd` จัดการที่นั่งกับที่ยืนด้วยโค้ดรูปแบบเดียวกัน ไม่ต้องแยกสองสาย

🚨 **ต้องจองช่องที่ NPC ยืนอยู่ตั้งแต่เปิดฉาก** ไม่งั้นทุกช่องถือว่าว่างทั้งที่มีคนยืนอยู่จริง
ลากตัวอื่นมาปล่อยทับได้ตั้งแต่วินาทีแรก · จองแบบ `call_deferred` เพราะลำดับ `_ready`
ระหว่าง `Stand` กับ NPC ขึ้นกับลำดับโหนดในซีน ซึ่งคนจัดซีนสลับได้ตลอด ห้ามพึ่งพา

⚠️ **`_leave_current_spot()` ต้องคืนทั้งที่นั่งและช่องยืน ไม่ใช่เฉพาะอันที่เดาว่าถืออยู่**
ลืมคืนช่องยืนตอนไปนั่ง = ช่องนั้นถูกจองค้างตลอดเกม ไม่มีใครมายืนได้อีกและไม่มีอะไรฟ้อง

⚠️ **ลุกจากที่นั่งไปยืนก็ต้อง `SeatManager.check_all()`** — ไม่ใช่เช็คเฉพาะตอนมานั่ง
เงื่อนไขของเพื่อนบ้าน (`next_to_boy` ฯลฯ) เปลี่ยนผลตามที่นั่งที่เพิ่งว่างลง

**เจอบั๊กเก่าระหว่างยืนยันว่า "ที่พักไม่ตัดสินถูกผิด" — `check_all()` ตั้งได้แต่หน้าบึ้ง**
```gdscript
if seat.current_npc and not correct:
    seat.current_npc.set_seated_mood(correct)   ## correct เป็น false เสมอตรงนี้
```
เข้าเงื่อนไขได้เฉพาะตอนผิด → สั่งได้แค่ `sit_angry` ทางเดียว **ไม่มีทางกลับเป็น `sit_happy`**
เพื่อนบ้านย้ายออกแล้วคนที่เหลือกลายเป็นนั่งถูก หน้าก็ยังบึ้งค้างตลอดไป

เป็นบั๊กที่มีมาก่อน แต่**เพิ่งมีเส้นทางไปเจอบ่อย ๆ ตอนมีช่องยืน** —
เมื่อก่อนย้ายคนออกจากที่นั่งได้ทางเดียวคือเอาไปนั่งช่องอื่น (ซึ่งมักผิดอยู่ดี)
ตอนนี้ลากไปพักที่ที่ยืนได้ ทำให้เกิดสถานการณ์ "ที่นั่งเดิมกลายเป็นถูก" บ่อยขึ้นมาก
แก้เป็นอัปเดตอารมณ์ทุกกรณี แล้วแยก `all_correct = false` ออกมาเป็นเงื่อนไขของตัวเอง

**เทสต์กับซีนจริง (ผ่านทุกข้อ):** เจอช่องยืนครบ 10 · NPC จองครบ 10 ตั้งแต่เปิดฉาก ·
คนยืนที่พักไม่ถูกตัดสิน (ยังเป็น `idle` หลัง `check_all`) · หน้าบึ้งค้างกลับเป็นยิ้มได้แล้ว ·
ไปนั่งแล้วช่องยืนเดิมว่าง · **NPC2 มายืนช่องของ NPC1 ได้** (พิสูจน์ว่าไม่ผูกกับตัวใดตัวหนึ่ง) ·
ปล่อยทับช่องที่มีคนยืน → ไม่สำเร็จ กลับที่นั่งเดิม · ปล่อยกลางที่ว่าง → กลับที่เดิม

---

## 2026-08-18 — ช่องที่นั่งใน align_1: เส้นขอบตอนว่าง ติดสีตอนลากมาลอยเหนือ

**ที่ผู้ใช้ขอ:** `Line2D` ของช่องปรากฏเฉพาะตอนไม่มีตัวละคร และติดสีเมื่อตัวละครอยู่ในเขต

**ถามก่อนทำ 2 ข้อ** — คำว่า "Stand" ไม่มีอยู่เป็นชื่อโหนดในซีน และ "อยู่ในเขต" ตีความได้ 2 แบบ
ผู้ใช้ยืนยัน: *Stand = ช่องที่นั่ง `Seat/seatN`* · *อยู่ในเขต = **ลากมาลอยเหนือ ยังไม่ปล่อย***
(ถ้าเดาเองแล้วเดาผิด จะได้ระบบไฮไลต์ที่ติดตอนวางเสร็จแล้ว ซึ่งไร้ประโยชน์สิ้นเชิง —
ประโยชน์ทั้งหมดของไฟนี้คือบอก**ล่วงหน้า**ว่าปล่อยแล้วจะลงช่องไหน)

**กฎสุดท้ายหลังปรับ 3 รอบ:** เส้นขอบ **โชว์ตลอด** · สี่เหลี่ยมสี **เฉพาะตอนเล็ง**

| สถานะ | เส้นขอบ | สี่เหลี่ยมสี |
|---|---|---|
| ว่าง | ✅ | ❌ |
| ลากมาลอยเหนือ | ✅ | ✅ |
| มีคนนั่ง | ✅ | ❌ |

เส้นขอบเป็น**ผังของกระดาน** ต้องอ่านได้ทั้งกระดานเสมอ ไม่ใช่หายเป็นหย่อม ๆ ตามที่คนไปนั่ง
→ สคริปต์ไม่แตะ `visible` ของเส้นขอบเลย ตั้งไว้ยังไงในซีนก็เป็นแบบนั้น
⭐ **และเส้นขอบที่โชว์ตลอดกลายเป็นตัวรับไฮไลต์เขียว/แดงไปในตัว** (`modulate` ตกถึงลูกทุกตัว)
ปัญหา "ไฮไลต์ถูก/ผิดหายไป" ที่เกิดตอนกฎรอบก่อนจึงหายไปเองโดยไม่ต้องเพิ่มอะไร

**บันทึกไว้ว่ากฎเปลี่ยนมาแล้ว 3 รอบในชั่วโมงเดียว** — จึงคง `refresh_visual()` เป็นทางเข้าจุดเดียว
และยังเรียกจากตอนคนนั่ง/ลุกอยู่ ถึงตอนนี้จะไม่มีอะไรเปลี่ยนตามสถานะนั้นแล้วก็ตาม
รอบหน้าที่กฎเปลี่ยนอีกจะได้แก้ที่ฟังก์ชันเดียวจบ

**ประวัติการปรับ (เก็บไว้เพราะเหตุผลยังใช้ได้):**
รอบแรกคงสี่เหลี่ยมสีไว้ตอนมีคนนั่ง (เพื่อให้ไฮไลต์เขียว/แดงยังเห็น) — ผู้ใช้แก้ทีหลังว่าไม่เอา
หลักการที่เขาต้องการคือ *"สี่เหลี่ยมสีขึ้นเฉพาะตอนมีของลอยเหนือ เพื่อบอกว่ากำลังเลือกตำแหน่งนี้อยู่"*
= สีเป็น**ตัวชี้ตำแหน่งที่กำลังเล็ง** ไม่ใช่พื้นของช่อง
ติดค้างไว้ตอนมีคนนั่งจะเป็นสัญญาณสองความหมายในภาพเดียว แยกไม่ออกว่า "เล็งอยู่" หรือ "มีคนแล้ว"

⚠️ **ผลที่ตามมา — `set_highlight()` (เขียว/แดง) ไม่เห็นผลบนจออีกต่อไป**
ช่องที่มีคนนั่งไม่เหลือลูกที่มองเห็นให้ `modulate` ย้อม
ผลถูก/ผิดตอนนี้เหลือสื่อผ่านอนิเมชัน `sit_happy` / `sit_angry` ของ NPC อย่างเดียว
เก็บฟังก์ชันไว้ทั้งก้อน (SeatManager ยังเรียกอยู่) พร้อมคอมเมนต์บอกว่าทำไมถึงไม่เห็นผล —
วันหลังจะให้เส้นขอบโผล่กลับมาเป็นเขียว/แดงก็แก้ที่ `seat.gd` จุดเดียว

ℹ️ **เล็งช่องที่มีคนนั่งอยู่ก็ยังติดสี** — ตรงตามคำว่า "กำลังเลือกตำแหน่งนี้อยู่"
แต่ปล่อยจริงจะไม่ลง (`_end_drag` เช็ค `is_empty()`) · ยังไม่แก้เพราะการทำให้
`_find_nearby_seat()` ข้ามช่องที่มีคนจะเปลี่ยนกฎการวางไปด้วย (NPC จะเด้งไปลงช่องว่างข้าง ๆ แทน)

**สถานะ "ลากอยู่เหนือช่องไหน" เก็บที่ `SeatManager` ไม่ใช่ที่ตัวช่อง**
เป็นสถานะที่มีได้ทีละช่องเดียวทั้งกระดาน — เก็บที่แต่ละช่องจะต้องคอยไล่บอกช่องอื่นให้ดับเอง
ซึ่งเป็นงานของตัวกลางอยู่แล้ว · และอัปเดตเฉพาะช่องที่สถานะเปลี่ยนจริง ไม่ไล่รีเฟรชทั้งกระดานทุกเฟรม

⚠️ **ใช้ `_find_nearby_seat()` ตัวเดียวกับตอนปล่อยจริง**
ถ้าไฟใช้เกณฑ์หนึ่ง การวางใช้อีกเกณฑ์ จะมีมุมที่ไฟติดช่องหนึ่งแต่ NPC ลงอีกช่อง
ผู้เล่นจะโทษเกมทันทีว่า "วางไม่ลง" ทั้งที่ไฟบอกว่าลงได้

⚠️ **`_end_drag()` ต้องดับไฟก่อน `return` ทุกทาง** — ทางที่ "คลิกเฉย ๆ ไม่ได้ลาก"
ออกจากฟังก์ชันก่อนถึงท้าย ถ้าวางบรรทัดดับไฟไว้ล่างสุดจะมีช่องติดสีค้างบนจอ (เทสต์ข้อนี้แยกไว้)

**เทสต์:** ว่าง→เส้นขอบอย่างเดียว · ลอยเหนือ→ติดสี · ย้ายช่อง→ช่องเก่าดับ · ปล่อย→ดับ ·
นั่งแล้ว→เส้นขอบหายแต่สีอยู่ · ไฮไลต์เขียว/แดงยังทำงาน · เอา NPC ออก→กลับเป็นเส้นขอบ ·
คลิกเฉย ๆ แล้วปล่อย→ไม่มีไฟค้าง

---

## 2026-08-18 — "ฉาก wokeup ใช้กล่องคำพูดผิดใบ" (ที่จริงเป็นเรื่องการจัดวาง)

**อาการที่ผู้ใช้เจอ:** ปรับตำแหน่ง/ขนาด `DialogueBox` แล้ว แต่ตอนรัน `wokeup` กล่องยังเป็นแบบเก่า

**ไล่ตรวจก่อนแก้ — ไม่ใช่คนละใบจริง ๆ**
ทั้งโปรเจกต์มี `DialogueBox.tscn` ใบเดียว · `wokeup.gd` preload ไฟล์นั้นตรง ๆ ·
`under.tscn` ก็ instance ไฟล์เดียวกันโดยไม่มี override สักช่อง
รันเทียบแล้วได้ค่า offsets ตรงกันหมด ปุ่ม `Next` มี สี hover มี — ทุกอย่างชี้ว่าเป็นไฟล์เดียวกัน

**สาเหตุจริง: `Root/Box` เป็น `VBoxContainer`**
บทของ `wokeup` เป็นความคิดในใจ ไม่มีคีย์ `"name"` → โค้ดสั่ง `name_plate.visible = false`
→ **VBoxContainer ตัดช่องนั้นออกจากการจัดวางทั้งช่อง** กล่องข้อความเลื่อนขึ้นไปแทนที่ป้ายชื่อ

```
wokeup (ไม่มีชื่อผู้พูด) : TextPanel อยู่ที่ y = 780
under  (มีชื่อผู้พูด)    : TextPanel อยู่ที่ y = 847   ← ต่างกัน 67px
```

ผู้ใช้จัดตำแหน่งกล่องโดยดูจากฉากที่มีชื่อผู้พูด พอไปเจอฉากที่ไม่มีชื่อจึงดูเหมือน "กล่องคนละใบ"

**แก้: จางเป็น 0 แทนการซ่อน** (`modulate.a`) — ป้ายยังกินที่เท่าเดิม
ตำแหน่งกล่องข้อความจึงเท่ากันทุกบรรทัดไม่ว่าจะมีชื่อผู้พูดหรือไม่
ทางเลือกอื่นที่ไม่เอา: รื้อ `Box` จาก VBoxContainer เป็น Control ธรรมดาแล้ววางเอง —
ได้ผลเหมือนกันแต่ต้องแก้ซีน (ซึ่งเปิดค้างอยู่) และเสียการจัดวางอัตโนมัติที่ใช้ได้ดีอยู่แล้ว

⚠️ ตั้ง `mouse_filter = IGNORE` ให้ป้ายด้วย — ของที่จางเป็น 0 **ยังกินคลิกอยู่**
ปล่อยไว้จะกลายเป็นแถบใสขวางคลิกโดยไม่มีใครรู้ (เพิ่งเพิ่มระบบคลิกเลือกเป้าหมายไปเมื่อวาน)

**บทเรียน:** "ฉากนี้ใช้ resource ผิดใบ" เป็นสมมติฐานที่ต้องพิสูจน์ก่อนเสมอ
ถ้าเชื่อตามอาการแล้วไปไล่แก้เรื่อง preload/uid จะไม่มีวันเจอ เพราะต้นเหตุอยู่คนละเรื่องกันเลย

---

## 2026-08-18 — ปุ่มตัวเลือกเปลี่ยนสีตอนเมาส์ชี้

**ที่ผู้ใช้ขอ:** ชี้ปุ่มใน `ChoiceBox` → พื้นเป็น `#564aff` ตัวอักษรเป็นสีดำ

**ตั้งจากโค้ด ไม่ตั้งในซีน**
4 ปุ่ม × 3 สถานะ × (พื้นหลัง + สีอักษร) = 24 ช่องที่ต้องกรอกให้ตรงกันเป๊ะด้วยมือ
ลืมช่องเดียวก็มีปุ่มหนึ่งตัวสีเพี้ยนแบบที่ตาไม่จับจนกว่าจะบังเอิญเลือกข้อนั้น
(และซีนก็เปิดอยู่ตลอด แก้ `.tscn` จากข้างนอกไม่ได้)

**ก๊อป `normal` ของปุ่มนั้นมาแก้เฉพาะสีพื้น** ไม่ได้สร้าง `StyleBoxFlat` ใหม่เปล่า ๆ
ค่าอื่น (มุมโค้ง ขอบ ระยะขอบใน) จึงเหมือนตอนไม่ชี้เป๊ะ — วันหลังใส่มุมโค้งในซีน
ตอนชี้ก็โค้งตามเอง ไม่ต้องกลับมาแก้โค้ดอีก

⚠️ **ต้องทับ `pressed` กับ `hover_pressed` ด้วย ไม่ใช่แค่ `hover`**
ซีนทับไว้แค่ `normal` สถานะที่เหลือยังเป็นของธีมมาตรฐาน Godot
ทับแค่ `hover` แล้วตอนกดค้างปุ่มจะเด้งไปเป็นสีเทาหนึ่งจังหวะ

🚨 **จงใจไม่แตะ `focus` และ `font_focus_color`**
สถานะโฟกัสวาดทับบนพื้น `normal` ซึ่งเป็นสีดำ — เผลอเปลี่ยนสีอักษรตอนโฟกัสเป็นดำตามไปด้วย
ตัวหนังสือจะหายไปเลยตอนเลื่อนเลือกด้วยคีย์บอร์ด (`show_choices()` สั่ง `grab_focus()` ทุกครั้ง)

**เทสต์:** ทั้ง 4 ปุ่มได้ `hover`/`pressed` = `564affff` · สีอักษรตอนชี้/ตอนกด = ดำ ·
`font_focus_color` ยังเป็น `f2f2f2` (ขาว) ตามธีมเดิม · กดเลือกแล้วบทเดินต่อได้ปกติ

---

## 2026-08-18 — คลิกที่ NPC / กล่อง เพื่อมีปฏิสัมพันธ์ได้

**ที่ผู้ใช้ขอ:** ของที่กด E ได้ ต้องคลิกเอาก็ได้เหมือนกัน

**ทำที่ `player.gd` ที่เดียว ไม่แตะสคริปต์เป้าหมายเลย**
ทางที่ตรงไปตรงมากว่าคือให้แต่ละเป้าหมายต่อสัญญาณ `Area2D.input_event` ของตัวเอง
แต่จะกลายเป็นโค้ดชุดเดียวกันวางไว้สองที่ (NPC + กล่อง) แล้วของชิ้นที่สามก็ต้องก๊อปอีก
และ `input_event` ต้องเปิด `physics_object_picking` ของ viewport ก่อนถึงจะยิง — เป็นสวิตช์ซ่อนอีกตัว

เลือกยิงจุดเข้าไปในโลกฟิสิกส์เอง (`intersect_point`) แล้วเทียบกับ `nearby_targets`
→ ใช้รูปร่างจริงของแต่ละตัว ไม่ต้องเดาระยะ ไม่ต้องเพิ่มโหนดรับคลิก
และของชิ้นใหม่ที่ลงทะเบียนเป็นเป้าหมายจะคลิกได้เองทันทีโดยไม่ต้องแก้อะไร

⚠️ ต้องเปิดทั้ง `collide_with_areas` และ `collide_with_bodies` เพราะสองระบบวางรูปร่างไว้คนละที่
(NPC = `CharacterBody2D` โดนที่ตัวเอง · กล่อง = สไปรท์ที่มี `InteractArea` เป็นลูก โดนที่ลูก)
จึงเช็คทั้งตัวที่โดนและพ่อของมัน

**คลิกกับกด E ใช้ตัวตัดสินเดียวกัน (`_pick_best`)** — คลิกโดนของซ้อนกันสองชิ้น
จะได้กฎเดียวกับกด E (priority ก่อน แล้วค่อยระยะ) ไม่ใช่ "ตัวไหนอยู่บนสุดใน tree"

**ยังบังคับว่าต้องอยู่ในระยะ** — คลิกคุยข้ามครึ่งแมพไม่ได้ เพราะเป็นคนละกฎกับที่เกมใช้อยู่
ผ่อนกฎทีหลังง่ายกว่าเก็บกฎที่ปล่อยหลุดไปแล้ว

🚨 **พิกัดต้องมาจากตัวอีเวนต์ ไม่ใช่ `get_global_mouse_position()`**
รอบแรกใช้ตัวหลังแล้วคลิกไม่โดนอะไรเลย — มันคือ "เมาส์อยู่ตรงไหนตอนนี้"
ซึ่งไม่จำเป็นต้องเป็น "คลิกลงตรงไหน" (และใน headless ไม่อัปเดตเลย)
แปลงด้วย `get_canvas_transform().affine_inverse()` เพราะกล้องเลื่อน/ซูมได้

**ผลข้างเคียงที่ยอมรับไว้ก่อน:** `InteractArea` ของ NPC ตั้ง `collision_layer = 0`
จุดยิงจึงไม่เจอ → **คลิก NPC ต้องคลิกโดนตัวจริง** (24×14) ส่วนกล่องคลิกได้ทั้งวงรัศมี
พื้นที่คลิกสองอย่างเลยไม่เท่ากัน · อยากให้คลิก NPC ง่ายขึ้นต้องไปตั้ง layer ให้ `InteractArea` ในซีน

**เทสต์ด้วยคลิกจริง (ผ่านทุกข้อ):** คลิกวาวา → บทเริ่ม · คลิกพื้นว่าง → ไม่มีอะไร ·
คลิกกล่อง → หยิบขึ้นมา · คลิกซ้ำกล่องที่หยิบไปแล้ว → ไม่มีอะไร ·
คลิก NPC ระหว่างคุยอยู่ → บทไม่เด้งกลับไปเริ่มใหม่

---

## 2026-08-16 — ปุ่ม `Next` กดไม่ได้ (สองสาเหตุซ้อนกัน)

**อาการ:** เพิ่มปุ่มในซีนแล้ว วางถูกที่ ใส่รูปแล้ว แต่กดไม่มีอะไรเกิดขึ้น

**สาเหตุที่ 1 — `TextureButton` ไม่ใช่ลูกหลานของ `Button`**
โค้ดเดิมทำ `find_child(...) as Button` ซึ่งคืน **null** เพราะผู้ใช้ใช้ `TextureButton` (ปุ่มรูปล้วน)
ทั้งสองแยกสายกันใต้ `BaseButton` — `Button` คือปุ่มที่มีข้อความ+สไตล์
เปลี่ยนตัวแปรเป็น `BaseButton` ครอบทั้งสองสาย (สัญญาณ `pressed` กับ `focus_mode` อยู่ที่ชั้นนั้นอยู่แล้ว)

**สาเหตุที่ 2 — ปุ่มมีขนาด 0×0**
`offset_left == offset_right` และ `offset_top == offset_bottom` ในซีน = กรอบกว้าง 0 สูง 0
แถมติ๊ก `Ignore Texture Size` ซึ่งแปลว่า *"อย่าเอาขนาดรูปมาเป็นขนาดขั้นต่ำ"*
→ ต่อให้ใส่รูปแล้วก็ไม่มีพื้นที่ให้เมาส์ชน · เลือกยืดให้เท่าขนาดรูปจากโค้ดพร้อมเตือน
เพราะปุ่มขนาดศูนย์ไม่เคยเป็นสิ่งที่ตั้งใจ แต่ยังบอกให้ไปตั้งในซีนเพราะการจัดหน้าจอต้องเห็นผลในเอดิเตอร์

**บทเรียนเรื่องการเทสต์ headless:** ครั้งแรกคลิกไม่โดนทั้งที่โค้ดถูกแล้ว
เพราะ headless ไม่มีหน้าต่างจริง (`window = (0,0)`, screen transform สเกล 1/30)
พิกัดที่ `push_input()` ต้องเป็น**พิกัดจอ** ไม่ใช่พิกัดใน viewport
ต้องคูณด้วย `get_viewport().get_screen_transform()` ก่อนเสมอ
(เกือบสรุปผิดว่าปุ่มยังใช้ไม่ได้ ทั้งที่ปัญหาอยู่ที่ตัวเทสต์เอง)

**เทสต์ด้วยคลิกเมาส์จริง (ไม่ใช่ emit สัญญาณ):** ปุ่มที่พิกัด (1680,1014)-(1744,1078) —
คลิก 1 ข้ามการพิมพ์ (`visible_ratio` 1.0) · คลิก 2 เลื่อนไปบรรทัดสอง · คลิกต่อจนหมดบทแล้วปิดเอง

---

## 2026-08-16 — ผู้เล่นหันหน้าเข้าหา NPC ตอนเริ่มคุย

**ที่ผู้ใช้ขอ:** NPC หันมาหาผู้เล่นอยู่แล้ว ให้ผู้เล่นหันกลับไปด้วย

**`player.face_toward(target_position)`** — รับตำแหน่ง ไม่ใช่ตัวโหนด NPC
ผู้เล่นไม่ต้องรู้จัก `NPCBase` เลย ของอย่างอื่นในฉาก (กล่อง ประตู ป้าย) เรียกใช้ได้ทันที

**สแนปเข้าทิศตรงเสมอ ไม่ใช้ทิศเฉียง** — กฎเดียวกับ `NPCBase.face_player()`
(เทียบว่าแกนไหนห่างกว่า) ทั้งสองฝั่งใช้กฎเดียวกันจึงหันเข้าหากันจริง ไม่ใช่ต่างคนต่างหัน
ปล่อยเป็นเฉียงจะไปตกที่ `_side` อยู่ดีเพราะไม่มีชุด `idle_obliquely*` แต่ผลจะขึ้นกับ
มุมที่เดินเข้าไปแบบเดาไม่ได้

🚨 **ต้องตั้ง `animation_direction` ไม่ใช่แค่เรียก `play()` ครั้งเดียว**
`player._process()` เล่นท่ายืนจาก `animation_direction` **ทุกเฟรม**ระหว่างที่ `can_move = false`
เรียก `play()` เฉย ๆ จะถูกทับในเฟรมถัดไปแล้วหันกลับไปทางเดิมเหมือนไม่มีอะไรเกิดขึ้น

**เรียกจาก `interact()` ไม่ใช่ `_on_interact()`** — ตัวหลังมีไว้ให้ซีนลูก override
NPC ที่เขียนพฤติกรรมพิเศษทับโดยไม่เรียก `super()` จะไม่หลุดข้อนี้ไป

**เทสต์:** NPC 6 ทิศรอบตัว (ขวา/ซ้าย/บน/ล่าง + เฉียงที่ x ห่างกว่า + เฉียงที่ y ห่างกว่า)
ได้ท่าและ flip ถูกทุกกรณี

**เพิ่มตามมา: หันตอนหยิบกล่องด้วย** (ผู้ใช้ขอต่อทันที)
⚠️ ต้องตั้ง `carrying = true` **ก่อน** เรียก `face_toward()` — เมธอดนั้นเล่นท่ายืนทันทีในบรรทัดนั้น
สลับลำดับจะได้ท่ามือเปล่าหนึ่งเฟรมก่อนสลับเป็นชุดถือของ
เทสต์กล่อง 4 ทิศ ได้ `idle_side_box` (flip ถูกทั้งสองข้าง) · `idle_up_box` · `idle_down_box`

---

## 2026-08-16 — หยิบกล่องแล้วผู้เล่นสลับเป็นชุดอนิเมชัน `_box`

**ที่ผู้ใช้ขอ:** กด E ใส่กล่อง → ผู้เล่นเปลี่ยนเป็นชุดอนิเมชันถือกล่อง

**ตัดสินใจ 3 เรื่องที่คำขอไม่ได้ระบุ** (เลือกทางที่ย้อนกลับได้ทั้งหมด)

1. **กล่องหายจากพื้น** — ชุด `_box` ของผู้เล่น**วาดกล่องมาในภาพอยู่แล้ว**
   ปล่อยกล่องจริงไว้บนพื้นจะเห็นกล่องสองใบพร้อมกัน
   และไม่ได้ย้ายโหนดไปแปะที่มือผู้เล่นด้วยเหตุผลเดียวกัน
2. **ซ่อน ไม่ใช่ `queue_free()`** — ลบทิ้งแล้วระบบวางของ (ยังไม่มี) จะเอากลับมาไม่ได้
   ต้องไปหาตำแหน่ง/อนิเมชัน/สถานะปลดล็อกเดิมมาประกอบใหม่เองทั้งหมด
3. **ถอดออกจาก `nearby_targets` + ปิด `monitoring` ทันที** ไม่รอให้เดินออกนอกระยะ
   ไม่ถอด = กด E ซ้ำตรงนั้นจะเลือกกล่องที่ถืออยู่แล้ว แทนที่จะเป็น NPC ข้าง ๆ

**ชุด `_box` ในผู้เล่นครบทุกท่าอยู่แล้ว** (idle 3 + walk 5) จึงไม่มีท่าไหนตกกลับไปเป็นมือเปล่า
ฝั่งภาพไม่ต้องสั่งอะไรเพิ่มเลย — `player._process()` เล่นอนิเมชันใหม่ทุกเฟรมอยู่แล้ว
แค่ตั้ง `carrying = true` ก็เปลี่ยนตั้งแต่เฟรมถัดไป

**เจอระหว่างเทสต์: วาวายืนใกล้กล่องเกินไป**
กล่องอยู่ห่างวาวา ~49px แต่ `InteractArea` ของวาวารัศมี 40 + ตัวผู้เล่นอีกหน่อย
= ยืนตรงกล่องแล้ว**วาวาก็อยู่ในระยะด้วย** และ priority ของ NPC สูงกว่าเสมอ
→ กด E จะได้คุยกับวาวาซ้ำ ไม่ได้หยิบกล่อง (เทสต์เจอตรง ๆ ต้องย้ายวาวาออกก่อนถึงหยิบได้)
**ยังไม่แก้ — เป็นเรื่องการจัดวางในซีนที่ผู้ใช้ต้องเลือก** (ย้ายกล่อง / ลดรัศมีวาวา / อย่างอื่น)

**เทสต์ (ผ่านทุกข้อ):** คุยจบ → กล่องปลดล็อก → หยิบ → `carrying=true` ·
กล่อง `visible=false` · กดไม่ได้อีก · หายจาก `nearby_targets` · เดินกลับเข้ามาไม่ลงทะเบียนซ้ำ ·
เดินลง = `walk_down_box` · ยืนนิ่ง = `idle_down_box`

🚨 **ยังไม่มีระบบวางลง และ `carrying` หายเมื่อเปลี่ยนฉาก** (ผู้เล่นถูก instance ใหม่)
ยกกล่องข้ามฉากไปห้องรับรองยังทำไม่ได้จนกว่าจะผูกกับ `GameState`

---

## 2026-08-16 — คุยจบแล้วเปลี่ยนเป็นบทอื่น + ข้อความสุ่ม

**ที่ผู้ใช้ขอ:** บทที่คุยจบแล้วต้องไม่เล่นซ้ำ กลับมาคุยใหม่ให้ขึ้นบทอื่นแทน
วาวาสุ่มพูด *"มีอะไรสงสัยไหม?"* / *"อยากให้ช่วยอะไรเหรอ?"* ตัวเลือกเดียวฝั่งซ้าย *"ไม่มีค่ะ"*
เลือกแล้วน้ำฟ้าตอบ *"ไม่มีอะไรค่ะ"* — เป็นของชั่วคราวจนกว่าจะตั้งเป็นอย่างอื่น

**`dialogue_id_after` แทนที่จะใช้ `talk_once`**
`talk_once` ที่เตรียมไว้เดิมคือ *"พูดจบแล้วเงียบ"* ซึ่งไม่ใช่สิ่งที่ต้องการแล้ว
สิ่งที่ต้องการคือ *"พูดจบแล้วเปลี่ยนเรื่อง"* — คนละความตั้งใจ จึงแยกเป็นคนละช่อง
ตั้ง `dialogue_id_after` แล้ว `talk_once` ถูกทับ (คุยได้ต่อ แค่เปลี่ยนบท) เขียนกำกับไว้ทั้งสองที่

🚨 **`_main_dialogue_used` ต้องแยกจาก `_talked` — จังหวะต่างกัน**
`_talked` ถูกตั้งตั้งแต่ *เริ่ม* คุยใน `interact()` ซึ่งเกิด **ก่อน** `_get_dialogue_lines()`
เอามาตัดสินบทตรง ๆ จะกลายเป็น **คุยครั้งแรกก็ได้บทสำรองไปเลย** บทหลักไม่มีวันถูกเล่น
(จุดที่พลาดง่ายมากเพราะทั้งสองตัวแปรฟังดูเหมือนกัน)
และตั้งธงเฉพาะตอนยืนยันแล้วว่าบทหลักมีอยู่จริงใน `DialogueData` —
พิมพ์คีย์ผิดแล้วนับว่าคุยไปแล้วคือบทหลักหายถาวรทั้งที่ยังไม่เคยเล่นสักครั้ง

**`random_text` เป็นคีย์ใหม่ ไม่ยอมให้ `"text"` เป็น Array ได้ด้วย**
ทางที่พิมพ์สั้นกว่าคือให้ `"text"` รับได้ทั้ง String และ Array แต่บทมีเป็นร้อยบรรทัด
การกวาดตาอ่านต้องบอกได้ทันทีว่าบรรทัดไหนสุ่ม — คีย์ที่ชื่อตรงตัวทำหน้าที่นั้นได้ดีกว่า

**สุ่มแบบไม่ซ้ำประโยคเดิมติดกัน**
สุ่มล้วน ๆ กับตัวเลือก 2 ข้อ = โอกาส 50% ที่จะได้ประโยคเดิม ผู้เล่นที่กด E รัว ๆ เจอแน่นอน
แล้วจะดูเหมือนระบบสุ่มพัง · จำประโยคล่าสุดไว้แล้วเลื่อนไปอีกอันถ้าสุ่มได้ตัวเดิม

**ตามมาทันที: "กดคุยอีกรอบแล้วไม่มีอะไรเกิดขึ้น"**
สาเหตุคือช่อง `Dialogue Id After` ในซีนถูกพิมพ์เป็น `"wawa_idle "` — **มีช่องว่างปิดท้าย**
คีย์ไม่ตรง → `_get_dialogue_lines()` คืน array ว่าง → `_on_interact()` ออกกลางคัน → เงียบสนิท
มี `push_warning` บอกอยู่ แต่ไม่มีใครนั่งดูคอนโซลตอนเล่นเกม

แก้ที่ต้นเหตุ: คีย์บททุกช่องผ่าน `_clean_key()` ที่ `strip_edges()` ให้ + เตือนครั้งเดียวต่อค่า
**เลือกตัดให้แล้วเตือน ไม่ใช่ปล่อยพัง** — คนพิมพ์ตั้งใจพิมพ์ `wawa_idle` อยู่แล้ว
ช่องว่างเป็นอุบัติเหตุจากการพิมพ์/ก๊อป ไม่เคยเป็นสิ่งที่ตั้งใจ (แนวเดียวกับที่ `_resolve_anim_name()`
ใน `dialogue_system.gd` ทำกับชื่ออนิเมชันมาตั้งแต่แรก)
⚠️ ต้องแก้จุดเทียบธง `key != dialogue_id_after` ให้เทียบกับค่าที่ตัดช่องว่างแล้วด้วย
ไม่งั้นธง `_main_dialogue_used` จะถูกตั้งผิดทุกครั้งที่ช่องนั้นมีช่องว่างติดมา

**เทสต์ (ผ่านทุกข้อ):** รอบ 1 ได้บทหลัก · รอบ 2-7 ได้บทคุยซ้ำสลับประโยคไม่ซ้ำติดกันเลย ·
เจอครบทั้ง 2 ประโยค · ปุ่มโชว์ใบเดียวฝั่งซ้าย (`[true, false]`) ข้อความ "ไม่มีค่ะ" ·
เลือกแล้วน้ำฟ้าพูด "ไม่มีอะไรค่ะ" · `talk_once` ติ๊กไว้ก็ยังคุยได้ (ถูกทับตามที่ออกแบบ)

---

## 2026-08-16 — ผู้เล่นเป็นคนเลือกเป้าหมายตอนกด E (ทางเลือก ข)

**ที่ผู้ใช้ขอ:** กด E แล้วเลือกเป้าหมายจาก `nearby_targets` — `priority` สูงกว่าชนะก่อน
เสมอกันเอาตัวที่ใกล้ที่สุด แล้วเรียก `.interact()`

**ชื่อไฟล์ในคำขอกับในโปรเจกต์ไม่ตรงกัน** — `Npc.gd` = `npc/npc_base.gd` ·
`Item.gd` = `scripts/interact_box.gd` · `nearby_targets` ยังไม่มี ต้องสร้างที่ `player.gd`
(ทำตามเจตนา ไม่ได้สร้างไฟล์ใหม่ตามชื่อ — จะกลายเป็นระบบซ้อนระบบเดิมที่ทำงานอยู่แล้ว)

**นี่คือ "ทางเลือก ข" ที่คอมเมนต์ใน `npc_base.gd` เขียนค้างไว้ตั้งแต่ต้น**
เดิม NPC แต่ละตัวดัก E เองใน `_unhandled_input` แล้วใครได้คิวก่อนก็ชนะ ซึ่ง
**ขึ้นกับลำดับใน tree ล้วน ๆ** — ยืนคาบเกี่ยววาวากับกล่องแล้วได้ตัวไหนก็เดาไม่ได้
และไม่มีที่ให้บอกว่า "คนสำคัญกว่าของ" · ตอนนี้ย้ายการตัดสินใจมาไว้ที่ผู้เล่นจุดเดียว

🚨 **ต้องถอด `_unhandled_input` ออกจาก `NPCBase` พร้อมกัน** ไม่งั้นกด E ครั้งเดียวทำงานสองทาง
(เขียนคำเตือนไว้ตรงที่ที่โค้ดเดิมเคยอยู่ เพราะเป็นจุดที่คนอ่านโค้ดจะเผลอเติมกลับ)

**เป้าหมายลงทะเบียนตัวเอง ไม่ได้เพิ่ม Area2D ให้ผู้เล่น**
ทั้ง NPC และกล่องมี `InteractArea` ที่จับผู้เล่นอยู่แล้ว ให้มันเรียก `add_nearby_target()`
จึงไม่ต้องแตะซีนไหนเลย (ทางเลือกอื่นคือให้ผู้เล่นมี Area2D ของตัวเองไปคลำหา
ซึ่งต้องแก้ `player.tscn` + ตั้ง layer/mask ใหม่ทั้งชุด แลกมาแค่การรวมโค้ดไว้ที่เดียว)

**เช็คด้วย `has_method` ไม่ผูกกับ `class_name`**
ของในฉากที่อยากให้กดได้แค่มี `interact()` ก็พอ — ไม่ต้องสืบทอดคลาสกลางที่ยังไม่มี
และไม่ต้องกลับมาแก้ `player.gd` ทุกครั้งที่เพิ่มของชนิดใหม่
`can_interact()` มีก็เช็คให้ ไม่มีก็ถือว่ากดได้ · `priority` ไม่มีก็เป็น 0 ตามที่ผู้ใช้กำหนด

**ลงทะเบียนแม้ `can_interact()` เป็น false แล้วค่อยกรองตอนกด**
สถานะกดได้/ไม่ได้เปลี่ยนระหว่างที่ผู้เล่นยืนอยู่ในระยะได้จริง — คุยกับวาวาจบตรงข้างกล่องเลย
(กล่องปลดล็อกกลางคัน) หรือ `talk_once` ที่เปลี่ยนหลังคุยจบ
กรองตั้งแต่ตอนลงทะเบียนแล้วรายการจะค้างสถานะเก่าจนกว่าจะเดินออกแล้วเข้าใหม่

**กินอีเวนต์เฉพาะตอนมีเป้าหมายจริง** — `stair.gd` ก็ใช้ E เหมือนกัน
เรียก `set_input_as_handled()` ทุกครั้งที่กดจะไปบังปุ่มขึ้นบันไดแบบเงียบ ๆ

**`distance_squared_to` ไม่ใช่ `distance_to`** — ผลการเรียงเหมือนกันเป๊ะ ไม่ต้องถอดรากที่สอง

**เทสต์ (ผ่านครบ 8 ข้อ):** วาวา priority 1 ชนะกล่องที่อยู่ใกล้กว่า · วาวาออกไปแล้วได้กล่องใบใกล้สุด ·
กล่องที่ล็อกถูกข้าม · ล็อกหมดได้ null · กด E จริงผ่าน `Input.parse_input_event` แล้วบทของวาวาเริ่ม ·
กด E ระหว่างคุยไม่เด้งบทใหม่ (`can_move=false`) · `box.interact()` ยิงสัญญาณ (ลบไฟล์เทสต์แล้ว)

---

## 2026-08-16 — กล่องในห้องใต้ตึกตอบสนองผู้เล่น (หลังคุยกับวาวาจบ)

**ที่ผู้ใช้ขอ:** โหนด `Box` ใน `under_ch_1.tscn` — เข้า `InteractArea` แล้วเปลี่ยนท่า
แต่เริ่มใช้งานได้เฉพาะหลังคุยกับวาวาจบ

**ตอนแรกออกแบบเป็น "เลขเฟรม" แล้วเปลี่ยนเป็น "ชื่ออนิเมชัน" กลางทาง**
`Box` ในไฟล์ตอนนั้นเป็น `Sprite2D` ที่เปิด `region_enabled` — โหมดนั้น **Godot ไม่สนช่อง `frame` เลย**
สั่งเปลี่ยนได้ ค่าเปลี่ยนจริง แต่ภาพนิ่งสนิทและไม่มี error สักบรรทัด (กับดักที่หาสาเหตุยากมาก)
ระหว่างคุยกันผู้ใช้เปลี่ยนโหนดเป็น `AnimatedSprite2D` + 2 อนิเมชัน `normal` / `nearby` เอง
→ เขียนใหม่ให้สลับด้วย**ชื่อ** ซึ่งดีกว่าเดิม: ชื่อบอกความหมายในตัว (`nearby` ไม่ใช่ `frame = 1`)
และวันหลังอยากให้กล่องขยับจริง ๆ แค่เพิ่มเฟรมใน `nearby` ไม่ต้องแตะสคริปต์

**ปลดล็อก: ดัก `interacted` ของ NPC แล้วค่อย `await dialogue_finished`**
ทางที่ง่ายกว่าคือดัก `dialogue_finished` ตรง ๆ ตั้งแต่ `_ready` — **แต่ผิด**
สัญญาณนั้นเป็นของกล่องคำพูดกลาง ยิงตอนบทของใครจบก็ได้
ห้องนี้วันหลังมี NPC เพิ่มอีกตัว คุยกับใครก็ได้แล้วกล่องจะปลดล็อกไปด้วย
ผูกกับ `interacted` ของโหนดที่ระบุจึงเจาะจงถูกคนแน่นอน

**ชี้ที่โหนด NPC ไม่ใช่ชื่อบท** (`unlock_after_npc: NodePath`)
กล่องสนแค่ "คุยจบหรือยัง" ไม่ได้สนว่าบทชื่ออะไร — เปลี่ยน `dialogue_id_override` ของวาวาแล้ว
ไม่ต้องกลับมาแก้กล่อง (ถ้าเก็บเป็น `dialogue_id` จะกลายเป็นข้อมูลซ้ำสองที่ที่ต้องแก้พร้อมกัน)

**หา NPC ไม่เจอ → ล็อกไว้ ไม่ปล่อยผ่าน**
ปล่อยผ่าน = กล่องทำงานผิดลำดับเนื้อเรื่องแบบเงียบ ๆ ซึ่งเจอยากกว่ากล่องที่ไม่ขยับเลย
ใช้ `push_error` (ไม่ใช่ warning) เพราะสถานะนี้คือฟีเจอร์ตายทั้งอัน ต้องเห็นชัดในคอนโซล

**`_player_inside` อัปเดตตลอดเวลาแม้ยังล็อกอยู่**
ผู้เล่นยืนแช่ข้างกล่องแล้วคุยกับวาวาจบตรงนั้นเลยเป็นทางเดินที่เป็นธรรมชาติที่สุด
ถ้าเช็คแค่ตอน `body_entered` กล่องจะไม่เปลี่ยนท่าจนกว่าจะเดินออกแล้วเดินกลับเข้ามาใหม่
(เทสต์ข้อนี้โดยเฉพาะ — ผ่าน)

**เทสต์:** ซีนชั่วคราวใช้วาวาตัวจริง + `DialogueBox.tscn` ตัวจริง + ผู้เล่นจำลอง layer 1
ผลครบทุกข้อ: ก่อนคุย `normal` · ระหว่างคุย `normal` · คุยจบขณะยืนแช่ → `nearby` ทันที ·
เดินออก → `normal` · เดินเข้าใหม่ → `nearby` (ลบไฟล์เทสต์แล้ว)

---

## 2026-08-16 — ปุ่ม `Next` ในกล่องคำพูด (กดต่อด้วยเมาส์ได้)

**ที่ผู้ใช้ขอ:** เพิ่มปุ่มในซีน `DialogueBox` กดแล้วไปบทพูดถัดไป หมดบทแล้วกดคือออกจากบท

**หาโหนดด้วยชื่อ ไม่ใช่ path ตายตัว** — `find_child("Next", true, false)`
ปุ่มไปต่อเป็นของที่ย้ายที่บ่อยตอนจัดหน้าจอ (มุมขวาล่างของ `TextPanel` / ลอยนอกกล่อง / ใต้ `Root`)
ผูก `@onready get_node("Root/Box/Next")` ไว้ = ลากปุ่มย้ายที่ใน editor เมื่อไหร่ปุ่มก็ตายเงียบ ๆ
ทางเลือกอื่นที่ไม่เอา: ใส่ `@export var next_button: Button` ให้ลากเอง — ต้องกลับไปตั้งใน Inspector
ทุกครั้งที่สร้างกล่องคำพูดใหม่ และลืมแล้วไม่มีอะไรเตือน (ตอนนี้ไม่เจอปุ่ม = `push_warning` บอกชัด)
ตั้ง `owned = false` ด้วย เผื่อวันหลังปุ่มไปอยู่ในซีนย่อยที่ instance เข้ามา

**ปุ่มกับ E ต้องเดินทางเดียวกัน — แยกเป็น `_step()`**
เดิมตรรกะ "ยังพิมพ์ไม่จบ → ข้ามการพิมพ์ / พิมพ์จบแล้ว → `_advance()`" ฝังอยู่ใน `_input()`
ถ้าก๊อปไปไว้ที่ `_on_next_pressed()` อีกชุด วันหลังแก้ข้างหนึ่งแล้วลืมอีกข้าง
พฤติกรรมของเมาส์กับคีย์บอร์ดจะเพี้ยนคนละแบบโดยไม่มีใครรู้ (บั๊กแบบที่หายากที่สุด)
จึงดึงออกมาเป็น `_step()` แล้วให้ทั้งสองทางเรียกตัวเดียวกัน
→ "บรรทัดสุดท้ายกดแล้วออก" ได้มาฟรี เพราะ `_advance()` เช็ค `index >= size` แล้วเรียก `end_dialogue()` อยู่แล้ว

**ปิดโฟกัสคีย์บอร์ดของปุ่ม (`FOCUS_NONE`)**
ปุ่มที่ถือโฟกัสอยู่จะกิน Enter/Space (`ui_accept` ของ Godot) → เกมมีปุ่มเลื่อนบทที่ไม่ได้ประกาศไว้
และตอนถึงคิวเลือกคำตอบ `choice_buttons[0].grab_focus()` จะแย่งโฟกัสกันเอง

**ซ่อนปุ่มระหว่างรอเลือกคำตอบ**
ฝั่งคีย์บอร์ดกัน `_waiting` ไว้แล้วที่ `_input()` แต่ฝั่งเมาส์ยังกดปุ่มหนีคำถามได้
ซ่อนที่ `show_choices()` แล้วเอากลับตอนเลือกเสร็จ + `start_dialogue()` เปิดกลับทุกครั้ง
(เผื่อบทก่อนหน้าถูกสั่งจบตอนปุ่มยังซ่อนอยู่) · `_on_next_pressed()` ยังเช็ค `_waiting` ซ้ำอีกชั้น

**เทสต์:** ซีนชั่วคราวจำลองการกดปุ่ม 8 สถานการณ์ — ผ่านหมด ลบทิ้งแล้ว
(กดตอนพิมพ์ → ขึ้นเต็มไม่เลื่อนบรรทัด · กดหนีคำถามไม่ได้ · แตกบทย่อยแล้วปุ่มกลับมา ·
กดรัวจนหมดบท → `is_talking=false` · กดตอนบทจบแล้วไม่มีอะไรเกิดขึ้น)

⚠️ **ตอนแก้ ไฟล์ `DialogueBox.tscn` บนดิสก์ยังไม่มีโหนด `Next`** (ผู้ใช้ยังไม่เซฟจาก editor)
โค้ดจึงถูกทดสอบกับปุ่มที่สร้างจากโค้ดแทน — ต้องเซฟซีนแล้วลองในเกมจริงอีกที

---

## 2026-08-15 — กด E ปิดบทแล้วบทเด้งกลับไปเริ่มใหม่ (+ `talk_once`)

**อาการ:** คุยจบ กด E ปิด → บทเด้งกลับไปบรรทัดแรกทันที เหมือนกดเริ่มคุยใหม่เอง

**สาเหตุ — เรื่องลำดับล้วน ๆ**
`dialogue_system._input()` กิน E ไว้เลื่อนบรรทัด **แต่ไม่เคย `set_input_as_handled()`**
ปกติไม่มีปัญหาเพราะ `NPCBase._unhandled_input()` เช็ค `is_talking` ก่อนเริ่มบท
แต่ในจังหวะที่กดครั้งสุดท้าย ทุกอย่างเกิดในอีเวนต์เดียวกัน:

```
กด E → dialogue_system._input() → _advance() → end_dialogue() → is_talking = false
     → (อีเวนต์เดิมไหลต่อ) → NPCBase._unhandled_input() → เห็นว่า "ไม่ได้คุยอยู่" → interact()
```

การเช็ค `is_talking` จึงกันไม่ได้ เพราะมันเพิ่งกลายเป็น false ไปเสี้ยววินาทีก่อนในสายเดียวกัน
แก้ที่ต้นเหตุ: ให้ `dialogue_system._input()` mark handled ทุกครั้งที่กิน E
(เทสต์ยืนยันก่อนแก้: `is_talking=true index=0` ข้อความกลับเป็นบรรทัดแรกจริง)

**`talk_once` — คุยได้ครั้งเดียว (ที่ผู้ใช้ขอ)**
บทวาวาเป็น "เหตุการณ์" ไม่ใช่คำทักทาย คุยซ้ำได้จะกลายเป็นฝากกล่องซ้ำ ๆ ทั้งที่เนื้อเรื่องเดินไปแล้ว

ตั้งที่ **ตัวโหนด** ไม่ใช่ใน `NPCData` ด้วยเหตุผลเดียวกับ `dialogue_id_override` —
`wawa.tres` ใช้ร่วมกับ `level_2.tscn` ที่วาวาต้องคุยซ้ำได้ตามปกติ

ตั้งธง `_talked` ตอน **เริ่มคุย** ไม่ใช่ตอนบทจบ: ผู้เล่นที่ออกกลางคันก็ถือว่าได้คุยแล้ว
และเป็นการกันบทเด้งซ้ำในเฟรมเดียวกันอีกชั้น (ต่อให้ลืม mark handled ไปในอนาคต)

รวมทุกอย่างไว้ใน `can_interact()` จุดเดียว จึงคุม **ทั้งการกด E และการโผล่ของบับเบิล** พร้อมกัน
ผู้เล่นไม่เห็นสัญญาณชวนคุยค้างอยู่บน NPC ที่คุยไม่ได้แล้ว

⚠️ **จำแค่ภายในฉาก** — ออกไปแล้วกลับมา NPC ถูกสร้างใหม่ ค่ารีเซ็ต
จะให้จำถาวรต้องมีระบบธงเนื้อเรื่องใน `GameState` ซึ่งเป็นการออกแบบอีกก้อน ยังไม่ทำ

---

## 2026-08-15 — บทแตกตามตัวเลือก (`{"choices": [...]}` ในบทพูด)

ต่อจากกล่องเลือกคำตอบเมื่อวาน — ตอนนี้ **เขียนตัวเลือกลงในบทได้ตรง ๆ** ไม่ต้องเรียก API เอง
ใช้จริงที่บท `wawa_under_ch1` ตอนวาวาถามว่ากินข้าวรึยัง (ตอบ "กินแล้ว" / "ยังเลย" แล้วบทแยกสาย)

**เลือกรูปแบบเดียวกับระบบแชท** (`{"text": ..., "then": [...]}`) จาก 3 ทางที่ชั่งไว้
(ต่อ `then` · กระโดดไปคีย์บทอื่น · ตั้งธงในเนื้อเรื่อง) เพราะ `chat_group.gd` ใช้แบบนี้อยู่แล้ว
**คนเขียนบทจำแบบเดียวใช้ได้ทั้งสองระบบ** — ต่อให้ทางอื่นจะยืดหยุ่นกว่าก็ไม่คุ้มกับการมีสองภาษาในเกมเดียว

**ทำงานยังไง:** `show_line()` เจอคีย์ `choices` → ไม่พิมพ์อะไร (ค้างคำถามไว้บนจอ) → รอกด
→ `insert()` บทของข้อที่เลือกเข้าคิวต่อจากบรรทัดนั้น → เดินต่อตามปกติ
บทย่อยจึงเป็นบทธรรมดา ใช้ `anim` / `hold` ได้ครบเหมือนบรรทัดอื่น

### 🚨 บั๊กที่เกือบหลุด: `insert()` ลงบทต้นฉบับ

`start_dialogue()` เดิมเขียน `dialogue_queue = lines` ซึ่งเป็น**การอ้างถึง Array ตัวเดียวกัน**
กับที่อยู่ใน `DialogueData` พอบทมีตัวเลือกแล้ว `insert()` = แก้บทต้นฉบับถาวร
คุยรอบสองจะเจอบทย่อยของรอบแรกค้างอยู่ **บทบวมขึ้นทุกครั้งที่เลือก**

แก้เป็น `lines.duplicate()` (ตื้นพอ เพราะเราไม่แก้ Dictionary ข้างใน แค่เพิ่ม/ลดรายการ)
เทสต์ยืนยัน: เล่นครบสองสายแล้วบทต้นฉบับยังเป็น 6 บรรทัดเท่าเดิม

### จุดอื่นที่ต้องแก้ตามกันเพราะมีตัวเลือกแล้ว

**1. `_apply_portrait_cast` ต้องไล่เข้า `then` ด้วย**
มันไล่หาคีย์ `"name"` ทั้งบทก่อนเริ่มเล่นเพื่อตัดสินว่าจะเอารูปใครขึ้นจอ
ถ้าดูแค่บรรทัดชั้นบนสุด ตัวละครที่โผล่**เฉพาะสายใดสายหนึ่ง**จะถูกซ่อนตั้งแต่ต้น
แล้วพอถึงคิวจริงก็ไม่โผล่ เพราะ `visible` ถูกปิดไปแล้ว (`update_character_focus` ปรับแค่สี/ขนาด)
แยกเป็น `_collect_speakers()` แบบเรียกซ้ำตัวเอง

**2. จบบทระหว่างรอเลือก → `emit(-1)` ปลด `await`**
เดิมแค่เตือนแล้วปิดกล่อง ตัว `await` ยังค้างตลอดไป (coroutine ค้าง)
ตอนนี้ยิง `choice_made.emit(-1)` ปลดให้ แต่ **ต้องยิงหลัง `is_talking = false` เท่านั้น**
เพราะสัญญาณทำงานทันที ตัวที่รออยู่จะเดินต่อในบรรทัดนั้นเลย ยิงก่อนจะสั่งบทเดินต่อทั้งที่เพิ่งจบไป

**3. ปุ่มกับคำพูดจริงเป็นคนละข้อความ**
ตั้งใจไม่ให้ระบบเอาข้อความบนปุ่มมาพูดเองอัตโนมัติ — ปุ่มมีที่จำกัด คำพูดจริงมักยาวกว่า
อยากให้คำตอบขึ้นเป็นบทพูดด้วยก็ใส่บรรทัดของน้ำฟ้าไว้ต้น `then` เอง (บทนี้ใส่ทั้งสองสาย)

**ปิดบทเอง** ใช้คีย์ `hold` ที่มีอยู่แล้ว วางที่บรรทัดสุดท้ายของแต่ละสาย (ตั้งไว้ 5 วินาที)

### 🚨 กด E ข้ามการพิมพ์แล้วเวลาปิดเองหายไป

เวลาของ `hold` ถูก**ต่อท้าย tween ตัวเดียวกับการพิมพ์** (พิมพ์ → รอ → ไปต่อ)
การกด E ข้ามการพิมพ์คือ `kill()` ทั้งสาย — เวลาที่ตั้งไว้จึงหายไปพร้อมกัน
ผลคือผู้เล่นที่กดข้ามแล้วปล่อยมือจะ **ค้างอยู่บรรทัดสุดท้ายตลอดไป** ทั้งที่บทควรปิดเอง

เพิ่ม `_restart_hold_if_needed()` ตั้งเวลาใหม่หลังข้ามการพิมพ์
(ไม่ย้าย `hold` ออกไปเป็น Timer แยกเพราะ tween เดิมให้ลำดับ "พิมพ์เสร็จ → เริ่มนับ" มาฟรีอยู่แล้ว
แยกออกมาจะต้องมาจัดการเองว่านับตอนไหน แล้วมีสองแหล่งที่สั่งบทเดินต่อ)

เทสต์ครบ 3 ทางออก: ปล่อยไว้ปิดที่ 5.5 วิ · กดข้ามการพิมพ์แล้วปล่อยปิดที่ 5.0 วิ · กดหลังพิมพ์จบออกทันที

---

## 2026-08-14 — กล่องเลือกคำตอบในกล่องคำพูด (`show_choices`)

เพิ่ม `ChoiceBox` + `show_choices()` ลง `dialogue_system.gd` (โครงโค้ดมาจากผู้ใช้ ปรับ 4 จุด)

**1. พาธไม่ตรงซีน** — โค้ดที่ร่างไว้ใช้ `$Root/ChoiceBox` แต่ในซีนจริง `ChoiceBox` เป็นลูกของ
`DialogueBox` (CanvasLayer) โดยตรง ไม่ได้อยู่ใต้ `Root` เหมือน NamePlate/TextPanel/Portraits
ถ้าใส่ตามร่างจะได้ `null` แล้ว crash ที่ `choice_box.visible` ตั้งแต่ `_ready()`

**2. ลำดับปุ่มไม่ตรงกับชื่อโหนด — จงใจ**
ซีนตั้งชื่อ `Choice0,1` ไว้ในกล่องซ้าย และ `Choice2,3` ในกล่องขวา
แต่ถ้าเรียง index ตามชื่อ **2 ตัวเลือกจะกองกันฝั่งซ้าย ฝั่งขวาโล่ง** ซึ่งอ่านยากกว่า
เลยเรียงสลับฝั่ง (`0=ซ้าย 1=ขวา 2=ซ้าย 3=ขวา`) เก็บไว้ในตาราง `CHOICE_BUTTON_PATHS`
**แก้การจัดวางที่ตารางเดียว ไม่ต้องเปลี่ยนชื่อโหนดในซีน** (เปลี่ยนชื่อจะทำให้ path ในสคริปต์พัง)

**3. `Array[String]` → `Array`**
บทพูดใน `DialogueData` เป็น `Array` ไม่มีชนิด ส่งเข้าพารามิเตอร์ `Array[String]` จะ error ตอนรัน
รับ `Array` แล้ว `str()` เอาตอนใส่ข้อความลงปุ่มแทน — เปิดทางให้บทส่งตัวเลือกมาตรง ๆ ได้ในอนาคต

**4. ต้องกันบทเดินต่อระหว่างรอเลือก (จุดที่สำคัญที่สุด)**
`_input()` กิน E ไว้เลื่อนบรรทัดอยู่แล้ว ถ้าไม่กัน **ผู้เล่นกด E หนีคำถามได้**
บทจะเดินต่อทั้งที่ `await` ยังค้างรออยู่ → ฝั่งที่เรียกค้างตลอดกาล
กันสองทาง: `_input()` และ `_advance()` (ทางหลังเผื่อบรรทัดที่มีคีย์ `"hold"` เดินต่อเอง)

และปิด `_waiting` **ก่อน** `emit` ไม่ใช่หลัง — กดปุ่มที่สองในเฟรมเดียวกัน (คลิกพร้อมเคาะ Enter)
จะถูกตีตกตั้งแต่บรรทัดแรกของ `_on_choice_pressed` แทนที่จะยิงสัญญาณซ้ำโดยไม่มีใครรับ

**ต่อจนใช้ได้จริงแล้ววันเดียวกัน** — ดูหัวข้อ "บทแตกตามตัวเลือก" ด้านล่าง

---

## 2026-08-14 — บทพูดผูกกับ "ฉาก" ไม่ใช่ "ตัวละคร" (`dialogue_id_override`)

**ที่มา:** เพิ่มบทวาวา–น้ำฟ้าสำหรับห้องใต้ตึกบทที่ 1 (`wawa_under_ch1`)
น้ำฟ้ามาช่วยยกของ วาวาฝากกล่องสุดท้ายให้เอาไปวางที่ห้องรับรอง — เป็นบทที่ต่อกับกล่องในฉากโดยตรง

**ปัญหาที่เจอตอนจะเสียบบท:** วาวามีที่เดียวคือ `npc/data/wawa.tres` และ **`level_2.tscn` ก็ใช้ใบเดียวกัน**
แก้ `dialogue_id` ในนั้นเพื่อให้ห้องใต้ตึกพูดเรื่องยกกล่อง → วาวาที่ level_2 จะพูดเรื่องเดียวกันไปด้วย

**ทางเลือกที่ชั่งกัน**
- ก๊อป `.tres` แยกใบต่อฉาก — ได้ `SpriteFrames` ซ้ำอีกชุด คือปัญหาเดิมที่ระบบซีนแม่หนีมาเป๊ะ ๆ
- ยัดบททุกฉากไว้ในคีย์เดียวแล้วแตกด้วยเงื่อนไขในโค้ด — บทสนทนากลายเป็นตรรกะ แก้บทต้องแก้โค้ด
- **`dialogue_id_override` ที่ตัวโหนด** ← เลือกอันนี้ ตั้งใน Inspector ของ instance นั้น ๆ

**เกณฑ์:** `.tres` = "สิ่งที่ติดตัวละครไปทุกที่" (หน้าตา ชื่อ ท่ายืน) · โหนดในซีน = "สิ่งที่ขึ้นกับว่าอยู่ฉากไหน"
บทพูดอยู่ฝั่งหลัง เพราะคนเดียวกันเจอกันคนละที่ก็คุยคนละเรื่อง

**ผลพลอยได้จากการเทสต์:** บทเดิม `wawa_intro` / `npc1` ใช้ชื่อผู้พูดว่า `"พี่วา"`
ซึ่งไม่ตรงกับโหนดรูปใด (`Wawa` `Namfha` `Chochang` `Kirin`) → บทนั้น**รูปวาวาไม่เคยขึ้นเลย**
ไม่แก้ให้ในรอบนี้เพราะเปลี่ยนชื่อแล้วชื่อที่โชว์ในกล่องเปลี่ยนตาม เป็นเรื่องน้ำเสียงบท ไม่ใช่บั๊กเทคนิคล้วน

**แก้คำในบทที่ผู้ใช้ส่งมา:** `เหลทอ` → `เหลือ` (2 จุด) · `เรานิเอง` → `เรานี่เอง` · `ให้องรับรอง` → `ให้ห้องรับรอง`
และแยกประโยคสุดท้าย (`จะว่าไปแล้วเรากินข้าวรึยังนะ`) ออกเป็นอีกบรรทัด เพราะเป็นคนละความคิดกับการฝากของ
กล่องคำพูดพิมพ์ทีละตัว บรรทัดยาวเกินจะรอนาน

**ปรับบทรอบสอง (15 ส.ค.):** `ครับ` → `คับ` ให้เข้ากับน้ำเสียงวาวา · เพิ่มน้ำฟ้าตอบ `ได้เลยค่ะ` คั่น
ก่อนวาวาเปลี่ยนเรื่อง (บทเดิมวาวาพูดรวดสามบรรทัดติดจนน้ำฟ้าเหมือนไม่อยู่ในฉาก) ·
ต่อท้ายประโยคสุดท้ายเป็น `...ไม่กินเนี่ยแย่เลย` — รวมเป็น 5 บรรทัด

---

## 2026-08-12 — เดินเฉียงล่างใช้สไปรท์แยกซ้าย/ขวา แทนการ flip

**ที่มา:** วาด `walk_obliquelyL` (เฉียงซ้ายล่าง) กับ `walk_obliquelyR` (เฉียงขวาล่าง) แยกกัน
แล้วเอา `walk_obliquelydown` ชุดรวมเดิมออก — โค้ดยังหาชื่อเก่าอยู่ **เดินเฉียงขวาล่างเลยตกไปใช้ `walk_side`**
(เทสต์จับได้ ไม่ใช่ error ไม่มีอะไรฟ้อง เพราะโค้ดออกแบบให้ถอยไปท่าด้านข้างอย่างเงียบ ๆ อยู่แล้ว)

**ทำไมชุดที่วาดตรงทิศดีกว่า flip** — ภาพที่พลิกมาจะกลับด้านทั้งตัว กระเป๋าสลับข้าง แสงเงาผิดด้าน ผมปัดคนละทาง
ตอนไม่มีชุดจริงให้เทียบก็ดูผ่าน ๆ ได้ แต่พอมีชุดจริงข้าง ๆ ตาจะจับได้ทันที

**เปลี่ยนวิธีเลือกชุดจาก if/else เป็นตาราง 2 ชั้น**
- `DIAGONAL_EXACT_SUFFIX` — ชุดที่วาดตรงทิศ ใช้ก่อนเสมอ ไม่ flip
- `DIAGONAL_FLIP_FALLBACK` — ชุดที่วาดทางเดียวแล้วพลิกเอา (ตอนนี้เหลือแค่ `_obliquelyup` ที่ยังใช้จริง)
- ไม่เข้าทั้งสองอัน → ถอยไป `_side` เหมือนเดิม

เหตุผลที่ทำเป็นตาราง: **วาดชุดใหม่เพิ่มแล้วเติมชื่อที่เดียวจบ** ไม่ต้องแก้ตรรกะ
และเช็คด้วย `has_animation` ทุกชั้น ไม่ฮาร์ดโค้ดว่า prefix ต้องเป็น `"walk"` — วาด `idle_obliquelyL`
เมื่อไหร่ ท่ายืนนิ่งหันเฉียงก็จะทำงานเองทันที

**บทเรียน:** fallback ที่ออกแบบให้ "ไม่พัง" แลกมาด้วยการที่ของหายไปแบบไม่มีใครบอก
เปลี่ยนชื่ออนิเมชันในซีนแล้วต้องไล่เช็คโค้ดที่ประกอบชื่อเป็น string เสมอ
(เรื่องเดียวกับ `preload("res://...")` ที่ไม่อัปเดตตามตอนเปลี่ยนชื่อไฟล์ `.tscn`)

### ต่อท้าย — ชุด `_box` (ท่าตอนถือของ) ใช้งานได้แล้ว

ชุด `*_box` คือท่าเดียวกันแต่มีของอยู่ในมือ เกิดตอนผู้เล่นหยิบสิ่งของ

**ปัญหาที่ทำให้เติมง่าย ๆ ไม่ได้:** `_box` อยู่**ท้ายสุด หลังทิศ** (`walk_obliquelyL_box`)
เติมที่ prefix ไม่ได้เพราะตอนนั้นยังไม่รู้ทิศ · จะเติมได้ต้องรอชื่อทิศประกอบเสร็จก่อน

**ทางออก:** ยุบทุกจุดที่เรียก `animated_sprite_2d.play()` (มี 7 จุดกระจายอยู่) ให้เหลือ
ฟังก์ชันเดียวคือ `_play(anim, flip)` แล้วเติม `_box` ตรงนั้นที่เดียว
เลือกทางนี้แทนการเติมตามจุดต่าง ๆ เพราะ **จุดที่ลืมเติมจะกลายเป็นท่าที่ถือของแล้วมือเปล่า**
ซึ่งเป็นบั๊กที่ต้องเดินไปทิศนั้นพอดีถึงจะเห็น — หาไม่เจอง่าย ๆ

`carrying` เป็นตัวแปรเปล่า ๆ ไม่มี setter เพราะ `_process()` เล่นอนิเมชันใหม่ทุกเฟรมอยู่แล้ว
ตั้งค่าปุ๊บภาพเปลี่ยนในเฟรมถัดไปเอง ไม่ต้องมี API สั่ง refresh

⚠️ ตั้งใจให้ `carrying` คุมแค่ "ภาพ" ไม่เก็บว่าถืออะไรอยู่ — เรื่องตัวของจริง (ซ่อนโหนด ย้ายตำแหน่ง วางคืน)
เป็นหน้าที่ของระบบหยิบของที่ยังไม่ได้ทำ แยกกันไว้จะได้ไม่ต้องรื้อ player.gd ตอนออกแบบระบบนั้น

**ท่าที่ไม่มีชุด `_box`** → เล่นท่ามือเปล่าแทน + `push_warning` **ครั้งเดียวต่อชื่อ**
(เก็บชื่อที่เตือนแล้วไว้ใน `_warned_anims`) เพราะฟังก์ชันนี้ทำงานทุกเฟรม เตือนทุกครั้งจะท่วมคอนโซล
ตอนนี้มีครบทุกท่าที่ระบบเรียกใช้จริง 8 ทิศ × walk/idle จึงยังไม่มีใครเห็น warning นี้

---

## 2026-08-12 — ห้องใบเดียวใช้ได้ทุกบท (สลับของตกแต่งด้วย ChapterSlot)

**ปัญหา:** ห้อง Under ใช้เลย์เอาต์เดิมทุกบท เปลี่ยนแค่ของตกแต่งกับ NPC
ถ้าก๊อปซีนห้องทั้งใบต่อบท → แก้ตำแหน่งบันไดทีเดียวต้องไล่แก้ทุกไฟล์ (ปัญหาเดียวกับที่หนีมาตอนทำ NPC base scene)

**ทางเลือกที่ชั่งกัน**
- ก๊อปซีนห้องต่อบท — เลย์เอาต์แตกคอกันแน่นอน ไม่เอา
- ใส่ของทุกบทไว้ในซีนเดียวแล้ว `visible = false` สลับเอา — ซีนบวมขึ้นเรื่อย ๆ แถม NPC ที่ซ่อนอยู่ยังกิน physics/process
- **แยกของตกแต่งเป็นซีนย่อยแล้วเสียบเข้า slot ตอนรัน** ← เลือกอันนี้ บทที่ไม่ได้เล่นไม่ถูกโหลดเลย

**โครงสร้าง:** `Under` แนบ `scripts/room.gd` + มีลูกเปล่าชื่อ `ChapterSlot`
`room.gd` อ่าน `GameState.chapter` แล้ว instantiate ซีนจาก `chapter_decors[chapter - 1]` ใส่ slot

**ทำไมใช้ `Array[PackedScene]` ไม่ใช่ `Dictionary`** — ลากซีนใส่ช่องใน Inspector ได้ตรง ๆ
แลกกับการที่ต้องท่องว่า index 0 = บท 1 · ช่องว่าง (null) แปลว่า "บทนั้นห้องโล่ง" ไม่ใช่ error
บทที่เกินขอบ array → `push_warning` แล้วคงของเดิม ไม่ crash กลางเกม

**ทำไมสัญญาณ ไม่ใช่โหลดฉากใหม่** — `GameState.chapter` มี setter ที่ emit `chapter_changed`
เฉพาะตอนค่าเปลี่ยนจริง ห้องที่เปิดอยู่จึงสลับของได้ทันทีโดยผู้เล่นไม่ต้องออกจากฉาก
(เผื่ออีเวนต์ที่เนื้อเรื่องเปลี่ยนบทกลางฉาก) · ตั้งค่าเดิมซ้ำไม่รื้อของทิ้งสร้างใหม่

**ใช้ `GameState` ตัวเดิม ไม่สร้าง autoload ใหม่** — โจทย์บอกให้สร้าง `res://autoload/game_state.gd`
แต่โปรเจกต์มี `Game_State.gd` เป็น autoload ชื่อ `GameState` อยู่แล้ว (เก็บ `next_spawn_point` / `pending_intro`)
สร้างซ้ำจะได้ตัวแปรสถานะเนื้อเรื่อง 2 แหล่ง แถม CLAUDE.md เตือนไว้ว่าห้ามเติม autoload ลง `project.godot` เอง
จึงเติมฟิลด์ `chapter` ลงตัวเดิมแทน — **ไม่ต้องแตะ Project Settings เลย**

### บทเรียนที่เจอระหว่างทาง

**1. `remove_child()` ก่อน `queue_free()`**
`queue_free` ลบจริงตอนท้ายเฟรม ถ้า `add_child` ของบทใหม่ทันที ของสองบทจะซ้อนกันอยู่ 1 เฟรม
แล้วชื่อโหนดชนกันจน Godot เปลี่ยนชื่อตัวใหม่ให้ (`@Under_ch1@2`) ทำให้ `get_node()` ที่อื่นหาไม่เจอ

**2. y-sort ต้องเปิดต่อกันเป็นทอด ๆ**
Godot จัดลำดับ Y เฉพาะ**ลูกโดยตรง**ของโหนดที่เปิด y-sort · โหนดกลางที่ปิดไว้จะถูกวาด**ยกกลุ่ม**
ที่พิกัดของตัวเอง `Under` เปิดอยู่แต่ `ChapterSlot` กับรากซีน `Under_ch1` ปิด → เก้าอี้กับวาวา
จะสอร์ตที่ y=0 คือจมอยู่หลังทุกอย่างในห้อง จึงเปิด y-sort ทั้งสองตัว

**3. ของตกแต่งเคยแขวนผิดที่ — เป็นลูกของ `player`**
เดินไปไหนของตกแต่งจะตามไปด้วย (ไม่เห็นชัดเพราะทั้งกลุ่มถูกวาดยกกลุ่มอยู่แล้ว)
ตอนแยกออกมาเป็นซีนย่อยเลยได้แก้ไปในตัว

**4. NPC ในซีนของตกแต่งต้องเป็น `npc/scenes/wawa.tscn` ไม่ใช่ `npc_base.tscn` ดิบ ๆ**
ใส่ base ดิบแล้ว override `data` เองได้ก็จริง แต่จะไม่ได้ collision ที่ปรับไว้รายตัว
และวันหลังแก้ขนาดวาวาต้องไล่แก้ทุกที่ที่เผลอใส่ base ตรง ๆ

---

## 2026-08-09 — NPC เปลี่ยนมาใช้ซีนแม่ตัวเดียว (base + inherited scene)

**ปัญหาเดิม:** NPC แต่ละตัวเป็นซีนแยกที่ก๊อปโครงกันมา แก้ระบบทีต้องไล่แก้ทุกตัว
`wawa.tscn` ตัวเดียวมี 210 บรรทัด ซึ่งเกือบทั้งหมดคือ `SpriteFrames` กับโครงโหนดที่ทุกตัวเหมือนกัน

**ทางเลือกที่ชั่งกัน**
- ก๊อปซีนแล้วแก้ทีละตัว — ที่ทำอยู่ ไม่เอาแล้ว
- ซีนเดียวแล้วสลับข้อมูลด้วยโค้ด — ปรับ collision รายตัวไม่ได้ NPC ตัวใหญ่เล็กต่างกัน
- **base scene + inherited scene** ← เลือกอันนี้ ได้ทั้งระบบร่วมกันและ override รายตัว

**แบ่ง 3 ชั้น**

| ไฟล์ | เก็บอะไร |
|---|---|
| `npc_base.tscn` / `.gd` | โครงโหนด + พฤติกรรมที่ทุกตัวเหมือนกัน |
| `npc_data.gd` (`NPCData`) | **เฉพาะสิ่งที่ต่างกันรายตัว** — ชื่อ, บท, สไปรท์, offset |
| `scenes/*.tscn` | ซีนสืบทอด override เฉพาะ collision กับ `data` |

เกณฑ์ตัดสินว่าอะไรควรอยู่ชั้นไหน = **"ถ้าวันหนึ่งอยากเปลี่ยน ต้องไล่แก้กี่ที่"**
บับเบิล "คุยได้" จึงอยู่ที่ `NPCBase` ไม่ใช่ `NPCData` เพราะทุกตัวใช้ใบเดียวกัน
ถ้าไปอยู่ใน `.tres` จะต้องกรอกซ้ำทุกตัว = ปัญหาเดิมที่หนีมา

**ยืนยันว่าเป็น inheritance จริง ไม่ใช่ก๊อป** — `wawa.tscn` เขียนไว้ 2 โหนด
แต่ `instantiate()` ได้ 5 โหนด แปลว่าลูกมาจากซีนแม่ · ถ้าเป็นการก๊อปจะได้ 5 เท่ากันทั้งสองฝั่ง

### บทเรียนที่เจอระหว่างทาง

**1. `bubble.gd` เดิมมีบั๊กบับเบิลหายถาวร**
มันอ่าน `original_scale` จาก `scale` ปัจจุบันตอน `_ready()` แล้ว tween ย่อลง 0 ก่อนป่องออก
ถ้ามีอะไรทำให้ `_ready()` ทำงานตอน scale เป็น 0 เป้าหมายจะกลายเป็น 0 แล้วหายตลอด
ตัวใหม่เก็บเป้าหมายไว้ที่ `@export indicator_scale` แยกต่างหาก ไม่อ่านจากค่าปัจจุบัน

**2. 🚨 `dialogue_system._input()` กิน E แต่ไม่เรียก `set_input_as_handled()`**
อีเวนต์เดียวกันจึงไหลต่อไปถึง `_unhandled_input` ของ NPC
**กด E เลื่อนบรรทัดทีนึง แล้วบทเด้งกลับไปเริ่มใหม่ทันที**
แก้ที่ฝั่ง NPC (เช็ค `is_talking` ก่อน) ไม่ไปแตะ `dialogue_system` เพราะระบบเก่ายังใช้อยู่
— **ตอนเลิกใช้ `NPC.gd` แล้วควรกลับมา mark handled ที่ต้นทางให้จบ**

**3. ไม่ cache `get_first_node_in_group()` ตอน `_ready()`**
`NPC.gd` เดิมเก็บ `player` ไว้ตั้งแต่ `_ready` ซึ่งผู้เล่นอาจยังไม่เข้า tree → ได้ `null` ค้างทั้งฉาก
ตัวใหม่รับ `body` จาก `body_entered` มาเก็บแทน · ส่วน `dialogue_ui` หาสดตอนใช้จริงทุกครั้ง
เพราะกล่องคำพูดอยู่คนละซีนและถูกสร้าง/ทิ้งตามฉาก

**4. อย่าเรียก `play()` ทื่อ ๆ** ชื่ออนิเมชันผิดแล้ว Godot พ่น error ทุกเฟรมจนอ่าน log ไม่ออก
เช็ค `has_animation()` ก่อน แล้ว warning บอกว่ามีชุดอะไรให้เลือก จะได้แก้ถูกที่
(เจอทันทีเพราะชุดของวาวาใช้ชื่อ `idle` ไม่ใช่ `idle_down` แบบที่โค้ดเดิมเรียก)

**5. `walk_side` ในสไปรท์ชีตวาวาถูกตั้งเป็นชุดว่าง 0 เฟรม**
ภาพจริงมีอยู่ที่แถวล่างสุด (y=320) แค่ไม่ได้ผูก — เติมให้แล้ว
สั่งเล่นชุดว่างจะได้ตัวละครหายไปเฉย ๆ ไม่มี error บอก

---

## 2026-07-31 — ม่านเฟดคลุม UI ไม่ได้ เพราะ CanvasLayer อยู่ชั้นเดียวกันหมด

**อาการ:** เปลี่ยนฉากแล้ว UI แชทกับกล่องคำพูด**ไม่มืดตามม่านเฟด** ลอยเด่นอยู่บนจอดำ

**สาเหตุ:** `SceneTransition`, `Chat`, `DialogueBox` เป็น `CanvasLayer` และ **`layer = 1` เหมือนกันหมด**
(ค่า default ที่ไม่มีใครเคยตั้ง) พอ `layer` เท่ากัน Godot เรียงตามลำดับใน tree
ซึ่ง**เดาไม่ได้** เพราะม่านเฟดถูก `add_child` ที่ `get_tree().root` ส่วน UI อยู่ในซีน — คนละกิ่งกัน

**แก้:** `SceneTransition` ตั้ง `layer = 128` (ค่าสูงสุดของ Godot สูงกว่า `ClickEffect` ที่ 100 ด้วย)

**ตั้งจากโค้ดใน `_ready()` ไม่ใช่ตั้งในซีน** — เพราะเรื่อง `.tscn` ถูก merge/เซฟทับเกิดมาแล้วหลายรอบ
ค่าที่อยู่ในโค้ดรอดเสมอ · เปิดเป็น `@export fade_layer` ไว้ให้ปรับได้ถ้าวันหลังมี CanvasLayer ที่ต้องอยู่เหนือกว่า

**กฎที่ได้:** เพิ่ม `CanvasLayer` ใหม่ต้องตั้ง `layer` ให้ชัดเจนเสมอ อย่าปล่อย default 1
ปล่อยไว้แล้วลำดับการวาดจะขึ้นกับลำดับใน tree ซึ่งเปลี่ยนได้ตลอดเวลาที่แก้ซีน

---

## 2026-07-31 — ล้าง TileSet เสีย 2,549 นิยาม และเจอบั๊กภาพจริงใน p.tscn

เริ่มจากคำสั่ง *"debug ซีน under"* — output 2,324 บรรทัด ไม่มี script error เลย เป็น TileSet ล้วน

### ก่อนลบต้องรู้ก่อนว่า "มีคนใช้ไหม"

error บอกแค่ว่านิยาม tile หลุดขอบภาพ ไม่ได้บอกว่ากระทบภาพในเกมหรือเปล่า
จึงถอดรหัส `tile_map_data` (base64 → header 2 ไบต์ + 12 ไบต์ต่อช่อง: x, y, source_id, atlas_x, atlas_y, alt)
ของทุกเลเยอร์ แล้วเทียบพิกัดที่ใช้จริงกับตารางของแต่ละภาพ

**นี่คือส่วนที่ขาดไม่ได้** — ถ้าลบตามที่ error ฟ้องอย่างเดียว จะลบของที่ยังมีคนใช้ทิ้งไปด้วย

### ⚠️ พลาดเกือบทำของพัง: ลืมคิด `margins`

รอบแรกคำนวณตาราง atlas เป็น `ขนาดภาพ ÷ texture_region_size` แล้วสรุปว่า **ทุกซีน 0 ช่องเสีย**
พอลบไป 2,543 บรรทัดกลับพบว่ายังเหลือ error 12 อัน — ตรงนั้นเองที่รู้ว่าสูตรผิด

สูตรจริง: **`(ขนาดภาพ − margins) ÷ (texture_region_size + separation)`**

พอแก้สูตร ผลพลิก: `p.tscn` มี **37 ช่องที่ใช้ tile ที่โหลดไม่ขึ้นจริง** ไม่ใช่ 0
ถ้าเชื่อผลรอบแรกแล้วลบต่อ จะลบนิยามที่ยังมีคนใช้ทิ้งไป 6 อัน แล้วบั๊กจะเงียบหายไปตลอดกาล

**บทเรียน:** ตัวเลข "0 ปัญหา" ที่ได้มาง่ายเกินไปควรถูกสงสัย
ที่จับได้เพราะ**ยอดที่ลบไม่ตรงกับยอด error ที่ engine ฟ้อง** — ต้องเทียบสองทางเสมอ ไม่ใช่เชื่อสคริปต์ตัวเองอย่างเดียว

### ผลสุดท้าย

ลบนิยามที่ไม่มีใครใช้ **2,549 บรรทัด** จาก 6 ซีน → 5 ซีนเหลือ 0 error
เก็บไว้ 6 นิยามใน `p.tscn` ที่ยังมีช่องใช้อยู่ **โดยตั้งใจ**
error 18 บรรทัดที่เหลือคือสัญญาณเดียวที่บอกว่าต้นไม้แถวล่างหายไป 37 ช่อง — ลบเมื่อไหร่ปัญหาหายเงียบทันที

### ⚠️ อีกเรื่อง: `Get-Content -Raw` ทำไฟล์ภาษาไทยพัง

ใช้ PowerShell แก้เลขหัวข้อใน `CLAUDE.md` ด้วย `Get-Content -Raw` + `-replace` + เขียนกลับเป็น UTF-8
→ **ภาษาไทยทั้งไฟล์กลายเป็นขยะ** เพราะ PS 5.1 อ่านไฟล์ด้วย ANSI ของเครื่อง (windows-874) ไม่ใช่ UTF-8

กู้คืนโดยกลับด้าน: อ่านเป็น UTF-8 → `Encoding.Default.GetBytes()` → ตีความไบต์นั้นเป็น UTF-8 อีกที

**กฎ:** ห้ามใช้ PowerShell แก้ไฟล์ที่มีภาษาไทย ใช้ Edit/Write เท่านั้น
ถ้าจำเป็นต้องใช้จริง ๆ ต้องระบุ encoding ทั้งขาอ่านและขาเขียน: `[IO.File]::ReadAllText($p, [Text.Encoding]::UTF8)`

---

## 2026-07-31 — เพลงฉาก, ทรานสิชั่นแชท, และรูปตัวละครเฉพาะคนที่พูด

### เพลง BG ของ wokeup ไม่เคยดังเลย

`wokeup.tscn` มี node `BG` (AudioStreamPlayer) เสียบไฟล์ *Morning Bus Stop.mp3* ไว้เรียบร้อย
แต่**ไม่ได้ติ๊ก autoplay** — มีทุกอย่างครบยกเว้นคำสั่งให้เล่น

**เลือกสั่งเล่นจากโค้ดแทนการติ๊ก autoplay** เพราะได้ 2 อย่างที่ติ๊กเอาไม่ได้:
1. **วนลูป** — `loop` อยู่ที่ตัว *ทรัพยากร* `AudioStream` ไม่ใช่ที่ตัวเล่น
   ติ๊ก autoplay อย่างเดียวได้เพลงที่เล่นรอบเดียวแล้วเงียบตลอดฉาก
2. **ค่อย ๆ ดังขึ้น** — เปิดฉากมาแล้วเพลงตูมใส่เต็มเสียงจะสะดุด
   เริ่มที่ −60 dB (เงียบสนิท ไม่ใช่ 0 ซึ่งแปลว่าดังเต็ม) แล้ว tween ขึ้นมา

### ทรานสิชั่นตอนแผงแชทโผล่

แผงเลื่อนขึ้นจากด้านล่างพร้อมจางเข้า เหมือนยกโทรศัพท์ขึ้นมาดู — เดิมเด้งขึ้นมาเฉย ๆ ดูเหมือนภาพกระตุก

**เล่นเฉพาะตอนแผงเปลี่ยนจากซ่อนเป็นแสดง** ไม่ใช่ทุกครั้งที่เรียก `open_group()`
กดกลับเข้าห้องที่เห็นอยู่แล้วไม่ควรเลื่อนซ้ำให้เวียนหัว

พ่วงแก้: เก็บ `_base_pos` ของแต่ละแผงตอน `_collect_groups()`
เดิม `popout_group()` สมมติว่าแผงอยู่ที่ (0,0) — พอมีทรานสิชั่นมาขยับตำแหน่ง สมมติฐานนั้นพังทันที
ตอนนี้ทุกการเคลื่อนที่อ้างจากตำแหน่งที่ตั้งไว้ในซีน ย้ายแผงใน editor แล้วระยะยังถูก

### รูปตัวละคร: โชว์เฉพาะคนที่มีบทพูด

`portraits.visible = true` เดิมโชว์ตัวละคร **ทุกตัว** ที่มีใน `Root/Portraits`
บทที่มีคนพูดคนเดียว (เช่นน้ำฟ้าพูดคนเดียวที่ป้ายรถเมล์) จึงมีอีก 3 คนยืนหมอง ๆ อยู่ด้วยโดยไม่มีเหตุผล

**ไล่หาคีย์ `"name"` ทั้งชุดก่อนเริ่มเล่น** ไม่ใช่ทยอยโชว์ทีละคนตอนถึงคิว
ถ้าโผล่มากลางบทจะดูเหมือนตัวละครวาร์ปเข้าฉาก — ต้องยืนรออยู่แล้วตั้งแต่ต้น

เก็บเป็น `@export only_show_speakers` ปิดได้ เผื่อฉากประชุมที่ทุกคนนั่งอยู่จริง
และ `_reset_all_characters()` ต้องคืน `visible = true` ด้วย ไม่งั้นบทถัดไปจะมีคนหายค้างจากบทก่อน

### ⚠️ เปลี่ยนชื่อ .tscn แล้ว preload ใน .gd ไม่อัปเดตตาม (เจอซ้ำ)

`canvas_layer.tscn` ถูกเปลี่ยนชื่อเป็น `DialogueBox.tscn` → `preload()` ใน `wokeup.gd`
กับ `parking.gd` พังทั้งคู่ ทำให้ **สคริปต์ทั้งไฟล์โหลดไม่ขึ้น** ไม่ใช่แค่บรรทัดนั้น

Godot แก้การอ้างอิงให้เฉพาะในซีนกับ `@export` — **string ในโค้ดต้องไล่แก้เอง**
นี่เป็นครั้งที่สองแล้ว (ครั้งแรกตอนย้าย `.tscn` เข้าโฟลเดอร์) — วิธีเช็ค: `Grep` ชื่อไฟล์เก่าทั้งโปรเจกต์

---

## 2026-07-31 — ต่อแชทเข้าฉากลานจอดรถ: รถออกก่อน แล้วค่อยพูด

**ลำดับที่ตั้งใจ:** ตอบตัวเลือก → ล็อกอินพุต → เฟด → `p.tscn` → รถเมล์ออก → น้ำฟ้าพูด → ขยับได้

**ทำไมรถต้องออกก่อนบทพูด** — บทพูด *"สุดท้ายก็มาสายจนได้ต้องรีบแล้ว"* คือ**ปฏิกิริยาต่อการตกรถ**
ถ้าพูดก่อนแล้วรถค่อยออก เหตุกับผลจะสลับกัน ผู้เล่นจะงงว่าพูดถึงอะไร

### ปัญหา: ฉากเดียวเข้าได้หลายทาง แต่อินโทรต้องเล่นทางเดียว

`p.tscn` เข้าได้ทั้งจากแชทตอนเช้า และจากประตูฝั่งอื่น (`portal.gd`)
ถ้าใส่อินโทรใน `_ready()` ตรง ๆ เดินออกแล้วกลับเข้ามาก็จะโดนล็อกให้ดูรถออกใหม่ทุกรอบ

**ทางเลือกที่พิจารณา**
1. แยกเป็นอีกซีน `p_intro.tscn` — แก้ฉากทีต้องแก้ 2 ที่ตลอดไป ตกหล่นแน่นอน
2. เช็คจาก `next_spawn_point` ว่ามาจากไหน — ผูกอินโทรติดกับตำแหน่ง spawn ซึ่งคนละเรื่องกัน
3. **เพิ่ม `GameState.pending_intro` เป็นข้อความฝาก** ← เลือกอันนี้

ฝั่งที่สั่งเปลี่ยนฉากเป็นคนตั้ง ฉากปลายทางอ่านแล้ว**ล้างทิ้งทันที** (ใช้ครั้งเดียวโดยธรรมชาติ)
รูปแบบเดียวกับ `next_spawn_point` ที่ใช้อยู่แล้ว — คนอ่านโค้ดไม่ต้องเรียนกลไกใหม่
ต่อไปมีอินโทรฉากอื่นก็เพิ่มค่าคงที่ตัวใหม่ ไม่ต้องแก้โครง

### กับดัก: ตัวเฟดถูกลบไปพร้อมซีนเดิม

`change_scene_to_file()` ลบซีนปัจจุบันทิ้ง — ถ้าตัวเฟดเป็นลูกของซีนนั้น มันหายไปด้วย
แล้ว `await` ที่ค้างอยู่จะไม่ทำงานต่อ → **จอค้างดำถาวร**

`Start.gd` เลี่ยงด้วยการ `add_child` ที่ `get_tree().root` แล้วเรียก `fade.get_tree()` แทน `self`
(มีคอมเมนต์เตือนไว้ว่า *"self อาจถูก free ไปแล้ว"*) — แต่ตัว coroutine ยังอยู่ใน `Start.gd` อยู่ดี

**แก้ให้ถูกที่:** ย้ายลำดับทั้งหมดเข้าไปใน `scene_transition.gd` เป็น `transition_to(path)`
node ตัวนี้อยู่ใต้ root อยู่แล้ว จึงรอดจากการเปลี่ยนฉาก — ผู้เรียกแค่สั่งแล้วจบ ไม่ต้อง await อะไร
(ของเดิม `fade_out()` / `fade_in()` ไม่แตะ `Start.gd` ยังทำงานเหมือนเดิม)

### ล็อกอินพุตต้องปิดปุ่มด้วย ไม่ใช่แค่ตัวแม่

ตั้ง `mouse_filter = IGNORE` ที่ Control แม่อย่างเดียว**ไม่พอ** — ปุ่มลูกตั้ง filter ของตัวเองไว้
ยังรับคลิกได้อยู่ ต้องไล่ `disabled = true` ทุก `BaseButton` ในกิ่ง
และต้องล็อก**ทันทีที่ตอบเสร็จ** ไม่ใช่ตอนเริ่มเฟด เพราะระหว่างหน่วง `end_delay`
ผู้เล่นยังกดปุ่มย้อนกลับได้

**ยืนยันด้วยการรันจริง:** ใส่ print ชั่วคราวแล้วรัน headless
ได้ `bus_driving=true can_move=false -> start_dialogue` = ลำดับถูกต้อง แล้วลบ print ออก

---

## 2026-07-31 — แชทหลายกลุ่ม: เด้งออกมาข้าง ๆ แทนการกดสลับหน้า

**ปัญหาการเล่าเรื่อง:** วาวาบอกในกลุ่ม SMO ว่า *"ฝ่ายสวัสดิการ เช็คข้อความในกลุ่มสวัสกันด้วยน้า"*
ถ้าให้ผู้เล่นกดย้อนกลับ → หารายการ → เลือกกลุ่มสวัสฯ เอง จังหวะเรื่องจะขาดตรงนั้น

**ทางเลือกที่พิจารณา**
1. ทำหน้ารายการแชท (ChatList) ให้ผู้เล่นกดเลือกเอง — สมจริงเหมือนแอปจริง แต่ผู้เล่นอาจกดผิดกลุ่มหรือหาไม่เจอ
2. สลับหน้าอัตโนมัติไปกลุ่มสวัสฯ — ลื่นไหล แต่**ผู้เล่นเสียบริบท** มองไม่เห็นแล้วว่าใครสั่งให้มา
3. **เด้งแผงใหม่ออกมาข้าง ๆ ทั้งสองแผงค้างอยู่พร้อมกัน** ← เลือกอันนี้

**เหตุผล:** จอ 1920 กว้างพอวางแผงกว้าง 600px ได้ 2 แผงสบาย ๆ
ผู้เล่นเห็นข้อความต้นเหตุกับกลุ่มปลายทางพร้อมกัน **ความสัมพันธ์ของสองกลุ่มเล่าตัวเองได้โดยไม่ต้องอธิบาย**
ยังทำ ChatList เพิ่มทีหลังได้ — `popout_group()` แยกจาก `open_group()` ไม่ทับกัน

**ผลข้างเคียงที่ต้องจัดการ:** `open_group()` เดิมซ่อนห้องอื่นทั้งหมด
ต้องเพิ่ม `_popped` ไว้ยกเว้นห้องที่เด้งออกมาแล้ว ไม่งั้นกดกลับเข้ากลุ่มแรกแล้วแผงที่เพิ่งเด้งจะหายไปเฉย ๆ

### ตัวเลือกคำตอบ: สร้างจากโค้ด ไม่ทำเป็นซีน

**ตัดสินใจ:** ปุ่มตัวเลือกไม่มี node ในซีนเลย `chat_group.gd` สร้างเองตอนเรียกใช้แล้วลบทิ้ง

**เหตุผล** — เป็นของชั่วคราวที่โผล่มาแล้วหายไป ถ้าทำเป็นซีนจะต้องนั่งตั้งค่าใน Inspector
ทั้งที่**มองไม่เห็นมันตอนแก้ซีนอยู่ดี** (มันซ่อนอยู่) แถมจำนวนตัวเลือกต่อรอบไม่เท่ากัน
ทำเป็นซีนก็ต้องสร้างลูกจากโค้ดอยู่ดี · หน้าตาปรับได้ผ่าน `@export` แทน

**บทเรียนที่ยืมมา:** ปักความกว้างปุ่มเองเหมือนที่ `message_bubble.gd` ทำ
`Button` ที่เปิด `autowrap_mode` มี minimum width เกือบศูนย์ (ตัดทีละคำได้)
ถ้าไม่ปัก VBox จะบีบจนปุ่มเหลือกว้างเท่าคำเดียว — **บั๊กตัวเดียวกับ RichTextLabel เป๊ะ**

### เส้นแบ่ง HISTORY / INCOMING = "ผู้เล่นหยิบโทรศัพท์ตอนไหน"

ตอนแรกวางข้อความ 2 อันสุดท้ายของวาวา (*"ฝ่ายสวัสดิการ เช็คข้อความในกลุ่มสวัสกันด้วยน้า"*) ไว้ใน `INCOMING`
ให้ค่อย ๆ พิมพ์เข้ามาหลังผู้เล่นเปิด UI — **ผิดกับที่ `wokeup` เล่าไว้แล้ว**

ใน `wokeup` มีเสียงเตือนดัง **2 ครั้ง** ตอนน้ำฟ้าเพิ่งตื่น *ก่อน* จะหยิบโทรศัพท์ขึ้นมา
เสียง 2 ครั้งนั้นคือข้อความ 2 อันนี้มาถึงแล้ว → พอเปิดแชทมันต้อง**ขึ้นรออยู่แล้ว** ไม่ใช่เพิ่งพิมพ์

**กฎที่ได้:** เส้นแบ่งไม่ใช่ "เก่า/ใหม่" แต่คือ *ผู้เล่นเห็นตอนไหน*
มาก่อนเปิด UI → `HISTORY` · มาตอนดูอยู่ → `INCOMING`

**ผลข้างเคียงที่เกือบทำให้พัง:** `open()` เดิม `return` เงียบ ๆ เมื่อ `INCOMING` ว่าง
พอกลุ่ม SMO ไม่มีข้อความสดแล้ว `chat_finished` เลยไม่ยิง → **กลุ่มสวัสฯ ไม่เด้งออกมาเลย**
แก้เป็นยิง `chat_finished` ด้วยแม้ไม่มีข้อความใหม่ (ห้องที่มีแต่ประวัติเก่าก็ถือว่าเล่าบทจบแล้ว)
ยืนยันด้วยการรัน headless จริงว่า popout ยังทำงาน

### เสียงเตือนอยู่ที่ chat_group ไม่ใช่ที่ผู้เรียก

เสียงดัง **พร้อมข้อความโผล่** ไม่ใช่ตอนเริ่มพิมพ์ — เลียนแอปแชทจริงที่เตือนเมื่อข้อความถึง
เงื่อนไข: เฉพาะ `INCOMING` จากคนอื่น · ประวัติเก่าไม่ดัง (ไม่งั้นเปิดแชทมาจะรัวเป็นสิบครั้ง)
· น้ำฟ้าส่งเองไม่ดัง (ไม่มีใครเตือนตัวเอง) · ปิดรายบรรทัดได้ด้วย `"sound": false`

`stop()` ก่อน `play()` ทุกครั้ง เพื่อให้ข้อความที่มาติด ๆ กันได้ยินเป็นจังหวะแยก ไม่ทับกันจนฟังเป็นเสียงเดียว

### บทแชทย้ายไป ChatDatabase ทั้งหมด

เดิมบทฝังอยู่ใน `phone.gd` · ย้ายมา `HISTORY` / `INCOMING` เพราะ**หน้ารายการแชทต้องอ่านข้อความล่าสุด
ของทุกกลุ่มได้โดยไม่ต้องเปิดห้องนั้นก่อน** → `get_preview()` คำนวณจากบทจริง ไม่ต้องกรอกซ้ำ
(ถ้าให้กรอกมือ วันหนึ่งแก้บทแล้วลืมแก้อีกที่แน่นอน)

พ่วงผลดี: เพิ่ม `{"choices": [...]}` เป็นชนิดบรรทัดหนึ่งในบทได้เลย
บทกับจุดที่ผู้เล่นต้องตอบอยู่ไฟล์เดียวกัน อ่านแล้วเห็นลำดับเรื่องต่อเนื่อง

---

## 2026-07-31 — ⚠️ Reload from disk ของ Godot merge ไฟล์ ไม่ได้ทับ

**เกิดขึ้นจริง:** แก้ `.tscn` จากข้างนอกตอน Godot เปิดอยู่ → Godot เด้งถาม → กด **Reload from disk**
→ ไฟล์ที่ได้**ไม่ใช่ของบนดิสก์ แต่เป็นของเก่าในหน่วยความจำผสมกับของใหม่**

ความเสียหายที่เจอในรอบเดียว:
- `script = null` โผล่มาที่ instance → ลบสคริปต์ทิ้งเฉพาะ instance นั้น
- `ext_resource` ที่ลบไปแล้วกลับมา แถม `uid=` หายไปทั้งไฟล์
- property override (`group_id`) หายเกลี้ยง
- `texture` ext_resource กลายเป็น `SubResource` ที่ `load_path` ชี้เข้า `.godot/imported/` (cache)

**กฎที่ได้:** แก้ `.tscn` จากข้างนอกต้อง **ปิด Godot ให้สนิทก่อน** แล้วค่อยเปิดใหม่
ปุ่ม Reload from disk **ไว้ใจไม่ได้กับไฟล์ที่มี instance ของซีนอื่น** — เกี่ยวกับตาราง uid ที่ค้างในหน่วยความจำ
(ต่างจาก `.gd` ที่ reload ได้ปลอดภัย เพราะไม่มีตารางอ้างอิงให้ merge)

---

## 2026-07-30 — ⚠️ กฎเหล็ก: autoload ต้องเพิ่มผ่าน Godot เท่านั้น

**เกิดขึ้น 2 ครั้งในวันเดียว** — autoload ที่เติมลง `project.godot` จากข้างนอกตอน Godot เปิดอยู่ **หายไปเอง**

**กลไก:** Godot อ่าน `project.godot` ตอนเปิดโปรเจกต์ครั้งเดียวแล้วเก็บรายการ autoload ไว้ในหน่วยความจำ
เมื่อผู้ใช้แตะ Project Settings เรื่องใดก็ตาม Godot เขียนไฟล์ใหม่ทั้งบล็อกจากรายการในหน่วยความจำ
→ **autoload ที่มันไม่รู้จักถูกลบทิ้ง** โดยไม่มีคำเตือนอะไร

| ครั้ง | เกิดอะไร |
|---|---|
| 1 | `ClickEffect` ที่เพิ่มไว้หาย ตอนปิด-เปิด `Snap 2D Transforms To Pixel` เพื่อทดสอบเรื่องแสง |
| 2 | `ChatDatabase` (เติมจากข้างนอก) หาย ตอนเพิ่ม `ClickEffect` ผ่าน Project Settings |

**อาการเวลาเกิด:** สคริปต์ทุกไฟล์ที่เรียก autoload นั้นขึ้น `Identifier "X" not declared in the current scope`
พร้อมกันหมด ทั้งที่โค้ดไม่ได้ผิดอะไร

**กฎ:** เพิ่ม autoload ผ่าน `Project > Project Settings > Globals > Autoload` เท่านั้น
Claude ห้ามเติมบรรทัดลง `project.godot` เอง — ถ้าจำเป็นต้องให้ผู้ใช้ปิด Godot ก่อน

---

## 2026-07-30 — ปรับกล่องคำพูด: กำหนดอนิเมชันต่อบรรทัด + อารมณ์ค้าง

**ตัดสินใจ: refactor ไม่ใช่เขียนใหม่**
ตรรกะพิมพ์ทีละตัวอักษร / กดข้าม / เลื่อนบรรทัด / ไฮไลต์คนพูด ของเดิมทำงานถูกแล้วและใช้เวลาจูนนาน
ปัญหาทั้งหมดอยู่ที่**วิธีจัดเก็บข้อมูลตัวละคร** ซึ่ง refactor แก้ตรงจุด

**ฟีเจอร์ใหม่: `anim` ต่อบรรทัด**
```gdscript
{"name": "วาวา", "text": "...", "anim": "Angry"}   # ไม่ใส่ anim = ใช้ default_anim ("talk")
```

**ตัดสินใจ: อารมณ์ค้างไว้ ไม่รีเซ็ตเป็น idle**
คนไม่พูด → ย่อลง (`scale_idle` 0.9) + มืดลง (`color_idle` 0.35) แต่**คงอนิเมชันเดิมไว้**

**วิธีทำ:** ลบบรรทัด `sprite.play("idle")` ของคนที่ไม่ได้พูดออก — แค่นั้น
อารมณ์ค้างเองโดยไม่ต้องเขียนระบบจำสถานะเพิ่ม

**เหตุผล:** ถ้ารีเซ็ตเป็น idle ทุกครั้งที่อีกฝ่ายพูด อารมณ์ในฉากจะขาดตอน
คนโกรธควรโกรธค้างจนกว่าจะมีบรรทัดสั่งเปลี่ยน

**โครงสร้าง UI ใหม่ (`canvas_layer.tscn`)** — สร้างด้วย node ของ Godot ล้วน ไม่ใช้ nine-patch texture
รายละเอียดทั้งหมดอยู่ใน [CHAT_UI.md](CHAT_UI.md) และ [DIALOGUE_UI.md](DIALOGUE_UI.md)

**กลไกสำคัญ 2 อย่าง:**
- **กล่องข้อความขนาดคงที่** — `TextPanel.custom_minimum_size.y = 200` + `TextLabel.fit_content = false`
  ถ้าปล่อยให้ขยายตามข้อความ กล่องจะกระโดดเปลี่ยนขนาดทุกบรรทัด กวนสายตา
- **ป้ายชื่อยืดตามชื่อ** — `NamePlate.size_flags_horizontal = Shrink Begin`
  ชื่อ `"น้ำฟ้า"` กับ `"พี่วาวา หัวหน้าสวัสดิการ"` ยาวไม่เท่ากัน ต้องยืดหดได้

**บั๊กที่แก้ไปด้วย:**
1. **มี 3 node ในกลุ่ม `dialogue_ui`** (CanvasLayer, Control, Label) → `get_first_node_in_group()`
   อาจคืน node ที่ไม่มีเมธอด `start_dialogue()` แล้ว crash — เหลือ 1 node แล้ว
2. **`player.can_move` ไม่เช็ค null** → บทพูดอัตโนมัติในซีนที่ไม่มีผู้เล่น (รถเมล์) จะ crash
3. **`Root` มีขนาดแค่ 40×40** (ค่าเริ่มต้นตอนสร้าง Control) → กล่องคำพูดไปโผล่มุมซ้ายบนแทนที่จะอยู่ล่างจอ
4. ลบ `Game/NPC/np_cdialogue.tscn` — เวอร์ชันเก่าซ้ำซ้อน ไม่มีใครอ้างถึง

---

## 2026-07-30 — ซีน wokeup: ไล่หาสาเหตุแสงกระพริบ (ซ้อนกัน 3 ชั้น)

ใช้เวลานานเพราะเป็นปัญหาซ้อนกันหลายชั้น และแต่ละชั้นบังชั้นถัดไป

**ชั้นที่ 1 — `shimmer_strength` ในซีนทับค่า default ของ shader**
shader ตั้ง default `0.0` ไว้พร้อมคอมเมนต์ว่าปิดเพื่อกันกระพริบ
แต่ `ShaderMaterial` ใน `wokeup.tscn` เก็บ `0.06` ไว้ — **ค่าที่บันทึกในซีนชนะ default ของ shader เสมอ**

**ชั้นที่ 2 — Polygon2D ไม่มี `uv`**
shader คำนวณจาก `UV` โดยสมมติว่าไล่ 0→1 แต่ Polygon2D ไม่มี uv จึงได้ค่าที่ไม่ใช่ 0–1
→ `edge_fade * length_fade` เป็น 0 ทั้งรูป → **แสงที่เห็นมาตลอดคือ shimmer เพียงอย่างเดียว**
อธิบายทั้งสองอาการพร้อมกัน: กระพริบทั้งลำ (shimmer เต้นทั่วรูป) และปิด shimmer แล้วหายเกลี้ยง (เหลือ 0×0)

แก้โดยใส่ `uv = PackedVector2Array(1, 0, 0, 0, 0, 0.39, 0, 1, 1, 1)` คำนวณจากรูปทรง polygon จริง

**ชั้นที่ 3 — ตั้ง `shimmer_strength` เป็น 0.3 (ค่าสูงสุดของสไลเดอร์)**
shader **บวก** shimmer เข้ากับความสว่าง ที่ 0.3 แกว่งได้ถึง ±0.45
→ ขึ้นสุด `clamp` ตัดเป็น 1.0 ทั่วรูป (เฉดไล่แสงหาย) → ลงสุดเป็น 0 (แสงดับ)
รอบคลื่น ~10.5 วิ ตรงกับอาการ "จางหายไปสักพักแล้วจางกลับมา"

**ทฤษฎีที่ผิด:** เคยสงสัย `camerashaking..gd` (สั่น 1 Hz) + `snap_2d_transforms_to_pixel`
แต่จังหวะ 1 Hz ไม่เข้ากับอาการที่ช้าเป็นสิบวินาที — **กล้องสั่นถูกต้องแล้ว** เพราะซีนนี้คือตื่นบนรถเมล์

**บทเรียน:** อาการที่ผู้ใช้อธิบายละเอียด ("ค่อย ๆ จางหายแล้วค่อย ๆ กลับมา") มีค่ามากกว่าคำสั้น ๆ
("กระพริบเป็นจังหวะ") — รอบคลื่นที่ต่างกัน 10 เท่าชี้ไปคนละสาเหตุเลย

**ตัดสินใจ: ถอดระบบซูมกล้องตอนปลุกออก**
ทำเสร็จแล้วแต่ผู้ใช้ทดลองแล้วรู้สึกว่าไม่เวิร์ค — ถอดออกหมด ไม่เก็บไว้เพราะเสียดายที่เขียนไปแล้ว
เหตุผลเชิงออกแบบ: ซีนนี้มีกล้องสั่นอยู่แล้ว การซูมเพิ่มเข้าไปทำให้ภาพวุ่นแทนที่จะตื่นเต้น

**เก็บไว้: เพิ่มจำนวนคลิกปลุก** 8–13 → **22–32 ครั้ง** (ทำเป็น `@export` ปรับได้ที่ Inspector)
เพื่อให้ผู้เล่นต้องกดย้ำ ๆ เหมือนพยายามปลุกคนจริง

---

## 2026-07-30 — เอฟเฟกต์คลิก (ripple + เสียง)

`click_effect.tscn` เป็น autoload → ทำงานทุกซีนโดยไม่ต้องลากใส่ทีละซีน (มี 24 ซีน ลืมแน่)

**ตัดสินใจ: ใช้ `_input` ไม่ใช่ `_unhandled_input`**
`_unhandled_input` ทำงานหลังสุด ถ้าคลิกโดนปุ่มหรือ Control ที่ `mouse_filter = Stop` จะไม่เกิด ripple
`_input` ได้รับก่อนใคร และ**ไม่เรียก `set_input_as_handled()`** คลิกจึงยังส่งต่อให้ปุ่มทำงานปกติ
(ถ้าเรียก จะกลืนคลิกทั้งเกม ปุ่มกดไม่ได้เลยสักปุ่ม)

**บั๊กที่แก้:** `Ripple` ตั้ง `anchors_preset = 15` (เต็มจอ) แต่ `Circle` ข้างใน anchor กึ่งกลาง
→ วงกลมไปโผล่ที่ จุดคลิก + (960, 540) เยื้องครึ่งจอ แก้เป็น `anchors_preset = 0`

**เพิ่ม: สุ่มระดับเสียง ±12% ทุกครั้งที่คลิก**
เพราะเกมมีฉากกดรัว 20–30 ที ถ้าเสียงเหมือนกันเป๊ะจะฟังเหมือนเครื่องจักรและน่ารำคาญเร็ว

---

## 2026-07-30 — จัดระเบียบโฟลเดอร์ asset

แยกรูปตามหน้าที่ ทำใน Godot editor จึงอัปเดต path ในซีนให้เองอัตโนมัติ

| โฟลเดอร์ | เก็บอะไร |
|---|---|
| `image_avatar/` | รูปโปรไฟล์วงกลมสำหรับแชท (256×256) |
| `image_fullAvatar/` | รูปครึ่งตัวสำหรับกล่องคำพูด |
| `image_ui/` | ไอคอน UI (ปุ่มส่ง) |
| `image/align_npc/` | สไปรท์ NPC ปริศนาที่นั่ง |
| `soud_effects/` | เสียงเอฟเฟกต์ |

> ⚠️ นามสกุลไฟล์รูป avatar เป็น **`.PNG` ตัวใหญ่** — `res://` ของ Godot เป็น case-sensitive ตอน export
> Windows ไม่สนตัวพิมพ์ จึงไม่พังตอนทดสอบ แต่รูปจะหายตอน build จริง ต้องเขียนให้ตรงเป๊ะ

---

## 2026-07-29 — ออกแบบระบบแชทกรุ๊ปสโม

**ตัดสินใจหลัก: แยก "ใครเป็นใคร" ออกจาก "พูดอะไร"**

บทแชทอ้างถึงคนด้วย `sender_id` เท่านั้น ส่วนชื่อ/รูป/ฝั่งซ้ายขวา เก็บใน `chat_database.gd` (autoload `ChatDatabase`)

**เหตุผล:** ถ้าใส่รูปทีละข้อความ พอคนหนึ่งพูด 20 ครั้งต้องลากรูปเดิม 20 รอบ
วันไหนเปลี่ยนรูป profile ต้องไล่แก้ 20 จุด ลืมจุดเดียวจะมี avatar ผิดคนโผล่
และการผูกฝั่งซ้าย/ขวาไว้กับ **ตัวคน** แทนที่จะเป็น **ข้อความ** ทำให้ใส่ผิดฝั่งไม่ได้เลยตั้งแต่ต้น

ใช้แพตเทิร์นเดียวกับ `npcalign_database.gd` ที่มีอยู่แล้ว เพื่อให้โค้ดทั้งเกมเป็นแนวเดียวกัน

**ตัดสินใจ: น้ำฟ้าส่งข้อความได้ แต่ไม่โชว์รูปและชื่อ**
ตรงกับพฤติกรรมแชทจริง (LINE/Discord ไม่โชว์ profile ตัวเองฝั่งขวา) — เก็บเป็น `side: "right"` + `avatar: null`
ถ้าอยากให้โชว์ชื่อตัวเองด้วย ลบ 2 บรรทัดใน `message_bubble.gd` ได้

**ตัดสินใจ: `ChoiceContainer` ย้ายไปอยู่ใต้ `Down`**
เดิม `ChoiceContainer` เป็นลูกของ `Phone` (CanvasLayer) ตรง ๆ และ `Down` เป็นแถบว่างเปล่า
**เหตุผล:** น้ำฟ้าตอบด้วยการ**เลือกตัวเลือก** ไม่ได้พิมพ์เอง แถบล่างจึงไม่ควรเป็นช่องพิมพ์
แต่ควรเป็นที่อยู่ของปุ่มตัวเลือก — ย้ายที่เดียวแก้ได้ 2 ปัญหาพร้อมกัน

**ปัญหาความกว้างบับเบิล และวิธีแก้**
Godot ไม่มี property "ความกว้างสูงสุด" ให้ container และ Label ที่เปิด autowrap
มี minimum width เท่ากับคำที่ยาวที่สุด → ถ้าปล่อยอิสระบับเบิลจะยืดยาวเป็นบรรทัดเดียว
แต่ถ้าปัก `custom_minimum_size` ตายตัว ข้อความสั้นอย่าง "รับทราบค่ะ" ก็จะกลายเป็นแท่งยาว

**วิธีที่เลือก:** วัดความกว้างข้อความจริงตอนสร้างบับเบิล (วัดทีละบรรทัดแล้วเอาบรรทัดกว้างสุด
เพราะ `get_string_size` ไม่เข้าใจ `\n`) ถ้าเกิน `max_width` (420) ค่อยปักไว้ให้ autowrap ตัดบรรทัด
ถ้าไม่เกินก็ตั้งเป็น 0 ปล่อยให้หดตามเนื้อหา — และตั้ง y = 0 เสมอเพื่อให้ความสูงมาจากจำนวนบรรทัดจริง

**auto-scroll ต้องรอ 2 เฟรม**
เฟรมแรก VBox คำนวณความสูงใหม่หลังเพิ่มบับเบิล เฟรมที่สอง scrollbar ถึงอัปเดต `max_value`
ถ้าเลื่อนทันทีจะได้ค่าเก่า ข้อความใหม่ยังโผล่ไม่พ้นจอ

**จังหวะข้อความ:** หน่วงตามความยาวข้อความ (`0.6 + 0.02 ต่อตัวอักษร` เพดาน 2.5 วิ)
ข้อความสั้นเด้งเร็ว ข้อความยาวรอนานขึ้น เหมือนอีกฝ่ายกำลังพิมพ์จริง

**ไฟล์ที่เพิ่ม:** `chat_database.gd`, `message_bubble.gd`, `phone.gd` + autoload `ChatDatabase` ใน `project.godot`

---

## 2026-07-29 — ⚠️ แก้ความเข้าใจผิด: `--check-only` ใช้ตรวจโค้ดโปรเจกต์นี้ไม่ได้

เดิมบันทึกไว้ใน CLAUDE.md ว่าใช้ `--headless --check-only --script` ตรวจ syntax ได้ — **ผิด**

**สิ่งที่เกิดขึ้น:** สคริปต์แชททั้ง 3 ไฟล์ FAIL ด้วย `Compile Error: Identifier not found: ChatDatabase`
ทดสอบเทียบกับ `SeatManager.gd` ซึ่งทำงานได้จริงในเกมมาตลอด → **FAIL ด้วยข้อความเดียวกัน**
(`Identifier not found: NPCalignDatabase`)

**สรุป:** โหมด `--check-only` ไม่โหลด autoload จึงมองไม่เห็น `Global`, `GameState`,
`NPCalignDatabase`, `SeatManager`, `ChatDatabase` — รายงาน error ที่ไม่มีอยู่จริง

**ผลกระทบ:** ถ้าเชื่อผลจากโหมดนี้จะเสียเวลาไล่แก้บั๊กที่ไม่มีอยู่ หรือแย่กว่านั้นคือ
ไปแก้โค้ดที่ถูกอยู่แล้วจนพังจริง

**ใช้ `--import` แทนเสมอ** — โหลด autoload ครบ แก้ CLAUDE.md แล้ว

---

## 2026-07-29 — วางระบบบันทึกความรู้ของโปรเจกต์

**ตัดสินใจ:** แยกเอกสารเป็น 2 ไฟล์ แทนที่จะรวมไว้ไฟล์เดียว

| ไฟล์ | ตอบคำถาม | ลักษณะ |
|---|---|---|
| `CLAUDE.md` | "ตอนนี้เกมเป็นยังไง" | เขียนทับได้ ให้สะท้อนสถานะล่าสุดเสมอ |
| `DEVLOG.md` | "ทำไมถึงมาเป็นแบบนี้" | สะสมเรื่อย ๆ ไม่ลบของเก่า |

**เหตุผล:** ถ้ารวมไฟล์เดียว เอกสารจะบวมจนหาข้อมูลปัจจุบันไม่เจอ เพราะปนกับประวัติเก่า
และเวลากลับมาอ่านย้อน มักอยากรู้อย่างใดอย่างหนึ่ง ไม่ใช่ทั้งสองพร้อมกัน

**กฎการทำงานที่ตกลงกันไว้:**
1. สิ่งที่คุยกันแล้วมีข้อสรุป → จดลง MD ไม่ปล่อยให้หายไปกับ session
2. การตัดสินใจที่กระทบแนวคิดพื้นฐานของเกม → จดลง DEVLOG ทุกครั้ง
3. เวลาแก้โค้ด → อธิบายเหตุผลและทางเลือกที่พิจารณา ไม่แก้เงียบ ๆ
4. หลังแก้ `.gd` / `.tscn` → ตรวจด้วย Godot headless ก่อนบอกว่าเสร็จ

---

## 2026-07-29 — ตั้ง git และ GitHub repo

**ตัดสินใจ:** ตั้ง git ก่อนเริ่มแก้โค้ดใด ๆ

**เหตุผล:** โปรเจกต์ทำมาตั้งแต่เมษายน 2026 โดยไม่มี version control เลย — แก้อะไรพลาดแล้วย้อนไม่ได้
ความเสี่ยงนี้สูงกว่าความเสี่ยงจากการให้ AI แก้โค้ดหลายเท่า จึงต้องมี safety net ก่อน

- Branch `main` → https://github.com/pnadda-boop/Game-smo (**private**)
- Commit จุดเซฟแรก `f360362` — 258 ไฟล์

**ตัดสินใจย่อย:** ไม่เอา `SmoGame/Test.exe` (127MB) ขึ้น git
**เหตุผล:** เป็น exported build ที่สร้างใหม่ได้เสมอ + เกินลิมิต 100MB ของ GitHub (push จะถูกปฏิเสธ)
ไฟล์ยังอยู่ในเครื่องปกติ แค่ git ไม่แตะ — เพิ่ม `/SmoGame/`, `*.exe`, `*.pck` ลง `.gitignore`

---

## 2026-07-29 — ตรวจสุขภาพโปรเจกต์ครั้งแรก

รัน Godot headless ตรวจทั้งโปรเจกต์ เพื่อตอบคำถามว่า "โค้ดยังเชื่อมกับ Godot สมบูรณ์ไหม"

**ผล: โครงสร้างแข็งแรง** — 0 script error, 0 warning, 0 resource หาย, autoload ครบ 4 ตัว, เกมรันจบ exit 0

**แต่เจอปัญหา 3 ข้อ** (รายละเอียดใน [CLAUDE.md](CLAUDE.md) หัวข้อ "ปัญหาที่รู้อยู่แล้ว"):

1. **TileSet อ้างพิกัดเกินขอบภาพ** — ~4,000 errors
   ยืนยันแล้วว่าเกิดตอน **runtime จริง** ไม่ใช่แค่ใน editor (รัน `level1.tscn` ตรง ๆ ได้ 816 errors)
   วินิจฉัย: ไฟล์ภาพถูกเปลี่ยนเป็นขนาดเล็กลงทีหลัง แต่ tile ที่วางไว้ในฉากยังอ้างพิกัดของภาพเวอร์ชันเดิม
   เช่น `image/walk.png` กว้าง 192px = 6 คอลัมน์ (0–5) แต่ฉากอ้างถึง (6,0) และ (7,0)

   **ยังไม่แก้ เพราะเป็นการตัดสินใจเชิงอาร์ต ไม่ใช่เชิงโค้ด** — มี 2 ทางที่ให้ผลต่างกันมาก:
   - (ก) หาไฟล์ภาพเวอร์ชันเดิมที่ใหญ่กว่ามาใส่คืน → แมปกลับมาเหมือนเดิม
   - (ข) ลบ tile ที่เกินขอบทิ้ง → แมปจะโหว่ตรงนั้นจริง ๆ ต้องวาดใหม่

2. **Signal ต่อซ้ำใน Start.gd** — ต่อทั้งในโค้ดและใน scene file
3. **โค้ด debug ค้างใน Start.gd** — `print()`, `print_stack()`, ตั้ง `mouse_filter` ซ้ำ

---

## 2026-07-29 — บันทึกตัวตนของเกม

**เกม:** Pixel Art Top-Down Narrative Simulator ภาษาไทย
เล่าเรื่องของ **น้ำฟ้า** กับการทำงาน **สโม** — มีระบบแชทข้อความและอีเวนท์ต่าง ๆ

**สถานะฟีเจอร์ ณ วันที่บันทึก:**

| ฟีเจอร์ | สถานะ |
|---|---|
| บทสนทนา (NPC + บับเบิล) | 🟢 ใช้งานได้ |
| ปริศนาจัดที่นั่ง (15 เงื่อนไข) | 🟢 ใช้งานได้ |
| เดินสำรวจ / บันได / เปลี่ยนชั้น | 🟢 ใช้งานได้ |
| มินิเกมปลุกตัวเอง | 🟢 ใช้งานได้ |
| ระบบแชทข้อความ | 🔴 UI เปล่า ยังไม่มีโค้ด |
| ระบบอีเวนท์ | ❓ ยังไม่เจอระบบกลางที่คุมอีเวนท์ |

**ข้อสังเกต:** ระบบปริศนาจัดที่นั่งออกแบบไว้ดี — ขอบเขตแถว/คอลัมน์คำนวณจากที่นั่งที่ลงทะเบียนจริง
ไม่ hardcode จึงเพิ่ม/ลดที่นั่งได้โดยไม่ต้องแก้ logic และรองรับเงื่อนไขหลายข้อพร้อมกัน (AND)

**คำถามที่ยังไม่มีคำตอบ:** "อีเวนท์" หมายถึงอะไร — เหตุการณ์ตามเนื้อเรื่องที่ trigger เอง,
งานของสโมที่ผู้เล่นต้องไปทำ, หรือระบบสุ่มเหตุการณ์? ยังไม่เจอ event manager กลางในโค้ด
